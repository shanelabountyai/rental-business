# 06 — Prioritized Product Backlog

**Product:** Single-family rental operations platform (10–50 units, multi-entity)
**Status:** v1.0 — 2026-07-31
**Sources:** `00-master-prd.md` (single master PRD; §5 holds the epics and acceptance criteria), `07-decisions.md`

This is the single, strictly-ordered build backlog. Items are sequenced so that every item is buildable when reached (all dependencies appear earlier), foundation comes first, and the shortest path runs to the two golden paths: **(1) a tenant reports a leak from their phone, it reaches a vendor by text with photos, the vendor closes it with a photo and an invoice, and the cost lands on the property's books** (end of Milestone 2) and **(2) rent posts, autopay collects it, a delinquent lease accrues a jurisdiction-correct late fee, the reminder ladder fires, and the owner sees all of it on one rent roll** (end of Milestone 3). Numbering is global and continuous across milestones.

Sizes: **S** ≈ one short session, **M** ≈ one focused session, **L** ≈ 2–3 sessions or one long one, **XL** ≈ should be split during planning. Phase = the phase the feature belongs to per the PRD. A ✅ on the ID means the item is built and committed. A 🟡 means it is PARTLY built and the remainder has its own lettered row — used when a coherent, gated half shipped and the rest was blocked or deliberately deferred, so that a half-done item is never mistaken for a done one.

**Numbering.** R-numbers are global, continuous and permanent — never renumbered, never reused. The **#** column is positional. Rows added later carry a letter (`16a` = inserted between 16 and 17) so inserting an item does not renumber seventy rows below it. The `R-` prefix distinguishes this repo from the storage platform's `B-` series; the conventions are otherwise identical.

**Story IDs.** The PRD/Feature column points at `00-master-prd.md` §5 story IDs (PROP-, LEASE-, PAY-, MAINT-, COMM-, INSP-, DOC-, RPT-, NOTIF-, ROLE-, RISK-). Those IDs are the authoritative acceptance criteria — the row here is the work order, the PRD story is the definition of done.

---

## Milestone 1: Foundation

Everything else builds on this: repo, schema, auth, RBAC, jurisdiction rules, jobs/events, the one task queue, and the property/unit inventory that maintenance and billing both consume.

| # | ID | Item | PRD/Feature | Size | Depends on | Phase |
|---|---|---|---|---|---|---|
| 1 | ✅ R-001 | Monorepo & app scaffold: Next.js (App Router) + TypeScript, Tailwind/shadcn, Prisma + Postgres, Vitest/Playwright, CI (incl. axe + Lighthouse hooks), env/secret handling | D-1; PRD §6.5 | M | — | MVP |
| 2 | ✅ R-002 | Core data model & migrations: LegalEntity, Property, Unit, Tenant, Guarantor, Lease, Charge, Payment, LedgerEntry, Deposit, Ticket, WorkOrder, Vendor, Inspection, Thread/Message, Notice, Task, Document, JurisdictionRule, AuditLog, StaffUser — money as integer cents, `propertyId` everywhere, **two-payer ledger shape decided here (OQ-2)** | PRD §13; D-3 | L | R-001 | MVP |
| 3 | ✅ R-003 | Auth foundation: Auth.js staff sessions (password + MFA enrolment), tenant magic-link + optional password, **signed single-use vendor links** (no account, per D-6); rate-limited login, password reset | PRD ROLE-05 | M | R-002 | MVP |
| 4 | ✅ R-004 | RBAC roles-as-data (tenant/guarantor/tech/manager/owner/read-only) + server-side **property and legal-entity scoping on every endpoint**; monetary authority ceilings config; **deactivation that preserves all history and kills access within a minute** (departing staff, past tenants). **No superuser flag, ever** (D-5) | PRD ROLE-01, ROLE-02, ROLE-06 | M | R-003 | MVP |
| 5 | ✅ R-005 | Append-only audit log service (actor, timestamp, entity, before/after, reason codes) as a shared package; **includes access-code reveal events** — a vendor seeing a lockbox code is a privileged read | PRD ROLE-03, PROP-03 | M | R-002 | MVP |
| 6 | ✅ R-006 | Background jobs + event bus foundation: job runner (Vercel Cron or Inngest-class), domain-event outbox, idempotent consumers, **per-property-local-time scheduling primitive** (D-3) — every nightly job in this product is property-local, not UTC-midnight | PRD §6.5; D-3 | M | R-001 | MVP |
| 7 | ✅ R-007 | Admin shell: left nav, global header, **property switcher with "All properties" and an entity filter**, universal search stub, role-gated routes — plus `db:create-owner` bootstrap script (first StaffUser with an all-properties `owner` assignment, single-use setup link, refuses a second owner without `--force`) per D-5 | PRD RPT-01, ROLE-01 | M | R-004 | MVP |
| 8 | ✅ R-008 | LegalEntity + Property CRUD: address validation/geocode, **timezone and state** (both drive everything downstream), acquisition date, entity assignment, duplicate-address warning | PRD PROP-01, PROP-04 | M | R-007, R-005 | MVP |
| 9 | ✅ R-009 | Unit management: units per property (main house, ADU, duplex side), status machine (occupied / vacant / make-ready / down), market rent, attributes; auto-transition to make-ready when a lease ends without renewal | PRD PROP-02 | S | R-008 | MVP |
| 10 | ✅ R-010 | **JurisdictionRule engine**: versioned, effective-dated config records consumed by payments, notices, deposits and scheduling — grace days, late-fee type/caps, deposit max + disposition deadline + escrow/interest flags, entry-notice hours, notice day-counts, payment allocation order, just-cause flag, application-fee cap, RUBS permissibility. Texas seeded (D-4); resolver is `rulesFor(property, asOf)` and **nothing anywhere reads a statutory number any other way** | PRD §6.7; D-4 | L | R-008 | MVP |
| 11 | ✅ R-011 | **One `Task` entity and queue** (D-9): property, type, subject entity, property-local business date, priority, assignee (nullable = anyone), status, typed proof fields, completion actor, source event id; idempotent creation on (type, entityId, businessDate); mobile-first "my day" list with overdue escalation and portfolio roll-up; proof-gated completion; audit on sensitive types. Built *before* its first consumer precisely so nothing later invents a second queue | PRD §5 (queues appear in MAINT, INSP, PAY, COMM); D-9 | M | R-006, R-005 | MVP |
| 12 | ✅ R-012 | Document & photo store: upload pipeline (client-side compression, resumable, EXIF timestamp preserved), attach to any entity, findable by entity/type/date, versions, soft-delete with 30-day undelete, **per-class retention rules as config** (leases and ledgers ≥7 years post-termination; screening data purged to decision + adverse-action record), and a **versioned per-unit photo library** persisting across turns. **Photo timestamps are evidence** — never strip them | PRD DOC-01, DOC-05, PROP-08 | M | R-002 | MVP |
| 13 | ✅ R-013 | Seed & demo data script: ≥2 legal entities, ≥6 properties across ≥2 states, units incl. an ADU and a duplex, tenants in every lifecycle state (current, late, in-notice, moving out, inherited-at-acquisition) | PRD §8 walking skeleton | S | R-009, R-010 | MVP |

## Milestone 2: Maintenance end-to-end (Golden Path 1)

Tenant reports it → triage → work order → vendor gets a text with photos → schedules with entry notice → closes with a photo → tenant verifies → cost lands on the property. This milestone forces the five hardest shared subsystems into existence: the notification engine, comms threading, file/photo evidence, magic-link identity, and the task queue in anger.

| # | ID | Item | PRD/Feature | Size | Depends on | Phase |
|---|---|---|---|---|---|---|
| 14 | ✅ R-014 | Unit operational data: lock/lockbox codes with history log and **per-work-order reveal (logged)**, appliance make/model/serial/install date, HVAC filter sizes, utility accounts, **shutoff locations (water main, breaker panel, gas) with photos** | PRD PROP-03 | M | R-009, R-012 | MVP |
| 15 | ✅ R-015 | Property filing cabinet: deed/mortgage (ARM-adjustment and balloon-maturity alerts), insurance declarations with 45–60 day renewal alert, HOA docs incl. **rental caps**, warranties (roof/HVAC/water heater/appliances/home-warranty contracts) | PRD PROP-06 | M | R-008, R-012 | MVP |
| 16 | ✅ R-016 | Notification engine core: events → recipient → channel (per preference) → template → delivery; Resend email with domain auth, Twilio SMS with **10DLC registration kicked off here** (external lead time), idempotent sends as a hard invariant, append-only message log, kill switch + sandbox redirect, quiet-hours primitive | PRD NOTIF-01, NOTIF-02 | L | R-006 | MVP |
| 17 | ✅ R-017 | Comms threading core: one threaded history per tenancy / property / vendor across portal, SMS and email; inbound SMS to the business number lands in the right thread; **manual call logging** (a timestamped note is contemporaneous evidence); staff attribution on every message | PRD COMM-01 | L | R-016 | MVP |
| 18 | ✅ R-018 | Tenant portal shell: magic-link sign-in (no password ceremony), mobile-first PWA shell, WCAG 2.1 AA baseline, **large-text and plain-language pass**, and **document access scoped to the tenant's own records** with a permission test asserting it. This shell is where the accessibility standard gets set, not retrofitted | PRD §6.4, DOC-03; D-8, D-10 | M | R-003, R-012 | MVP |
| 19 | ✅ R-019 | **Tenant maintenance request flow**: category → 2–3 structured clarifying prompts → photos (upload never blocks submission on a slow link) → entry permission → pet warning; under 2 minutes end to end. Includes the **category-specific troubleshooting scripts with pictures** (breaker, GFCI incl. "the one controlling this outlet may be in another room", disposal reset, thermostat battery, furnace switch, flapper, pilot) logging tried-or-declined before dispatch is allowed | PRD MAINT-01 | L | R-018, R-012, R-014 | MVP |
| 20 | ✅ R-020 | **Emergency intake path**: emergency categories (active flooding, sewage backup, gas smell, no heat in freezing temps, electrical burning/sparking, break-in, only toilet inoperable, CO alarm) show safety-first instructions (gas → call the gas company and 911 first), surface **the shutoff photo from R-014**, skip troubleshooting, and page on-call immediately regardless of hour | PRD MAINT-01, PROP-03 | M | R-019, R-016 | MVP |
| 21 | ✅ R-021 | SMS-to-ticket: a text to the business number creates a ticket and threads into it. Roughly half of real requests arrive this way; a portal-only intake measures adoption it created rather than demand that exists | PRD MAINT-01, COMM-01 | M | R-017, R-019 | MVP |
| 22 | ✅ R-022 | Staff-logged requests: phone-reported issues produce a structurally identical ticket (source: `phone_logged`) so the non-digital tenant is never a second-class record | PRD MAINT-01; D-10 | S | R-019 | MVP |
| 23 | ✅ R-023 | Triage queue **as a `Task` view**: suggested priority from category with override, merge duplicates, waiting-on-tenant state, first-response SLA timer with visual escalation, **habitability keyword auto-elevation** (mold, leak, no heat, sewage, infestation) into a tracked-response category with response-time logging | PRD MAINT-02, RISK-05 | M | R-019, R-011 | MVP |
| 24 | ✅ R-024 | Work order creation & assignment: scope, access details, priority, cost estimate; in-house job list carrying full context (photos, appliance data from R-014, codes, tenant contact); **warranty surfacing from R-015 before dispatch** plus the `warranty_claim_pending` state so a slow home-warranty claim reads as progress, not silence | PRD MAINT-03, PROP-06 | L | R-023, R-014, R-015 | MVP |
| 25 | ✅ R-025 | **Vendor magic-link work orders** (D-6): signed expiring link by SMS/email carrying scope, photos, address, tenant phone; accept / decline / propose a time; upload completion photos and an invoice (a napkin photo is a valid invoice); per-work-order code reveal, logged; no-response timer prompting re-dispatch to the trade's fallback vendor. **This item is the difference between a maintenance module vendors use and one they ignore** | PRD MAINT-03; D-6 | L | R-024, R-016 | MVP |
| 26 | ✅ R-026 | Approval thresholds: owner two-tap mobile approve/deny/ask with photos and estimate visible; staff ceilings from R-004 routing up automatically; **actuals-over-approval re-approval** at a configurable tolerance; bid-collection workflow above the bid threshold (scope + photos to N vendors, responses compared in one view) | PRD MAINT-04, ROLE-02 | M | R-025, R-004 | MVP |
| 27 | ✅ R-027 | Scheduling with **entry-notice compliance**: tenant coordination or logged permission-to-enter; the property's entry-notice hours from R-010 checked before any non-emergency entry, with a warn-and-override-with-reason path; notice generated, delivered and logged; T-1-day reminders; tenant no-show logged as trip-charge evidence | PRD MAINT-05, COMM-02 | M | R-025, R-010, R-017 | MVP |
| 28 | ⏸️ R-028 | **DEFERRED TO PHASE 3 by D-17** (OQ-3 answered 2026-08-06: all external vendors, so there is no in-house tech to be offline). **Offline-tolerant tech flow**: cached job list and details, status changes, photos, voice-to-text notes, time and materials queued in IndexedDB and synced on reconnect ("server wins on state, merge on notes/photos"), explicit sync indicator, required completion photo. Basements and rural properties have no signal; a flow that loses a tech's afternoon is a flow he stops using | PRD MAINT-06; D-8 | L | R-025 | ~~MVP~~ Phase 3 |
| 29 | ✅ R-029 | After-hours routing: on-call toggle (a two-person operation still needs one), escalation chain page → SMS → call → backup, emergency vendor list per trade, quiet hours respected everywhere except emergencies | PRD MAINT-12, NOTIF-05 | M | R-020, R-016 | MVP |
| 30 | ✅ R-030 | Verify & close (dependency on R-028 is soft — see D-17): tenant one-tap "was this resolved?" with rating, "no" reopens with a flag, **vendor reopen-rate captured from day one**; labor/materials/invoice attach to the property and to the work order — the WO → invoice → property-books chain is one chain with no re-keying, which is the specific place owners abandon software | PRD MAINT-07 | M | ~~R-028,~~ R-011 | MVP |
| 32 | ✅ R-032 | Work-order comms threading: tenant thread, vendor thread and staff notes hang off **one work order** with a single exportable timeline. Reconstructing an incident from three separate text histories six months later for an insurance adjuster is the problem this solves | PRD COMM-06 | M | R-017, R-025 | MVP |
| 32a | ✅ R-032a | **Somebody is subscribed to the vendor now.** Every vendor outcome raises work in the one queue (D-9), and the two that will not keep also notify. **The decline was the defect**: `sweepUnansweredDispatches` filters `vendorRespondedAt: null`, so a vendor who *ignores* us raises a re-dispatch prompt and one who *says no* raised nothing at all — the job returned to the unassigned queue silently and an urgent leak sat there overnight. Fixed at the event rather than by widening the sweep's query, because the sweep needs a timer to notice an absence and a decline is a present, timestamped fact that cannot afford an hour of latency. **A second silent hole found while building it:** a *proposed time* also sets `vendorRespondedAt`, so the sweep stepped over that too and nobody was ever asked to accept or counter the vendor's window. Accept and propose raise `workorder_schedule` (accepting is not scheduling — R-027 owns the window); a message raises `workorder_vendor_message` and notifies; an invoice raises `workorder_invoice_review` unless the ceiling already raised an approval, since two rows for one decision is worse than none. New `vendor_response` category so an operator can take the declines without the chatter | PRD MAINT-03, COMM-06; D-9 | S | R-032, R-025 | MVP |
| 32b | ✅ R-032b | **Two of this row's four claims were already fixed, and a third was wrong — verified before building.** *Already true:* the confirmed `scheduledStart`/`scheduledEnd` IS passed to the component and every timestamp on that page already used `utcToWallClock`; R-098's vendor-surface pass did both, and the row went stale. *Wrong:* `petWarning`/`entryPermission` are NOT "written nowhere" — all three ticket-creation paths persist them. **The real gap was that the vendor never saw them.** The PM's screen showed both; the person who actually opens the door was told neither, for every job since R-025. The pet warning now renders above the address as a bordered note — not a `<details>`, because a warning you have to expand is one that gets missed — and the entry answer is stated explicitly rather than inferred from silence, since "the tenant said no" and "nobody asked" send a vendor to different places. **And the completion photo is now actually required** (MAINT-06). D-17 asserted it was "already built into R-025's vendor upload" while justifying deferring R-028; it was not — the upload existed and nothing required it, so a job could reach WORK_COMPLETE with no evidence it was ever done, which is the state R-030 asks the tenant to confirm against and R-031 charges from. Gated on the server, not only in the page, and D-17 amended rather than left standing (D-41) | PRD MAINT-01, MAINT-03, MAINT-06; D-6, D-17 | S | R-025, R-019 | MVP |
| 32c | ✅ R-032c | **One-tap verification is now one link and one tap.** The SMS carried a portal URL behind `requireTenant`, which redirects to an **email-only** login with no return-to — so for the SMS-only tenant, the exact persona R-021 exists for, the message asking "was this fixed?" was a dead end, and for everyone else it was six steps. It now carries a single-purpose `TENANT_VERIFY` token: scoped to one work order, one tenant and one **round**, seven days, no session, no portal, no document, no money. **The answer is a POST, never a GET** — the row asked for a token "carrying the answer", and that is unsafe here because SMS clients and carrier link-safety scanners follow URLs, so a "yes" link would close jobs before the tenant read the message. The page presents both answers as equally prominent submit buttons, which is also what keeps the record honest. **Two bugs found building it:** `audit()` resolves its actor from a session and would have recorded the tenant's answer as `SYSTEM / anonymous` (fixed with `auditAsTenant`, attribution now an explicit argument at each door); and the recording logic was duplicated the moment a second doorway existed, so it was extracted to `verify-record.ts` before it could drift. **Left behind:** a "yes"/"no" texted straight back still reaches nobody — deliberately, see PROGRESS | PRD MAINT-07, NOTIF-03, COMM-02; D-10, D-16 | M | R-030, R-021 | MVP |
| 32d | ✅ R-032d | **Both halves, because they fix different failure modes.** The lifetime now tracks the JOB — three days for emergency and urgent work, which is attended within hours and where a long-lived credential that can reveal a gate code buys nothing; a fortnight for routine, which is booked out a week and where the link used to die before the vendor arrived. **And an expired link reissues itself** rather than saying "call the office", which was a phone call, somebody to answer it, and an invoice retyped by hand — the exact re-keying D-6 exists to prevent. That covers the month-end-invoice tail that no reasonable TTL reaches, without stretching every link in the product to serve it. **Safe because the new link goes to the vendor, not to whoever tapped**: a stale URL causes a text to the number on file and grants the clicker nothing. It refuses through `vendorLinkAccess()` — the same gate a live link passes — so reassigned, cancelled and closed jobs all decline, as does a link a PM deliberately revoked, a still-live token, and a forged one. One dead link sends one text however many times it is tapped | PRD MAINT-03; D-6, D-16 | S | R-025 | MVP |
| 32e | ✅ R-032e | **Decided: two numbers, two jobs, and the comment was the bug** (D-42). No behaviour changed — both functions were already correct and already used correctly. `jobCostCents()` is the books' number and takes the invoice; `actualTotalCents()` is the control's number and takes the higher of invoice and recorded parts. They diverge only when the parts exceed the invoice, which is why a doc-comment claiming they were "deliberately the same rule" survived until R-042's export had to pick. **Unifying either way breaks the other**: on the invoice, a $1,000 job billed at $600 stops tripping a $700 ceiling; on the maximum, a Schedule E return claims money never billed. A test now pins the divergence so a later unification fails loudly. **The spend tile says what it covers** — every job closed since the property was added, not a period or entity report — because `closedJobCostsForProperty` takes neither filter, and an unlabelled total reads like an annual figure somebody re-keys. R-081 still owns the real report | PRD MAINT-07, RPT-02; D-19, D-42 | S | R-030 | MVP |

> **Demo checkpoint 1 (Golden Path 1).** A seeded tenant reports a leak with a photo → the ticket auto-elevates on the habitability keyword → triage sets urgent → a work order dispatches to a seeded vendor by magic link → the vendor accepts a window (entry notice generated and logged) → uploads a completion photo and invoice → the tenant taps "resolved" → the cost appears on that property. No one logged into a vendor portal and no one re-typed an invoice.
>
> **Run it via the portal wizard, not SMS.** The checkpoint originally said "texts a photo", but R-021 deliberately deferred MMS (`NumMedia`/`MediaUrl0` are ignored; fetching media needs an outbound Twilio call that D-15 defers). The deferral stands — the checkpoint text was simply describing a path that was never built, and a demo that "fails" for a reason nobody wrote down is worse than one scoped honestly.
>
> **First walked 2026-08-10, and it did not run.** Four defects, all in items already marked ✅, fixed in R-036b above. The lesson worth keeping: every one of them was invisible to the item that introduced it and obvious the moment somebody followed the tenant, the vendor and the PM through in one sitting. Walk the checkpoint when the milestone closes, not when it is convenient.

## Milestone 3: The money loop (Golden Path 2)

Leases, the append-only ledger, Stripe, autopay, jurisdiction-correct late fees, and the rent roll the owner actually opens on Monday.

| # | ID | Item | PRD/Feature | Size | Depends on | Phase |
|---|---|---|---|---|---|---|
| 33 | ✅ R-033 | Lease records: parties (tenants + guarantors with distinct roles), term, rent, deposit held, utility responsibility, status machine (pending / active / notice-given / ended), MTM rollover rate; **inherited-tenant intake path** for acquisitions (no application on file, unknown deposit status, condition-as-found baseline) | PRD LEASE-06, RISK-08 | M | R-009, R-002 | MVP |
| 34 | ✅ R-034 | **Stripe Billing foundation** (D-11): Customer per tenant/payer, Price per lease, Subscription anchored to the lease's billing day computed in **property-local** time, SetupIntents for saved methods, signed webhooks with idempotency keys, and the **webhook → projection** pipeline that every later financial screen reads. Underwriting application submitted at R-001, not here — multi-LLC KYB has a real lead time | PRD §7; D-11 | L | R-002, R-006 | MVP |
| 35 | ✅ R-035 | **Ledger projection + money rules in core** (D-11, D-12): an append-only `LedgerEntry` projection built from Stripe invoice and payment webhooks — never written directly, and reconciled against Stripe on a schedule with drift alarmed, because a projection nobody checks is a lie with a timestamp. Alongside it, the calculations Stripe cannot express: late-fee amount **and its state-cap clamp**, proration in both directions, RUBS allocation, payment-allocation policy from R-010, `daysPastDue` from the original due date. All unit-tested in `packages/core` | PRD PAY-03; D-11, D-12 | L | R-033, R-010, R-034 | MVP |
| 36 | ✅ R-036 | Subscription lifecycle: create on lease activation, update on rent change or renewal (R-065), pause/resume, cancel on move-out; **billing-cycle anchor derived in property-local time**; a Billing Runs screen reading Stripe's invoice events with per-lease outcomes and a re-sync action for drift. Much smaller than a hand-rolled scheduler — this is the item where D-11 actually pays off | PRD PAY-03, NOTIF-03 | M | R-035, R-034 | MVP |
| 36b | ✅ R-036b | **Golden Path 1 repair** — four defects found by walking the demo checkpoint end to end for the first time, none of them in the item that surfaced them. `VERIFIED` was in neither `OPEN_STATUSES` nor the vendor link's `ACTIONABLE_STATUSES`, so the tenant's one tap deleted the job from every screen and killed the vendor's link before the invoice arrived; nothing ever closed the originating `Ticket`, so every later text from that tenant threaded onto the fixed job forever; the entry window was parsed in the **server's** timezone and rendered in the **property's**, so a served legal notice stated hours nobody intended to keep; and `reapprovalCheck` was wired only to the hand-typed-actuals path, so a vendor's invoice could close a job at any multiple of what the owner approved. Plus a `workorder_ready_to_close` Task, because "yes, it's fixed" is where a job stops being maintenance and starts being money | PRD MAINT-04, MAINT-05, MAINT-07; D-6, D-9, D-19 | S | R-030, R-027, R-026 | MVP |
| 37 | ✅ R-037 | Tenant payments: ACH free, card with a disclosed pass-through fee, **retail cash network** for cash-preferring tenants; Stripe-hosted fields only (no card or bank data ever stored); pending → settled states from Stripe; receipts on the tenant's preferred channel; balance and itemized charges visible before paying. **Collection-method split gated on OQ-11** — Stripe's Hosted Invoice Page cannot take partial payments, so the tenant-facing payment UI is ours regardless of how OQ-11 lands | PRD PAY-01; OQ-11 | M | R-034, R-035, R-018 | MVP |
| 37a | R-037a | **Retail cash network** (PAY-01's last unbuilt clause) — **GATED ON A VENDOR DECISION, and on nothing else.** Stripe has no US retail-cash rail and waiting for one is not a plan — it offers the pattern elsewhere (Konbini, OXXO) and not here, and under D-30 the US is the only market. Cash App Pay is US but is a *wallet* funded by a Cash App balance or a linked debit card — a different persona from the tenant who deals in physical notes, and **rejected 2026-08-10** because it would have to be absorbed rather than passed through (see the surcharge note below). Real options are third parties: PayNearMe, VanillaDirect (Green Dot), CheckFreePay. Each needs a contract, a fee schedule, and a settlement webhook or file to reconcile against — none of which can be designed before one is chosen. The seam is already built and waiting: `PaymentRail` and `PaymentChannel` both carry `RETAIL_CASH`, and the tenant is shown the option as unavailable *with a reason* rather than having it hidden. **Do not build a simulated driver for this** (D-15): a cash rail that mints fake settlements is untested code that looks finished, against a vendor whose actual settlement shape is unknown. Until it lands, R-038 carries the cash-preferring tenant | PRD PAY-01; D-15 | M | R-037, vendor decision | MVP |
| 37b | R-037b | **Surcharge compliance: debit vs credit.** The card pass-through built in R-037 applies to any `card` payment, but several states bar surcharging **debit** specifically — Tex. Bus. & Com. Code §604A.003 among them — and the funding type is not known at the moment the fee is quoted and disclosed. Stripe's own answer is a surcharge-provider app (Yeeld, InterPayments) that computes a compliant amount from the card's brand, funding type and issuing country; its automatic-surcharge product is US-only and in preview. Either integrate one, or make `cardSurchargePermitted` a three-state rule (credit-only / all / none) and quote the fee after the payment method is attached rather than before — which trades a compliance risk for a worse disclosure, since PAY-01 wants the number shown *before* the choice. **Needs the attorney review D-4 already requires, not just an engineering call** | PRD PAY-01; D-4, D-12 | M | R-037 | MVP |
| 37c | ✅ R-037c | **Test isolation: portfolio-wide staff bleed across concurrent test files.** `escalation.test.ts` fails roughly one unit run in two with `Field staffUser is required to return data, got null` from `onCallStaffForProperty`. Not a product bug — no orphaned rows exist, the `staffUserId` FK is RESTRICT, and R-004 deactivates staff rather than deleting them. It is a race: `auth/scoping.test.ts` creates **portfolio-wide** assignments (no `propertyId`, no `legalEntityId`) because that is exactly what it tests, `onCallStaffForProperty` correctly matches them for every property, and scoping's `afterAll` deletes them between Prisma's two queries. **Fixed** by marking that one fixture's staff member `active: false`: `onCallStaffForProperty` already filters `staffUser: { active: true }`, so an inactive user is invisible to paging and the bleed closes at its source, with no coupling to permissions and nothing changed about what the test asserts (a database unique index over the scope columns has no opinion on employment). Rejected: giving the fixture a role without `ticket.write` (couples one spec to another spec's query), serialising the files (hides the cause), per-file schemas (correct, but real infrastructure for a one-line problem). Went from failing 2 of 4 full runs to 3 of 3 clean. This was the fourth distinct cross-file interference this suite has produced, after the outbox sweep, the hard-coded phone and the leaked browser contexts | — | S | — | MVP |
| 38 | ✅ R-038 | **Offline payment recording**: check (with check number), money order, cash-at-retail — recordable by staff in under 15 seconds, printable receipt, deposit batching. The non-digital tenant never logs in and never becomes an exception path | PRD PAY-05; D-10 | S | R-037 | MVP |
| 38a | R-038a | **Part-payments recorded offline.** R-038 refuses one: Stripe's out-of-band mechanism settles the whole invoice, so applying half of one would mark the rest paid and write it off silently — the tenant would owe nothing and nobody would notice. This is a real counter situation (somebody arrives with half the rent in cash), so the refusal is a stated gap, not a design. Likely route is a customer-balance credit (`customer_balance_transactions`), which Stripe applies to the next invoice finalized — note "next", which is the wrinkle: it does not settle the invoice already in front of you. **Needs the real Stripe account to design against**, because the mechanics turn on invoice/balance interactions the simulator cannot faithfully reproduce and D-15 forbids guessing at | PRD PAY-05; D-11, D-31 | M | R-038, a test Stripe key | MVP |
| 39 | ✅ R-039 | Autopay on Stripe `charge_automatically` + SetupIntent: tenant-chosen day, owner able to require full balance, T-2 pre-debit notice; **ACH returns, retries and dunning attempts come from Stripe** — our job is reflecting the reversal into the projection, posting the NSF fee (core-computed), notifying both sides, and leaving no stale downstream state behind. ~~Blocked on OQ-11~~ **OQ-11 answered (D-29) and the remainder shipped as R-039a in five parts — enrolment screen, debit day, full-balance switch, T-2 notice, NSF fee. Originally: a subscription on `charge_automatically` cannot accept partial payments, and both are Must stories | PRD PAY-02; OQ-11 | M | R-037, R-016 | MVP | (PAY-02 closed 2026-08-14.)**
| 39a | ✅ R-039a | **The rest of autopay (PAY-02).** **The NSF-fee half landed 2026-08-13** — `Lease.nsfFeeCents` (nullable; null means the lease is silent, which means no fee), `Charge.assessedOnPaymentId` with a partial unique index so one returned payment can raise only one fee, the push to Stripe as an invoice item, and the tenant notice finally quoting a number on the LOCKED SMS channel rather than staying silent. Still outstanding: R-039 built the returned-payment half — the correctness half — and left the enrolment half. a tenant-facing **saved payment method** flow (`createSetupIntent` has existed since R-034 and nothing calls it, so autopay cannot actually be switched on by a tenant today); the **tenant-chosen debit day** and the owner's "require full balance" switch; the **T-2 pre-debit notice**; and **a lease form that can set `nsfFeeCents`** — the column exists and the fee works when it is populated, but nothing writes it, so every lease is currently silent (the safe direction to be wrong in). The payment-method UI is Stripe Elements against a live key and **cannot be meaningfully tested without one** (D-15), which is why it was not guessed at here | PRD PAY-02; D-15, D-32 | M | R-039, a test Stripe key | MVP |
| 40 | ✅ R-040 | Late fees: computed in core from jurisdiction config (grace days, flat/percent/daily, **clamped to the state cap**) and pushed to Stripe **as an invoice item — never generated by Stripe** (D-12); one-click waiver as a credit with reason and approver logged; **waiver-pattern report by tenant** — inconsistent fee enforcement across tenants is a fair-housing pattern risk, so the report is part of the feature, not a later analytic | PRD PAY-04; D-4, D-12 | M | R-036, R-010 | MVP |
| 40b | ✅ R-040b | **The charge half of the ledger.** Nothing in the product ever wrote a `CHARGE` entry — `invoice.finalized` was excluded in R-034 on reasoning that turned out to be wrong — so the ledger was single-entry and **every balance was wrong**: a tenant who paid rent went further into credit each month and the pay screen told them they owed nothing. Found by asking what actually writes a ledger row rather than by a failing test, because the fixtures create charges by hand and so build a state the app cannot. Fixed and recorded as D-35 | PRD PAY-03; D-11 | S | R-034 | MVP |
| 40d | ✅ R-040d | **First real execution of the Stripe driver**, and the bug it found. A smoke test against test mode (`STRIPE_SMOKE_KEY`, inert without it) walks customer → subscription → collection-method switch → open-invoice read → price change → cancel. Everything passed except the switch: **Stripe refuses `send_invoice` for a customer with no email**, which D-29's guard did not know. Fixed with a `no_email_for_invoicing` refusal (D-36). The collision with D-10's non-digital tenant is stated, not solved — see D-36 | PRD PAY-03; D-15, D-29 | S | R-037, a test Stripe key | MVP |
| 40e | ✅ R-040e | **`STOP` is unhandled, and it breaks the evidence trail.** Nothing in the product recognises an opt-out. Two consequences: a tenant texting STOP opens a maintenance ticket titled *STOP*; and — the serious one — **entry notices are legally significant and we treat them as a locked category a tenant cannot switch off, but a carrier-level STOP overrides us entirely.** The tenant stops receiving them and our notification log still records them as sent, so we would believe we had served notice when we had not. For a product whose premise is that the evidence trail is the product, a delivery record that can be silently false is the defect that matters. **D-38 answers the rule (2026-08-12): fall back AND raise a Task.** Scope confirmed by reading the code first: the fallback half is already true — `entry.notice` declares `channels: ['SMS','EMAIL','PORTAL']`, so a blocked SMS already leaves two live channels behind it and there is no fallback mechanism to build. What remains: **(1)** STOP/START/HELP recognised at intake, matched against the WHOLE message — carriers do, and *"please stop the leak"* must not unsubscribe a tenant from legally significant notices; **(2)** an `SmsOptOut` keyed by phone number rather than by tenant, because a carrier block is a fact about the number and numbers get reassigned; **(3)** delivery-status callbacks so `SENT` (the provider accepted it) becomes `DELIVERED`/`UNDELIVERED` — `deliveredAt` and `failureCode` already exist on `NotificationDelivery` and nothing populates them, and this is what makes the record honest for every failure mode rather than only for STOP; **(4)** the Task, which is the half D-38 genuinely adds. Found while preparing 10DLC registration | PRD NOTIF-01, COMM-02, MAINT-05; D-4 | M | R-016, R-021 | MVP |
| 42 | ✅ R-042 | **Acting on the cloud code review of the projection pipeline.** Two findings, both confirmed: a returned payment reversed only the remainder row and left every linked charge credited (the PAY-02 failure the pipeline exists to prevent), and a stale decline delivered after a successful retry reversed money that had actually cleared. Writing the test for the first found a **third** defect neither the review nor I had seen — payments never linked to charges at all, so a paid late fee showed as outstanding for ever. Also raised the global test timeout: 5s is Vitest's unit-test default and most of this suite is integration against a remote database | PRD PAY-02; D-11, D-35 | S | R-040c | MVP |
| 42b | ✅ R-098 | **UX and accessibility remediation — the safety tier.** A full-product UX and accessibility review (logged in `docs/UX-ACCESSIBILITY-LOG.md`) found two systemic defects and thirty-odd downstream ones. This item fixes the highest-stakes of them: the **emergency maintenance flow was entirely `onClick`**, so the safety instructions — the highest-consequence text in the product — did not exist until hydration, and two questions were required before a tenant could page anybody; and the **vendor surface** rendered proposed times in UTC, offered no phone number at any dead end, and destroyed focus on the single action it exists to perform. The rest of the report is R-099 | PRD MAINT-01, MAINT-03; D-6, D-10, D-20 | M | R-020, R-025, R-032 | MVP |
| 42c | ✅ R-099 | **The missing route boundaries, and the gate gap that hid them.** The product had **no `error.tsx`, `not-found.tsx` or `loading.tsx` anywhere** — zero files — while fourteen pages call `notFound()`: a tenant following a stale link got Next's bare 404, and an unhandled throw showed "Application error: a client-side exception has occurred" to a plumber in a driveway. Seven files, one set per audience. Also closes the gate's own gap — `expectFocusSurvived()`, the assertion whose absence let the entire R-098 tier through, verified by making it fail — and the four findings that were mine (H5, M7, M8) | PRD §12 accessibility; D-6, D-10 | M | R-098 | MVP |
| 42d | ✅ R-100 | **Durable object storage — D-14's swap, now due.** `LocalDiskStorageAdapter` writes under `DOCUMENT_STORAGE_PATH`, which on Vercel is an ephemeral per-invocation filesystem: every uploaded document, vendor invoice and maintenance photo is written to a disk that is gone on the next request, while the `Document` row claiming it exists survives. D-14 named this exact trigger — *"whenever this deploys somewhere the filesystem isn't durable across instances"* — and that condition was met the day the Vercel project was created. One assignment in `lib/storage/index.ts`, plus a Vercel Blob (or S3-class) adapter behind the existing `StorageAdapter` interface, plus a decision about the documents already written to local disk in dev. **The evidence trail is the product; an evidence trail that loses its photos is not one** | PRD DOC-01, PROP-08; D-14 | S | R-012 | MVP |
| 42e | ✅ R-101 | **The live-region tier, fixed at its source.** `FormAlerts` inserted the region TOGETHER with its text — its own comment claimed messages were "announced on arrival" and they generally were not, because a live region announces CHANGES TO ITSELF and so must be in the accessibility tree before the text lands. One file reaching **49 components**: every form in the product, including the ones a tenant uses to report a leak and pay rent. Two bespoke sites with the same defect (the card-fee disclosure, the autopay confirmation) now share a `LiveRegion` primitive rather than five ad-hoc patches. **Proven by making it fail** (the R-099 standard): reverting `pay-form` produced `A live region was CREATED to announce… (3 → 4)`. `expectAnnouncedInPlace()` counts regions before and after an action — the first version only asserted "a region exists", which was worthless once `FormAlerts` always renders two, and would have passed while the region under test was still broken. **Verifying the row found every claim understated**: 22 `onClick` handlers in the wizard rather than 7, 15 status regions rather than 11, 40 raw `toISOString()` rather than ~35, and `expectFocusSurvived` in 3 of 33 spec files. The rest is split into R-101b and R-101c rather than rushed | PRD §12 accessibility | M | R-099 | MVP |
| 42g | ✅ R-101b | **The wizard's choices are real radios, and its Next buttons stay reachable.** Every option was a `<button>` styled to look selected — announced as "button" and never as "radio, 3 of 7", with the selected state carried entirely by CSS, which assistive technology cannot see; no arrow-key navigation; and every option its own tab stop, so reaching Next on the category step took seven presses. A visually-hidden `<input type="radio">` inside the styled label buys all four back from the platform, with no roving-tabindex code to maintain. **And `disabled` became `aria-disabled`**: a disabled button leaves the tab order entirely, so a keyboard user tabs past the only thing between them and submitting with nothing said about why. It now stays focusable, is announced as unavailable, and names what is missing through `aria-describedby` into a live region. **A near-miss worth keeping:** the radios were first hidden with `sr-only`, which clips them to one pixel — so every real click landed on the label and the control was never the thing being clicked. Stretching the transparent input over the label fixed it, and the e2e went from 4.3 minutes of timeouts to 16 seconds. **The pre-hydration rewrite was deliberately NOT attempted** — see PROGRESS; R-098 already fixed the case that carries risk | PRD §12 accessibility, MAINT-01 | M | R-101 | MVP |
| 42h | ✅ R-101c | **One date formatter, and the timezone is a required argument.** There were four copies of `friendlyDate`; ONE took a timezone and three did not — and `Intl.DateTimeFormat` with no `timeZone` formats in the runtime's zone, which on Vercel is UTC. A work order closed at 7pm Central on the 14th was shown to staff as the 15th. The shared version cannot be called without a zone, so the broken form no longer compiles, and three list queries now carry `timezone` because a date cannot be rendered in the right zone if the zone was never fetched. **The 40 `toISOString()` sites were mostly correct**, which the row did not say: ~24 are `@db.Date` calendar days where slicing the ISO string is the RIGHT reader and a timezone must never touch them. Six genuine ones were fixed, chosen by consequence — ledger `occurredAt`, `noticeGivenAt` (twice, legally significant), `waivedAt`, a condition photo's `capturedAt`, the RUBS `allocatedAt`, and a work order's `closedAt`. **`expectFocusSurvived` rolled from 3 spec files to 7, and found nothing** — recorded as a negative result rather than dressed up: those surfaces use `<details>`/`<summary>`, which survives its own activation, so R-099's pattern was already holding. **Left behind, and it is a different defect than this row described**: three whole-panel swaps (verify, intake, MFA enrolment) put `role="status"` on content that replaces its own container — for those the right fix is moving focus to the new heading, not a live region. See R-101d | PRD §12 accessibility; D-3 | M | R-101 | MVP |
| 42i | ✅ R-101d | **Focus on a panel that replaces itself.** Three components swap their whole section on success — the tenant's "was this fixed?", the inherited-lease intake, MFA enrolment — and each put `role="status"` on the content that replaced its own container. That announces nothing: a live region inside a replaced container is a new node either way, so there is no change to report, and the control that had focus was just unmounted, so focus fell to `<body>` — the user is returned to the top of the document and hears nothing about whether their answer registered. `useFocusWhen` moves focus to the new heading with `tabIndex={-1}`, announcing the whole new context rather than one sentence of it. **The hazard designed around**: all three also render their done-state on an ordinary page load, so the trigger is CLIENT ACTION STATE, never a server prop — "something just happened" versus "something happened once", and only the first is worth interrupting for. **Proven by making it fail**: removing the ref gave `Expected: focused / Received: inactive`. Invisible to axe, which is the whole reason it is asserted | PRD §12 accessibility | S | R-101 | MVP |
| 42f | ✅ R-102 | **The global-sweep tests pay for every other test's debris, and a real backlog would be invisible.** Filed first with the wrong diagnosis — *"the sweeps take no batch limit"* — which reading the code disproved: `dispatchPendingNotifications` has taken `limit = 100` since R-016 and `unacknowledgedEmergencies` is bounded to 100 rows inside a 24-hour window. The actual defects are two. **(a) Test debris.** The one spec that exercises the genuine unfiltered sweep passes `limit = 5_000` deliberately — the sweep orders by id and a fresh cuid sorts last, so a small batch would crowd out its own rows — and then sends the entire accumulated backlog, two round trips per row, sequentially. It has outgrown its 60s timeout as the shared database reached 27,392 delivery rows; the escalation sweep pays the same way for stale unacknowledged emergencies. This is R-037c's family — the suite shares one database — and CLAUDE.md's own warning that an unfiltered sweep "becomes the slowest, flakiest thing in the suite three items later". **(b) No visibility.** The hourly cron drains at most 100 notifications and records only `sent`/`failed`, so a portfolio queueing more than 100 an hour would fall permanently behind with nothing on the audit row saying so | PRD NOTIF-01, MAINT-12; D-15 | S | R-016, R-029 | MVP |
| 41 | ✅ R-041 | Deposits as liabilities: held amount tracked separately from income everywhere and in every export, per-state max / escrow / interest rules from R-010, surety-alternative recorded as "no cash deposit held" | PRD PAY-07 | M | R-035, R-010 | MVP |
| 42 | ✅ R-042 | **Complete 2026-08-14, in three parts.** *Proration (2026-08-13):* `moveInProration` in core with the daily-rate method per lease (`prorationMethod`, ACTUAL or BANKER30), pushed to Stripe as an invoice item, with the arithmetic on the charge description so PAY-08's "shown transparently" is literal — **our proration, not Stripe's**, because Stripe's is built for mid-cycle plan changes rather than move-in on a calendar rent. Found and fixed an extra day of rent on every move-in west of UTC. *Recurring charges (2026-08-14):* pet rent and flat utility fees as real Stripe **subscription items** — `RecurringCharge` had carried `stripePriceId` and `stripeSubscriptionItemId` since R-002 with no code at all — behind one reconciler that is the only writer, which is what makes `endsOn` more than decoration (D-40). *RUBS (2026-08-14):* `UtilityBill` with the scanned bill attached as a `Document`, split across the units by equal / bedrooms / floor area, **the vacant unit's share left with the owner**, the arithmetic on every charge, and the whole split in the audit trail — gated on `JurisdictionRule.rubsPermitted`, which R-010 had carried unread since it was written (D-39). `allocate()` finally has its caller. **Left behind:** move-out proration (belongs with R-071's disposition, where the final balance is settled), and changing a recurring charge's amount is stop-and-add-a-new-one rather than an edit — deliberately, because the old row is the record of what was agreed | PRD PAY-08; D-4, D-12, D-39, D-40 | M | R-035 | MVP |
| 43 | ✅ R-043 | **Most of this already existed; the gap was history.** Balance, itemized breakdown, one-time payment in ≤3 taps, saved or new method and the receipt were all built by R-035/R-037/R-040 — what the portal could never answer was *"did you get my payment?"*, which is the call this row is costed against. `/portal/pay/history` answers it: every charge and payment, newest first, with what was owed after each one, **computed by the same `statement()` and `balanceCents()` the staff view calls** so a tenant and a property manager can never be shown two different balances. A reversed payment stays on the statement and says so, because D-11 makes a correction a new row rather than an edit. **Left behind:** the receipt is still per-payment rather than downloadable as a statement — R-046's magic links are the natural place for that | PRD PAY-03, PAY-01; D-10, D-11 | M | R-037, R-018 | MVP |
| 42a | ✅ R-031 | **The flag existed since R-002 and nothing consumed it.** `WorkOrder.tenantCaused` was written at close and its own schema comment said it "drives the mid-lease chargeback flow"; `ChargeType.CHARGEBACK` was in the enum, labelled in the tenant portal and ordered in `DEFAULT_ALLOCATION_ORDER`; `workorder.chargeback_posted` was in the audit vocabulary **and asserted in a test — with no writer anywhere**. This joined them. A separate `ledger.adjust` panel (D-43) posts the charge with an amount that may be **less** than the repair cost and may never be **more**, serves a notice quoting the reason back verbatim, and tells the tenant on a locked category. Closing as tenant-caused now raises a Task, so a flagged job cannot be silently forgotten. **Found along the way:** `FieldError` — the primitive every admin form uses — had R-101's live-region defect, inserting `role="alert"` together with its text, so a PM on a screen reader got silence on every back-office validation failure; and the structural gap that a `WorkOrder` has no lease at all (nullable `ticketId` → nullable `Ticket.leaseId`), resolved ticket-lease → live lease on the unit, refusing rather than guessing at an ended one. **Left behind:** the charge reaches the tenant's portal when Stripe invoices it, not on posting — D-11 keeps `LedgerEntry` a projection, same as every other charge type | PRD MAINT-07; D-9, D-11, D-42, D-43 | S | R-030, R-035 | MVP |
| 43a | ✅ R-049 | **A managed template gives up the compile-time safety the automated ones have, and the whole build is about buying it back.** `packages/core/notifications/templates.ts` says in its own header why its templates are typed functions rather than `{{placeholder}}` strings — *"a build error rather than a rent reminder that says 'Hi undefined'"*. A PM-edited string has no compiler, so: a **closed merge-field catalogue** (no internal identifiers, per D-10; nothing needing a jurisdiction decision, per D-4), **validation on save** — the last moment a human is present — a renderer that **never prints `undefined` and never prints a blank** but leaves the token standing and reports it, and **live preview against a REAL tenancy** rather than example values that would make every preview look perfect. Preview and send share one resolver, so they cannot drift. **The compliance rule:** an unapproved translation is usable for a rent reminder and refused for a notice to vacate, falling back to English with a named reason rather than serving words nobody with authority read — and `template.approve` is its own **privileged** permission, because if whoever pasted the machine translation can also approve it the rule is decorative. Editing a translation clears its approval. **Found along the way:** the repo has written `_previous` since R-008 meaning "unused on purpose" but never configured `argsIgnorePattern` — hidden because ESLint only reports *trailing* unused args. **Left behind:** a PM still cannot reword the 13 automated templates (D-44 names this deliberately); sending from the library to a selection is R-044 | PRD COMM-03; D-4, D-10, D-44 | M | R-017 | MVP |
| 44 | ✅ R-044 | **"Which bucket" and "past grace" are different questions, and the whole item is shaped around not conflating them.** The 0–5 / 6–15 / 16–30 / 30+ buckets are counted flat from the due date; *past grace* is a legal line that moves by state (`JurisdictionRule.graceDays`, D-4). Texas grants **one** day — so a tenant one day late is visibly delinquent on the report AND must not be chased, and treating bucket membership as chaseable is how a rent-roll screen becomes a fair-housing complaint. They are computed separately, never derived from one another, `graceDays: null` never resolves to "chase them", and grace is strictly-greater-than so the last day is still inside it. Aged from the **oldest** unpaid charge, so a tenant who paid this month while March is outstanding reads as months late rather than current. Rent roll carries rent, balance, days late, autopay, deposit held as a liability and subsidy portion, in **four bulk reads** rather than a query per lease. CSV export is injection-guarded (a lender opens it in Excel) while leaving negative numbers summable. The bulk chase sends to an **explicit selection**, re-checks past-grace server-side, skips any recipient with an unfillable merge field rather than posting a raw token, authorises **each lease against its own property**, and is idempotent per lease per day. **Found along the way:** three resource-less permission checks — page, export route and send action — each of which denied the property-scoped manager whose job this screen is. **Left behind:** a courtesy reminder before grace has no path here; date-range filtering and PDF are RPT-02's remainder | PRD PAY-06, RPT-02; D-4, D-9, D-11, D-13, D-44 | M | R-035, R-049 | MVP |
| 45 | R-045 | Payment lifecycle notices: due-soon (T-3), due-date, autopay pre-debit and receipt, payment-failed with a fix path, **card-expiring-soon** from a nightly scan — the last one keeps a long-tenured on-time tenant out of the dunning ladder over something visible a month ahead | PRD NOTIF-03, PAY-02 | M | R-039, R-016 | MVP |
| 46 | R-046 | Pay-now magic links: single-purpose ≥128-bit tokens, a portal session scoped to paying only, expiry and revocation, click and payment attributed back to the send record | PRD PAY-01, COMM-02 | M | R-043, R-016 | MVP |
| 47 | R-047 | **Legal-action payment controls**: per-tenant block-online / block-partial / certified-funds-only switches implemented as **pausing collection on the Stripe subscription and disabling our payment UI together** — both, because either alone leaves a path open; refused attempts logged to the case file with a neutral tenant-facing message. In many states accepting a partial payment after serving notice voids the notice, so **an autopay charge that fires the morning after a notice is served is a defect with legal consequences** — this item must prove, with a test, that pausing actually stops Stripe | PRD PAY-12; D-4, D-11 | M | R-037, R-035, R-039 | MVP |
| 48 | R-048 | **Section 8 / HCV split ledger** (gated on OQ-2 and **OQ-12**): a Stripe invoice has exactly one payer, so a voucher lease runs **two Customers and two Subscriptions** — HAP payer and tenant payer — joined in our projection so the lease shows one balance. Mid-stream portion changes prorated on recertification effective dates; one bulk HAP deposit reconciled across many tenant projections; housing authority / caseworker / HAP contract / inspection dates / abatement status per tenancy; **dunning never pursues the HAP portion**; failed HQS inspection auto-creates urgent work orders against the re-inspection deadline | PRD PAY-13; OQ-12 | L | R-035, R-024 | MVP\* |
| 50 | R-050 | Owner dashboard: exception-first tiles — collected vs billed, aged delinquency, open tickets by priority and age (emergency/urgent >48h glowing), vacancies with days-on-market and daily cost of vacancy, leases expiring ≤90/120 days, pending approvals, compliance items ≤30 days — **every tile drills into the underlying filtered list**, entity filter throughout, no dead-end numbers | PRD RPT-01, RPT-04 | L | R-044, R-023 | MVP |

\* R-048's phase is decided by OQ-2 (`07-decisions.md`). **The two-payer shape is now settled and built** — R-002 shipped `LeasePayer` + `PayerAllocation` per D-13, so a voucher lease already has somewhere to live. What is still gated is the *feature* work in this row: OQ-2 was answered "unsure / possible later", which is not enough to schedule recertification proration and HAP reconciliation against. Re-ask before Milestone 3 planning.

> **Demo checkpoint 2 (Golden Path 2).** The nightly run posts rent on a seeded lease → autopay collects one → a second lease goes unpaid → on grace+1 a Texas-correct late fee posts and the reminder fires → a pay-now magic link collects it and halts the ladder → the owner dashboard shows all of it, filtered to one LLC.

## Milestone 4: Notices, evidence & the communication record

The paper trail that wins deposit disputes, evictions, habitability claims and retaliation claims. Built as its own milestone because the requirement is uniform across every module and retrofitting proof-of-delivery is miserable.

| # | ID | Item | PRD/Feature | Size | Depends on | Phase |
|---|---|---|---|---|---|---|
| 51 | R-051 | **Notice delivery proof**: certified-mail tracking logged (physical-mail API deferred to R-081), portal delivery with read receipt, **posted-on-door with a photo of the posted notice and its timestamp**, print-ready PDF for tenants who get paper; the state-valid service methods per notice type come from R-010; TCPA consent captured at onboarding and STOP honoured automatically | PRD COMM-02; D-4 | L | R-049, R-010, R-012 | MVP |
| 52 | R-052 | Immutable communications audit + **thread export as a timestamped PDF transcript** with delivery metadata, plus the **court-ready ledger statement** (chronological, plain language, no cryptic codes — a judge has to read it; under D-11 it renders from the projection with Stripe invoice PDFs attached as the underlying record) — together, the packet you hand an attorney, an adjuster or a judge | PRD COMM-05, PAY-09 | M | R-017, R-005, R-035 | MVP |
| 53 | R-053 | Segment announcements with per-recipient delivery status (all tenants / one property / one metro / tag) — "the city is flushing hydrants Tuesday" | PRD COMM-04 | S | R-049 | MVP |
| 54 | R-054 | Message history, shared suppression list, and a bounce/failure path where a hard bounce flags the tenant and creates a **`Task`** rather than a private queue table (D-9); unanswered tenant messages past N days surface on the dashboard; daily digest option for non-urgent events | PRD COMM-07, NOTIF-04, COMM-01 | M | R-017, R-011 | MVP |
| 55 | R-055 | **Retaliation-claim guard**: when a rent increase, non-renewal or notice is drafted inside the post-complaint window configured for that property, warn with the specific complaint and date and require a logged business reason. No mainstream tool does this; it is cheap here because R-052 already knows when the complaint arrived | PRD RISK-06; D-4 | M | R-052, R-051 | MVP |

## Milestone 5: Leasing & tenant lifecycle

Marketing a vacancy through screening, lease execution and renewal. Deep because it carries three external integrations — all simulated first (D-7).

| # | ID | Item | PRD/Feature | Size | Depends on | Phase |
|---|---|---|---|---|---|---|
| 56 | R-056 | Listing creation + hosted listing page: photos from the unit library, rent, deposit, requirements, pet policy, available date, **jurisdiction disclosure templates** (deposit amount, fee limits, source-of-income acceptance where required) | PRD LEASE-01 | M | R-009, R-012, R-010 | MVP |
| 57 | R-057 | `SimulatedSyndicationAdapter` + feed builder with lead-source attribution and ≤24h delist on lease-up (D-7); real network feeds are Phase 3 and partner-gated | PRD LEASE-02; D-7 | M | R-056 | MVP |
| 58 | R-058 | Prospect pipeline + **identical auto-sent pre-screening questions to every inquiry** (move date, occupants, pets, income range, prior evictions) — identical is the fair-housing requirement and it also kills most tire-kicker back-and-forth | PRD LEASE-07 | M | R-057, R-049 | MVP |
| 59 | R-059 | Online application: one per adult 18+, co-applicant grouping and links, save-and-resume, ID and income document upload, application fee payment with **jurisdiction fee caps**, completion timestamp (which drives order-of-processing) | PRD LEASE-03 | L | R-058, R-037 | MVP |
| 60 | R-060 | `SimulatedScreeningAdapter` + **written screening criteria as versioned config** (D-7, gated on OQ-6): provider-hosted flow shape so no SSN ever touches our systems, results displayed *alongside* criteria, accept/decline always an explicit human action, order-of-completed-application enforced or deviation logged, individualized-assessment notes field retained. **No AI or algorithmic applicant scoring, ever** | PRD LEASE-04; D-7 | L | R-059 | MVP |
| 61 | R-061 | **FCRA adverse action**: generated on any decline or approve-with-conditions citing a consumer report (CRA details, applicant rights), delivery logged, and it **blocks closing the application** until sent or overridden with a logged reason. Most small landlords skip this and don't know they're exposed | PRD LEASE-05 | M | R-060, R-051 | MVP |
| 62 | R-062 | Document generation service: templated PDFs with merge-field validation that **fails loudly on a missing field**, shared by leases, notices, receipts and dispositions; PDFs tagged (heading structure, reading order, table headers, `lang`, `/Title`, real text not raster) | PRD DOC-04, §6.4 | M | R-012 | MVP |
| 63 | R-063 | Lease generation + `SimulatedESignAdapter`: **the owner's attorney-drafted template** with merge fields and property-specific addenda (lead paint pre-1978, mold, bedbug, HOA rules, pool, well/septic), per-state template selection, signer order incl. guarantors, executed PDF with audit certificate, and on execution the rent schedule and deposit charge create themselves | PRD LEASE-06, DOC-02; D-7, OQ-7 | L | R-062, R-060, R-035 | MVP |
| 64 | R-064 | Showings: self-serve slot booking with reminders, **occupied-unit showings generate and log the tenant entry notice** from R-027's machinery; smart-lockbox self-showing (ID capture, one-time codes, entry log, instant kill) deferred to R-087 | PRD LEASE-08 | M | R-058, R-027 | MVP |
| 65 | R-065 | Renewals: 120/90-day flags, **rent-increase rule check** (notice period and any cap jurisdiction — blocked with the specific rule when violated), offer → e-sign → ledger updates on the effective date, MTM rollover applying the configured rate automatically, renewal rate tracked as a metric | PRD LEASE-09; D-4 | M | R-063, R-016 | MVP |
| 66 | R-066 | Notice to vacate + non-renewal: tenant portal form (date, forwarding address, lease-notice-period validation) timestamped; owner non-renewal with delivery logging and just-cause-jurisdiction flags; **R-055's retaliation guard fires here** | PRD LEASE-11, RISK-06 | M | R-051, R-055 | MVP |
| 67 | R-067 | Renter's insurance tracking with expiry and lapse alerts | PRD LEASE-10 | S | R-012, R-016 | Phase 2 |

## Milestone 6: Inspections, deposits & the turn

The deposit-defense chain: matched move-in/move-out evidence, a statutory countdown that cannot be missed, and the make-ready project that follows.

| # | ID | Item | PRD/Feature | Size | Depends on | Phase |
|---|---|---|---|---|---|---|
| 68 | R-068 | Inspection engine: room-by-room checklist templates, per-item condition + notes + photos (timestamped, geotagged), ~~offline-capable on the same machinery as R-028~~ **offline capability re-scoped by D-17** — R-028 moved to Phase 3, so there is no shared offline machinery to build on. R-068 ships online-only unless its own inspection use case justifies building that machinery itself, which is a decision for whoever picks R-068 up. Staff inspecting a vacant unit are usually standing in a house with a phone signal; a tech in a basement was the case that justified it, e-sign or auto-finalize after a stated window, immutable lock on completion | PRD INSP-01 | L | ~~R-028,~~ R-062 | MVP |
| 69 | R-069 | Move-in inspection + keys: tenant-signed condition report, key/code issuance logged, and **door codes withheld until move-in funds show cleared** (certified funds supported) | PRD INSP-01 | M | R-068, R-037, R-014 | MVP |
| 70 | R-070 | Move-out inspection with **side-by-side per-item comparison against move-in** (move-in photo beside move-out photo); pre-move-out walkthrough calendared automatically where the state grants that right | PRD INSP-02 | M | R-069 | MVP |
| 71 | R-071 | **Deposit disposition**: itemized deductions each linked to evidence (inspection photo / work order / invoice) or flagged unsupported, **age-based depreciation guidance** (full replacement cost on nine-year-old carpet loses in court), statutory countdown from the recorded move-out date with escalating reminders and a 50%-elapsed owner alert, generated letter, delivery to the forwarding address logged, deposit liability zeroed | PRD INSP-03; D-4 | L | R-070, R-041, R-051 | MVP |
| 72 | R-072 | Turnover / make-ready as a project: sequenced templated checklist (trash-out → repairs → paint → floors → clean → **re-key, logged**), vendor task assignment, target rent-ready date, cost roll-up per turn, **days-vacant clock** from move-out to new move-in; move-out findings auto-draft the punch list | PRD LEASE-12, INSP-06 | M | R-070, R-024, R-011 | Phase 2 |
| 73 | R-073 | Periodic inspections: annual interior, seasonal exterior, drive-bys on the same checklist machinery — **this is where the unreported leak and the unauthorized dog get caught** | PRD INSP-04 | S | R-068, R-011 | Phase 2 |
| 74 | R-074 | Tenant self-guided move-in report (photo walkthrough on their phone within N days, auto-finalize window) | PRD INSP-05 | S | R-068, R-018 | Phase 2 |

## Milestone 7: Reporting, tax, compliance & preventive maintenance

The five reports the owner actually opens, the year-end packet the CPA needs, and the calendar that prevents the quiet failures.

| # | ID | Item | PRD/Feature | Size | Depends on | Phase |
|---|---|---|---|---|---|---|
| 75 | R-075 | **Shared `metrics` module in core**: every definition with a written formula and unit tests — occupancy, days-vacant, days-to-fill, delinquency buckets, `daysPastDue(lease)` computed from the *original* due date rather than the last retry, time-to-first-response, time-to-resolve by priority, maintenance cost per unit per month, renewal rate, turn cost. No screen computes any of them inline, and roll-up-equals-sum is asserted in a test | PRD §9 | M | R-044, R-030 | MVP |
| 76 | R-076 | The five weekly saved views: rent roll + delinquency aging · open work orders by age and priority · vacancy and turn status · cash summary per entity vs reserve target · upcoming critical dates next 60 days | PRD RPT-04 | M | R-075, R-050 | MVP |
| 77 | R-077 | **Compliance calendar**: rental licenses and registrations, CO and periodic city inspections, smoke/CO certifications, lead disclosures for pre-1978, insurance renewals, HOA dates, property tax due dates **and assessment-appeal windows**, Section 8 inspection and recertification dates, entity-level items (LLC annual reports, registered agent); lead-time alerts and a permanent completion log that answers "when was this last done" in one lookup | PRD PROP-05 | M | R-008, R-011, R-016 | MVP |
| 78 | R-078 | Capital-improvement log per property (cost, in-service date, warranty docs) feeding a **QuickBooks-mapped income/expense export per legal entity** with Schedule E-aligned categories and **CapEx flagged separately with in-service dates**; every line carries a mapping or lands on an unmapped exception list — nothing silently dropped (OQ-8 decides whether the QBO API follows or CSV suffices) | PRD RPT-03, PROP-07 | M | R-035, R-030, R-015 | MVP |
| 79 | R-079 | Vendor management: trades and service areas, **W-9 gate** (payment blocked or flagged without one), **COI expiry tracking that warns or holds dispatch on a lapsed vendor**, license numbers, preferred and fallback lists per trade, cumulative calendar-year payment totals by payment method for 1099-NEC, and **invoice status visible to the vendor** (received → approved → paid) with tolerance-based auto-routing — which is what stops the "where's my check" calls | PRD MAINT-11, MAINT-09 | M | R-025, R-016 | Phase 2 |
| 80 | R-080 | Preventive maintenance + **seasonal batch templates**: filters, HVAC spring/fall, gutters, winterization (tenant hose notices, vacant-unit checklist, sprinkler blowouts), smoke/CO test-and-battery logged at every entry, water heater flush, dryer vent, sump pump, pest, well/septic, chimney — one action creates the batch across properties, assigned by vendor territory | PRD MAINT-08 | M | R-024, R-073 | Phase 2 |
| 81 | R-081 | Per-property operating snapshots, monthly P&L per property and entity, vendor spend by trade, turnover cost, **year-end tax packet** (Schedule E, CapEx schedule, 1098 reconciliation, deposit liability, 1099-NEC candidates), leasing funnel by source, maintenance analytics incl. repeat-issue detection and vendor reopen rate | PRD RPT-05, RPT-06, RPT-07, MAINT-10 | L | R-078, R-079, R-075 | Phase 2 |
| 82 | R-082 | Invoice splitting across properties and categories (the $900 handyman invoice covering three houses) + reserve targets and a crude capital plan projecting component replacement by age | PRD PAY-10, PAY-11 | M | R-078, R-015 | Phase 2 |

## Milestone 8: Phase 2 — the risk workflows

Twenty years of scar tissue, encoded. Each is a case file with timestamps and deadline clocks; none of them make a legal judgment on the owner's behalf.

| # | ID | Item | PRD/Feature | Size | Depends on | Phase |
|---|---|---|---|---|---|---|
| 83 | R-083 | **Eviction case files**: state-specific pay-or-quit notice generation with service-method logging (defective service restarts everything), stage tracking notice → filing → court → judgment → writ → lockout, cost tracking, cash-for-keys as a documented alternative outcome, and a one-click **attorney packet export** assembling ledger, notices with service proof, communications, photos, lease and addenda. **Never files anything anywhere** | PRD PAY-14 | L | R-047, R-052, R-051 | Phase 2 |
| 84 | R-084 | Lease holds (carried from the storage build's pattern): typed holds — `military_scra`, `deceased`, `bankruptcy`, `dispute`, `payment_plan`, `do_not_contact` — with effects declared per type as configuration: halt dunning, halt late fees, halt access changes, suppress marketing. Persistent banner on the tenant record and on every notice-generation screen; audited placement; manager-or-above to lift SCRA | PRD RISK-12, RISK-11 | M | R-083, R-040 | Phase 2 |
| 85 | R-085 | **SCRA**: military-status flag, orders-based early termination, and the eviction workflow **prompting the SCRA affidavit / DOD lookup** — courts require it for default judgments | PRD RISK-12 | S | R-084 | Phase 2 |
| 86 | R-086 | **ESA / assistance-animal requests**: intake → documentation review with dated records → written determination → recorded as an assistance animal distinct from a pet, with **no pet fee, deposit, pet rent, or breed/size limit applied**. The request-to-response timeline must be airtight; mishandling these is a top fair-housing complaint source | PRD RISK-13 | M | R-052, R-063 | Phase 2 |
| 87 | R-087 | Abandonment and tenant-gone-dark case workflow: escalating logged contact attempts, state-compliant welfare check with proper entry notice, entry photos, statutory belongings handling with its clock. Done wrong this converts into an unlawful-eviction claim | PRD RISK-01 | M | R-083, R-027 | Phase 2 |
| 88 | R-088 | Unauthorized occupant/pet flow with a first-class **"legitimize it" path** (add occupant via application + screening; convert pet to authorized with screening and fees) — because that is the common real-world outcome — plus hoarding case management targeting lease and safety terms rather than the person, with accommodation-request tracking | PRD RISK-02, RISK-03 | M | R-060, R-073 | Phase 2 |
| 89 | R-089 | Insurance claims: a claim object linking work orders, immediate photo/video capture prompts (mitigation speed decides water claims), mitigation invoices, claim number, adjuster contact, correspondence, payout vs actual repair cost, loss-of-rents evidence from the rent roll, proceeds accounting on the property P&L | PRD RISK-07 | M | R-032, R-078 | Phase 2 |
| 90 | R-090 | Roommate changes and lease assignment: departing-tenant release, replacement screened to full criteria, amendment e-signed by all, ledger continuity, and the hard rule that **the deposit stays with the unit until full turnover** — roommates settle between themselves | PRD RISK-10 | S | R-063, R-041 | Phase 2 |
| 91 | R-091 | DV / VAWA workflows: confidential flags visible to Owner only, lock-change work orders with restricted-party notes, lease bifurcation as an amendment path, early-termination rights processing | PRD RISK-04 | M | R-084, R-063 | Phase 2 |
| 92 | R-092 | Property sale and acquisition handoff: full-file bulk export per property (leases, ledgers, deposit amounts with transfer-notification templates, keys and codes, vendor history, warranties), estoppel certificate generation | PRD RISK-09, DOC-06 | M | R-062, R-015 | Phase 2 |

## Milestone 9: Phase 3 — depth, real vendors & reach

| # | ID | Item | PRD/Feature | Size | Depends on | Phase |
|---|---|---|---|---|---|---|
| 93 | R-093 | Real vendor drivers replacing the simulators (D-7): screening provider (partner + FCRA agreement), e-sign provider, listing syndication feeds, certified-mail API, QuickBooks Online API sync. Each slots behind its existing adapter interface | D-7; PRD §7 | L | R-060, R-063, R-057, R-078 | Phase 3 |
| 94 | R-094 | Smart-lockbox self-showings: identity verification, one-time codes, entry logs, instant kill; and smart-lock code lifecycle tied to lease state | PRD LEASE-08, PROP-03 | M | R-064 | Phase 3 |
| 95 | R-095 | Investor / partner read-only role: property-scoped access, owner statements (income, expenses, work done, cash position), contribution and distribution ledger per ownership split (gated on OQ-4) | PRD ROLE-04, RPT-08 | M | R-004, R-081 | Phase 3 |
| 96 | R-096 | Spanish tenant surface + attorney-approved notice translations (gated on OQ-9 — may be Must, not Could) | PRD §6.4; D-10 | M | R-049, R-018 | Phase 3 |
| 97 | R-097 | Growth and depth bundle: rent credit-bureau reporting opt-in, inbound-email auto-threading, calendar two-way sync, vendor bill-pay rails, native tech app **only if R-028 field data shows the PWA hitting real offline limits** (D-8). **XL — split before starting** | PRD PAY-15, COMM-08, MAINT-13 | XL | R-093 | Phase 3 |

---

## How to use this backlog with Claude Code

- **Work strictly top to bottom.** The order encodes the dependency graph; when you reach an item, everything it depends on is already built. Don't cherry-pick a later item unless its entire "Depends on" chain is done.
- **One item (or one small cluster) per session.** S/M items are a single focused session. L items should be split into 2–3 sessions at the start ("build the state machine," then "build the UI on it"). R-097 (XL) must be split before starting.
- **Load the source PRD section as context.** The PRD/Feature column points at the authoritative story in `00-master-prd.md` §5. That story's Given/When/Then criteria are the item's definition of done — not this row's prose, which is the work order.
- **Start each session with:** the backlog row, the PRD story, `07-decisions.md`, the cross-cutting requirements in PRD §6 (auth, WCAG 2.1 AA, mobile-first, PCI scope, jurisdiction config), and the current schema. End each session with tests passing — **money math, jurisdiction-rule resolution, and the maintenance funnel are the three places tests are non-negotiable** — and the seed script updated if entities changed.
- **Check the gate before starting.** If the item's row or `07-decisions.md` names an unanswered OQ, either get the answer or build to the PRD's stated default and record the assumption in `PROGRESS.md`. Don't decide it silently.
- **Two demo checkpoints:** after R-032, run Golden Path 1 (leak text → vendor magic link → closed with photo → cost on the property). After R-050, run Golden Path 2 (rent posts → autopay collects → late fee → reminder → magic-link payment → owner dashboard).

## Rationale: ordering decisions & trade-offs

1. **Foundation before any feature (R-001–R-013).** Every module reads and writes the shared schema and assumes property scoping, RBAC, audit logging, an event bus, and jurisdiction rules. Building these once, first, prevents each module re-inventing them.
2. **The jurisdiction-rules engine is foundation, not a compliance feature (R-010).** It is deliberately the largest Milestone 1 item. Every later module reads statutory numbers from it; if it arrives late, dozens of hardcoded grace periods and deadlines have to be hunted down, and the ones missed are silent. Its resolver signature — `rulesFor(property, asOf)` — is what makes rule changes prospective rather than retroactive.
3. **The task queue before its first consumer (R-011).** The storage build had to consolidate seven separate queues after the fact. This is that lesson spent once, in advance, for the cost of building R-011 before anything needs it.
4. **Maintenance is Golden Path 1, ahead of money.** Two reasons. It forces the five hardest *shared* subsystems into existence early — notification engine, comms threading, photo evidence, magic-link identity, task queue — which the money loop then inherits rather than builds. And it is the differentiated 30% that justifies a custom build at all: rent collection is available off the shelf; a vendor who fixes things by text without an account is not. Building it first also means the riskiest adoption assumption (D-6, vendors will use magic links) gets tested in week 6 rather than month 5.
5. **Stripe Billing owns collection; core owns every number a statute could change (R-034, R-035; D-11/D-12).** Delegating invoicing, retries and ACH-return handling removes a genuine pile of scheduler code, and it is the more common architecture in industry — worth learning deliberately, which is why this repo diverges from the storage platform rather than repeating it. The discipline that makes it safe is the split: Stripe executes, core decides. A late fee Stripe generated is a late fee that ignores the state cap, and a fee that ignores the state cap is one a tenant's lawyer gets to talk about. The projection is built from webhooks and **reconciled on a schedule with drift alarmed** — the failure mode of an event-sourced read model is silent divergence, and money that silently diverges is the worst bug this system can have.
6. **Notices and evidence get their own milestone (R-051–R-055).** Proof-of-delivery, immutable transcripts and the retaliation guard are uniform requirements across leasing, billing and maintenance. Building them as a milestone rather than per-module is why the eviction packet (R-083) and the deposit disposition (R-071) are later assembly jobs instead of new plumbing.
7. **Leasing after the operating core.** Leasing events happen 10–20 times a year; maintenance happens weekly and rent monthly. Leasing also carries all three external integrations, so it benefits from landing on a stable base — and its simulated adapters (D-7) mean an unavailable screening partner never blocks the build.
8. **Screening's compliance machinery is built even though the provider is simulated (R-060, R-061).** Criteria versioning, order-of-application processing, decision audit and adverse-action generation are the parts with legal consequence, and they are fully testable against a simulator. Swapping in a real bureau later is a driver, not a redesign.
9. **Risk workflows are Phase 2, deliberately, with two exceptions.** Habitability auto-elevation (R-023) and the retaliation guard (R-055) ship in MVP because both are cheap once the comms record exists and both are existential if absent. The rest — abandonment, VAWA, SCRA, ESA, hoarding — are real but rarer, and each is a case file over machinery MVP already built.
10. **Real vendor drivers last (R-093).** Following the storage build's precedent: partner-gated integrations are the wrong thing to be blocked on in a learning project, and a simulator with fault injection is a better test fixture than a sandbox you don't control.

## Flagged gaps & conflicts

Unlike the storage backlog at the same stage, this list is short — there is one master PRD rather than six, so most cross-document conflicts don't exist. What remains:

1. ~~**Autopay and partial payments are mutually exclusive under D-11 (OQ-11) — this blocks Milestone 3.**~~ **RESOLVED 2026-08-10 (D-29).** The owner took option (a): collection method is a per-payer column on `LeasePayer`, autopay tenants on `charge_automatically` and partial-paying tenants on `send_invoice`, switched as their circumstances change. Both Must stories ship in full. Milestone 3 is unblocked. **What moved from a decision to a build problem**: a mid-cycle switch has to settle or explicitly void the open invoice on the old mode before the new mode's subscription bills, or the tenant is asked for the same rent twice — R-037 owns proving that with a test.
2. ~~**Section 8 phasing (R-048).**~~ **Schema half closed 2026-08-01 (D-13).** R-002 shipped the payer-agnostic ledger, so the expensive retrofit this risk warned about can no longer happen. The remaining risk is only phasing: PAY-13 is Must-if-present / Should-if-not and OQ-2 answered "unsure", so R-048 is still unschedulable. That is a planning question now, not an architectural one.
3. ~~**Offline tech flow vs all-vendor operation (R-028).**~~ **RESOLVED 2026-08-06 (D-17).** OQ-3 answered "all external vendors", so R-028 moved to Phase 3 and Milestone 2 shortened by an L exactly as anticipated. R-030's listed dependency on R-028 is soft — verify-and-close needs work orders reaching WORK_COMPLETE, which R-025's vendor path already does — so R-030 is not blocked.
4. **Screening cannot start without written criteria (OQ-6).** This is a hard block, not a preference: R-060's entire fair-housing posture is "apply documented criteria uniformly and prove it." Absent criteria there is nothing to version.
5. **Spanish surface may be Must, not Phase 3 (OQ-9).** R-096 is sequenced as Phase 3 on the assumption of a predominantly English-speaking tenant base. If that is wrong, it moves into Milestone 2 alongside the portal shell, because retrofitting localization after fifty templates exist is the expensive order.
6. **Deposit-deadline automation is a liability if the config is wrong (R-071, R-010).** The system enforcing a countdown means the system can enforce the *wrong* countdown at scale. Legal review of each jurisdiction config before activation is a release gate, not a nice-to-have — recorded here because it is a process requirement no code review will catch.
