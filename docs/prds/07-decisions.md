# Decision Log — Single-Family Rental Operations Platform

**Status:** D-1 through D-10 recorded 2026-07-31 at project setup, carrying forward the conventions proven on the self-storage build. **D-11 (2026-07-31) supersedes D-2** and deliberately diverges from the storage platform. **D-13 (2026-08-01) settles the two-payer ledger shape** and closes OQ-12; OQ-2 is partially answered and R-048 remains gated on the rest of it. **D-14 (2026-08-03) settles file/photo storage** for R-012. **D-15 (2026-08-05) settles notification delivery providers** for R-016 and flags the 10DLC registration as the owner's longest external lead time. **D-16 (2026-08-05) amends "single-use" for vendor links only** — they are multi-use until expiry, for R-025. **D-17 (2026-08-06) answers OQ-3** — all external vendors, so R-028 defers to Phase 3. Open owner decisions are listed under "Needs owner decision"; several gate specific milestones.
**Precedence:** This document amends the PRDs. Where a PRD conflicts with a decision below, this document wins. When building from the backlog, treat each decision as settled — do not re-open unless the owner amends this log.

| # | Topic | Decision | What it means for the build |
|---|---|---|---|
| D-1 | Tech stack | **Same stack as the self-storage platform.** | Next.js (App Router) + TypeScript, Postgres + Prisma, Auth.js, Tailwind + shadcn/radix, Stripe, Resend, Vitest + Playwright + axe + Lighthouse CI, Vercel. Monorepo `apps/web` + `packages/core` + `packages/db`. Rationale: this is a learning project and the second repo in the same idiom — reusing the stack means the second build reinforces the first instead of splitting attention across two ecosystems. Patterns (facility→property scoping, RBAC-as-data, audit service, event outbox) transfer nearly one-for-one. |
| D-2 | ~~Stripe Billing vs ledger-driven charges~~ | ~~Ledger-driven PaymentIntents.~~ **SUPERSEDED BY D-11 (2026-07-31).** | Retained for history only. Do not build to this row. The original reasoning — that jurisdiction-configurable fees, proration and allocation are ours — survives into D-11; what changed is where the invoice and payment record lives. |
| D-3 | Money and time representation | **Integer cents; UTC in the DB, property-local timezone for display.** | No floats anywhere in money math. Every nightly job (charge posting, late-fee assessment, notice countdowns) runs per-property in property-local time — a late fee assessed at UTC midnight is a wrong late fee in every timezone west of London. |
| D-4 | Jurisdiction rules | **Versioned, effective-dated configuration. Texas seeded as the default; built per-state configurable.** | `JurisdictionRule` rows hold grace days, late-fee type/caps, deposit maximums and disposition deadlines, entry-notice hours, notice periods, payment-allocation order, just-cause flags, application-fee caps, RUBS permissibility. Adding a state = adding a reviewed config record + lease template, not a release. Rules apply prospectively; changing a rule never rewrites history. All legal artifacts are drafts requiring attorney review before real-world use — this is a learning project, not legal advice. |
| D-5 | Superuser accounts | **No permission bypass. The `owner` role with an all-properties assignment is the only super user.** Mirrors storage D-12. | Never add an `isSuperuser` flag or a short-circuit in `can()` / `propertyScope()` / `checkMonetaryAuthority()`. Unrestricted access is an ordinary assignment row with `roleId = owner` and `propertyId = null` — grantable, revocable, visible to the audit log. Bootstrap via a `db:create-owner` script scoped to R-007. |
| D-6 | External vendor access | **Zero-login magic-link work orders. No vendor accounts in v1.** | A vendor receives a signed, expiring link by SMS or email carrying scope, photos, address, tenant contact, and a per-work-order code reveal. Accept / propose time / upload completion photos / upload invoice all happen without an account. Rationale: the operator interview was unambiguous — a plumber with five landlord clients will not learn a sixth portal, and a maintenance module vendors won't touch has no data in it. Optional accounts for high-frequency vendors are Phase 3, never a prerequisite. |
| D-7 | Screening, e-sign, and listing syndication vendors | **Simulated adapters first, real drivers behind a partner agreement.** Follows the storage build's gate-hardware precedent (D-4 there). | `SimulatedScreeningAdapter`, `SimulatedESignAdapter`, and `SimulatedSyndicationAdapter` are the primary adapters for this build and the contract fixtures for future real drivers, including fault injection. Screening in particular is FCRA-regulated and partner-gated; a simulated provider lets the compliance workflow (criteria versioning, adverse-action generation, decision audit) be built and tested for real without a bureau relationship. Real drivers are Phase 3. |
| D-8 | Mobile strategy | **PWA, not native, for v1.** | One codebase; no app-store install wall in front of tenant activation; magic-link deep linking works cleanly. The tech's job flow gets service-worker offline caching with an IndexedDB write queue. Revisit native only if field data shows PWA offline limits being hit (R-089). SMS is the reliable notification backstop for tenants and vendors regardless. |
| D-9 | One task queue | **A single `Task` entity serves every staff work queue.** Carried over from the storage build, where seven queues had to be consolidated after the fact (its B-095). | Facility→property, type, subject entity, property-local business date, priority, assignee (nullable = anyone), status, typed proof fields, completion actor, source event id; idempotent creation on (type, entityId, businessDate). Delinquency steps, failed payments, move-out verification, inspection follow-ups, compliance items, bounced messages, and vendor no-shows are all *views* over this. Built early (R-011) precisely so nothing later invents its own. |
| D-10 | Tenant-facing lexicon | **One word per concept on every tenant-facing surface.** Mirrors storage D-15. | **home** or **unit** (not "door"), **rent**, **maintenance request** (not "ticket" or "work order" — those are staff words), **balance**, **payment**, **inspection**. Industry words — delinquency, disposition, dunning, chargeback, work order, ledger — stay on staff screens. No internal identifier (backlog ID, entity name, status enum) may render on a tenant- or vendor-reachable route, and a test asserts it. One money formatter for tenant-facing amounts. |
| D-11 | **Billing engine — supersedes D-2** (owner call, 2026-07-31) | **Stripe Billing drives recurring rent, and Stripe is the source of truth for invoices and payments.** | Each active lease gets a Stripe Customer, a Price at the lease's rent amount, and a Subscription anchored to the lease's billing day in **property-local** time (D-3 still holds — the anchor is computed locally, then expressed to Stripe). Stripe owns: invoice generation, collection, retries, ACH-return handling, invoice PDFs, and the payment record. Our `LedgerEntry` table becomes an **append-only projection** built from Stripe webhooks — still the audit surface, still exportable, but it no longer originates charges. **Deliberate divergence from the storage repo**, which stays ledger-driven per its D-6: the two projects now demonstrate the two real billing architectures, which is worth more in a learning portfolio than a second identical implementation. |
| D-12 | What stays in `packages/core` under D-11 | **Every jurisdiction-dependent calculation. Stripe executes; it does not decide.** | Late-fee amount **and its state cap clamp**, proration math for move-in/move-out, RUBS allocation, payment-allocation policy, `daysPastDue`, and deposit accounting are computed in core and pushed to Stripe as invoice items or subscription updates. Stripe has no concept of "clamp this fee to the Texas maximum," and a fee it generated is a fee we cannot defend. Rule of thumb: **if a statute could change the number, core computes it.** |
| D-13 | **Two-payer ledger shape** (owner call, 2026-08-01 — answers OQ-2, confirms OQ-12) | **The ledger is payer-agnostic from the schema up, but no voucher feature is built until OQ-2 has a firm answer.** OQ-2 was answered "unsure / possible later". | Section 8 is neither confirmed nor ruled out, so R-002 takes the low-regret option: build the shape, defer the features. Two tables carry it. `LeasePayer` is one payer of one lease — one Stripe Customer and one Subscription each, which is what OQ-12's "an invoice has exactly one payer" forces. `PayerAllocation` splits every `Charge` across the lease's payers, so the lease still shows **one balance**. An ordinary lease is exactly one `LeasePayer` carrying the whole charge: **one code path, not two** — allocation always happens. Nothing in the schema knows what a housing authority is beyond a `PayerType` value. The voucher *features* — recertification proration, bulk HAP deposit reconciliation across many tenant ledgers, abatement, HQS re-inspection deadlines, and "dunning never pursues the HAP portion" — remain **R-048's and remain gated on OQ-2**. Rationale: the schema affordance costs a column and a join; retrofitting a split payer onto a single-payer ledger costs every financial query and a data migration. |
| D-14 | File/photo storage (R-012) | **A `StorageAdapter` interface, backed by local disk in dev/test; a real object store (Vercel Blob/S3-class) is a same-interface swap, not a rewrite, whenever this deploys somewhere the filesystem isn't durable across instances.** | Unlike D-7's simulated adapters, storage is not partner-gated or regulated — it is commodity infrastructure (master PRD §7: "Buy") that can run for real against disk today. `LocalDiskStorageAdapter` writes under `DOCUMENT_STORAGE_PATH` (default `.data/documents/`, gitignored); every caller goes through `StorageAdapter.put/get/delete`, never `fs` directly, so the swap touches one file. EXIF `capturedAt` is extracted and stored distinct from `createdAt` and is never stripped — a photo's timestamp is the evidence (PROP-08, master PRD §5). Client-side compression and resumable/chunked upload (both named in DOC-01 and §6.5) are deliberately NOT built yet: server-side single-shot upload via a plain file input covers correctness for typical photo sizes on a decent connection; the mobile-3G stress case is a real gap, left for whoever revisits upload UX with field data in hand. |
| D-15 | Notification delivery providers (R-016) | **A `ChannelAdapter` interface with a logging adapter wired today; Resend and Twilio are a same-interface swap, blocked on external workstreams the owner has to start — a verified sending domain and an approved 10DLC campaign.** Every other guarantee of the engine — idempotency, preference resolution, quiet-hours deferral, kill switch, sandbox redirect, the append-only log — is built and tested for real against the logging adapter. | The same shape as D-14, arrived at for a different reason. Storage is commodity infrastructure that *could* run for real against disk on day one, so D-14 ran it. Email and SMS are equally commodity but *cannot* run at all yet: Resend needs a domain with SPF/DKIM published, and Twilio will not deliver to a US mobile number without an approved 10DLC brand and campaign, which takes days to weeks. Writing an HTTP client that has never once been executed — against an account that does not exist — produces code that looks finished and is untested, which is strictly worse than an honest seam. So the seam is the deliverable, and the two provider drivers are a later, small, genuinely-testable change to one assignment in `apps/web/lib/notifications/provider.ts`. **Two controls make an enabled-by-default engine safe in the meantime and are not optional in any non-production environment**: `NOTIFICATIONS_ENABLED=false` stops all sending without a deploy (notifications are still decided and recorded as suppressed, so nothing is lost), and `NOTIFICATIONS_SANDBOX_TO` redirects every send to one address while preserving the intended recipient on the record — which is what prevents a laptop pointed at a copy of production data from texting real tenants. **The 10DLC registration is the long pole and should be started now**, per the backlog's own note; nothing in the code can shorten it. |
| D-16 | **Vendor magic-link lifetime (R-025)** — amends "single-use" in ROLE-05, §6.1, §7 and §13 | **A vendor work-order link is scoped, expiring and revocable, but MULTI-USE until it expires — not single-use.** Single-use remains correct and unchanged for every other token purpose (tenant magic link, both password resets, the MFA challenge). | The PRD's "single-use" is right for a token whose job is to **establish** a longer-lived credential: a tenant magic link is redeemed once and becomes a session, so burning it costs the user nothing. A vendor link **is** the credential — there is no account and no session behind it (D-6) — so burning it on first click means the plumber who opened the text at 7am to accept the job cannot reopen it at 4pm to upload the invoice. That is not a hypothetical edge: accept-now / photograph-the-work-later / photograph-the-napkin-invoice-later is the *only* real vendor workflow, and it spans hours or days by nature. A single-use link would fail on the second step of every single job, and "the vendor phones the invoice in instead" is precisely the outcome D-6 exists to prevent — the backlog's own line for R-025 calls this item "the difference between a maintenance module vendors use and one they ignore." **The alternative considered and rejected** was single-use redemption into a work-order-scoped session cookie: it preserves the letter of "single-use" but dies on a cleared cookie, a second device, or simply re-clicking the original SMS days later, which is the most likely way a vendor returns. **What replaces single-use as the control set**, all of it built: the token is scoped to exactly ONE work order and grants nothing else anywhere in the product; it expires (3 days, `TOKEN_TTL_MINUTES.VENDOR_WORK_ORDER`); reissuing revokes every prior token for that work order, so a link believed compromised is killed by resending; every privileged action taken through it is audited with the vendor named as actor; and each access-code reveal is logged individually (PROP-03). Worth stating plainly: the status quo this replaces is an operator texting the address, gate code and tenant's phone number in cleartext with no expiry, no scope and no audit trail whatsoever — the link is strictly better than what it is displacing, on every one of those axes. |
| D-17 | **OQ-3 answered: all external vendors** (owner call, 2026-08-06) — closes OQ-3 | **There is no in-house maintenance tech. Every job goes to an external vendor, so the zero-login magic link (D-6, R-025) IS the entire maintenance edge.** R-028's offline tech PWA moves to Phase 3. | The backlog's own risk note 3 anticipated this: "If OQ-3 answers 'all external vendors,' R-028 is a Phase 3 item and Milestone 2 shortens by an L." That is what happened. **Why deferring is right rather than merely cheaper**: R-028 exists for somebody on payroll standing in a basement with no signal — cached job list, IndexedDB-queued status changes and photos, sync on reconnect. A vendor holding a magic link needs none of it: the link works on any phone, needs no install, and if signal drops they reload it later, which is exactly the multi-use behaviour D-16 already guarantees. Building a service worker, an offline queue and a sync-conflict policy for a persona that does not exist would be the most expensive wrong turn available in this backlog. **What this does NOT defer**: MAINT-06's "required completion photo" is already built into R-025's vendor upload, and R-030's verify-and-close depends on work orders reaching WORK_COMPLETE, which the vendor path already does — so R-030's listed dependency on R-028 is soft and does not block it. **Reversible**: if an in-house tech is ever hired, R-028 returns as a self-contained item; nothing built since R-024 assumes its absence, and `assignWorkOrder()` still supports staff assignment because a manager doing a job themselves is not the same as a maintenance employee. |
| D-18 | **After-hours routing without an answer to OQ-10 (R-029)** | **The rota is self-service and the fallback is to page EVERYBODY. Nobody on call for a property means an emergency there reaches every staff member who can act on it — narrowing is earned by somebody taking responsibility, never assumed.** Escalation fires at everyone not already paged, all at once, 15 minutes after an unacknowledged emergency; there is no ranked, tier-by-tier chain and no priority column behind one. | OQ-10 ("who is on-call today, and what is the actual protocol?") is still open, and the backlog is right that "the escalation chain encodes a real human arrangement; inventing one produces an escalation path nobody answers." So R-029 deliberately encodes **no** arrangement. It provides the mechanism — a window a person opens on themselves, an acknowledgement that stops the clock, a chain that reaches everybody else — and lets the operator's real protocol be whatever they actually do. **Why the fallback pages everybody**: this is R-026's commitment held. R-026 measured that paging every `ticket.write` holder was making emergency submission slow and recorded that capping recipients "would be a safety regression dressed as a performance fix: paging one person too few during a gas leak is not symmetrical with one too many." A rota narrows that set honestly, because somebody volunteered; a lapsed or never-configured rota must not, and the basis (`on_call_rota` vs `no_rota_paging_everyone`) is recorded on every escalation so a gap reads as a gap rather than as a working rota. **Why no tiered chain**: MAINT-12 says "page → SMS → call → backup", and three of those four are channels the notification engine already sends simultaneously. A rank column would have ordered a list that is paged all at once — a field somebody would maintain believing it changed something. Build tiering the day escalation actually waits between tiers, and not before. **Why self-service only**: putting yourself on call is volunteering, not a privilege, so it needs no permission; putting somebody *else* on call is a privilege and this cannot do it — the row written is always the actor's own. A rota screen where one person schedules the team is a real thing to want and needs a real permission. **Re-ask OQ-10 anyway** before assuming the chain matches reality: if the true protocol is "the owner's phone, always, and the PM only during business hours", that is a scheduling UI on top of this mechanism, not a change to it. |
| D-19 | **Verify & close: where the money is entered, and who owns a reopen (R-030)** | **The cost of a job is typed ONCE, on the work order, and every downstream reader computes from that row — there is no maintenance-spend table and never will be one.** A tenant's verification is its own append-only-in-practice row carrying **the vendor who did the work, captured at the moment of the answer**, not read off the work order later. Closing is refused over a live "not fixed" and permitted without any answer at all, with the absence recorded. | Three things, each of which a later session could plausibly get wrong. **(1) One place for the money.** The backlog calls the WO → invoice → P&L chain "the specific place owners abandon software", and the reason it breaks elsewhere is always the same: somebody types the invoice total a second time into the books, the two copies drift, and neither can be trusted again. So `closeWorkOrder()` writes `invoiceCents` onto the work order and the property's maintenance spend is a projection of those rows through `jobCostCents()` — the same function R-026's re-approval check uses, so "what did this cost" cannot mean two numbers on two screens. R-042's QuickBooks export must MAP from these rows, not ask anybody to re-enter them. **(2) The vendor is captured on the answer.** MAINT-07 says reopen rate is tracked "from day one" and this is what that costs: a reopened job is normally reassigned, so by the time anybody runs a vendor report the work order names whoever eventually fixed it. Reading attribution off the work order would credit the second vendor with the first one's failure, and once the column has moved no later analysis can recover it. `WorkOrderVerification.vendorId` is written when the tenant taps, and the reopen-rate report reads only that. There is a test that reassigns mid-flow and asserts the failure stays with the first vendor. **(3) The two close refusals are asymmetric on purpose.** A tenant's current-round "no" blocks the close outright — a live complaint recorded as resolved is what a tenant later shows a judge, and it outranks every bookkeeping concern in the ordering. But *silence* does not block: a tenant who never replies must not be able to hold a work order open forever, so the close proceeds and `workorder.closed` records `unverified: true`, which the work order page then states permanently rather than as a toast. A $0 close needs an explicit "this job cost nothing", because a job closed at nothing by accident is a hole in a property's cost history nobody notices until a year of spend is quietly wrong. **What R-030 deliberately did NOT build**: the `INVOICED` status and MAINT-09's vendor-visible invoice lifecycle (received → approved → paid) belong to R-079's vendor management, and the chargeback that a `tenantCaused` close makes possible is R-031's — this item sets the flag and posts nothing to any ledger. |
| D-20 | **Work-order comms threading: what stays continuous, what gets scoped, and why an "attach" cannot be an update (R-032)** | **The tenant's thread stays the ONE continuous conversation per tenancy R-017 built (COMM-01); individual Messages are tagged with `ticketId`/`workOrderId` at send time instead. The vendor gets a genuinely SEPARATE, work-order-scoped thread (`vendor:<id>:workorder:<id>`), alongside the unchanged property-scoped thread inbound SMS already routes into. Attaching an existing untagged message to a job afterward is a new `WorkOrderMessageLink` row, never an update to the message.** | Three calls, each forced by something already built. **(1) The tenant thread does not fragment per ticket.** COMM-01's whole point was one continuous history, and re-litigating that to satisfy COMM-06 would break the earlier decision instead of building on it. Tagging individual messages is additive and costs nothing when absent. **(2) The vendor DOES get a new per-job thread**, and this is not the same call - D-16 already scopes a vendor's magic link to exactly one work order, and a vendor with two concurrent jobs for this operator (ordinary for a preferred trade) must not have both conversations arrive in one mixed thread. `threadKey()`'s VENDOR case now branches on whether a `workOrderId` is supplied: absent, the key is unchanged (`vendor:<id>:property:<id>`, still what inbound SMS resolves to, since a text has no way to say which job it is about); present, it is a new, different key. Nothing that existed before this item changes shape. The new thread is PORTAL-only by construction - offering SMS on it would let a vendor's reply land in the OTHER (property-scoped) thread instead, silently splitting one conversation into two nobody reads together. **(3) "Attach this stray message to a job" cannot touch the Message row**, discovered only after building it the update way first: `Message` has been append-only since R-017 (`Message_append_only` rejects every UPDATE, not only DELETE), so `Message.ticketId`/`workOrderId` are `RESTRICT`, not the Prisma-default `SET NULL` - a `SET NULL` cascade would require exactly the UPDATE the trigger exists to refuse. `WorkOrderMessageLink` is an insert-only join table instead: a message that already existed gets a new row pointing at the job, and the original evidence is never rewritten. The unique constraint on `messageId` is what keeps a message from being claimed by two jobs at once. |
| D-21 | **The lease status machine, and what "notice given" is (R-033)** | **A guarded transition machine in `packages/core/leases`, and NO `NOTICE_GIVEN` status.** Notice is a fact about a running lease (`noticeGivenAt` + `noticeGivenBy`), never a state of it. Nothing leaves ENDED or TERMINATED: a tenancy that restarts is a NEW lease. TERMINATED demands a stated reason, enforced at the audit writer (`lease.terminated` sits in `REASON_REQUIRED`), not merely in a form. Activating a lease occupies its unit immediately; ending one frees it. | **Why a machine rather than a status column.** Everything downstream keys on lease status: R-034 opens a Stripe subscription when a lease activates, R-035 posts rent against a lease in force, R-062 computes notice periods, R-071 starts a deposit clock at move-out. A column anybody can assign means each of those has to defend itself against states that should have been impossible - naming the legal transitions once is what keeps that from becoming fourteen separate problems. **Why no NOTICE_GIVEN status.** A tenancy under notice is still a live tenancy: rent is still due, repairs are still owed, entry notice still applies. As a status it would have to be remembered by every "is this lease in force?" check in the product, and the one that forgot would stop charging rent to somebody who still lives there. The backlog's "pending / active / notice-given / ended" describes what a PM sees on screen, and that is exactly how it renders - a notice badge ALONGSIDE the status pill, never instead of it. **Why nothing reopens.** The old lease's dates, rent and signatures are the evidence R-071's deposit disposition is defended with; editing them to mean something else destroys it. **Why the unit is coupled here.** R-009's `unit.auto_make_ready` job already anticipated this item taking over ("the process that activates the new lease owns what happens to the unit next"); it remains the backstop for a fixed term that simply runs out with nobody touching it. The MAKE_READY transition is guarded on the unit still being OCCUPIED, so a unit somebody marked DOWN for a renovation is not quietly reclassified as ready to turn. |
| D-22 | **Inherited tenancies: the gaps are tracked, never defaulted away (RISK-08, R-033)** | **An INHERITED lease starts with `depositTransferStatus: UNKNOWN`, never the `NOT_APPLICABLE` column default, and `inheritedGaps()` treats NOT_APPLICABLE on an inherited lease as an unanswered question rather than a resolution.** The three outstanding items - estoppel confirmation, deposit position, condition-as-found baseline - are raised as ordinary `Task` rows in the one queue (D-9), daily, until somebody answers. | RISK-08's own framing: "you cannot charge move-out damage against a baseline never captured." All three gaps share a shape - cheap to establish in the weeks after closing, expensive-to-impossible later, and costing nothing until the day they cost a lot. The seller stops answering, the tenant stops remembering, and damage that was already there becomes damage nobody can prove was already there. **The single most dangerous line of code in this item is a default.** `depositTransferStatus` defaults to NOT_APPLICABLE because that is correct for the ordinary path, where we collected the deposit ourselves - so an inherited lease that inherited the default would sit looking settled while representing an unquantified liability. In most states the current owner owes the deposit back at move-out whether or not the seller ever handed it over. Both the create action (which sets UNKNOWN explicitly) and the gap check (which counts NOT_APPLICABLE as unanswered) defend against it, deliberately twice. **"The seller kept it" is an ANSWER, not an absence** - we are very likely still liable either way, and what matters is that somebody established the fact and when. **Why Tasks rather than a checklist table**: D-9 forbids a second queue, and a PM should find "confirm the terms with Grant Okafor" beside "triage this ticket", not on a separate screen they have to remember exists. They are raised URGENT and re-raised daily while outstanding: going quiet after one ask is exactly how this survives to move-out. |
| D-23 | **Stripe is not reachable yet, so the seam is split by DIRECTION (R-034)** — **outbound half AMENDED by D-26 (2026-08-10)** | **The INBOUND half — signature verification, the idempotency claim, and the webhook → projection pipeline — is built for real and tested exhaustively. The OUTBOUND half — create Customer, Price, Subscription, SetupIntent — is a `BillingProvider` interface with a SIMULATED adapter behind it, and no Stripe HTTP client is written until there is an account to call.** | This deployment has no Stripe credentials: the multi-LLC KYB the backlog flags at R-001 has a real lead time and has not cleared. D-15 already decided what to do in this exact situation for Resend and Twilio — "writing an HTTP client that has never once been executed, against an account that does not exist, produces code that looks finished and is untested, which is strictly worse than an honest seam" — and R-021 already drew the line that makes the split principled: a RECEIVER can be exercised completely with synthetic requests, so leaving it unbuilt would mean shipping a public money-writing endpoint later, with no test coverage, under time pressure. **The simulator is not a stub.** It mints ids in Stripe's own shape (`cus_`, `sub_`, `price_`, `seti_`, with the `_secret_` suffix a client secret carries) precisely so every column, index, log line and screen downstream is exercised against realistic values — an id shaped like `sim-1` would let a length or prefix assumption survive until the day real ones arrive. Because they are indistinguishable by inspection, **the label is the only thing that tells them apart**, so `billingProvider.name` is on the audit entry for every provisioning and the lease screen says "not real Stripe" in as many words. It holds no state of its own: whether a customer exists is a question about our database, and a simulator with its own registry would answer a different question than production would, which is how a simulator starts lying. **What wiring the real thing costs**: one assignment in `lib/billing/provider.ts`, plus a class implementing four methods. |
| D-24 | **Webhook idempotency is a claim-before-project INSERT (R-034, D-11)** | **The `ProcessedStripeEvent` insert happens BEFORE the projection, and the Stripe event id is the PRIMARY KEY.** A duplicate delivery loses on the key and stops. Every outcome we have decided about — projected, ignored, duplicate — returns 2xx. | Stripe retries a webhook until it gets a 2xx and explicitly does not promise exactly-once delivery. Under D-11 `LedgerEntry` is append-only, so a double projection is money that cannot be corrected by editing — only by a reversing entry somebody first has to notice is needed. **Claiming before projecting is the whole guarantee**: claiming afterwards leaves a window in which two concurrent deliveries both project, and a check-then-write leaves the same window with extra steps. Making the event id the primary key rather than a surrogate with a unique index is what makes the INSERT itself the lock. There is a test that fires three concurrent deliveries of one event and asserts exactly one projects. **Why ignored events still return 200**: Stripe retries anything else forever, and a dashboard full of red for events we deliberately did nothing with is a monitoring surface nobody will trust. They are recorded with a stated reason, because "we saw it and chose to do nothing" is an answer silence cannot give. **The handled-event list is deliberately closed and short** — `charge.*` is excluded alongside `invoice.*` specifically because projecting both would double-count every payment. |
| D-25 | **What the reconciliation can and cannot see (R-035, D-11)** | **The nightly sweep compares the ledger projection against OUR OWN processed-event log, and deliberately does NOT compare amounts.** It catches a projection that was lost, a ledger row naming an unknown event, and a claim the pipeline died inside. It is blind, by construction, to an event Stripe sent that we never received. `externalChecked` reports which comparison ran, on the record and on screen. | The backlog asks for the projection to be "reconciled against Stripe on a schedule with drift alarmed, because a projection nobody checks is a lie with a timestamp." Two comparisons would satisfy that sentence and only one is possible today (D-23: no Stripe account). **Why no amount comparison internally**: `Payment.amountCents` and `LedgerEntry.amountCents` are both written from the same event, in the same transaction, by the same code — checking one against the other would always pass and would *look* like coverage, which is worse than an absent check because somebody would trust it. The comparison exists in `detectDrift` and takes an amount; the internal caller passes null and an explicit `expectsLedgerRow` flag instead, so the most serious drift — a lost projection — is still caught without one. **Why the blindness is stated rather than glossed**: a webhook that never arrived leaves no trace on our side at all, so no amount of internal checking will ever find it. Only asking Stripe what it thinks it sent will. Saying "reconciled" without that qualifier is how a check stops being trusted the first time somebody discovers what it never looked at. **Drift is alarmed into the append-only audit trail**, not merely logged, because a projection that has drifted cannot be quietly corrected — the fix is a reversing entry somebody has to decide on — so the discrepancy needs the same permanence as the rows it is about. |
| D-26 | **A real Stripe driver, TEST MODE ONLY** (owner call, 2026-08-10) — amends D-23's outbound half | **`StripeBillingProvider` is written and wired, and it REFUSES a live key at construction.** The provider is selected by whether `STRIPE_SECRET_KEY` is set: with a test key, this talks to Stripe; without one, to the simulator. There is no third mode and no override flag. | D-23 deferred the outbound driver on D-15's reasoning — "an HTTP client that has never once been executed, against an account that does not exist, produces code that looks finished and is untested." That premise held while there was no account. A live Stripe account (`Lab Intelligence LLC`) turned out to be reachable, and the owner's call is that this product may use it in **test mode**. A driver that CAN be executed is a driver worth writing, so the objection is answered rather than overridden — and the simulator stays, because a deployment with no key still needs to work. **The live-key refusal is not a warning, it is a throw.** The difference between `sk_test_` and `sk_live_` is four characters in an environment variable that gets copied between machines by hand, and a live key reaching this code would move real money on a real business's account. A refusal at construction is the only control that survives a careless copy; it also means a live key does not silently fall back to the simulator, which would let somebody believe billing was running when it was not. **Every write carries an idempotency key derived from the FACT, never randomly** — `customer:<payerId>`, `subscription:<payerId>`, `price:<leaseId>:<amount>:<currency>` — because the most likely network failure is a request that times out *after* Stripe processed it, and a retry without a stable key creates a second subscription that bills the tenant twice every month thereafter. **Not the `stripe` SDK**: eight endpoints, form encoding and one header do not justify the dependency; if the surface outgrows one file, take the SDK — that is a real threshold, not a prejudice. |
| D-27 | **The simulator reports what it was TOLD, not what the lease says (R-036)** | **`LeasePayer.stripeAmountCents` records the rent we last successfully pushed, and the simulated provider reports that.** It is written only after the provider accepts the change, and cleared on cancellation. | Found by a test that should have passed and did not. The simulator's `getSubscription` originally read `lease.rentCents` — our own current row — which made it agree with the lease *by construction*: a rent change could never be detected, and the whole `update_price` path was dead code that would only ever have run against real Stripe. Reading a record of the last INSTRUCTION reproduces the one behaviour that actually matters about an external system: **it remembers what it was told, and disagrees when we have not told it something.** The column is not a duplicate of Stripe's truth — Stripe remains the authority under D-11 and the sweep still asks it — it is the answer to a different and cheaper question, "have we pushed this change yet", available without an API call. Written after the push and not before, because writing it first would leave us believing we had pushed a change that failed. |
| D-28 | **A demo checkpoint is walked when its milestone closes, not when it is convenient (2026-08-10)** | **Every "Demo checkpoint" line in `06-backlog.md` is an acceptance gate for the milestone above it, and gets walked end to end — tenant, vendor and PM, one sitting — before the next milestone's items start.** Findings become backlog items in the milestone that owns them, not TODOs. | Checkpoint 1 was defined after R-032 and first walked after R-036, four items later. It did not run. `VERIFIED` was in neither the open-work-order list nor the vendor link's actionable set, so the tenant's one tap removed the job from every screen and killed the vendor's link *before the invoice arrived*; nothing ever closed the originating `Ticket`, so every later text from that tenant threaded onto the fixed job forever; the entry window was parsed in the server's timezone and rendered in the property's, so a **served legal notice stated hours nobody intended to keep**; and the re-approval ceiling was wired only to the rare hand-typed path, so a vendor's invoice closed a job at any multiple of the approved amount. **Every one of the four was invisible from inside the item that introduced it and obvious the moment somebody followed one job through.** They are also all seam defects — a status added to an enum and not to the two lists that read it, a lifecycle that ends in one table and not the other, a value crossing a timezone boundary, a check with one caller. No amount of per-item testing finds those, because each item's own tests are written from inside its own seam. The walk is cheap; four defects reaching a demo is not. |
| D-29 | **Collection method is PER PAYER, and it switches** (owner call, 2026-08-10 — answers OQ-11) | **`LeasePayer` carries the Stripe collection method.** A payer on autopay sits on `charge_automatically`; a payer who needs to pay in parts sits on `send_invoice`. Staff move a payer between them, and the move is an audited event with a stated reason. Both PAY-02 (autopay) and PAY-03 (partial allocation) ship in full — for the payers on the mode that supports each. | Stripe cannot do both on one subscription: [partial payments are not supported for automatically-charged subscriptions](https://docs.stripe.com/invoicing/partial-payments). Both stories are Must, so any option that picks one globally demotes the other. Option (b) — autopay wins, partials recorded as offline payments — was rejected because it makes partial payment a staff-mediated path, and **the tenants most likely to pay in parts are the ones least able to reach an office during business hours**; a Must story satisfied only by somebody else typing it in is not satisfied. Option (c) — `send_invoice` everywhere, driving collection ourselves — is most of D-2 rebuilt behind a Stripe façade, and would be a reason to re-open D-11 rather than a way to honour it. **Why per payer rather than per lease**: D-13 already puts two payers on a voucher lease, and the housing authority's half and the tenant's half have genuinely different collection needs. **The switch is the feature, not an escape hatch** — a tenant going onto a payment plan after a bad month is exactly the moment the mode changes, and R-047's legal-action controls already need per-payer billing state, so this is a column that was coming regardless. **What it costs, stated plainly**: two collection modes means two webhook shapes to reason about, and a mid-cycle switch has to be safe — the open invoice on the old mode must be settled or explicitly voided before the new mode's subscription bills, or a tenant is asked for the same rent twice. That is R-037's hardest test, not a footnote. **The tenant-facing payment UI is ours under every option**, because Stripe's Hosted Invoice Page cannot take a partial payment at all. |
| D-30 | **The United States is the only market** (owner call, 2026-08-10) | **US tenants, US properties, US dollars, US payment rails.** No multi-currency, no international payment methods, no locale machinery. `usd` stays a literal in the three places it appears rather than becoming configuration, and `Cents` stays a plain integer rather than a currency-tagged type. | Stated because the whole product already assumed it — two-letter state codes on `Property`, ACH as the free rail, a Texas `JurisdictionRule` seed — and nobody had written it down, which is how a later session ends up adding currency scaffolding "for later" against a need that will never arrive. It also settles a real question R-037 ran into: Stripe HAS a retail-cash pattern (Konbini in Japan, OXXO for customers in Mexico) and simply does not offer it for US customers, so R-037a's third-party integration is the only route and no amount of waiting on Stripe changes that. **What this decision does NOT license, and the distinction is the whole point: US-only is not single-jurisdiction.** D-4 stands unchanged — grace periods, late-fee caps, deposit deadlines, entry-notice hours and surcharge legality all vary by state and stay versioned, effective-dated configuration. Texas is the seeded default, never a hardcoded assumption, and R-037b exists precisely because one state's surcharge statute differs from another's. Nor does it touch OQ-9: a Spanish-speaking tenant in Houston is a US tenant, and PRD §6.4's language question is domestic accessibility, not internationalisation. |
| D-31 | **An offline payment is pushed to Stripe, not just recorded here** (R-038, PAY-05) | **A check, money order or cash is applied by marking the payer's open Stripe invoice paid OUT OF BAND.** Stripe stops collecting; the `LedgerEntry` still arrives only through the webhook, so there remains exactly one way money enters the projection. `checkNumber` and `receivedByStaffId` stay on our own `Payment` row. The push happens FIRST — if it fails, nothing is written at all. | The obvious reading of D-11 is that this is an architecture question. It is not: **Stripe is driving collection.** A payer on `charge_automatically` is debited again on the billing day and one on `send_invoice` keeps an open invoice and keeps being dunned, so a check recorded only in our ledger means a tenant who paid gets chased for the same rent and eventually charged twice. Telling Stripe is operationally required, and it happens to be what D-11 wanted anyway. **Why Stripe-first**: a `Payment` row we hold while Stripe goes on debiting is worse than no record at all, because the staff member walks away believing it is handled. **Why the offline detail stays ours**: Stripe cannot hold which numbered instrument arrived or which member of staff took it, and PAY-05 is explicit that a cash payment with no named recipient is an unauditable cash payment — R-002's schema already carried both columns for exactly this. **Why SETTLED and not PENDING**: a check in hand is money received; whether it later bounces is a separate future event (a reversal, which R-039's NSF handling owns), and holding it pending would leave the tenant's balance wrong for days after they paid. **The known limit, stated rather than discovered**: Stripe's out-of-band payment settles the WHOLE invoice, so a part-payment applied this way would forgive the remainder silently. R-038 refuses one and says so in words; R-038a owns it. |
| D-32 | **A returned payment is a REVERSING ENTRY, and the same Stripe event means two different things** (R-039, PAY-02) | **`invoice.payment_failed` is read against our own `Payment` row before it is acted on.** Against a settled payment it is a RETURN: the payment moves to `REVERSED` and a positive `REVERSAL` entry points at the entry it undoes. Against anything else it is a failed attempt, recorded with no ledger movement. A second delivery of the same return is ignored. | ACH Direct Debit gives what Stripe calls "instant provisional access" and takes **up to four business days** to confirm, so everything downstream of a payment runs on money that may not arrive. Stripe emits the SAME event for a first decline and for a return days later — reading only the event they are indistinguishable, and the shipped code treated both as "nothing has changed about what is owed". That left the credit in place on a return, so **a tenant appeared to have paid rent they had not**. PAY-02 names this failure directly: "no downstream action that was triggered by the provisional payment remains incorrectly in effect." **Why the amount comes from the settled payment and not the invoice**: a partial payment that returns must give back exactly what it gave, and an invoice total is a different number. **Why ignore rather than reverse twice**: Stripe promises neither ordering nor exactly-once delivery, and in an append-only ledger a double reversal is money nobody can correct by editing. **What "no stale downstream state" means today**: the balance is currently the only such state — late fees are computed from the ledger on demand (R-035), so restoring the balance restores the correct fee position automatically. When something else keys off a payment — a notice cancelled on payment, most obviously (R-062) — it has to unwind in the same place, and the code carries that reminder. |
| D-33 | **A scheduled fee charges the DELTA, never the cumulative figure** (R-040, PAY-04) | **`lateFeeFor()` answers "what is the total fee owed as of this date"; the nightly job charges that minus what the overdue rent has already attracted.** `Charge.assessedOnChargeId` links each fee to the rent that produced it, which is what makes the question askable. A waived fee still counts as assessed. | The cumulative answer is the right one for a `DAILY` or `FLAT_PLUS_DAILY` rule and is a trap for anything on a schedule: charging it nightly turns a $10/day fee into $10, then $20, then $30 — **$60 owed by day three instead of $30**. Three behaviours fall out of the delta for free, and all three are what you want: a FLAT fee charges once and then produces zero forever; a DAILY fee charges one day's worth each day; and a fee that has hit its statutory ceiling produces zero from then on, because the cumulative figure stops moving — no separate "have we capped out" state to keep. **Never negative**: a waiver leaves more assessed than the cumulative figure allows, and the answer is to charge nothing further rather than to invent a credit, because reversing an over-charge is a deliberate act with a reason on it (PAY-04's waiver), not an arithmetic side effect. **Why a waived fee still counts as assessed**: waiving is a decision to forgive a fee that was correctly charged, not a statement that it was never owed — so excluding it would re-charge the fee the next night and quietly undo the waiver, which is the opposite of what the person who granted it meant. |
| D-34 | **The waiver-pattern report ships with the waiver, and names tenants who were never forgiven anything** (R-040, PAY-04) | **`waiverPatternByTenant()` reports every tenant with an assessed fee, waived or not, ordered by the share waived.** It reports a pattern and never a verdict. | PAY-04 asks for the report and the backlog is explicit that it is "part of the feature, not a later analytic". The reason is fair housing: **waiving fees for some tenants and not others, along lines that correlate with a protected class, is a discrimination pattern regardless of intent.** An operator cannot avoid a pattern they cannot see, so the ability to see it ships with the ability to create it — building the waiver first and the report "later" is precisely how a year of uneven waivers accumulates before anybody looks. **Why it includes tenants with zero waivers**: a report of waivers alone shows only generosity and hides its distribution, which is the opposite of what a review needs — the tenants who were never forgiven anything are half the pattern. **Why a pattern and not a verdict**: an uneven distribution has plenty of lawful explanations. What it must not be is invisible. |
| D-35 | **`invoice.finalized` is the charge half of the ledger** (R-040b, PAY-03; amends D-11's event list) | **A finalized Stripe invoice projects a POSITIVE `CHARGE` entry.** Not `invoice.created` — a draft has not been presented to anybody and can still change. The amount is `amount_due`, not `total`. No `Payment` row is written: a bill is not a payment. | **This closes a bug that made every balance in the product wrong.** R-034 excluded `invoice.finalized` on the reasoning that an unpaid invoice "changes nothing about what is owed that `invoice.payment_succeeded` will not say more precisely later." That is wrong in one word: `payment_succeeded` reports money ARRIVING, never rent becoming OWED, and nothing else in the pipeline said the second thing. The result was a **single-entry ledger** — payments and no charges — so a tenant who paid rent went further into credit every month, and the pay screen told them they were ahead and owed nothing. Balances everywhere are computed from `LedgerEntry` alone, so this was not a display bug. **Why `amount_due` and not `total`**: `amount_due` is what the invoice actually asks the tenant for after any credit balance Stripe has already applied; projecting `total` would put a charge on the ledger the tenant was never asked to pay. A finalized invoice with nothing due is ignored for the same reason. **Why no test caught it**: every fixture in the suite creates `CHARGE` entries by hand, so the tests build a state the application itself could not produce — which is worth remembering as a class of blind spot, not just this instance. |
| D-36 | **Invoiced collection needs an email, and that collides with the non-digital tenant** (R-040d; amends D-29) | **`switchDecision` refuses `send_invoice` for a payer with no email address**, with a message telling staff to add one or keep the payer on autopay. Moving BACK to autopay needs no email — the constraint is one-directional. | Stripe refuses outright: *"Missing email. In order to create invoices that are sent to the customer, the customer must have a valid email."* **Found by executing the driver against test mode for the first time, not by reading documentation** — every unit test asserted we sent the right request, and only Stripe could say the customer was not ready to receive it. That is the argument for the smoke test in `apps/web/lib/billing/smoke.stripe.test.ts`, which is inert without `STRIPE_SMOKE_KEY` and is the only thing that has ever executed this driver. **The collision is the important part, and it is not resolved.** D-10 and R-021 exist for the tenant who has no email and pays by check; D-29 puts partial payments on `send_invoice`; and a tenant who needs to pay in parts is disproportionately the tenant who has no email. So **the person most likely to need a payment plan is the one Stripe will not invoice** — and R-038 refuses offline part-payments too, so today there is no path at all for them. The refusal message says what a staff member can do about it (collect an email, or take part-payments in person and record them when R-038a lands); it does not pretend the gap is closed. |
| D-37 | Blob access level and how the storage implementation is chosen (R-100) | **Uploads go to Vercel Blob with `access: 'private'`, never `'public'`; and the implementation is selected by the PRESENCE of `BLOB_READ_WRITE_TOKEN`, never by `NODE_ENV`.** | Two decisions a later session must not quietly reverse, both inside D-14 rather than replacing it. **Private, because of what is stored.** Blob's better-known mode mints a permanent unauthenticated URL guarded only by an unguessable suffix — a capability URL. This product stores signed leases, identity documents and photographs of the inside of somebody's home; one leaked URL (a support ticket, a browser history, a screenshot) is public for ever, with no revocation and nothing in the audit log to say it was read. `private` keeps every read authenticated against the store's own token, so the routes that serve files carry on doing exactly what they did — authorise, then stream — and a storage swap changes nothing about who may see a document, which is the property such a swap has to have. **Token-presence, because a label can lie.** `NODE_ENV === 'production'` is wrong in both directions: a production build on a laptop would reach the real store, and — the failure R-100 exists to end — a deployed environment whose Blob store was detached would silently resume writing to a per-invocation filesystem and look entirely healthy until somebody opened a photo months later. The environment is asked what it actually has. Local disk stays the dev and test implementation deliberately (D-14's reasoning, unchanged): the suite must not depend on a network round trip or on anybody's Blob quota to assert that an upload worked. `storageDurable` is reported on the cron response so a silent reversion is visible rather than inferred. |
| D-38 | What happens when a legally-required notice cannot be delivered on the tenant's chosen channel (R-040e) | **ANSWERED 2026-08-12 by the owner: BOTH — fall back to another channel automatically AND raise a `Task` for a human to serve it another way. Neither alone is sufficient.** | The case is a tenant whose carrier has an active `STOP` against our number. `entry_notice` is in `LOCKED_CATEGORIES` precisely because it is legally significant and a tenant may not switch it off — but a carrier-level opt-out overrides us entirely, and our own record would still say SENT, because SENT means *the provider accepted it*. A delivery record that can be silently false is the worst defect this product can have, in a build whose stated premise is that the evidence trail **is** the product. **The fallback alone is not enough** because email and portal prove receipt no better than SMS did — they move the problem rather than closing it. **The task alone is not enough** because a notice that could have gone out electronically in seconds should not wait on somebody reading a queue. Doing both means the tenant gets the best available electronic attempt AND a human is told to serve it in a way that holds up: Texas permits written notice, so posting on the door is a real answer, not a formality. The evidence trail must read as a chain rather than a status — *"SMS blocked by opt-out → emailed 14:02 → task raised to post notice"* — because that is the sentence that has to survive being read out in a dispute. **Checked before building: the fallback half was ALREADY TRUE.** `entry.notice` has declared `channels: ['SMS', 'EMAIL', 'PORTAL']` since R-021, so an entry notice already goes out on all three and a blocked SMS already leaves two live channels behind it. There is no fallback mechanism to build, and building one from this decision's text would have added redundant machinery around behaviour the template already had. What R-040e actually owes this decision is the **Task** — the half that is genuinely missing, and the half that matters most, because email and portal prove receipt no better than SMS did — together with a delivery record that says `SUPPRESSED / sms_opt_out` instead of `SENT`. |
| D-39 | **How a single-meter utility bill is split, and who absorbs the vacant unit's share** (R-042, PAY-08; D-4, D-12) | **Three bases and no more — `EQUAL`, `BEDROOMS`, `SQUARE_FEET` — chosen per bill, with the weights taken over EVERY unit at the property and only the occupied ones charged. Occupant count is deliberately not offered. A unit missing the figure the method needs is REFUSED, never estimated.** Gated on `JurisdictionRule.rubsPermitted`, which R-010 has carried since it was written and nothing had ever read. | **Per bill, because the right basis differs by utility**: water tracks occupancy, heat tracks floor area, and an owner billing both on one basis is defending the wrong number for one of them. **Occupant count is the basis most landlords reach for and the one this product will not offer**, because `LeaseTenant` is adults-only by design (LEASE-06): a family of four reads as two, so the honest options were a maintained `occupantCount` column that nobody updates when a baby arrives, or nothing. Billing on a stale count is worse than not offering the basis, and occupant-based RUBS is also the one most likely to be challenged. **The vacant unit's share stays with the owner, and this is the decision that matters most.** Spreading it across the tenants makes somebody's water bill go up because their neighbour moved out — an amount they can neither predict nor do anything about — and it is exactly what several states' RUBS restrictions are aimed at. The remainder is named on the bill (`landlordCents`) rather than silently dropped, so the split still adds up to the bill and a reader can see where the rest went. **Refusal over estimation**: a unit with no square footage cannot be split on square footage, and an invented average puts a number on a tenant's invoice that nobody can defend — the wrong direction to be wrong in for a charge several states regulate. A recorded **zero** is different from missing and is honoured: a studio has no bedrooms and owes nothing on a bedroom split. **Why it is an invoice item and not a subscription item** (contrast with the pet rent in the same build): a RUBS share is a different number every month because the bill is, so D-12 applies in full — core computes it per bill and Stripe is handed a finished figure. **Why the arithmetic is on the charge**: "Water 2026-07-01 to 2026-07-31 — $412.00 × 1,150/4,600 sq ft = $103.00" can be checked against the bill attached to it; "Utility allocation $103.00" has to be taken on trust, and how the number was reached is the first question every tenant asks. |
| D-40 | **Pet rent and flat utility fees are Stripe subscription items, and Stripe may own the repetition** (R-042, PAY-08; refines D-12) | **A `RecurringCharge` becomes a line on the lease's Stripe subscription, with no monthly `Charge` row minted to mirror it. One reconciler — `syncRecurringCharges` — is the only writer, called on add, on end, on provisioning and nightly. Only `PET_RENT` and `UTILITY` may recur.** | D-12's rule is about **statutes**: if a statute could change a number, core computes it. No statute touches $35 of agreed pet rent — it is a term of the contract, exactly like the rent, and the rent has been a Stripe subscription since R-034. The line to hold is that the moment an amount stops being flat it stops being a subscription item, which is why RUBS in the same build is an invoice item instead. **The closed type list is the guard**: a list that admitted `LATE_FEE` would let somebody bill a late fee every month with the jurisdiction cap never once consulted. **No monthly `Charge` row**, because the projection already handles a subscription line with nothing behind it — it lands in the remainder entry beside the rent (R-040b's shape) — and minting rows to mirror what Stripe is already billing would be a second schedule to keep in sync with the first, which is the class of bug D-11 exists to avoid. **One reconciler rather than a push in each writer**, because that is what makes `endsOn` real: a landlord who says the pet rent stops in March has said something no other part of the product would ever act on, and a fee that outlives the pet is money taken from a tenant who agreed to no such thing. Three writers each pushing their own change gives three places our row and Stripe can disagree, and the disagreement is money. **Ending never prorates**: rent bills monthly in advance, so the period already invoiced stands and the line simply stops appearing on the next one — a credit for part of a month is a waiver somebody decides on with a reason recorded, not a side effect of a pet moving out. |
| D-41 | **The completion photo is required, and D-17's claim that it already was is corrected** (R-032b, MAINT-06) | **`markWorkComplete` refuses without at least one live `COMPLETION_PHOTO` on that work order.** Enforced on the server, not only in the page. It is a *refusal with the fix on the same screen*, never a dead end — D-6's rule that a vendor turned away phones the invoice in still governs. The staff-side `markWorkComplete` is deliberately NOT gated: a PM recording what a vendor phoned in is the override, and removing it would strand the job. | **D-17 said this was already true and it was not.** Its exact words, used to justify deferring R-028 to Phase 3: *"MAINT-06's 'required completion photo' is already built into R-025's vendor upload."* R-025 built the *ability* to upload; nothing ever required it. `vendorMayMarkComplete()` only inspected status, so a vendor could take a job to WORK_COMPLETE having photographed nothing — and that is the state R-030 then asks the tenant to confirm against, R-031 charges a tenant from, and a deposit dispute is defended with. For a product whose stated premise is that the evidence trail IS the product, "we marked it done and photographed nothing" is the wrong record to be able to create. **Gated rather than amending D-17 to match reality**, because the reason D-17 gave for deferring R-028 was sound *only if* the claim were true — weakening the requirement to fit the code would retroactively hollow out a decision that is otherwise still correct. **Why the server check and not just the button**: the client gate is an affordance, and a stale page rendered before a photo was soft-deleted would otherwise walk straight past it. **Four near-misses the count has to get right**, each with a test: an INVOICE is not a completion photo (half of vendors upload the bill first); a soft-deleted photo does not count (R-012's 30-day undelete means a removed photo is still a row); a photo on another job does not count; and the check runs before the status guard, so the message names the missing thing rather than the wrong one. |

## Needs owner decision

These gate the items named. Answer them in this file as new D-numbers rather than deciding silently mid-build. They are the PRD §10 open questions, narrowed to the ones with an actual build consequence.

| # | Question | Gates | Why it can't be deferred |
|---|---|---|---|
| OQ-1 | **Which states does the portfolio operate in today, and what's the 24-month plan?** | R-010 (jurisdiction seed), Milestone 6 (deposits), R-062 (notices) | Texas is seeded per D-4, but deposit deadlines and entry-notice hours are wrong-by-default for any other state, and wrong defaults manufacture compliance failures at scale. |
| ~~OQ-2~~ | ~~**Any Section 8 / HCV tenancies now or planned?**~~ **ANSWERED 2026-08-01: "unsure / possible later" → see D-13.** | R-048 | Partially answered. The *schema* consequence is settled and built (R-002 ships `LeasePayer` + `PayerAllocation`, D-13). The *phase* question is not: R-048 stays gated until the portfolio's Section 8 posture is firm. Re-ask before Milestone 3 planning. |
| ~~OQ-3~~ | ~~**In-house maintenance tech, all external vendors, or both?**~~ **ANSWERED 2026-08-06 — see D-17: all external vendors.** | R-028 (offline tech flow), Milestone 2 sequencing | Answered. R-028 deferred to Phase 3; the magic-link path (D-6, R-025) is the entire maintenance edge. |
| OQ-4 | **How many legal entities (LLCs), and do any have outside partners with distribution rights?** | R-004 (entity scoping), R-085 (investor role) | Entity scoping is schema-level and cheap now; adding it later touches every financial query. Partner distributions decide whether the investor persona is real or cut. |
| OQ-5 | **What share of tenants will never use a portal, and how do they pay today?** | R-036 (offline payment recording), activation planning | Sizes the staff-mediated path. The PRD treats it as a hard requirement either way; this decides how much UI it deserves. |
| OQ-6 | **Are written screening criteria documented today?** | R-055 (screening) — **hard block** | The product cannot ship a fair-housing-defensible screening flow without criteria to version and apply. If they don't exist, authoring them (with counsel) is a prerequisite task, not a parallel one. |
| OQ-7 | **Lease templates: attorney-drafted per state already, or starting from scratch?** | R-057 (lease generation) | Merge-field template work is small; sourcing a defensible lease is not. |
| OQ-8 | **Who does the bookkeeping today, in what system, cash or accrual?** | R-042 (QuickBooks export mapping) | Decides the chart-of-accounts mapping shape and whether Phase 2 needs the QBO API or CSV suffices indefinitely. |
| OQ-9 | **What share of tenants are non-English-primary?** | R-090 (Spanish surface) | Decides Should vs Must, and which notices need attorney-approved translations rather than machine translation. |
| OQ-10 | **Who is on-call after hours today, and what is the actual protocol?** | ~~R-029 (after-hours routing)~~ — **no longer a hard gate, see D-18** | Still worth answering, but R-029 shipped without it on purpose: it encodes no arrangement, only the mechanism (a self-service on-call window, an acknowledgement that stops the escalation clock, a chain reaching everybody else), and falls back to paging everybody when nobody is on call. Answering this decides whether a rota-scheduling UI is worth building on top — not whether the routing is right. |
| ~~OQ-11~~ | ~~**Autopay or partial payments?**~~ **ANSWERED 2026-08-10 by the owner: option (a) — see D-29.** | R-037, R-039, R-043 — **no longer blocking** | Answered. Collection method is per payer, not per product: autopay tenants on `charge_automatically`, partial-paying tenants on `send_invoice`, switched as their circumstances change. Milestone 3 is unblocked. |
| ~~OQ-12~~ | ~~**Section 8 / HCV under Stripe Billing.**~~ **CONFIRMED 2026-08-01 and built → see D-13.** | R-048 | Resolved as proposed: two Stripe Customers and two Subscriptions per voucher lease, joined in our projection so the lease shows one balance. `LeasePayer` holds the Stripe ids; `PayerAllocation` holds the split. Nothing left to decide here — R-048 now inherits a schema that already fits. |

## How to use this log

When starting a Claude Code session on a backlog item, include this file alongside the relevant PRD section. If an item's PRD text contradicts a decision here, the decision wins. New conflicts discovered during the build should be appended here with a new D-number rather than resolved silently. An item whose gating question above is unanswered should not be started — build to the PRD's stated default and record the assumption, or pick a different item.
