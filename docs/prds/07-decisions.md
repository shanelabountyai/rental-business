# Decision Log — Single-Family Rental Operations Platform

**Status:** D-1 through D-10 recorded 2026-07-31 at project setup, carrying forward the conventions proven on the self-storage build. **D-11 (2026-07-31) supersedes D-2** and deliberately diverges from the storage platform. **D-13 (2026-08-01) settles the two-payer ledger shape** and closes OQ-12; OQ-2 is partially answered and R-048 remains gated on the rest of it. **D-14 (2026-08-03) settles file/photo storage** for R-012. **D-15 (2026-08-05) settles notification delivery providers** for R-016 and flags the 10DLC registration as the owner's longest external lead time. **D-16 (2026-08-05) amends "single-use" for vendor links only** — they are multi-use until expiry, for R-025. **D-17 (2026-08-06) answers OQ-3** — all external vendors, so R-028 defers to Phase 3. Open owner decisions are listed under "Needs owner decision"; several gate specific milestones.
**Precedence:** This document amends the PRDs. Where a PRD conflicts with a decision below, this document wins. When building from the backlog, treat each decision as settled — do not re-open unless the owner amends this log.

| # | Topic | Decision | What it means for the build |
|---|---|---|---|
| D-1 | Tech stack | **Same stack as the self-storage platform.** | Next.js (App Router) + TypeScript, Postgres + Prisma, Auth.js, Tailwind + shadcn/radix, Stripe, Resend, Vitest + Playwright + axe + Lighthouse CI, Vercel. Monorepo `apps/web` + `packages/core` + `packages/db`. Rationale: this is a learning project and the second repo in the same idiom — reusing the stack means the second build reinforces the first instead of splitting attention across two ecosystems. Patterns (facility→property scoping, RBAC-as-data, audit service, event outbox) transfer nearly one-for-one. |
| D-2 | ~~Stripe Billing vs ledger-driven charges~~ | ~~Ledger-driven PaymentIntents.~~ **SUPERSEDED BY D-11 (2026-07-31).** | Retained for history only. Do not build to this row. The original reasoning — that jurisdiction-configurable fees, proration and allocation are ours — survives into D-11; what changed is where the invoice and payment record lives. |
| D-3 | Money and time representation | **Integer cents; UTC in the DB, property-local timezone for display.** | No floats anywhere in money math. Every nightly job (charge posting, late-fee assessment, notice countdowns) runs per-property in property-local time — a late fee assessed at UTC midnight is a wrong late fee in every timezone west of London. |
| D-4 | Jurisdiction rules | **Versioned, effective-dated configuration. Texas seeded as the default; built per-state configurable.** | `JurisdictionRule` rows hold grace days, late-fee type/caps, deposit maximums and disposition deadlines, entry-notice hours, notice periods, payment-allocation order, just-cause flags, application-fee caps, RUBS permissibility. Adding a state = adding a reviewed config record + lease template, not a release. Rules apply prospectively; changing a rule never rewrites history. All legal artifacts are drafts requiring attorney review before real-world use — this is a learning project, not legal advice. |
| D-5 | Superuser accounts | **No permission bypass. The `owner` role with an all-properties assignment is the only super user.** Mirrors storage D-12. | Never add an `isSuperuser` flag or a short-circuit in `can()` / `propertyScope()` / `checkMonetaryAuthority()`. Unrestricted access is an ordinary assignment row with `roleId = owner` and `propertyId = null` — grantable, revocable, visible to the audit log. Bootstrap via a `db:create-owner` script scoped to R-007. |
| D-6 | External vendor access | **Zero-login magic-link work orders. No vendor accounts in v1.** | A vendor receives a signed, expiring link by SMS or email carrying scope, photos, address, tenant contact, and a per-work-order code reveal. Accept / propose time / upload completion photos / upload invoice all happen without an account. Rationale: the operator interview was unambiguous — a plumber with five landlord clients will not learn a sixth portal, and a maintenance module vendors won't touch has no data in it. Optional accounts for high-frequency vendors are Phase 3, never a prerequisite. |
| D-7 | Screening, e-sign, and listing syndication vendors | **Simulated adapters first, real drivers behind a partner agreement.** Follows the storage build's gate-hardware precedent (D-4 there). | `SimulatedScreeningAdapter`, `SimulatedESignAdapter`, and `SimulatedSyndicationAdapter` are the primary adapters for this build and the contract fixtures for future real drivers, including fault injection. Screening in particular is FCRA-regulated and partner-gated; a simulated provider lets the compliance workflow (criteria versioning, adverse-action generation, decision audit) be built and tested for real without a bureau relationship. Real drivers are Phase 3. |
| D-8 | Mobile strategy | **PWA, not native, for v1.** | One codebase; no app-store install wall in front of tenant activation; magic-link deep linking works cleanly. The tech's job flow gets service-worker offline caching with an IndexedDB write queue. Revisit native only if field data shows PWA offline limits being hit (R-089). SMS is the reliable notification backstop for tenants and vendors regardless. |
| D-9 | One task queue | **A single `Task` entity serves every staff work queue.** Carried over from the storage build, where seven queues had to be consolidated after the fact (its B-095). | Facility→property, type, subject entity, property-local business date, priority, assignee (nullable = anyone), status, typed proof fields, completion actor, source event id; idempotent creation on (type, entityId, businessDate). Delinquency steps, failed payments, move-out verification, inspection follow-ups, compliance items, bounced messages, and vendor no-shows are all *views* over this. Built early (R-011) precisely so nothing later invents its own. |
| D-10 | Tenant-facing lexicon | **One word per concept on every tenant-facing surface.** Mirrors storage D-15. | **home** or **unit** (not "door"), **rent**, **maintenance request** (not "ticket" or "work order" — those are staff words), **balance**, **payment**, **inspection**. Industry words — delinquency, disposition, dunning, chargeback, work order, ledger — stay on staff screens. No internal identifier (backlog ID, entity name, status enum) may render on a tenant- or vendor-reachable route, and a test asserts it. One money formatter for tenant-facing amounts. |
| D-11 | **Billing engine — supersedes D-2** (owner call, 2026-07-31) | **Stripe Billing drives recurring rent, and Stripe is the source of truth for invoices and payments.** | Each active lease gets a Stripe Customer, a Price at the lease's rent amount, and a Subscription anchored to the lease's billing day in **property-local** time (D-3 still holds — the anchor is computed locally, then expressed to Stripe). Stripe owns: invoice generation, collection, retries, ACH-return handling, invoice PDFs, and the payment record. Our `LedgerEntry` table becomes an **append-only projection** built from Stripe webhooks — still the audit surface, still exportable, but it no longer originates charges. **Deliberate divergence from the storage repo**, which stays ledger-driven per its D-6: the two projects now demonstrate the two real billing architectures, which is worth more in a learning portfolio than a second identical implementation. |
| D-12 | What stays in `packages/core` under D-11 | **Every jurisdiction-dependent calculation. Stripe executes; it does not decide.** | Late-fee amount **and its state cap clamp**, proration math for move-in/move-out, RUBS allocation, payment-allocation policy, `daysPastDue`, and deposit accounting are computed in core and pushed to Stripe as invoice items or subscription updates. Stripe has no concept of "clamp this fee to the Texas maximum," and a fee it generated is a fee we cannot defend. Rule of thumb: **if a statute could change the number, core computes it.** |
| D-13 | **Two-payer ledger shape** (owner call, 2026-08-01 — answers OQ-2, confirms OQ-12) | **The ledger is payer-agnostic from the schema up, but no voucher feature is built until OQ-2 has a firm answer.** OQ-2 was answered "unsure / possible later". | Section 8 is neither confirmed nor ruled out, so R-002 takes the low-regret option: build the shape, defer the features. Two tables carry it. `LeasePayer` is one payer of one lease — one Stripe Customer and one Subscription each, which is what OQ-12's "an invoice has exactly one payer" forces. `PayerAllocation` splits every `Charge` across the lease's payers, so the lease still shows **one balance**. An ordinary lease is exactly one `LeasePayer` carrying the whole charge: **one code path, not two** — allocation always happens. Nothing in the schema knows what a housing authority is beyond a `PayerType` value. The voucher *features* — recertification proration, bulk HAP deposit reconciliation across many tenant ledgers, abatement, HQS re-inspection deadlines, and "dunning never pursues the HAP portion" — remain **R-048's and remain gated on OQ-2**. Rationale: the schema affordance costs a column and a join; retrofitting a split payer onto a single-payer ledger costs every financial query and a data migration. |
| D-14 | File/photo storage (R-012) | **A `StorageAdapter` interface, backed by local disk in dev/test; a real object store (Vercel Blob/S3-class) is a same-interface swap, not a rewrite, whenever this deploys somewhere the filesystem isn't durable across instances.** | Unlike D-7's simulated adapters, storage is not partner-gated or regulated — it is commodity infrastructure (master PRD §7: "Buy") that can run for real against disk today. `LocalDiskStorageAdapter` writes under `DOCUMENT_STORAGE_PATH` (default `.data/documents/`, gitignored); every caller goes through `StorageAdapter.put/get/delete`, never `fs` directly, so the swap touches one file. EXIF `capturedAt` is extracted and stored distinct from `createdAt` and is never stripped — a photo's timestamp is the evidence (PROP-08, master PRD §5). Client-side compression and resumable/chunked upload (both named in DOC-01 and §6.5) are deliberately NOT built yet: server-side single-shot upload via a plain file input covers correctness for typical photo sizes on a decent connection; the mobile-3G stress case is a real gap, left for whoever revisits upload UX with field data in hand. |
| D-15 | Notification delivery providers (R-016) | **A `ChannelAdapter` interface with a logging adapter wired today; Resend and Twilio are a same-interface swap, blocked on external workstreams the owner has to start — a verified sending domain and an approved 10DLC campaign.** Every other guarantee of the engine — idempotency, preference resolution, quiet-hours deferral, kill switch, sandbox redirect, the append-only log — is built and tested for real against the logging adapter. | The same shape as D-14, arrived at for a different reason. Storage is commodity infrastructure that *could* run for real against disk on day one, so D-14 ran it. Email and SMS are equally commodity but *cannot* run at all yet: Resend needs a domain with SPF/DKIM published, and Twilio will not deliver to a US mobile number without an approved 10DLC brand and campaign, which takes days to weeks. Writing an HTTP client that has never once been executed — against an account that does not exist — produces code that looks finished and is untested, which is strictly worse than an honest seam. So the seam is the deliverable, and the two provider drivers are a later, small, genuinely-testable change to one assignment in `apps/web/lib/notifications/provider.ts`. **Two controls make an enabled-by-default engine safe in the meantime and are not optional in any non-production environment**: `NOTIFICATIONS_ENABLED=false` stops all sending without a deploy (notifications are still decided and recorded as suppressed, so nothing is lost), and `NOTIFICATIONS_SANDBOX_TO` redirects every send to one address while preserving the intended recipient on the record — which is what prevents a laptop pointed at a copy of production data from texting real tenants. **The 10DLC registration is the long pole and should be started now**, per the backlog's own note; nothing in the code can shorten it. |
| D-16 | **Vendor magic-link lifetime (R-025)** — amends "single-use" in ROLE-05, §6.1, §7 and §13 | **A vendor work-order link is scoped, expiring and revocable, but MULTI-USE until it expires — not single-use.** Single-use remains correct and unchanged for every other token purpose (tenant magic link, both password resets, the MFA challenge). | The PRD's "single-use" is right for a token whose job is to **establish** a longer-lived credential: a tenant magic link is redeemed once and becomes a session, so burning it costs the user nothing. A vendor link **is** the credential — there is no account and no session behind it (D-6) — so burning it on first click means the plumber who opened the text at 7am to accept the job cannot reopen it at 4pm to upload the invoice. That is not a hypothetical edge: accept-now / photograph-the-work-later / photograph-the-napkin-invoice-later is the *only* real vendor workflow, and it spans hours or days by nature. A single-use link would fail on the second step of every single job, and "the vendor phones the invoice in instead" is precisely the outcome D-6 exists to prevent — the backlog's own line for R-025 calls this item "the difference between a maintenance module vendors use and one they ignore." **The alternative considered and rejected** was single-use redemption into a work-order-scoped session cookie: it preserves the letter of "single-use" but dies on a cleared cookie, a second device, or simply re-clicking the original SMS days later, which is the most likely way a vendor returns. **What replaces single-use as the control set**, all of it built: the token is scoped to exactly ONE work order and grants nothing else anywhere in the product; it expires (3 days, `TOKEN_TTL_MINUTES.VENDOR_WORK_ORDER`); reissuing revokes every prior token for that work order, so a link believed compromised is killed by resending; every privileged action taken through it is audited with the vendor named as actor; and each access-code reveal is logged individually (PROP-03). Worth stating plainly: the status quo this replaces is an operator texting the address, gate code and tenant's phone number in cleartext with no expiry, no scope and no audit trail whatsoever — the link is strictly better than what it is displacing, on every one of those axes. |
| D-17 | **OQ-3 answered: all external vendors** (owner call, 2026-08-06) — closes OQ-3 | **There is no in-house maintenance tech. Every job goes to an external vendor, so the zero-login magic link (D-6, R-025) IS the entire maintenance edge.** R-028's offline tech PWA moves to Phase 3. | The backlog's own risk note 3 anticipated this: "If OQ-3 answers 'all external vendors,' R-028 is a Phase 3 item and Milestone 2 shortens by an L." That is what happened. **Why deferring is right rather than merely cheaper**: R-028 exists for somebody on payroll standing in a basement with no signal — cached job list, IndexedDB-queued status changes and photos, sync on reconnect. A vendor holding a magic link needs none of it: the link works on any phone, needs no install, and if signal drops they reload it later, which is exactly the multi-use behaviour D-16 already guarantees. Building a service worker, an offline queue and a sync-conflict policy for a persona that does not exist would be the most expensive wrong turn available in this backlog. **What this does NOT defer**: MAINT-06's "required completion photo" is already built into R-025's vendor upload, and R-030's verify-and-close depends on work orders reaching WORK_COMPLETE, which the vendor path already does — so R-030's listed dependency on R-028 is soft and does not block it. **Reversible**: if an in-house tech is ever hired, R-028 returns as a self-contained item; nothing built since R-024 assumes its absence, and `assignWorkOrder()` still supports staff assignment because a manager doing a job themselves is not the same as a maintenance employee. |
| D-18 | **After-hours routing without an answer to OQ-10 (R-029)** | **The rota is self-service and the fallback is to page EVERYBODY. Nobody on call for a property means an emergency there reaches every staff member who can act on it — narrowing is earned by somebody taking responsibility, never assumed.** Escalation fires at everyone not already paged, all at once, 15 minutes after an unacknowledged emergency; there is no ranked, tier-by-tier chain and no priority column behind one. | OQ-10 ("who is on-call today, and what is the actual protocol?") is still open, and the backlog is right that "the escalation chain encodes a real human arrangement; inventing one produces an escalation path nobody answers." So R-029 deliberately encodes **no** arrangement. It provides the mechanism — a window a person opens on themselves, an acknowledgement that stops the clock, a chain that reaches everybody else — and lets the operator's real protocol be whatever they actually do. **Why the fallback pages everybody**: this is R-026's commitment held. R-026 measured that paging every `ticket.write` holder was making emergency submission slow and recorded that capping recipients "would be a safety regression dressed as a performance fix: paging one person too few during a gas leak is not symmetrical with one too many." A rota narrows that set honestly, because somebody volunteered; a lapsed or never-configured rota must not, and the basis (`on_call_rota` vs `no_rota_paging_everyone`) is recorded on every escalation so a gap reads as a gap rather than as a working rota. **Why no tiered chain**: MAINT-12 says "page → SMS → call → backup", and three of those four are channels the notification engine already sends simultaneously. A rank column would have ordered a list that is paged all at once — a field somebody would maintain believing it changed something. Build tiering the day escalation actually waits between tiers, and not before. **Why self-service only**: putting yourself on call is volunteering, not a privilege, so it needs no permission; putting somebody *else* on call is a privilege and this cannot do it — the row written is always the actor's own. A rota screen where one person schedules the team is a real thing to want and needs a real permission. **Re-ask OQ-10 anyway** before assuming the chain matches reality: if the true protocol is "the owner's phone, always, and the PM only during business hours", that is a scheduling UI on top of this mechanism, not a change to it. |
| D-19 | **Verify & close: where the money is entered, and who owns a reopen (R-030)** | **The cost of a job is typed ONCE, on the work order, and every downstream reader computes from that row — there is no maintenance-spend table and never will be one.** A tenant's verification is its own append-only-in-practice row carrying **the vendor who did the work, captured at the moment of the answer**, not read off the work order later. Closing is refused over a live "not fixed" and permitted without any answer at all, with the absence recorded. | Three things, each of which a later session could plausibly get wrong. **(1) One place for the money.** The backlog calls the WO → invoice → P&L chain "the specific place owners abandon software", and the reason it breaks elsewhere is always the same: somebody types the invoice total a second time into the books, the two copies drift, and neither can be trusted again. So `closeWorkOrder()` writes `invoiceCents` onto the work order and the property's maintenance spend is a projection of those rows through `jobCostCents()` — the same function R-026's re-approval check uses, so "what did this cost" cannot mean two numbers on two screens. R-042's QuickBooks export must MAP from these rows, not ask anybody to re-enter them. **(2) The vendor is captured on the answer.** MAINT-07 says reopen rate is tracked "from day one" and this is what that costs: a reopened job is normally reassigned, so by the time anybody runs a vendor report the work order names whoever eventually fixed it. Reading attribution off the work order would credit the second vendor with the first one's failure, and once the column has moved no later analysis can recover it. `WorkOrderVerification.vendorId` is written when the tenant taps, and the reopen-rate report reads only that. There is a test that reassigns mid-flow and asserts the failure stays with the first vendor. **(3) The two close refusals are asymmetric on purpose.** A tenant's current-round "no" blocks the close outright — a live complaint recorded as resolved is what a tenant later shows a judge, and it outranks every bookkeeping concern in the ordering. But *silence* does not block: a tenant who never replies must not be able to hold a work order open forever, so the close proceeds and `workorder.closed` records `unverified: true`, which the work order page then states permanently rather than as a toast. A $0 close needs an explicit "this job cost nothing", because a job closed at nothing by accident is a hole in a property's cost history nobody notices until a year of spend is quietly wrong. **What R-030 deliberately did NOT build**: the `INVOICED` status and MAINT-09's vendor-visible invoice lifecycle (received → approved → paid) belong to R-079's vendor management, and the chargeback that a `tenantCaused` close makes possible is R-031's — this item sets the flag and posts nothing to any ledger. |
| D-20 | **Work-order comms threading: what stays continuous, what gets scoped, and why an "attach" cannot be an update (R-032)** | **The tenant's thread stays the ONE continuous conversation per tenancy R-017 built (COMM-01); individual Messages are tagged with `ticketId`/`workOrderId` at send time instead. The vendor gets a genuinely SEPARATE, work-order-scoped thread (`vendor:<id>:workorder:<id>`), alongside the unchanged property-scoped thread inbound SMS already routes into. Attaching an existing untagged message to a job afterward is a new `WorkOrderMessageLink` row, never an update to the message.** | Three calls, each forced by something already built. **(1) The tenant thread does not fragment per ticket.** COMM-01's whole point was one continuous history, and re-litigating that to satisfy COMM-06 would break the earlier decision instead of building on it. Tagging individual messages is additive and costs nothing when absent. **(2) The vendor DOES get a new per-job thread**, and this is not the same call - D-16 already scopes a vendor's magic link to exactly one work order, and a vendor with two concurrent jobs for this operator (ordinary for a preferred trade) must not have both conversations arrive in one mixed thread. `threadKey()`'s VENDOR case now branches on whether a `workOrderId` is supplied: absent, the key is unchanged (`vendor:<id>:property:<id>`, still what inbound SMS resolves to, since a text has no way to say which job it is about); present, it is a new, different key. Nothing that existed before this item changes shape. The new thread is PORTAL-only by construction - offering SMS on it would let a vendor's reply land in the OTHER (property-scoped) thread instead, silently splitting one conversation into two nobody reads together. **(3) "Attach this stray message to a job" cannot touch the Message row**, discovered only after building it the update way first: `Message` has been append-only since R-017 (`Message_append_only` rejects every UPDATE, not only DELETE), so `Message.ticketId`/`workOrderId` are `RESTRICT`, not the Prisma-default `SET NULL` - a `SET NULL` cascade would require exactly the UPDATE the trigger exists to refuse. `WorkOrderMessageLink` is an insert-only join table instead: a message that already existed gets a new row pointing at the job, and the original evidence is never rewritten. The unique constraint on `messageId` is what keeps a message from being claimed by two jobs at once. |
| D-21 | **The lease status machine, and what "notice given" is (R-033)** | **A guarded transition machine in `packages/core/leases`, and NO `NOTICE_GIVEN` status.** Notice is a fact about a running lease (`noticeGivenAt` + `noticeGivenBy`), never a state of it. Nothing leaves ENDED or TERMINATED: a tenancy that restarts is a NEW lease. TERMINATED demands a stated reason, enforced at the audit writer (`lease.terminated` sits in `REASON_REQUIRED`), not merely in a form. Activating a lease occupies its unit immediately; ending one frees it. | **Why a machine rather than a status column.** Everything downstream keys on lease status: R-034 opens a Stripe subscription when a lease activates, R-035 posts rent against a lease in force, R-062 computes notice periods, R-071 starts a deposit clock at move-out. A column anybody can assign means each of those has to defend itself against states that should have been impossible - naming the legal transitions once is what keeps that from becoming fourteen separate problems. **Why no NOTICE_GIVEN status.** A tenancy under notice is still a live tenancy: rent is still due, repairs are still owed, entry notice still applies. As a status it would have to be remembered by every "is this lease in force?" check in the product, and the one that forgot would stop charging rent to somebody who still lives there. The backlog's "pending / active / notice-given / ended" describes what a PM sees on screen, and that is exactly how it renders - a notice badge ALONGSIDE the status pill, never instead of it. **Why nothing reopens.** The old lease's dates, rent and signatures are the evidence R-071's deposit disposition is defended with; editing them to mean something else destroys it. **Why the unit is coupled here.** R-009's `unit.auto_make_ready` job already anticipated this item taking over ("the process that activates the new lease owns what happens to the unit next"); it remains the backstop for a fixed term that simply runs out with nobody touching it. The MAKE_READY transition is guarded on the unit still being OCCUPIED, so a unit somebody marked DOWN for a renovation is not quietly reclassified as ready to turn. |
| D-22 | **Inherited tenancies: the gaps are tracked, never defaulted away (RISK-08, R-033)** | **An INHERITED lease starts with `depositTransferStatus: UNKNOWN`, never the `NOT_APPLICABLE` column default, and `inheritedGaps()` treats NOT_APPLICABLE on an inherited lease as an unanswered question rather than a resolution.** The three outstanding items - estoppel confirmation, deposit position, condition-as-found baseline - are raised as ordinary `Task` rows in the one queue (D-9), daily, until somebody answers. | RISK-08's own framing: "you cannot charge move-out damage against a baseline never captured." All three gaps share a shape - cheap to establish in the weeks after closing, expensive-to-impossible later, and costing nothing until the day they cost a lot. The seller stops answering, the tenant stops remembering, and damage that was already there becomes damage nobody can prove was already there. **The single most dangerous line of code in this item is a default.** `depositTransferStatus` defaults to NOT_APPLICABLE because that is correct for the ordinary path, where we collected the deposit ourselves - so an inherited lease that inherited the default would sit looking settled while representing an unquantified liability. In most states the current owner owes the deposit back at move-out whether or not the seller ever handed it over. Both the create action (which sets UNKNOWN explicitly) and the gap check (which counts NOT_APPLICABLE as unanswered) defend against it, deliberately twice. **"The seller kept it" is an ANSWER, not an absence** - we are very likely still liable either way, and what matters is that somebody established the fact and when. **Why Tasks rather than a checklist table**: D-9 forbids a second queue, and a PM should find "confirm the terms with Grant Okafor" beside "triage this ticket", not on a separate screen they have to remember exists. They are raised URGENT and re-raised daily while outstanding: going quiet after one ask is exactly how this survives to move-out. |
| D-23 | **Stripe is not reachable yet, so the seam is split by DIRECTION (R-034)** — **outbound half AMENDED by D-26 (2026-08-10)** | **The INBOUND half — signature verification, the idempotency claim, and the webhook → projection pipeline — is built for real and tested exhaustively. The OUTBOUND half — create Customer, Price, Subscription, SetupIntent — is a `BillingProvider` interface with a SIMULATED adapter behind it, and no Stripe HTTP client is written until there is an account to call.** | This deployment has no Stripe credentials: the multi-LLC KYB the backlog flags at R-001 has a real lead time and has not cleared. D-15 already decided what to do in this exact situation for Resend and Twilio — "writing an HTTP client that has never once been executed, against an account that does not exist, produces code that looks finished and is untested, which is strictly worse than an honest seam" — and R-021 already drew the line that makes the split principled: a RECEIVER can be exercised completely with synthetic requests, so leaving it unbuilt would mean shipping a public money-writing endpoint later, with no test coverage, under time pressure. **The simulator is not a stub.** It mints ids in Stripe's own shape (`cus_`, `sub_`, `price_`, `seti_`, with the `_secret_` suffix a client secret carries) precisely so every column, index, log line and screen downstream is exercised against realistic values — an id shaped like `sim-1` would let a length or prefix assumption survive until the day real ones arrive. Because they are indistinguishable by inspection, **the label is the only thing that tells them apart**, so `billingProvider.name` is on the audit entry for every provisioning and the lease screen says "not real Stripe" in as many words. It holds no state of its own: whether a customer exists is a question about our database, and a simulator with its own registry would answer a different question than production would, which is how a simulator starts lying. **What wiring the real thing costs**: one assignment in `lib/billing/provider.ts`, plus a class implementing four methods. |
| D-24 | **Webhook idempotency is a claim-before-project INSERT (R-034, D-11)** | **The `ProcessedStripeEvent` insert happens BEFORE the projection, and the Stripe event id is the PRIMARY KEY.** A duplicate delivery loses on the key and stops. Every outcome we have decided about — projected, ignored, duplicate — returns 2xx. | Stripe retries a webhook until it gets a 2xx and explicitly does not promise exactly-once delivery. Under D-11 `LedgerEntry` is append-only, so a double projection is money that cannot be corrected by editing — only by a reversing entry somebody first has to notice is needed. **Claiming before projecting is the whole guarantee**: claiming afterwards leaves a window in which two concurrent deliveries both project, and a check-then-write leaves the same window with extra steps. Making the event id the primary key rather than a surrogate with a unique index is what makes the INSERT itself the lock. There is a test that fires three concurrent deliveries of one event and asserts exactly one projects. **Why ignored events still return 200**: Stripe retries anything else forever, and a dashboard full of red for events we deliberately did nothing with is a monitoring surface nobody will trust. They are recorded with a stated reason, because "we saw it and chose to do nothing" is an answer silence cannot give. **The handled-event list is deliberately closed and short** — `charge.*` is excluded alongside `invoice.*` specifically because projecting both would double-count every payment. |
| D-25 | **What the reconciliation can and cannot see (R-035, D-11)** | **The nightly sweep compares the ledger projection against OUR OWN processed-event log, and deliberately does NOT compare amounts.** It catches a projection that was lost, a ledger row naming an unknown event, and a claim the pipeline died inside. It is blind, by construction, to an event Stripe sent that we never received. `externalChecked` reports which comparison ran, on the record and on screen. | The backlog asks for the projection to be "reconciled against Stripe on a schedule with drift alarmed, because a projection nobody checks is a lie with a timestamp." Two comparisons would satisfy that sentence and only one is possible today (D-23: no Stripe account). **Why no amount comparison internally**: `Payment.amountCents` and `LedgerEntry.amountCents` are both written from the same event, in the same transaction, by the same code — checking one against the other would always pass and would *look* like coverage, which is worse than an absent check because somebody would trust it. The comparison exists in `detectDrift` and takes an amount; the internal caller passes null and an explicit `expectsLedgerRow` flag instead, so the most serious drift — a lost projection — is still caught without one. **Why the blindness is stated rather than glossed**: a webhook that never arrived leaves no trace on our side at all, so no amount of internal checking will ever find it. Only asking Stripe what it thinks it sent will. Saying "reconciled" without that qualifier is how a check stops being trusted the first time somebody discovers what it never looked at. **Drift is alarmed into the append-only audit trail**, not merely logged, because a projection that has drifted cannot be quietly corrected — the fix is a reversing entry somebody has to decide on — so the discrepancy needs the same permanence as the rows it is about. |
| D-26 | **A real Stripe driver, TEST MODE ONLY** (owner call, 2026-08-10) — amends D-23's outbound half | **`StripeBillingProvider` is written and wired, and it REFUSES a live key at construction.** The provider is selected by whether `STRIPE_SECRET_KEY` is set: with a test key, this talks to Stripe; without one, to the simulator. There is no third mode and no override flag. | D-23 deferred the outbound driver on D-15's reasoning — "an HTTP client that has never once been executed, against an account that does not exist, produces code that looks finished and is untested." That premise held while there was no account. A live Stripe account (`Lab Intelligence LLC`) turned out to be reachable, and the owner's call is that this product may use it in **test mode**. A driver that CAN be executed is a driver worth writing, so the objection is answered rather than overridden — and the simulator stays, because a deployment with no key still needs to work. **The live-key refusal is not a warning, it is a throw.** The difference between `sk_test_` and `sk_live_` is four characters in an environment variable that gets copied between machines by hand, and a live key reaching this code would move real money on a real business's account. A refusal at construction is the only control that survives a careless copy; it also means a live key does not silently fall back to the simulator, which would let somebody believe billing was running when it was not. **Every write carries an idempotency key derived from the FACT, never randomly** — `customer:<payerId>`, `subscription:<payerId>`, `price:<leaseId>:<amount>:<currency>` — because the most likely network failure is a request that times out *after* Stripe processed it, and a retry without a stable key creates a second subscription that bills the tenant twice every month thereafter. **Not the `stripe` SDK**: eight endpoints, form encoding and one header do not justify the dependency; if the surface outgrows one file, take the SDK — that is a real threshold, not a prejudice. |
| D-27 | **The simulator reports what it was TOLD, not what the lease says (R-036)** | **`LeasePayer.stripeAmountCents` records the rent we last successfully pushed, and the simulated provider reports that.** It is written only after the provider accepts the change, and cleared on cancellation. | Found by a test that should have passed and did not. The simulator's `getSubscription` originally read `lease.rentCents` — our own current row — which made it agree with the lease *by construction*: a rent change could never be detected, and the whole `update_price` path was dead code that would only ever have run against real Stripe. Reading a record of the last INSTRUCTION reproduces the one behaviour that actually matters about an external system: **it remembers what it was told, and disagrees when we have not told it something.** The column is not a duplicate of Stripe's truth — Stripe remains the authority under D-11 and the sweep still asks it — it is the answer to a different and cheaper question, "have we pushed this change yet", available without an API call. Written after the push and not before, because writing it first would leave us believing we had pushed a change that failed. |
| D-28 | **A demo checkpoint is walked when its milestone closes, not when it is convenient (2026-08-10)** | **Every "Demo checkpoint" line in `06-backlog.md` is an acceptance gate for the milestone above it, and gets walked end to end — tenant, vendor and PM, one sitting — before the next milestone's items start.** Findings become backlog items in the milestone that owns them, not TODOs. | Checkpoint 1 was defined after R-032 and first walked after R-036, four items later. It did not run. `VERIFIED` was in neither the open-work-order list nor the vendor link's actionable set, so the tenant's one tap removed the job from every screen and killed the vendor's link *before the invoice arrived*; nothing ever closed the originating `Ticket`, so every later text from that tenant threaded onto the fixed job forever; the entry window was parsed in the server's timezone and rendered in the property's, so a **served legal notice stated hours nobody intended to keep**; and the re-approval ceiling was wired only to the rare hand-typed path, so a vendor's invoice closed a job at any multiple of the approved amount. **Every one of the four was invisible from inside the item that introduced it and obvious the moment somebody followed one job through.** They are also all seam defects — a status added to an enum and not to the two lists that read it, a lifecycle that ends in one table and not the other, a value crossing a timezone boundary, a check with one caller. No amount of per-item testing finds those, because each item's own tests are written from inside its own seam. The walk is cheap; four defects reaching a demo is not. |
| D-29 | **Collection method is PER PAYER, and it switches** (owner call, 2026-08-10 — answers OQ-11) | **`LeasePayer` carries the Stripe collection method.** A payer on autopay sits on `charge_automatically`; a payer who needs to pay in parts sits on `send_invoice`. Staff move a payer between them, and the move is an audited event with a stated reason. Both PAY-02 (autopay) and PAY-03 (partial allocation) ship in full — for the payers on the mode that supports each. | Stripe cannot do both on one subscription: [partial payments are not supported for automatically-charged subscriptions](https://docs.stripe.com/invoicing/partial-payments). Both stories are Must, so any option that picks one globally demotes the other. Option (b) — autopay wins, partials recorded as offline payments — was rejected because it makes partial payment a staff-mediated path, and **the tenants most likely to pay in parts are the ones least able to reach an office during business hours**; a Must story satisfied only by somebody else typing it in is not satisfied. Option (c) — `send_invoice` everywhere, driving collection ourselves — is most of D-2 rebuilt behind a Stripe façade, and would be a reason to re-open D-11 rather than a way to honour it. **Why per payer rather than per lease**: D-13 already puts two payers on a voucher lease, and the housing authority's half and the tenant's half have genuinely different collection needs. **The switch is the feature, not an escape hatch** — a tenant going onto a payment plan after a bad month is exactly the moment the mode changes, and R-047's legal-action controls already need per-payer billing state, so this is a column that was coming regardless. **What it costs, stated plainly**: two collection modes means two webhook shapes to reason about, and a mid-cycle switch has to be safe — the open invoice on the old mode must be settled or explicitly voided before the new mode's subscription bills, or a tenant is asked for the same rent twice. That is R-037's hardest test, not a footnote. **The tenant-facing payment UI is ours under every option**, because Stripe's Hosted Invoice Page cannot take a partial payment at all. |
| D-30 | **The United States is the only market** (owner call, 2026-08-10) | **US tenants, US properties, US dollars, US payment rails.** No multi-currency, no international payment methods, no locale machinery. `usd` stays a literal in the three places it appears rather than becoming configuration, and `Cents` stays a plain integer rather than a currency-tagged type. | Stated because the whole product already assumed it — two-letter state codes on `Property`, ACH as the free rail, a Texas `JurisdictionRule` seed — and nobody had written it down, which is how a later session ends up adding currency scaffolding "for later" against a need that will never arrive. It also settles a real question R-037 ran into: Stripe HAS a retail-cash pattern (Konbini in Japan, OXXO for customers in Mexico) and simply does not offer it for US customers, so R-037a's third-party integration is the only route and no amount of waiting on Stripe changes that. **What this decision does NOT license, and the distinction is the whole point: US-only is not single-jurisdiction.** D-4 stands unchanged — grace periods, late-fee caps, deposit deadlines, entry-notice hours and surcharge legality all vary by state and stay versioned, effective-dated configuration. Texas is the seeded default, never a hardcoded assumption, and R-037b exists precisely because one state's surcharge statute differs from another's. Nor does it touch OQ-9: a Spanish-speaking tenant in Houston is a US tenant, and PRD §6.4's language question is domestic accessibility, not internationalisation. |
| D-31 | **An offline payment is pushed to Stripe, not just recorded here** (R-038, PAY-05) | **A check, money order or cash is applied by marking the payer's open Stripe invoice paid OUT OF BAND.** Stripe stops collecting; the `LedgerEntry` still arrives only through the webhook, so there remains exactly one way money enters the projection. `checkNumber` and `receivedByStaffId` stay on our own `Payment` row. The push happens FIRST — if it fails, nothing is written at all. | The obvious reading of D-11 is that this is an architecture question. It is not: **Stripe is driving collection.** A payer on `charge_automatically` is debited again on the billing day and one on `send_invoice` keeps an open invoice and keeps being dunned, so a check recorded only in our ledger means a tenant who paid gets chased for the same rent and eventually charged twice. Telling Stripe is operationally required, and it happens to be what D-11 wanted anyway. **Why Stripe-first**: a `Payment` row we hold while Stripe goes on debiting is worse than no record at all, because the staff member walks away believing it is handled. **Why the offline detail stays ours**: Stripe cannot hold which numbered instrument arrived or which member of staff took it, and PAY-05 is explicit that a cash payment with no named recipient is an unauditable cash payment — R-002's schema already carried both columns for exactly this. **Why SETTLED and not PENDING**: a check in hand is money received; whether it later bounces is a separate future event (a reversal, which R-039's NSF handling owns), and holding it pending would leave the tenant's balance wrong for days after they paid. **The known limit, stated rather than discovered**: Stripe's out-of-band payment settles the WHOLE invoice, so a part-payment applied this way would forgive the remainder silently. R-038 refuses one and says so in words; R-038a owns it. |
| D-32 | **A returned payment is a REVERSING ENTRY, and the same Stripe event means two different things** (R-039, PAY-02) | **`invoice.payment_failed` is read against our own `Payment` row before it is acted on.** Against a settled payment it is a RETURN: the payment moves to `REVERSED` and a positive `REVERSAL` entry points at the entry it undoes. Against anything else it is a failed attempt, recorded with no ledger movement. A second delivery of the same return is ignored. | ACH Direct Debit gives what Stripe calls "instant provisional access" and takes **up to four business days** to confirm, so everything downstream of a payment runs on money that may not arrive. Stripe emits the SAME event for a first decline and for a return days later — reading only the event they are indistinguishable, and the shipped code treated both as "nothing has changed about what is owed". That left the credit in place on a return, so **a tenant appeared to have paid rent they had not**. PAY-02 names this failure directly: "no downstream action that was triggered by the provisional payment remains incorrectly in effect." **Why the amount comes from the settled payment and not the invoice**: a partial payment that returns must give back exactly what it gave, and an invoice total is a different number. **Why ignore rather than reverse twice**: Stripe promises neither ordering nor exactly-once delivery, and in an append-only ledger a double reversal is money nobody can correct by editing. **What "no stale downstream state" means today**: the balance is currently the only such state — late fees are computed from the ledger on demand (R-035), so restoring the balance restores the correct fee position automatically. When something else keys off a payment — a notice cancelled on payment, most obviously (R-062) — it has to unwind in the same place, and the code carries that reminder. |
| D-33 | **A scheduled fee charges the DELTA, never the cumulative figure** (R-040, PAY-04) | **`lateFeeFor()` answers "what is the total fee owed as of this date"; the nightly job charges that minus what the overdue rent has already attracted.** `Charge.assessedOnChargeId` links each fee to the rent that produced it, which is what makes the question askable. A waived fee still counts as assessed. | The cumulative answer is the right one for a `DAILY` or `FLAT_PLUS_DAILY` rule and is a trap for anything on a schedule: charging it nightly turns a $10/day fee into $10, then $20, then $30 — **$60 owed by day three instead of $30**. Three behaviours fall out of the delta for free, and all three are what you want: a FLAT fee charges once and then produces zero forever; a DAILY fee charges one day's worth each day; and a fee that has hit its statutory ceiling produces zero from then on, because the cumulative figure stops moving — no separate "have we capped out" state to keep. **Never negative**: a waiver leaves more assessed than the cumulative figure allows, and the answer is to charge nothing further rather than to invent a credit, because reversing an over-charge is a deliberate act with a reason on it (PAY-04's waiver), not an arithmetic side effect. **Why a waived fee still counts as assessed**: waiving is a decision to forgive a fee that was correctly charged, not a statement that it was never owed — so excluding it would re-charge the fee the next night and quietly undo the waiver, which is the opposite of what the person who granted it meant. |
| D-34 | **The waiver-pattern report ships with the waiver, and names tenants who were never forgiven anything** (R-040, PAY-04) | **`waiverPatternByTenant()` reports every tenant with an assessed fee, waived or not, ordered by the share waived.** It reports a pattern and never a verdict. | PAY-04 asks for the report and the backlog is explicit that it is "part of the feature, not a later analytic". The reason is fair housing: **waiving fees for some tenants and not others, along lines that correlate with a protected class, is a discrimination pattern regardless of intent.** An operator cannot avoid a pattern they cannot see, so the ability to see it ships with the ability to create it — building the waiver first and the report "later" is precisely how a year of uneven waivers accumulates before anybody looks. **Why it includes tenants with zero waivers**: a report of waivers alone shows only generosity and hides its distribution, which is the opposite of what a review needs — the tenants who were never forgiven anything are half the pattern. **Why a pattern and not a verdict**: an uneven distribution has plenty of lawful explanations. What it must not be is invisible. |
| D-35 | **`invoice.finalized` is the charge half of the ledger** (R-040b, PAY-03; amends D-11's event list) | **A finalized Stripe invoice projects a POSITIVE `CHARGE` entry.** Not `invoice.created` — a draft has not been presented to anybody and can still change. The amount is `amount_due`, not `total`. No `Payment` row is written: a bill is not a payment. | **This closes a bug that made every balance in the product wrong.** R-034 excluded `invoice.finalized` on the reasoning that an unpaid invoice "changes nothing about what is owed that `invoice.payment_succeeded` will not say more precisely later." That is wrong in one word: `payment_succeeded` reports money ARRIVING, never rent becoming OWED, and nothing else in the pipeline said the second thing. The result was a **single-entry ledger** — payments and no charges — so a tenant who paid rent went further into credit every month, and the pay screen told them they were ahead and owed nothing. Balances everywhere are computed from `LedgerEntry` alone, so this was not a display bug. **Why `amount_due` and not `total`**: `amount_due` is what the invoice actually asks the tenant for after any credit balance Stripe has already applied; projecting `total` would put a charge on the ledger the tenant was never asked to pay. A finalized invoice with nothing due is ignored for the same reason. **Why no test caught it**: every fixture in the suite creates `CHARGE` entries by hand, so the tests build a state the application itself could not produce — which is worth remembering as a class of blind spot, not just this instance. |
| D-36 | **Invoiced collection needs an email, and that collides with the non-digital tenant** (R-040d; amends D-29) | **`switchDecision` refuses `send_invoice` for a payer with no email address**, with a message telling staff to add one or keep the payer on autopay. Moving BACK to autopay needs no email — the constraint is one-directional. | Stripe refuses outright: *"Missing email. In order to create invoices that are sent to the customer, the customer must have a valid email."* **Found by executing the driver against test mode for the first time, not by reading documentation** — every unit test asserted we sent the right request, and only Stripe could say the customer was not ready to receive it. That is the argument for the smoke test in `apps/web/lib/billing/smoke.stripe.test.ts`, which is inert without `STRIPE_SMOKE_KEY` and is the only thing that has ever executed this driver. **The collision is the important part, and it is not resolved.** D-10 and R-021 exist for the tenant who has no email and pays by check; D-29 puts partial payments on `send_invoice`; and a tenant who needs to pay in parts is disproportionately the tenant who has no email. So **the person most likely to need a payment plan is the one Stripe will not invoice** — and R-038 refuses offline part-payments too, so today there is no path at all for them. The refusal message says what a staff member can do about it (collect an email, or take part-payments in person and record them when R-038a lands); it does not pretend the gap is closed. |
| D-37 | Blob access level and how the storage implementation is chosen (R-100) | **Uploads go to Vercel Blob with `access: 'private'`, never `'public'`; and the implementation is selected by the PRESENCE of `BLOB_READ_WRITE_TOKEN`, never by `NODE_ENV`.** | Two decisions a later session must not quietly reverse, both inside D-14 rather than replacing it. **Private, because of what is stored.** Blob's better-known mode mints a permanent unauthenticated URL guarded only by an unguessable suffix — a capability URL. This product stores signed leases, identity documents and photographs of the inside of somebody's home; one leaked URL (a support ticket, a browser history, a screenshot) is public for ever, with no revocation and nothing in the audit log to say it was read. `private` keeps every read authenticated against the store's own token, so the routes that serve files carry on doing exactly what they did — authorise, then stream — and a storage swap changes nothing about who may see a document, which is the property such a swap has to have. **Token-presence, because a label can lie.** `NODE_ENV === 'production'` is wrong in both directions: a production build on a laptop would reach the real store, and — the failure R-100 exists to end — a deployed environment whose Blob store was detached would silently resume writing to a per-invocation filesystem and look entirely healthy until somebody opened a photo months later. The environment is asked what it actually has. Local disk stays the dev and test implementation deliberately (D-14's reasoning, unchanged): the suite must not depend on a network round trip or on anybody's Blob quota to assert that an upload worked. `storageDurable` is reported on the cron response so a silent reversion is visible rather than inferred. |
| D-38 | What happens when a legally-required notice cannot be delivered on the tenant's chosen channel (R-040e) | **ANSWERED 2026-08-12 by the owner: BOTH — fall back to another channel automatically AND raise a `Task` for a human to serve it another way. Neither alone is sufficient.** | The case is a tenant whose carrier has an active `STOP` against our number. `entry_notice` is in `LOCKED_CATEGORIES` precisely because it is legally significant and a tenant may not switch it off — but a carrier-level opt-out overrides us entirely, and our own record would still say SENT, because SENT means *the provider accepted it*. A delivery record that can be silently false is the worst defect this product can have, in a build whose stated premise is that the evidence trail **is** the product. **The fallback alone is not enough** because email and portal prove receipt no better than SMS did — they move the problem rather than closing it. **The task alone is not enough** because a notice that could have gone out electronically in seconds should not wait on somebody reading a queue. Doing both means the tenant gets the best available electronic attempt AND a human is told to serve it in a way that holds up: Texas permits written notice, so posting on the door is a real answer, not a formality. The evidence trail must read as a chain rather than a status — *"SMS blocked by opt-out → emailed 14:02 → task raised to post notice"* — because that is the sentence that has to survive being read out in a dispute. **Checked before building: the fallback half was ALREADY TRUE.** `entry.notice` has declared `channels: ['SMS', 'EMAIL', 'PORTAL']` since R-021, so an entry notice already goes out on all three and a blocked SMS already leaves two live channels behind it. There is no fallback mechanism to build, and building one from this decision's text would have added redundant machinery around behaviour the template already had. What R-040e actually owes this decision is the **Task** — the half that is genuinely missing, and the half that matters most, because email and portal prove receipt no better than SMS did — together with a delivery record that says `SUPPRESSED / sms_opt_out` instead of `SENT`. |
| D-39 | **How a single-meter utility bill is split, and who absorbs the vacant unit's share** (R-042, PAY-08; D-4, D-12) | **Three bases and no more — `EQUAL`, `BEDROOMS`, `SQUARE_FEET` — chosen per bill, with the weights taken over EVERY unit at the property and only the occupied ones charged. Occupant count is deliberately not offered. A unit missing the figure the method needs is REFUSED, never estimated.** Gated on `JurisdictionRule.rubsPermitted`, which R-010 has carried since it was written and nothing had ever read. | **Per bill, because the right basis differs by utility**: water tracks occupancy, heat tracks floor area, and an owner billing both on one basis is defending the wrong number for one of them. **Occupant count is the basis most landlords reach for and the one this product will not offer**, because `LeaseTenant` is adults-only by design (LEASE-06): a family of four reads as two, so the honest options were a maintained `occupantCount` column that nobody updates when a baby arrives, or nothing. Billing on a stale count is worse than not offering the basis, and occupant-based RUBS is also the one most likely to be challenged. **The vacant unit's share stays with the owner, and this is the decision that matters most.** Spreading it across the tenants makes somebody's water bill go up because their neighbour moved out — an amount they can neither predict nor do anything about — and it is exactly what several states' RUBS restrictions are aimed at. The remainder is named on the bill (`landlordCents`) rather than silently dropped, so the split still adds up to the bill and a reader can see where the rest went. **Refusal over estimation**: a unit with no square footage cannot be split on square footage, and an invented average puts a number on a tenant's invoice that nobody can defend — the wrong direction to be wrong in for a charge several states regulate. A recorded **zero** is different from missing and is honoured: a studio has no bedrooms and owes nothing on a bedroom split. **Why it is an invoice item and not a subscription item** (contrast with the pet rent in the same build): a RUBS share is a different number every month because the bill is, so D-12 applies in full — core computes it per bill and Stripe is handed a finished figure. **Why the arithmetic is on the charge**: "Water 2026-07-01 to 2026-07-31 — $412.00 × 1,150/4,600 sq ft = $103.00" can be checked against the bill attached to it; "Utility allocation $103.00" has to be taken on trust, and how the number was reached is the first question every tenant asks. |
| D-40 | **Pet rent and flat utility fees are Stripe subscription items, and Stripe may own the repetition** (R-042, PAY-08; refines D-12) | **A `RecurringCharge` becomes a line on the lease's Stripe subscription, with no monthly `Charge` row minted to mirror it. One reconciler — `syncRecurringCharges` — is the only writer, called on add, on end, on provisioning and nightly. Only `PET_RENT` and `UTILITY` may recur.** | D-12's rule is about **statutes**: if a statute could change a number, core computes it. No statute touches $35 of agreed pet rent — it is a term of the contract, exactly like the rent, and the rent has been a Stripe subscription since R-034. The line to hold is that the moment an amount stops being flat it stops being a subscription item, which is why RUBS in the same build is an invoice item instead. **The closed type list is the guard**: a list that admitted `LATE_FEE` would let somebody bill a late fee every month with the jurisdiction cap never once consulted. **No monthly `Charge` row**, because the projection already handles a subscription line with nothing behind it — it lands in the remainder entry beside the rent (R-040b's shape) — and minting rows to mirror what Stripe is already billing would be a second schedule to keep in sync with the first, which is the class of bug D-11 exists to avoid. **One reconciler rather than a push in each writer**, because that is what makes `endsOn` real: a landlord who says the pet rent stops in March has said something no other part of the product would ever act on, and a fee that outlives the pet is money taken from a tenant who agreed to no such thing. Three writers each pushing their own change gives three places our row and Stripe can disagree, and the disagreement is money. **Ending never prorates**: rent bills monthly in advance, so the period already invoiced stands and the line simply stops appearing on the next one — a credit for part of a month is a waiver somebody decides on with a reason recorded, not a side effect of a pet moving out. |
| D-41 | **The completion photo is required, and D-17's claim that it already was is corrected** (R-032b, MAINT-06) | **`markWorkComplete` refuses without at least one live `COMPLETION_PHOTO` on that work order.** Enforced on the server, not only in the page. It is a *refusal with the fix on the same screen*, never a dead end — D-6's rule that a vendor turned away phones the invoice in still governs. The staff-side `markWorkComplete` is deliberately NOT gated: a PM recording what a vendor phoned in is the override, and removing it would strand the job. | **D-17 said this was already true and it was not.** Its exact words, used to justify deferring R-028 to Phase 3: *"MAINT-06's 'required completion photo' is already built into R-025's vendor upload."* R-025 built the *ability* to upload; nothing ever required it. `vendorMayMarkComplete()` only inspected status, so a vendor could take a job to WORK_COMPLETE having photographed nothing — and that is the state R-030 then asks the tenant to confirm against, R-031 charges a tenant from, and a deposit dispute is defended with. For a product whose stated premise is that the evidence trail IS the product, "we marked it done and photographed nothing" is the wrong record to be able to create. **Gated rather than amending D-17 to match reality**, because the reason D-17 gave for deferring R-028 was sound *only if* the claim were true — weakening the requirement to fit the code would retroactively hollow out a decision that is otherwise still correct. **Why the server check and not just the button**: the client gate is an affordance, and a stale page rendered before a photo was soft-deleted would otherwise walk straight past it. **Four near-misses the count has to get right**, each with a test: an INVOICE is not a completion photo (half of vendors upload the bill first); a soft-deleted photo does not count (R-012's 30-day undelete means a removed photo is still a row); a photo on another job does not count; and the check runs before the status guard, so the message names the missing thing rather than the wrong one. |
| D-42 | **"What did this job cost" is TWO numbers with two jobs, and the comment claiming otherwise was the bug** (R-032e, MAINT-07, RPT-03; corrects D-19's assumption) | **`jobCostCents()` is the BOOKS' number and takes the invoice where there is one. `actualTotalCents()` is the CONTROL's number and takes the higher of the invoice and the recorded labour-plus-materials. Neither may be used for the other's question.** R-042's accounting export and every "what did this cost" display read `jobCostCents`; every approval and re-approval check reads `actualTotalCents`. | **No behaviour changed. Both functions were already correct and already used correctly** — the defect was a doc-comment on `jobCostCents` asserting it was "deliberately the same rule as R-026's `actualTotalCents()`", which is false, and D-19's text assuming one number existed. A false comment about money is worse than no comment: it is what a later session reads before deciding which function to call. **Why two rules is right rather than a failure to unify.** They answer different questions and diverge only when the recorded parts exceed the invoice — $800 labour plus $200 materials recorded against a $600 invoice gives $600 from one and $1,000 from the other. The books must take the invoice, because recording $1,000 of expense against a $600 bill overstates a Schedule E return; the ceiling check must take the maximum, because either figure exceeding what the owner approved is money they did not agree to, and a vendor whose invoice beats our own recorded actuals is exactly the case R-026 exists to catch. Unifying on either would break the other: on the invoice, a $1,000 job billed at $600 stops tripping a $700 approval ceiling; on the maximum, the tax return claims money that was never billed. **Why the false claim survived.** The two agree on every job where the invoice is the largest figure, which is most of them — so nothing ever disagreed on screen. There is now a test asserting the divergence explicitly, so a later unification fails loudly instead of quietly overstating a return or weakening a control. **The property spend tile now says what it covers** — every job closed since the property was added, not a period or entity report — because `closedJobCostsForProperty()` takes neither filter and an unlabelled total on a property page reads like an annual figure somebody re-keys into a spreadsheet, which is the re-keying D-19 exists to prevent. Per-period and per-entity reporting remains R-081's. |
| D-43 | **A tenant chargeback is a separate, `ledger.adjust` action — not a checkbox on the close form** (R-031, MAINT-07; owner decision 2026-08-15) | **Closing a job captures the three-way cause only, exactly as it already did. A distinct "Bill the tenant" panel, gated on `ledger.adjust`, posts the charge with an amount the person types — which may be less than the repair cost and may never be more. Closing as tenant-caused raises a `workorder_chargeback_decision` Task (D-9) so the decision sits in a queue rather than in somebody's memory.** | **Two reasons, and the first is a permission boundary that would have been crossed silently.** `closeWorkOrder` runs on `workorder.write`, the permission a maintenance coordinator holds; every other money-moving action in this product sits behind `ledger.adjust`, which RBAC deliberately keeps away from managers. Posting the charge inside the close would have quietly redefined "can close a job" as "can bill a tenant", and nothing on the screen would have said so. **Second, the amount is a decision, not the invoice.** Partial fault, betterment — a 12-year-old carpet replaced with a new one is not a 100% tenant cost — and goodwill splits all reduce the number; billing part of a repair is the normal outcome, and a flow welded to the close form can only ever charge the whole invoice. **The ceiling runs the other way and is absolute**: less than the cost is allowed, more is refused, because at that point it is a penalty rather than a chargeback and a penalty needs a basis in the lease and the statute that this flow does not establish. **What the split costs, and how it is paid for.** A job can now be flagged tenant-caused and never billed — revenue that leaks invisibly. The Task is the payment: one queue entity for every staff work queue (D-9), raised at close, ROUTINE whatever the job's own priority was, and raised OUTSIDE the close transaction because `createTask` catches its own unique violation and a P2002 inside a transaction leaves it aborted at the Postgres level — the COMMIT would then act as a ROLLBACK and silently undo the close. **A reason is required** (`workorder.chargeback_posted` joins REASON_REQUIRED): the tenant's notice quotes it back verbatim, and a chargeback with no stated reason is indistinguishable from retaliation, which is the claim it will be defended against. **One chargeback per job, enforced by a partial unique index** on `Charge.workOrderId`, not by a read-then-write check two concurrent submits would both pass. **The notice is served immediately; the charge reaches the tenant's portal when Stripe invoices it**, exactly like rent, late fees, NSF fees and RUBS shares — D-11 keeps `LedgerEntry` a projection and nothing writes it directly. |
| D-44 | **Managed templates are the PM's library and do not touch the 13 automated ones** (R-049, COMM-03; owner decision 2026-08-15) | **`MessageTemplate` rows cover what a person sends — violation notices, renewal offers, welcome packets, seasonal notices. The typed templates in `packages/core/notifications/templates.ts` keep driving every automated send and are not overridable. R-044 sends from the managed library.** | **The two are different artifacts and the difference is the compiler.** That file's own header says why the automated templates are typed FUNCTIONS rather than `{{placeholder}}` strings: *"a template referencing a field the caller does not pass is a build error rather than a rent reminder that says 'Hi undefined'"*. A managed template is a string somebody typed into a textarea, so no compiler will ever see it — every guarantee has to be re-earned at runtime by a closed merge-field catalogue, validation on save, a renderer that reports what it could not fill, and preview against real data. **Letting a managed row override an automated key would put that weaker guarantee on the paths that run unattended**, where a typo'd field goes out to everybody at once with no human in the room. COMM-03 does list rent reminders, and a PM genuinely cannot reword the automated one yet: that is a NAMED GAP, not a claim the requirement is met. **Preview and send share one resolver** (`templateValues`), because a preview built from different code than the send is a second implementation that agrees until the day it does not, and that day is the day four hundred tenants get a message nobody saw. **Preview renders a REAL tenancy**, never the catalogue's example values, which would make every preview look perfect — a PM needs to see `{{lease.ends_on}}` come out empty on their month-to-month leases before they send, not after. **`template.approve` is its own privileged permission, separate from `template.write`**: COMM-03's rule comes down to one `approvedAt` timestamp, and if the person who pasted in a machine translation can set it, the rule is decorative. The product cannot verify an attorney read it — it records who claimed so and why, which is why the action is on REASON_REQUIRED. **Editing a translation clears its approval**, or the stamp keeps vouching for words that have since been rewritten. |
| D-45 | **A pay-now link is a token-scoped PAGE, not "a portal session scoped to paying only"** (R-046, PAY-01; owner decision 2026-08-15) | **The link opens one standalone route, `/pay/<token>`, authorized entirely by `verifyPayLink()`. NO Auth.js session is created. The token is scoped to one `LeasePayer`, lives three days, is revoked by reissuing and by `revokePayLinks()`, and is re-verified inside the write action rather than trusted from the page that rendered the form.** | **The backlog's own words are "a portal session scoped to paying only", and building that literally would have been fail-OPEN.** A real session carrying a `scope: 'pay_only'` marker only works if EVERY portal route refuses it — twenty-four `requireTenant` call sites across ten pages — and any one missed hands a leaked pay link the tenant's messages, papers, maintenance history and lease documents. That is precisely the shape `lib/portal/guard.ts`'s own header warns about: *"defaulting to 'show everything' for a privileged principal is how a debugging convenience becomes a data leak."* **A token-scoped page is fail-CLOSED by construction**: there is no session for any other route to trust, so nothing else is reachable without adding a route that reads the token — a deliberate act, not an omission. It is also the third instance of a pattern this codebase already runs twice, the vendor link (D-6, D-16) and the verify link (R-032c), so it inherits their whole control set rather than inventing one. **The blast radius is stated rather than hand-waved:** a leaked token can see that lease's balance and pay that lease's rent, and can do nothing else. **Three controls are TIGHTER than the verify link's because this one moves money:** three days rather than seven (matching the vendor links); scoped to one PAYER rather than one tenant, so a tenant on two tenancies cannot see or pay the other from August's link for unit A; and refused outright for a paused tenancy (PAY-12), a deactivated payer or tenant, and a payer whose tenant changed since issue. **Revoked and expired are reported differently** because they tell a tenant different things — one says "ask for a new one", the other says "call us". **The write path is SHARED with the signed-in screen** (`chargeResolvedPayer`), because that is where every number is recomputed from the ledger; a second copy would be a second place for a stale amount or a missed hold to live. Recorded as a decision rather than settled silently, so a later session reading "portal session" in the backlog does not build the fail-open version. |
| D-46 | **The dashboard's "compliance items ≤30 days" tile is labeled "Renewals & alerts" and covers mortgage/insurance dates only, not statutory compliance** (R-050, RPT-01) | **`renewalAlertsSummary()` reads `filingCabinetAlertsDue()` — ARM rate adjustments, balloon maturities, insurance renewals. No permit, certificate or mandated-inspection tracking exists anywhere in the product yet. The tile and its drill-down page (`/renewals`) both say "Renewals & alerts", never "Compliance".** | **The backlog row names "compliance items ≤30 days"; the only query that exists answers a narrower question, and overclaiming coverage on an owner-facing dashboard is worse than a smaller honest tile.** `filingCabinetAlertsDue()` was written and tested at R-015, and its own header already said this in one line — *"the 'report, not automated action' surface PROP-05's future compliance calendar (R-077) and the notification engine (R-016) will read from once they exist"* — but nothing had ever called it, so the gap was invisible. A statutory compliance calendar (permits, certificates, inspections) is real future work with its own backlog row (R-077); wiring today's tile to a wider label would read as "we track your compliance dates" to an owner who would reasonably stop checking elsewhere. Renamed rather than deferred: the mortgage/insurance data is real, tested, and worth surfacing now under a name that matches what it actually watches. |
| D-47 | **Service of a notice is a child table, not columns on the notice** (R-051, COMM-02) | **`NoticeDelivery`: one row per service event, each with its own proof — photograph, tracking number, or portal read receipt. `Notice.serviceMethod`/`servedAt` are kept as the FIRST service and are never rewritten by a later one.** Append-only by trigger, with exactly one permitted mutation: `readAt` may go from null to a timestamp, once. | **Service is frequently more than one event, and the schema could only record one.** Several states require a notice to vacate be both posted on the door AND mailed; each half has its own separate proof, and `Notice` carried a single `serviceMethod` + `proofDocumentId` + `trackingNumber`. An owner who did the lawful thing could not record that they had. **Append-only for the same reason `LedgerEntry` and `AuditLog` are**: if "when did we serve it, and how" can be edited afterwards it is not proof of anything, and a correction is a new row. **`readAt` is the one exception** because it cannot be known at insert time — the tenant opens the notice later, and that opening is the only evidence portal service reached anybody. It is write-once, so a second view never overwrites the first: the evidence is when they FIRST read it. **Three CHECK constraints hold the shape each enum value promises** — POSTED_WITH_PHOTO requires a photograph, CERTIFIED_MAIL requires an article number, a read receipt exists only for PORTAL. Enforced in Postgres rather than in the action, because a row claiming POSTED_WITH_PHOTO with no photograph is not a weaker record, it is a false one, and this is the evidence an eviction turns on. **The old columns are kept, not dropped**: every reader written before this item keeps working, list screens sort without a join, and dropping a column that holds evidence is not a tidy-up. |
| D-48 | **A service method a state has not configured is `null`, never `false`, and never a refusal** (R-051, COMM-02, D-4) | **`JurisdictionRule.noticeServiceMethods` maps notice type → permitted methods. `servicePermitted()` returns `true`, `false`, or `null` for "nobody has told us". A method the state does not list is WARNED about and still recorded, flagged `permittedByJurisdiction: false`; an unconfigured state records `null` and says so on the record.** The verdict is stored as it stood at service and never recomputed. | **`NoticeServiceMethod` has listed PERSONAL / POSTED_WITH_PHOTO / CERTIFIED_MAIL since R-002, and that list has only ever meant "these are the mechanics we can record proof for".** It never said any of them was lawful anywhere — that is a statutory question, so under D-4 it is versioned configuration and not a constant in code. **Three answers rather than two**, the same call R-044 makes with `graceUnknown` and `assessLateFees` makes with a missing rule: "we do not know what this state allows" and "this state forbids it" are different facts, and only one of them is an accusation against the person who served the notice. **Recording is never blocked.** An owner in an unconfigured state still has to serve notices, and a product that refuses to record what they actually did produces no evidence at all — which is strictly worse than evidence carrying an honest "unverified" flag. The warning is loud; the refusal does not exist. **Stored at service, not recomputed**, because D-4 says rules apply prospectively and changing one never rewrites history: a state that legalises door-posting next year must not retroactively make last year's posting look compliant. |
| D-49 | **Consent is a basis, not a boolean — and the existing roster is grandfathered on a recorded basis** (R-051b, COMM-02; owner decision 2026-08-16) | **`TenantConsent` records WHO agreed, on WHAT BASIS, shown WHAT WORDS, recorded BY WHOM. `EXPRESS_WRITTEN` is the only basis that permits promotional sending; `EXISTING_RELATIONSHIP`, `VERBAL` and `IMPORTED` cover transactional messages about the tenancy and nothing else. Every tenant already on file with a phone number was backfilled as `EXISTING_RELATIONSHIP` / source `BACKFILL`, with a note saying exactly that nobody clicked anything.** Append-only by trigger except for withdrawal; the send path suppresses SMS with a new `no_consent` reason, distinct from `sms_opt_out`. | **The product had revocation and never had permission.** `SmsOptOut` has honoured STOP since R-030, so the only gate was withdrawal of a permission nothing ever established — backwards under a statute whose damages are per-message. **A boolean cannot defend a claim.** "They consented" is unfalsifiable six months later; what answers it is how consent was obtained, which is why the basis is the row's whole point and why `EXPRESS_WRITTEN` carries a CHECK constraint requiring the disclosure text. The basis that unlocks marketing is the one that must be able to show what was agreed to. **Grandfathering was chosen over blocking**, and it is a judgement not a shortcut: gating without a backfill would have silently switched off rent reminders, maintenance updates and notice delivery for the entire existing roster on ship day, until somebody worked through them by hand — a worse outcome than the risk removed, for messages that are transactional by definition and rest on a relationship the tenant is already in. The backfill row **claims nothing false**: it records `EXISTING_RELATIONSHIP`, names itself as a backfill, and leaves promotional sending barred to every one of them. **`no_consent` is not `sms_opt_out`** — one is a carrier fact about a NUMBER that the tenant can reverse, the other is our own gap about a PERSON that we go and close by asking; recording either as `preference_off` would hide both behind a choice the tenant never made. **Keyed on the tenant, not the number**, so a reassigned number cannot inherit the previous tenant's consent and a tenant who changes number does not lose it. **Staff-facing and vendor-facing SMS is deliberately ungated**: an employee and a counterparty are not the residential consumers the consent regime is written about, and gating them would stop the on-call pager for no benefit anybody can name. |

| D-50 | **The court packet embeds the payment processor's own invoice PDFs, and NAMES the ones it could not get** (R-052, PAY-09; owner decision 2026-08-17) | **`BillingProvider.getInvoicePdf()` fetches each cited invoice and `appendPdfs()` copies its pages onto the end of the statement, so one file carries both our reading of the record and the record. The method returns `null` rather than throwing on every failure, the simulated adapter returns `null` always, and any invoice cited on a line but not appended is printed by name in an "Underlying records" section.** | **D-11 makes Stripe the system of record for money and `LedgerEntry` a projection of it, so a statement of account is our READING of the evidence and the invoice is the evidence.** A packet containing only the reading asks a judge to trust the projection; appending the invoices lets them check it. **The alternative considered and rejected was citing the invoice id without embedding** — cheaper, no adapter change, no network call inside an export, and it produces the same document on the simulated and live paths. It was rejected because the whole value of the artifact is that it can be handed over complete, and "copies available on request" is precisely the friction a dispute exploits. **The cost is named rather than hidden: the demo and e2e paths run on the simulator, which has no invoice PDFs, so those runs exercise the could-not-attach branch and never the embedding.** That is the honest trade and it is why the simulator returns `null` instead of a fabricated PDF — a manufactured document that looks like a provider's is the one thing a court packet must never contain (D-27's rule, applied to an artifact rather than to a state machine). **Silence about a missing attachment would be a false claim.** A statement one invoice short with no note reads as "there was no invoice for that line", which is a different assertion from "we could not retrieve it" — so the missing ids are printed, and when an invoice arrives but will not parse the statement is RE-RENDERED before archiving rather than shipped with an attachment list that disagrees with its own contents. **One unavailable invoice never costs the owner the statement**: every fetch failure degrades to a named gap, because a produced document with a stated hole beats no document at the moment somebody needs one.  |
| D-51 | **The FCRA adverse-action notice is a `Notice`, not a `Notification`** (R-061, LEASE-05; owner decision 2026-08-18) | **`Notice.leaseId` became nullable and `Notice.applicantId` was added, with a CHECK constraint requiring exactly one of the two (never both, never neither).** An adverse-action notice goes through the same append-only-by-trigger `NoticeDelivery` proof-of-service machinery, the same `/notices` staff register, and the same PDF archiving every other legal notice uses. | **The alternative considered was logging delivery on `Notification`/`NotificationDelivery` instead** — a smaller diff, since that table already supports an `APPLICANT` recipient with no schema change at all. It was rejected because `Notification` is the routine-messaging system (reminders, invites, fee confirmations, not evidence-grade), while `NoticeDelivery` is the ONE table in this schema that is append-only by database trigger and carries statute-standing, tracking numbers and photographic proof — the exact shape a document that may need to be produced in an FCRA dispute years later requires. Consistency with "the evidence trail is the product" (CLAUDE.md's own framing) outweighed the smaller diff. The cost, paid once: ~7 existing call sites (`NOTICE_INCLUDE`/`noticeInclude`, `generateNoticePdf`, `recordNoticeService`, both `/notices` pages) now branch on lease vs. applicant instead of assuming a lease always exists. |
| D-52 | **OQ-7 answered: R-063 ships a placeholder lease template rather than blocking on a real attorney-drafted one** (R-063, LEASE-06; owner decision 2026-08-18) | **The same call OQ-6/R-060 made for screening criteria, applied to a signed legal document instead of a config table.** A generic TX-oriented lease body and six addendum bodies (`packages/db/prisma/seed-lease-templates.mts`) are installed by a standalone, manually-run script - not `seed.mts` - because `DocumentTemplate.createdByStaffId` is required and a fresh database has no StaffUser until `db:create-owner` runs; a "system" author would misrepresent who wrote the legal text. Every generated lease carries `LEASE_DISCLAIMER` (the same footer-disclaimer pattern R-051's `NOTICE_DISCLAIMER` established) saying it has not been reviewed by an attorney - the warning lives on the artifact itself, not only in an admin-only flag, because a tenant who is asked to sign the document is the person who most needs to see it. | **Rejected: blocking R-063 until a real lease is sourced.** The merge-field/addenda/e-signature machinery is the hard, reusable part and is correct whatever the words in the template say; a real attorney-drafted template is a content swap (edit the seeded `DocumentTemplate` row, or add a per-state one) once counsel delivers one, not a rebuild. **Rejected: seeding the placeholder as pure reference data in `seed.mts`**, the way ScreeningCriteria's own v1 placeholder shipped - that table has no author column, and `DocumentTemplate` genuinely does; inventing a "system" StaffUser to attach it to would misrepresent authorship of a document a real person is asked to sign, so this is a deliberate, later, human-run step instead (`npm run db:seed:lease-templates -- --staff <email>`), mirroring `db:create-owner`'s own bootstrap-after-the-fact shape. |
| D-53 | **R-064: every showing is staff-escorted, on a fixed 9am-6pm/30-minute slot grid, with no per-property override** (R-064, LEASE-08) | **No genuinely unaccompanied self-showing exists yet - `Showing` always raises an `escort_showing` Task (D-9) whether the unit is vacant or occupied, because the smart-lockbox identity-verification/one-time-code machinery that would make unaccompanied entry safe is R-094 (Phase 3), not this item.** The bookable window itself (`DEFAULT_SHOWING_WINDOW` in `packages/core/scheduling/showings.ts`) is one constant shared by every property, because no per-property business-hours concept exists anywhere in this codebase to read instead - inventing one for a single consumer would be speculative config for a value nobody has asked to vary yet. | **Rejected: waiting for R-094 before shipping any self-serve booking.** The backlog's own framing already splits the two - "self-serve slot booking" from "smart-lockbox self-showing" - and the booking/reminder/entry-notice machinery this item builds is exactly what R-094 will sit on top of, not something it has to precede. **Rejected: a per-property hours config table.** Nobody has asked for anything but a standard showing day, and the slot function already takes a `window` override - a real owner who wants 7am-8pm is a config field on `Property` away, not a rewrite. **Also corrected in this item**: the backlog row for R-064 named the deferred lockbox item as "R-087," which is an unrelated item (tenant-gone-dark case workflow); the real lockbox item is R-094, and its own "Depends on" column already said so - the row text just had not caught up. |
| D-54 | **R-065: a fixed-term renewal is always a NEW `Lease` row, activated only on its own effective date - never the predecessor mutated, never activated at signature time** (R-065, LEASE-09; D-4) | **`Lease.renewedFromLeaseId` links a successor back to what it replaces (not unique - a voided attempt is kept as evidence, D-52's own precedent). `offerRenewal` creates it DRAFT and hands off to R-063's existing `generateAndSendLease`/e-sign machinery unmodified. The completion path (`esign-actions.ts`) is renewal-AWARE: a fully-signed renewal successor whose `startsOn` has not yet arrived stays PENDING_SIGNATURE rather than activating immediately, and skips `chargeDeposit()` (the deposit already held under the predecessor is not recollected). A new daily job, `renewal-cutover-job.ts`, activates it and ends the predecessor (never `changeLeaseStatus` - that also flips the unit to MAKE_READY on the way off, which is wrong here since the tenant never left) in one transaction on the effective date. A second daily job, `renewal-rollover-job.ts`, is the fully-automatic path: a fixed term that runs out with NO renewal in flight rolls the SAME lease row to MONTH_TO_MONTH at `mtmRentCents` (already existed, unused until now) with no e-sign at all, since nothing is being newly agreed. A rent-increase cap (`JurisdictionRule.rentIncreaseCapPercentBps`, new column) BLOCKS an offer outright with no override; a notice-period shortfall (`rentIncreaseNoticeDays`, already existed) WARNS and requires a reason, the same two-postures split R-027's entry-notice guard and R-055's retaliation guard already established for a hard limit vs. a timing problem.** | **The alternative rejected: activate the successor the moment every signer signs, the same as an ordinary new lease.** That is what R-063's `completeEnvelope` already does unconditionally, and it is exactly wrong for a renewal specifically (not for an ordinary lease, which has no live predecessor to collide with): a renewal is routinely signed weeks ahead of its own start (the whole reason a statutory notice period exists), and activating early would open a SECOND live Stripe subscription on the same unit while the predecessor's own subscription is still billing - real double-billing, not a theoretical one - and would re-charge a deposit already on file. Waiting for the effective date and ending the predecessor in the same transaction is what makes "ledger updates on the effective date, with no manual edits" (LEASE-09's own acceptance criterion) literally true rather than "ledger updates whenever somebody happened to sign." **Rejected: extending `changeLeaseStatus`'s state machine with an ACTIVE→ACTIVE-with-new-terms move instead of a second Lease row.** `packages/core/leases/status.ts`'s own header already states the constraint this decision keeps: nothing leaves ENDED/TERMINATED, ACTIVE never returns to DRAFT, and MTM rollover is one-way — a renewed fixed term already had to be "a new lease" by that same logic before this item ever touched the file. **A real, named gap left behind**: the security deposit itself (a separate `Deposit` row, `leaseId` required, no re-charge on renewal) still points at the PREDECESSOR lease after a cutover — correct in that no money moved, but a future reader asking "what deposit does THIS tenancy hold" has to walk `renewedFromLeaseId` backward rather than reading it off the current lease directly. Left as a modeling gap for whoever builds `Deposit` continuity across a renewal, rather than solved speculatively here. |
| D-55 | **R-067: a renter's-insurance certificate is a new `RenterInsurancePolicy` row every time, never an update - and a lease with none on file is not this item's business** (R-067, LEASE-10) | **`RenterInsurancePolicy.leaseId` is deliberately not unique (LeaseEnvelope's own "history, not the latest edit" precedent) - an annual renewal is a new certificate with its own expiry, and overwriting the old row would erase the record of what was on file when it lapsed. Recording a fresh policy auto-closes any open `renter_insurance_expiring`/`renter_insurance_lapsed` Task raised against the one it supersedes. A new `RENTER_INSURANCE_COI` document type is deliberately distinct from `INSURANCE_COI` (a vendor's own proof of coverage) and `INSURANCE_DECLARATION` (the property owner's own policy) - three different entities insuring three different interests, sharing an English word. The daily alert job (`renter-insurance-job.ts`) only ever looks at the MOST RECENT policy per lease and raises nothing at all for a lease with none on file.** | **Rejected: alerting a lease with no renter's-insurance policy recorded at all, as a fourth Task type ("insurance missing").** LEASE-10 tracks the expiry/lapse of a certificate that EXISTS; whether a given lease REQUIRES one is a policy toggle nothing in this product's data model states (no `Lease.insuranceRequired` flag, nothing in the PRD story asking for one) - inventing the requirement here would be enforcing a rule the product was never told exists, the identical trap D-4 names for a jurisdiction number nobody configured. Left as a named gap: the toggle, and the "missing entirely" alert it would drive, are a real follow-on if an owner asks to REQUIRE coverage rather than only track it once provided. |
| D-56 | **R-066: one `noticePeriodCheck()` serves both a tenant's notice to vacate and an owner's non-renewal, and only the owner's side gets a warn-and-override round trip** (R-066, LEASE-11, RISK-06; D-4) | **`JurisdictionRule.noticeToVacateDays` and `.justCauseRequired` (both already on the schema, unused until now) are read symmetrically for either party - no state has ever been asked to split "how much notice must a tenant give" from "how much notice must an owner give before non-renewing," so this does not invent a second config number for a distinction nobody configured. Two new `Lease` columns, `noticeEffectiveOn` and `noticeForwardingAddress`, sit beside the existing `noticeGivenAt`/`noticeGivenBy` (never folded into a status - LeaseStatus's own comment on why notice is a fact, not a state, still holds). A LANDLORD notice is a real outbound `Notice` (type `NON_RENEWAL`) with `NoticeDelivery` and a tenant notification, generated with `recordLeaseNotice`'s existing entry-notice-style machinery; a TENANT's own notice is the opposite - an inbound fact recorded on `Lease` alone, never a served `Notice`, matching how `recordLeaseNotice` already treated a tenant's notice before this item.** | **On the STAFF side, a short notice period is a warn-and-override, same shape RISK-06's own retaliation guard and R-065's rent-increase notice-period check already established - two independent warnings (retaliation, notice-period) can fire on the SAME notice and are both surfaced in one round trip rather than staff clearing one only to be handed the other on the next submit.** **On the TENANT'S OWN portal submission, there is deliberately NO override round trip for the identical shortfall** - a tenant giving 12 days' notice on a 30-day lease is not defending a business decision a later dispute could turn on, they are stating a fact with a consequence (rent may still be owed through the required period), told to them plainly rather than demanded as a justification to their own landlord's software. **Rejected: enumerating which reasons count as "just cause" per state.** `validateJustCauseStatement()` only requires the field be non-empty when `justCauseRequired` is set - never a canned list this product would be picking on the owner's behalf, the same legal-advice line D-4's own closing statement already draws. **A real collision found and fixed while wiring this in**: R-065's `renewal-rollover-job.ts` auto-rolls a fixed-term lease with no successor in flight to MONTH_TO_MONTH the night after its term ends, and its candidate query never checked whether notice had been given at all - so a landlord's own served non-renewal (or a tenant's own notice to vacate) would have been silently overridden by that job the very next tick. Fixed with one `noticeGivenAt: null` clause in the query, covered by a new test case in `renewal-rollover-job.test.ts`. |
| D-57 | **R-068 ships as phase 1 of a deliberately split L item: the checklist engine and its status machine, stopping before photos, e-sign and auto-finalize** (R-068, INSP-01) | **No `status` column on `Inspection` - `scheduledFor`/`performedAt`/`tenantSignedAt`/`lockedAt` are facts, each written by the action that makes it true, and `inspectionStatus()` is the one place that reads them in order (LeaseStatus's own "derive, don't duplicate" call, restated for a fourth model). `InspectionTemplate.items` is one JSON column, not a child table - nothing ever queries into an individual template item on its own, only "copy the whole checklist" at inspection-creation time (`Lease.utilityResponsibility`'s own "structured but atomic" precedent). A checklist template is portfolio-wide, not per-property - the SAME template has to be walked at move-in and move-out for R-070's side-by-side comparison to mean anything, so scoping it to one property would work against the very feature that depends on it staying identical.** | **Phase 1 stops at a staff-recorded signature ("a tenant signed on the inspector's own phone", INSP-01's own wording) and a manual lock action - no tenant-portal e-sign, no auto-finalize-after-a-window job, no photo capture, no geotagging. Each is named rather than silently missing, and each is a real follow-on phase, not a redesign: `canRecordSignature`/`canLockInspection` already model the transitions a future e-sign/auto-finalize path would call into, they would just gain a second caller.** **Rejected: a client-side "Add item"/"Remove" button for building a checklist**, replaced with a fixed 8 blank row slots after a real e2e failure: CLAUDE.md's own documented trap ("onClick is inert until hydration") was not theoretical here - a Playwright click landing before hydration completed was a silent no-op, and the fixed-rows form needs no client JS to work at all, the same progressive-enhancement posture every `<form action>` server action in this codebase already gets for free. **Two pre-existing test-isolation flakes found while verifying the gate, neither caused by this item and both confirmed by rerunning in isolation**: `apps/web/lib/leases/renewal-check.test.ts` and `packages/core/property/property.test.ts` (and others) race on the same `(state: 'ZZ', version: 1)` `JurisdictionRule` row under full-suite parallel execution; `apps/web/lib/vendors/reissue.test.ts` asserts a global `AuthToken` count that can shift if another file mints one concurrently. Left unfixed as out of scope for this item; named here so a future session does not re-diagnose them from scratch. |
| D-58 | **R-068 phase 2: photos and geotags live on `Document` generically (`inspectionItemId`, `latitude`/`longitude`), a tenant's own portal signature locks the report in the same step a staff-recorded one does not** (R-068, INSP-01) | **`Document.inspectionItemId` follows the model's own "a named FK per real query path" rule (`workOrderId`'s own precedent) - a condition photo belongs to the CHECKLIST ROW it is evidence for, not the inspection as a whole. `latitude`/`longitude` are new, generic `Document` columns (not inspection-specific) since EXIF GPS is a property of the file itself; `extractGeotag()` sits beside the existing `extractCapturedAt()` in `documents/exif.ts`, using the SAME lite `exifr` build's own `.gps()` helper rather than pulling in the full build this file's own header already explains avoiding. Every inspection photo also gets `leaseId` set from the inspection's own lease, so the EXISTING tenant-portal document-visibility rule (`tenantCanSeeDocument`, DOC-03) already covers it with no new plumbing.** | **The tenant's own e-sign (`signInspectionAsTenant`) signs AND locks in one transaction; the staff path (`recordSignature`/`lockInspection`) stays two separate steps, unchanged from phase 1.** The difference is deliberate, not an oversight: a staff member recording a signature taken in person is a proxy for someone else's act and reasonably gets a review checkpoint before finalizing; a tenant signing from their own portal has already done their own review BY DEFINITION - the review page is the review - so a second staff-side checkpoint would just be a pointless extra click. Both paths write the identical `inspection.signed` audit action, distinguished by `actorType` (STAFF vs TENANT) rather than two separate actions for the same fact - the same choice R-063 already made between a staff-recorded and an e-signed lease signature. **Rejected: a public `/inspections/sign/[token]` magic-link page**, the shape `/sign/[token]` (R-063) uses for a lease. That page exists because a BRAND-NEW tenant signing their first lease has no portal account yet; an inspection happens DURING an active tenancy, so the tenant already has portal access by construction - a second, parallel public-token surface would duplicate `requireTenantWithScope()`'s own authorization for no reason. **A literal 3-day auto-finalize window** (`auto-finalize-job.ts`), the same "no JurisdictionRule concept exists for this, so a plain constant is honest rather than inventing config nobody asked to vary" call every sibling job (`renter-insurance-job.ts`, `renewal-window-job.ts`) already makes for its own day-count. |

| D-59 | **R-069: "move-in funds cleared" gates on the deposit charge only, checked live off `Payment.channel`/`status`, never a stored `clearedAt`; codes are withheld with a hard block, not a warn-and-override** (R-069, INSP-01) | **`chargeDeposit()`'s own R-063 comment named this item as the one that populates `Deposit` "once payment clears" - `deposit-clearing-job.ts` is that job, running daily (the same shape every window-watching job in this codebase takes) because a personal check's clearing moment is a time passing, not an event firing, so nothing can react to it synchronously. `fundsCleared()` (`packages/core/payments/clearing.ts`) is the one new rule: `Payment.status === 'SETTLED'` already means "received," per `recordOfflinePayment`'s own comment, but for `OFFLINE_CHECK` specifically that is NOT "safe to act on" - a personal check can still bounce days later (R-039's NSF path), while every other channel (ACH/card genuinely settled by Stripe, cash, money order) has no such gap. `CHECK_HOLD_DAYS = 5` is a plain literal, the same "no JurisdictionRule concept exists for this" call every sibling job already makes for its own day-count - how long a landlord waits on an uncertified check is business risk policy, not statute. A credited/waived deposit (balance zeroed with no real `Payment` behind it) never creates a `Deposit` row - there is no cash actually held, so there is no liability to record.** `accesscode.issue` is its own new privileged/MFA-gated permission (manager and owner, not maintenance tech - releasing keys to a tenant is leasing work, not a tech's own job-site reveal), and `accesscode.issued` is its own audit action keyed to the LEASE (not the AccessCode), read back with no new table - `accessCodesForLease()` derives "issued to this tenancy" from the latest matching AuditLog row, the same "derive, don't duplicate" call `inspectionStatus()` and `LeaseStatus` both already make. | **Only the deposit gates release, not first month's rent too.** `chargeDeposit()`'s own comment ties "move-in funds show cleared" specifically to the deposit charge; first month's rent is a separate, ordinary collection concern with its own dunning path, and folding it into a hard key-release gate would be scope invented past what the backlog row or its own breadcrumb asked for. A future item can widen the gate if an owner actually wants rent cleared too before handover. **A hard block, not a warn-and-override**, unlike the notice-period/retaliation warnings (R-055, R-066): those exist because a staffer might have a real reason to proceed anyway despite the warning; whether the money is actually safe to act on is not a judgment call, it is the entire point of the rule, so there is no reason field to override it with. **Rejected: a stored `clearedAt` timestamp on `Payment` or a `Deposit.clearedAt` column.** `fundsCleared()` is cheap, pure, and re-derivable from facts already on `Payment` (`channel`, `status`, `receivedAt`) - a stored flag would be one more place for the arithmetic to drift from the rule that produced it, the same "derive, don't duplicate" posture this file keeps reaching for. |

| D-60 | **R-070: the side-by-side comparison is the schema's own `InspectionItem.moveInItemId` FK, populated by copying the lease's own move-in checklist whenever a MOVE_OUT/PRE_MOVE_OUT inspection starts; the pre-move-out walkthrough right is a THIRD jurisdiction state (null = unreviewed)** (R-070, INSP-02; D-4) | **No pairing algorithm exists or is needed - `moveInItemId` already names which move-in item a move-out item compares against, so `itemsFromMoveIn()` (shared by `startInspection` and the new scheduling job) just copies the lease's own most recent MOVE_IN inspection's rooms/items into fresh rows with that FK set, and `getInspection`'s query includes the pair directly. `JurisdictionRule.preMoveOutWalkthroughRequired` is nullable, not a plain default-false boolean - the same "third state" posture `sourceOfIncomeProtected` (R-056) already established: asserting a state grants no such right without ever having reviewed it is exactly as wrong as asserting it does, so nothing is ever auto-scheduled for an unreviewed state. `preMoveOutWalkthroughDaysBefore` is real jurisdiction configuration (D-4), not a literal, because states that DO grant the right (e.g. California) specify how far ahead of the actual move-out it must happen - unlike R-069's `CHECK_HOLD_DAYS`, which is business-risk policy with no statutory analogue anywhere. A new daily job, `pre-move-out-scheduling-job.ts`, treats "a move-out is scheduled" as `Lease.noticeEffectiveOn` being set - R-066 already writes that column for either party's notice, and INSP-02 needs no second "move-out scheduled" fact to exist.** | **`startInspection` now silently prefers the lease's own move-in checklist over whatever template was picked, for MOVE_OUT and PRE_MOVE_OUT only.** Rejected: making the checklist field conditionally required/hidden by inspection type, which would need client JS a plain `<form action>` does not otherwise need anywhere else in this engine - a one-line hint under the field says what happens instead. **A real, unrelated bug found and fixed along the way**: `startInspection`'s lease lookup only ever matched `ACTIVE`/`MONTH_TO_MONTH` status, which meant a MOVE_OUT inspection started after `changeLeaseStatus` had already marked the tenancy `ENDED` - the ordinary, common case - found no lease at all, silently losing the `leaseId` link every downstream feature (portal visibility, the comparison itself, R-071's future deposit disposition) depends on. Widened to also match `ENDED`/`TERMINATED`, ordered by `startsOn desc`, on the reasoning that a single-family unit has at most one lease actually running or just finished at a time. **Rejected: enumerating "fixable" per state or per lease.** `isFixableCondition()` (POOR/DAMAGED/MISSING) is a fixed, product-wide read of the existing condition vocabulary, not a jurisdiction fact - no state statute defines what counts as fixable, so there is nothing here for D-4 to guard. |

| D-61 | **R-071: the disposition letter is a real `Notice` (DEPOSIT_DISPOSITION) reusing R-051's generate/serve machinery unmodified; the countdown freezes from `Lease.moveOutAt`, not `noticeEffectiveOn`; a shortfall beyond the deposit is disclosed, never auto-charged** (R-071, INSP-03; D-12) | **`Deposit`'s own disposition fields (`dispositionDueOn`, `dispositionSentAt`, `forwardingAddress`, `appliedCents`, `refundedCents`) all existed on the schema since R-041, unwritten until now - only `noticeId` (a real FK to the generated `Notice`) is a genuinely new column. `startDepositDisposition()` is called the same best-effort way `chargeDeposit()`/`syncLease()` already are, right after `changeLeaseStatus` commits a lease to ENDED/TERMINATED - `Lease.moveOutAt` (the REAL move-out fact that transition stamps) is what "the recorded move-out date" means, not `Lease.noticeEffectiveOn` (R-070's PLANNED date, which can predate the tenant actually leaving by weeks). A new `DepositDeduction` model is editable until `Deposit.dispositionSentAt` is set (app-layer gated, the same posture `InspectionItem.condition` takes against `Inspection.lockedAt` rather than a database trigger), each deduction carrying up to three independent evidence paths (`workOrderId`, `inspectionItemId`, an attached `Document`) - "unsupported" is derived from their absence, never stored. `depreciationGuidance()` is straight-line proration over staff-supplied age/useful-life estimates, not a jurisdiction fact (D-4 does not apply: no state publishes a useful-life table this product could read instead).** `finalizeDisposition` creates the `Notice` directly and hands off to `/notices/[id]` - the SAME page a PM already uses to generate a PDF and record service for every other notice type, so "delivery to the forwarding address logged" needed zero new code, only a new notice type in `KNOWN_NOTICE_TYPES`. | **Rejected: a second document-generation pipeline for the itemized letter.** `dispositionLetterText()` is a plain string template, the same shape `nonRenewalNoticeText()` (R-066) already established, rendered through R-051's existing `generateNoticePdf`/`recordNoticeService` rather than inventing a table-rendering block type for `renderBlocksPdf` - each deduction is a line, which is how a real deposit-return letter reads anyway. **Rejected: auto-charging a shortfall when deductions plus the outstanding ledger balance exceed what was held.** `DispositionTotals.additionalOwedCents` is computed and disclosed in the letter, but nothing creates a `Charge` for it - the tenant has moved out, billing has been canceled (`syncLease`'s own side effect on the same transition), and re-establishing collection against someone who left is a real, separate problem this item does not solve by half-building it. Left as a named gap for whichever future item owns post-move-out collections. **Rejected: a `DepositMovement` child table**, the shape `depositHeldCents()`'s own doc comment gestured at. `Deposit.appliedCents`/`refundedCents` are the schema's actual, already-existing shape (flat integers, not a movement log) - `computeDisposition()` writes those two numbers directly rather than inventing a table the schema was never given. |
| D-62 | **R-072: a turnover's checklist has no table of its own - each line is an ordinary `WorkOrder` tagged with `turnoverProjectId`/`turnoverStage`; `TurnoverProject` is only the envelope (target date, rent-ready timestamp); the days-vacant clock reads `Lease.moveOutAt` and the next lease's `moveInAt` live, storing neither** (R-072, LEASE-12, INSP-06; D-9) | **`TurnoverProject.leaseId` is unique - one project per move-out, started idempotently (`upsert`, not create-then-catch, so a racing duplicate cannot leave a caller's own transaction aborted) from BOTH places a unit goes MAKE_READY: the manual status change in `leases/actions.ts` and the nightly `unit.auto_make_ready` job. INSP-06's auto-draft hooks `lockInspection` directly - a locked MOVE_OUT report's POOR/DAMAGED/MISSING items (`isFixableCondition`, already built for R-070's pre-move-out list) become ticketless, unstaged `WorkOrder` rows in the same transaction as the lock, filed against whatever `TurnoverProject` already exists for that lease; there is deliberately no retroactive catch-up if the report is locked before the lease formally ends, since a project created ahead of a real `moveOutAt` would anchor the whole clock on a fact nobody has yet. Fixed along the way: `auto-make-ready.ts`'s own header already claimed it "writes moveOutAt for a lease that lapsed with no staff action" (`vacancy.ts`'s comment) - it never actually did; it now stamps `endsOn` as the honest move-out fact before starting the turn, closing a real gap in R-050's own vacancy-days reporting for exactly the lapsed-lease path this job exists to handle.** | **Rejected: a `TurnoverChecklistItem` table with its own status/vendor/cost columns.** D-9's "one Task queue, not a second one" is the same argument one level up - a make-ready line is a job somebody does, with a vendor, a schedule and a cost, which is what `WorkOrder` already is (`createWorkOrder`'s own comment already named a make-ready turn as ticketless from R-024). Duplicating that machinery for turnover alone would mean two places `closeDecision()`/`jobCostCents()`/vendor dispatch have to stay in sync. **Rejected: a hard-sequenced checklist that blocks PAINT until REPAIRS closes.** The backlog names an order, not a dependency graph, and no operator workflow in this product enforces one elsewhere (a PM who wants floors before paint is not wrong) - `TurnoverStage` is display vocabulary only. **Rejected: storing `daysVacant`/`actualDaysVacant` on `TurnoverProject`.** Both are always derivable from `Lease.moveOutAt` and the next lease's `moveInAt`, which already exist and are read by R-050's own `daysOnMarket()` - a third copy is exactly the drift class this codebase keeps naming and avoiding. |
| D-63 | **R-073: periodic/seasonal/drive-by inspections auto-schedule off a new `InspectionTemplate.defaultForType` flag, not a name guess or new config table; a unit never inspected before is due immediately, not a year from whenever its row was created; the cadence itself is a plain literal, not jurisdiction or property config** (R-073, INSP-04) | **`PERIODIC`/`SEASONAL`/`DRIVE_BY` have existed in `InspectionType` since R-068 and a PM could already start one by hand from `/inspections/new` - this item only adds the auto-scheduled half. `periodic-scheduling-job.ts` mirrors `pre-move-out-scheduling-job.ts`'s own PULL shape exactly: no event makes a unit "due," only the calendar does, so it re-checks every property-local day. A type is scheduled at all only once a PM marks one checklist as that type's default (`InspectionTemplate.defaultForType`, unique per type) - the same "nothing on file, so nothing happens" posture D-60's `preMoveOutWalkthroughRequired` already established for a jurisdiction fact nobody has reviewed yet, borrowed here for a checklist nobody has designated yet. `nextPeriodicDueDate()` (`packages/core/inspections/periodic.ts`) is the one piece of real logic: `lastPerformed` plus the type's interval, clamped to the target month's actual last day so a Feb-29 anchor does not roll into March. `PERIODIC_INTERVAL_MONTHS` (12/6/3) is a bare literal, marked `ponytail:` in the source - no PRD text makes inspection cadence statutory the way entry-notice hours or the pre-move-out window are (D-4), so treating it as owner-configurable business policy nobody has asked to vary yet would be inventing config, the same trap D-59's `CHECK_HOLD_DAYS` and D-60's own `preMoveOutWalkthroughDaysBefore` distinction both name. Idempotency is dual-layered exactly like every other scheduling job here: `inspections: { none: { type, performedAt: null } }` stops a second cycle queuing behind an unperformed one, and `Task`'s own `(type, subjectId, businessDate)` unique index stops a duplicate the same tick.** | **Named gap, not fixed here: no entry-notice compliance is wired into PERIODIC scheduling**, despite `packages/core/entry/notice.ts` (R-027) existing exactly for a non-emergency staff entry into an occupied unit. This follows R-070's own precedent rather than diverging from it - `PRE_MOVE_OUT` (also an interior walk of an occupied unit) does not serve one either, and fixing that gap for one lease-event-driven type and one calendar-driven type at once was more than this item's own scope. A future item should wire `entryDecision()`/`entryNoticeText()` into whichever inspection-scheduling path serves the interior first, covering both. **Rejected: guessing a template by name** (e.g. matching on "annual" or "periodic" in the checklist's title) - fragile, and a template's name is display text a PM can freely rename; a real column the scheduling job reads directly is the same "derive from a fact, not a string" posture this codebase keeps taking. **Rejected: a `TurnoverStage`-style new enum/table for periodic cadence.** Three types, one literal interval each, needs no more structure than a `Record`. **Rejected: anchoring a never-inspected unit's first due date to `Unit.createdAt`.** The system has no real evidence of when, or whether, a unit was ever periodically walked before this feature shipped; treating it as already-overdue is the conservative, honest default, not a guess dressed as one. |
| D-64 | **R-074: a tenant's self-guided move-in report is authorized by a new `Inspection.selfGuided` flag, not by `type === 'MOVE_IN'` alone; the walkthrough window escalates to a staff Task rather than auto-locking blank rows** (R-074, INSP-05) | **The staff walk (R-068) and the tenant's own self-guided walk share the identical `Inspection`/`InspectionItem` rows and lifecycle facts - `recordItemAsTenant`/`recordItemPhotoAsTenant`/`finishInspectionAsTenant` in `lib/portal/inspection-actions.ts` are the tenant-scoped mirror of `recordItem`/`recordItemPhoto`/`finishInspection`, with the actual writes factored into one shared `lib/inspections/item-writes.ts` both sides call. `type === 'MOVE_IN'` alone cannot gate the tenant's write: a traditional staff-performed MOVE_IN inspection staff hasn't gotten to yet is indistinguishable from one a tenant was asked to complete unless something says which is which - `selfGuided` (staff-set, via a checkbox on `/inspections/new` shown only for MOVE_IN) is that fact, and every tenant action checks it, not just the type. Signing reuses `signInspectionAsTenant` (R-068 phase 2) completely unmodified - once `finishInspectionAsTenant` sets `performedAt`, the existing sign form is already the right next step. The walkthrough-window job (`auto-finalize-job.ts`, a second `SCHEDULED_JOBS` entry alongside the existing 3-day signature window) never locks an unwalked report: a report with blank rows is not evidence of anything (`canFinishInspection`'s own reasoning, R-068), so unlike the signature window this one escalates - a staff `Task` (D-9's one queue) plus a tenant reminder, seven days after creation, idempotent via `createTask`'s own unique key keyed on the fixed due date rather than "today."** | **Rejected: reusing `type === 'MOVE_IN'` with no new column** - considered first, for the smaller diff, but rejected once it became clear it would let a tenant write items on a traditional staff-in-progress MOVE_IN inspection, and would force the walkthrough-window job to choose between ignoring every staff-performed-in-person MOVE_IN (defeating its purpose) or false-escalating on all of them. **Rejected: auto-locking a never-walked report at the window's end, mirroring the signature window exactly** - a locked report with every item still blank is not the deposit-defense evidence R-071 needs; escalating to a human (chase the tenant, or go walk it in person) is the honest response to nothing having happened, not manufacturing a record that looks like something did. **Left behind:** no "days remaining" countdown shown to the tenant on the walkthrough page itself - the existing signature-window job shows none either, and the reminder notification at day 7 is the parallel signal. No portfolio-wide "self-guided reports outstanding" list; only the single tenant-facing banner on `/portal/papers` and the escalation Task surface it. |
| D-65 | **R-075: `packages/core/metrics` holds only the metrics that had NO single tested home before it — occupancy, days-to-fill, first-response duration, time-to-resolve-by-priority, maintenance-cost-per-unit-per-month, renewal rate, turn cost. Days-vacant, delinquency buckets and `daysPastDue` deliberately stay in their own existing domain modules, not moved here** (R-075, PRD §9) | **Of the ten metrics the backlog names, three already had one correct, tested definition in a domain-appropriate home: `daysOnMarket` (`@rental/core/units`, R-050), `bucketFor`/`delinquencyFor`/`agingTotals` (`@rental/core/ledger`, R-044), and `daysPastDue` (`@rental/core/money`, R-045 - its own doc comment already names and defends against the exact "computed from the last retry, not the original due date" bug this backlog row warns about). Relocating them into a new `metrics` directory would re-point every existing caller and test file for no behavior change, and this codebase has no precedent anywhere of one domain's `index.ts` re-exporting a sibling's - each existing metrics test file (`vacancy.test.ts`, `aging.test.ts`, `money.test.ts`) stays exactly where callers already expect it. The remaining seven needed real work: `occupancyRate`, `daysToFill` and a `firstResponseHours`/`resolutionByPriority` pair were written fresh (nothing computed them anywhere, confirmed by grep before writing). `renewalRate` (extracted verbatim from `apps/web/lib/dashboard/queries.ts`'s own inline loop) and `turnCostCents` (extracted from `apps/web/lib/turnover/queries.ts`'s inline `.reduce()`) were already correct, just inline - both call sites now import the core function instead. `getTurnoverForUnit`'s own days-vacant figure was previously FAKED by calling `daysOnMarket()` (a different, open-ended metric - "how long has this unit been sitting empty as of today") with its `unitCreatedAt` fallback forced equal to `lastMoveOutAt` to neutralize a branch that does not apply to a terminal, already-filled vacancy - `daysToFill()` is that number, named and tested as its own thing rather than borrowed from a function built for a different question. The roll-up-equals-sum test the backlog text calls out lives in `turnover.test.ts`: the turnover panel's own itemized `costCents` per work order and its `totalCostCents` header are both built from the identical `jobCostCents()` call, so a test asserts they can never disagree.** | **"Every metric has ONE written, tested formula" is the rule this item satisfies - "every metric's file physically lives under `packages/core/metrics`" is not the same rule, and moving three already-correct, well-tested modules to satisfy a directory-naming reading of the backlog text would be churn with a real regression risk (every import site, every existing test) for zero behavior change. `costPerUnitPerMonth` is division only, not aggregation - the sum a caller feeds it is a plain `.reduce()` it already has to build the total, and wrapping a one-line sum in a core function called from a single future caller would be the same "config for a value that never changes" shape this codebase avoids elsewhere, just for arithmetic. Neither `occupancyRate` nor `resolutionByPriority`/`firstResponseHours`/`costPerUnitPerMonth` are wired into a screen yet - R-076 (the five weekly saved views) and R-081 (maintenance analytics) are their first real callers; this item's own scope is the formula and its test, not a new dashboard tile.** |

| D-66 | **R-076: three of the five weekly reports are existing pages, unchanged or fixed in place - not duplicated under a new route; only cash-summary-per-entity and upcoming-critical-dates are genuinely new** (R-076, RPT-04) | **Rent roll + delinquency aging was already a complete, tested report (`/money/rent-roll`, R-044) - the "first-class saved view" RPT-04 asks for already existed under a different name, so `/reports` links to it rather than rebuilding it. Open work orders by age/priority was `/workorders`, whose OWN prior comment already named the gap ("a bare, unsorted-by-priority list of everything open") - fixed on that page directly (sort by `priorityRank()` then `createdAt`, an age-in-days shown per row) rather than standing up a parallel report page nobody would then keep in sync with it. Vacancy/turn status extends `/vacancies` (R-050) with each unit's own turnover stage - derived, not stored, as the earliest-in-sequence `TurnoverStage` still open among a `TurnoverProject`'s work orders (`currentStageFor()`), matching `TurnoverProject`'s own schema comment that nothing tracks a status column of its own - plus a "this week" leasing-activity summary (three plain counts, not a metrics abstraction nothing else needs). Cash summary per entity groups `collectedVsBilled()`'s own per-property month-boundary arithmetic (R-050) by legal entity rather than summing across the whole scope, adding "biggest outflows" from `jobCostCents()` over the period's closed work orders. Upcoming critical dates unions four already-tested sources into one 60-day window - `filingCabinetAlertsDue()` (R-015), lease `endsOn`, `RenterInsurancePolicy.expiresOn` (R-067), `Deposit.dispositionDueOn` (R-071) - re-filtering each to the window this REPORT promises rather than trusting each source's own internal alert threshold (mortgage balloon maturity's is 180 days, wider than what "next 60 days" means here).** | **Rejected: a `SavedView` persistence model.** Grepped for one before building; none exists, and RPT-04's own "as first-class saved views" phrasing reads as emphasis that these get real page routes a PM can bookmark or return to - not a request for user-configurable, persisted filter state, which nothing else in this product has either. **Rejected: reserve-vs-target on the cash summary.** No reserve target is configured anywhere in the schema - R-082 is the item that will build reserve targets and a capital plan; showing a number here would be inventing one nobody set, the same "null means unconfigured, never guessed" posture this codebase takes everywhere else. **Rejected: including statutory compliance dates in the critical-dates report**, despite RPT-04's own prose naming "compliance" - that calendar is R-077's, does not exist yet, and R-050's "Renewals & alerts" tile already established the house style of naming the gap honestly rather than quietly overclaiming coverage. |

| D-67 | **R-077: one generic `ComplianceItem`/`ComplianceCompletion` pair covers every obligation PROP-05 names except mortgage ARM/balloon and insurance renewal, which already had their own tested date fields (R-015) and are unioned into the calendar rather than duplicated; recurrence is anchored to when an item was actually completed, not a fixed calendar slot** (R-077, PROP-05) | **`ComplianceItem.type` is free-form, the same posture `Notice.type` already takes - a license or filing this product has never heard of needs no migration. A hand-written CHECK constraint enforces exactly one of `propertyId`/`legalEntityId` (Prisma has no XOR of its own), since a property-level license and an entity-level LLC annual report are different facts, never both-optional. `nextComplianceDueDate()` is a deliberate ten-line near-duplicate of `nextPeriodicDueDate()` (R-073), generalized to take the interval as a parameter instead of a type-keyed lookup - D-65's own call again: moving or force-sharing an already-correct function across domains for a slightly different caller is churn with real regression risk, not a fix, for code this size. `ComplianceCompletion` rows are never edited or deleted (app-layer discipline, the same posture `RenterInsurancePolicy`/`AccessCode` versioning already take, not a database trigger) - "when was this last done" is always the most recent row, and a correction is a new one. The alert job runs once per property (`SCHEDULED_JOBS`' own shape) but needs no special-casing for an entity-level item spanning several properties: `Task.subjectId` is the compliance item's own id regardless of which property's run raised it, so the SAME "already flagged" check every other job in this codebase uses naturally dedupes it across every property that would otherwise re-check it the same day.** | **Closes the gap R-050 and R-076 both named honestly rather than overclaiming** ("Renewals & alerts... not statutory compliance"; "not yet tracked anywhere in this product — see R-077") - `upcomingCriticalDates()` (R-076) now unions `ComplianceItem` in alongside its other four sources, re-filtered to that report's own 60-day window, rather than this item standing up a second, separate "what's coming due" screen. **Rejected: per-category tables** (a `RentalLicense` model, a `SmokeDetectorCert` model, and so on for every PRD-named obligation) - every one of them is genuinely the same shape (a label, a due date, an optional recurrence, a completion log), and D-9's "one Task queue, not many" argument applies one level up here: a dozen near-identical tables would mean the alert job, the completion-recording action and the critical-dates union each re-implemented per category instead of once. **Rejected: a single global lead-time constant.** PROP-05's own examples span a 7-day smoke-detector check and a 60-day insurance-style notice; `leadTimeDays` is per-item because nothing in this product's history has ever found one number that fits every obligation shape. |

| D-68 | **R-079: invoice lifecycle status ("received → approved → paid") is derived, not a stored enum column - only `invoicePaidAt`/`invoicePaymentMethod` are new stored facts. `fallbackVendorsForTrade()`'s existing `trade` parameter widens to accept `null` rather than a second query function. `vendor.write`/`vendor.read` (record CRUD) are portfolio-wide, no resource, matching `jurisdiction.write`'s existing posture for an entity with no property column of its own** (R-079, MAINT-11, MAINT-09) | **`invoiceLifecycleStatus()` (`packages/core/vendors/invoice.ts`) reads three already-true facts - `invoiceCents`, the SAME `reapprovalCheck()`/`actualTotalCents()` tolerance check `closeWorkOrder`'s own action already runs (D-42), and the one genuinely new fact `invoicePaidAt` - rather than a fourth copy of ceiling math or a status column that could drift from what those functions would say. An in-tolerance invoice auto-routes straight to `approved`; there is no reachable `'received'` state, so the type does not name one. The vendor-facing page (`/vendor/[token]`) and the staff page (`/workorders/[id]`) both call the identical computation so the vendor and the PM can never be told two different stories. `fallbackVendorsForTrade(vendors, trade, now, excludeVendorIds)`'s `trade` parameter widened from `string` to `string \| null` - `null` skips the trade filter and ranks every active vendor, needed for a ticketless (standalone/turnover) work order that has no single category to filter by; existing callers passing a real trade are unaffected. `Vendor.trades` is free-form lowercase (its own schema comment) while `Ticket.category` is a closed uppercase enum - a real, pre-existing vocabulary mismatch, handled by lowercasing the category before matching rather than silently forcing one vocabulary to fit the other. `vendor.write`/`vendor.read` pass no resource to `requirePermission`/`actorCan` - `Vendor` has no `propertyId` at all, so there is no scope to check against; a property-scoped manager cannot manage the vendor list, matching the portfolio-wide posture `JurisdictionRule`/`DocumentTemplate` already take. This is separate from the existing, unchanged, resource-carrying `vendor.read` check on the work-order assignment page, which does let a property-scoped manager see vendors when dispatching a job on their own property. `FORM_1099_THRESHOLD_CENTS` ($600) is a plain literal, not jurisdiction config - D-4's "never hardcode" rule is about state/local law that varies and changes locally; this is one federal dollar figure, named as a constant only because Congress has moved it before, not because it is owner-configurable.** | **No admin surface for "manage vendor records" existed before this item at all** - a vendor row could only ever be created by hand in the database, despite MAINT-11's own PRD text opening with "As a PM, I manage vendor records." New `/vendors`, `/vendors/new`, `/vendors/[id]` pages are the entire CRUD surface. **`fallbackVendorsForTrade()` itself was already fully built and tested (R-025) but never called from anywhere in the app** - only referenced in a code comment; the work-order assignment picker was calling a flat, unranked `activeVendors()` instead. `w9OnFile`/`coiExpiresOn`/`preferredRank` existed on `Vendor` since R-025 but were never read by any dispatch flow before this item - the assignment picker now surfaces "no W-9"/"COI expired" as label warnings rather than filtering the vendor out: MAINT-11 blocks or flags PAYMENT without a W-9, it does not forbid dispatching the only plumber who answers at 2am. **Rejected: filtering an unqualified vendor out of the picker entirely** - a PM needs to see and choose them anyway; silently hiding the row would leave the list looking short with no explanation. **Rejected: a live QuickBooks API push for the 1099-NEC totals** - OQ-8 (bookkeeping system, cash vs. accrual) is still unanswered and gates R-078/R-081, not this item; `vendorPaymentTotalsForYear()` reads `jobCostCents()` (D-42's "what the books record" figure, not `actualTotalCents()`) so the number is correct whenever an export is eventually built. |

| D-69 | **R-080: no `PROPERTY`-vs-`UNIT` scope on a preventive-maintenance template - every recurring task creates one work order per unit, since `WorkOrder.unitId` is required and this is a single-family-first product. Cadence tracking tags `WorkOrder.pmTemplateId` rather than a separate schedule table. The batch run is PM-triggered, not a nightly `SCHEDULED_JOBS` entry. Vendor territory match is a new `vendorCoversProperty()`, empty `serviceAreas` means no restriction** (R-080, MAINT-08) | **`nextPreventiveDueDate()` (`packages/core/workorders/preventive.ts`) is a deliberate THIRD near-duplicate of the same clamped-month-rollover algorithm as `nextPeriodicDueDate()` (R-073) and `nextComplianceDueDate()` (R-077) - D-65/D-67's call made a third time, for a ten-line function. `PreventiveMaintenanceTemplate` is free-form and PM-created, the same "nothing hardcoded" posture `InspectionTemplate`/`ComplianceItem` already take - MAINT-08's own ~11 named categories (HVAC filters, gutters, winterization, smoke/CO, water heater flush, dryer vent, sump pump, pest, well/septic, chimney) are illustrative text, not seeded rows; a PM adds what their portfolio actually needs. `WorkOrder.pmTemplateId` tags which template a batch-created job fulfills, the identical shape `turnoverProjectId` (R-072) already established - "when was this last done" is read straight off the latest CLOSED job carrying that tag, not a second table tracking schedule state. The batch action is a plain server action (`runPreventiveBatch`), not a `SCHEDULED_JOBS` entry - MAINT-08's own PRD text says "one click creates the batch," a PM-initiated bulk action, unlike R-073's fully-automatic overnight scheduling. `vendorCoversProperty()` (`packages/core/vendors/territory.ts`) is new: `Vendor.serviceAreas` (R-079) existed but was never matched against anything before this item. Empty `serviceAreas` covers everywhere, the same "absence of a fact is not exclusion" posture `preferredRank: null` and `fallbackVendorsForTrade`'s `trade: null` (R-079/D-68) already take. Auto-assignment composes two calls, not one entangled function: `vendorCoversProperty()` filters by territory first, then the EXISTING `fallbackVendorsForTrade()` ranks by trade/preference on what's left - no modification to that already-tested function. Template CRUD and the batch run both use `requirePermission('workorder.write')` with no resource, the SAME coarse-permission-then-scope shape the existing `/workorders` list page already takes: any actor who may write work orders may manage templates, and the batch's actual reach is bounded by `currentScope()`, identical to every other work-order query in this product.** | **"Smoke/CO test-and-battery logged at every entry" has no hook to attach to - no generic "staff/vendor entered this unit" event table exists anywhere in this codebase (entry facts live only as columns on a specific `WorkOrder`, R-027). Rather than building new entry-log infrastructure for one phrase in MAINT-08's PRD text, smoke/CO is just another recurring seasonal-batch template like every other one (a PM sets its cadence, e.g. annual) - the per-visit "every entry" granularity is a named gap, not fixed here, matching D-63's own precedent for a gap named honestly rather than silently worked around. **Rejected: HVAC-filter "delivered to tenant with photo confirmation" as a distinct tenant-notification workflow** - reuses the EXISTING completion-photo convention on `markWorkComplete()` instead; a filter-change batch job is an ordinary work order, no new mechanism. **Rejected: a `PROPERTY` scope for gutters/chimney/well-septic/sump-pump** (nominally property-level tasks) - `WorkOrder.unitId` is required and this product has no "primary unit per property" concept; a multi-unit property gets one job per unit rather than one shared job, a known limitation, not built around, since the product's own framing (10-50 units, single-family-first) makes 1-property-1-unit the overwhelmingly common case. **No seed data for MAINT-08's ~11 example categories** - matches `InspectionTemplate`'s own precedent of a PM-populated list, not a fixture. |

| D-70 | **R-083: the cure clock is DERIVED from `NoticeDelivery.permittedByJurisdiction` (R-051's stored verdict), never recomputed — and it is the gate that refuses a premature filing. Stage is a Prisma enum whose ORDER is enforced in core, not in Postgres. `EvictionCost` is the first owner-side outlay in the schema; lost rent is deliberately not enterable. `eviction.manage` is its own permission** (R-083, PAY-14) | **"Defective service restarts everything" needed no new service machinery at all: R-051 already decided, at the moment of service, whether the method used was one the state names for that notice type, and stored it three-valued on the delivery row (D-48). `cureClock()` (`packages/core/evictions/cure.ts`) consumes it — the period runs from the EARLIEST GOOD service, so a bad Monday service followed by a good Wednesday one starts Wednesday, while two good services cannot be used to restart a clock by re-serving. `permittedByJurisdiction: null` counts as GOOD service, deliberately: refusing to start a clock because this product has not been taught a state's rules would invent a defect the law never asserted, the same call D-48 and R-044's `graceUnknown` already make. `readyToFile()` is the one place this product actively stops a PM, and it stops the two mistakes that get a case dismissed and everything restarted — filing while the tenant can still cure, and filing on service the state does not name. An UNCONFIGURED cure period does NOT block filing (that would substitute this product's ignorance for the owner's attorney); the packet prints "not configured for this jurisdiction" instead, and no deadline is ever guessed. `EvictionStage` is a real Prisma enum, unlike `Notice.type` — these are not operator data a state can invent but the fixed procedural ladder every court runs, the same reasoning that makes `NoticeServiceMethod` an enum. Order lives in `canAdvanceTo()` rather than the database: forward one rung at a time, but CLOSE FROM ANYWHERE, because the commercially right answer (they pay, they take cash for keys, they leave) can arrive at any point and a product that made "we settled" harder to record than "we got a writ" would be telling an owner something it has no business telling them. `EvictionCost` is neither a `LedgerEntry` (that is an append-only Stripe projection, D-11, and a district-clerk filing fee is money Stripe never saw) nor a `Charge` (billed TO a tenant, and recoverability is the court's judgment). The attorney packet is ASSEMBLY, not new machinery — `renderBlocksPdf`/`appendPdfs`/`DocumentBlock` (R-051→R-052) and `statementForPeriod` all already existed; what did not was one bundle. D-50's rule governs it: the exhibit index names every attachment that could not be included, and a second render corrects the index when one fails to parse — which for a packet is the COMMON case, not the rare one, since a JPEG is not a PDF and `appendPdfs` correctly refuses it.** | **`eviction.manage` is its own permission rather than folded into `lease.write` or `notice.send`** — starting an eviction is the most consequential thing this product does to a person, and it should be possible to hand somebody the leasing and notice-serving job without also handing them that. `notice.send` still gates the legally operative act (serving), so the two are checked at different moments. Both `eviction.case_opened` and `eviction.case_closed` are on REASON_REQUIRED, extending the reasoning R-047 already applied one step earlier to `payment.hold_changed`: a case opened for no recorded reason is indistinguishable from a retaliatory one, which is the exact claim it will be defended against. **NOTHING IN THIS ITEM FILES ANYTHING ANYWHERE**, and there is deliberately no integration with any court, clerk or process server — PAY-14's own "never files anything" is stated in the module headers, the schema comments and on the screen itself. **Rejected: a separate stage-transition history table** — `WorkOrderStatus`/`TurnoverStage` already established that this repo keeps current state in a column and reconstructs history from `AuditLog`, and `eviction.stage_changed` rows carry each transition. Forward-looking dates (a hearing has a time, and a PM who reads "the 14th" and misses it loses by default) are columns, which no transition log could hold. **Rejected: hand-entered "lost rent"** despite PAY-14 listing it beside filing and attorney fees — it is the ledger balance, the packet prints the statement, and a typed figure sitting next to a computed one could only agree by luck; the two disagreeing on the same exhibit is the one thing opposing counsel would need. `LOST_RENT_IS_DERIVED` exists so the next reader finds the reasoning rather than the gap. **Rejected: embedding Stripe's invoice PDFs in the packet** — `exportLedgerStatement` already does that (D-50) and repeating a slow, failure-prone provider round trip inside an already-large bundle buys nothing. |

| D-71 | **R-078: the accounting basis is a REQUIRED PARAMETER on the tax export, not a default (answers the buildable half of OQ-8). `CapitalImprovement` is a standalone record with an optional, unique work-order link. Every unmappable row goes on an exception list carried in the same file as the totals, and the reconciliation identity is asserted in a test** (R-078, RPT-03, PROP-07) | **The owner chose "both, basis is a parameter" on 2026-08-21 rather than picking one. That is the more expensive answer and it is the right one for a reason the question surfaced: the same work order books in two different tax YEARS depending on the basis — closed 28 December, invoice paid 6 January — so a basis baked in as a default would be a silent decision about which return a deduction lands on. It is a URL parameter, visible on the screen and in the CSV filename, and the two bases read genuinely different tables: cash-basis income is `LedgerEntry` rows that CARRY A PAYMENT (the Stripe projection, D-11), accrual-basis income is `Charge` rows by `dueOn`. The cash rule is `paymentId != null` rather than a `LedgerEntryType` filter, because `webhook.ts` sets `paymentId` on exactly the rows where money moved — the charge-linked rows, the unlinked subscription remainder, and the reversals a return produces — and on nothing else; a three-value type filter would have had to get all three right and would silently miss the remainder row, which is the LARGEST income line in the system. **`CapitalImprovement` is its own model, not a flag on `WorkOrder`**: PROP-07 names "roof 2024, HVAC 2023", improvements that predate the platform and have no work order to carry a flag, and a roof bought straight from a contractor never passes through maintenance at all. The optional `workOrderId` is `@unique` and load-bearing rather than tidy — the export excludes exactly the jobs named there from deductible repairs, so two improvements pointing at one job would make that exclusion ambiguous and the same dollars could be claimed twice, once on line 14 and again as a depreciable asset. **`inServiceOn` is nullable on purpose**: recording the project and pinning the in-service date are two moments, and a nullable date that lands on the exception list is honest where a required one would produce a typed guess that gets depreciated. Schedule E income mapping is nearly flat — late fees, pet rent, NSF and utility reimbursement are all line 3 alongside rent, because the form does not separate them however separately this product's ledger does. Two values are deliberately not income: `DEPOSIT` is a LIABILITY until forfeited or applied (R-071 decides which), and `OTHER` means nobody classified it, so mapping it to rents received would be a guess wearing the costume of a fact.** | **Rejected: adding a `paidOn` column to `UtilityBill`** so the owner's absorbed share could book on a cash basis. It would have made cash-basis utilities work, and it was still the wrong trade for this item: the column needs a form field and a RUBS-flow change, and the exception it avoids is one that NAMES ITS OWN FIX on screen. Left behind for whoever owns utility-bill CRUD, and recorded in PROGRESS rather than fixed quietly. **Rejected: computing depreciation.** Method, recovery period and convention are the preparer's call, and a product that guessed would be handing somebody a number they would file. The CapEx schedule gives a preparer everything needed to compute line 18 and says on the page that it deliberately stops there. **Rejected: per-entity chart-of-accounts overrides.** OQ-8's other half — whose books, in which system — is still unanswered, and inventing a mapping-override UI before anyone has said what they are mapping TO is the wrong order; the QuickBooks account names are QuickBooks' own rental defaults, written down and visible on every row. **Nine Schedule E lines cannot be filled at all** (advertising, auto/travel, commissions, insurance, management fees, mortgage interest, other interest, supplies, taxes) because this product records those as obligations or dates and never as money paid. They are NAMED ON THE PAGE with the reason, not left blank: a missing expense line reads as a zero, and a zero overstates income. Mortgage interest is R-081's 1098 reconciliation. **The `counts` block is an exhaustive reconciliation, not decoration** — `mapped + excepted + outOfYear + capitalised === facts`, asserted on both bases, and proven to fail by removing one counter. A count that does not have to add up cannot catch a dropped row, which is the whole of RPT-03's "nothing silently dropped". |

| D-72 | **R-081a: the operating report RE-GROUPS R-078's export lines rather than fetching income and expenses again; spend is attributed to a trade by what the work WAS, never by the vendor's registered trades; and the operating report defaults to ACCRUAL where the tax export defaults to cash** (R-081a, RPT-05) | **One income/expense pipeline, two reports.** `taxExportFacts` already returns per-property, Schedule-E-classified lines carrying a `BusinessDate`, and it is the only place that knows three rules that are all SILENT when wrong: the ledger sign-flip, the capitalised-job exclusion, and reading a timestamp in the property's own zone rather than the server's. A monthly P&L is that same list grouped by `(propertyId, month)`, so a second pipeline would have meant a second copy of all three. Spend-by-trade is derived from those same lines too — not from its own work-order query — which is why the trade columns cannot disagree with the expense total printed above them on the same page; the extra query is keyed on the work-order ids ALREADY in the report, so a job cannot appear in one and not the other. **Trade attribution is the ticket's category first, then the preventive template's trade, then an `UNATTRIBUTED` bucket — and deliberately never `Vendor.trades`**, for two independent reasons. It is a LIST: a vendor registered for `['plumbing','hvac']` has no single trade, so attributing by it either double-counts the money or picks one arbitrarily, and a spend-by-trade report whose columns sum to more than total spend is worse than no report. It is also the wrong FACT: a vendor's trades are what they are ABLE to do, and this report asks what the work WAS. Normalised to upper case because the two vocabularies were never unified (`Ticket.category` is an uppercase enum, `PreventiveMaintenanceTemplate.trade` is lowercase free text) — unfolded, one trade becomes two columns. **The operating report defaults to accrual and the tax export defaults to cash, and the difference is not an inconsistency.** On a cash basis R-078 sends owner-absorbed utility bills to the exception list, because nothing records when a utility bill was PAID — so a cash-basis operating P&L understates expenses by exactly that amount, silently. An operating report also wants a cost booked to the period it belongs to rather than the month a cheque cleared. Both keep the picker, because D-71 settled that the basis is the reader's call. | **A row's `createdAt` is not a fact about the world**, and using `unit.createdAt` as the vacancy floor deleted history: migrate a portfolio in and every unit's `createdAt` is the migration date, so all vacancy before it silently becomes zero. `availableFrom()` takes `acquiredOn` when recorded, and otherwise the earlier of "the property row existed" and "a tenancy had already started" — a lease predating the row is itself evidence the unit was there. **The move-out day counts as a vacant day and the move-in day does not**, matching `daysOnMarket`'s existing convention — the two numbers would otherwise disagree by one day for every turn, and the vacancy report and the vacancies page would be visibly inconsistent about the same unit. **`availableFrom` exists so a newly-acquired property is not the worst performer in the portfolio**: days before the owner had the house are not vacancy, they are days somebody else owned it, and `vacancyRate` returns **null rather than 0** when nothing was available, because 0% reads as "never empty". **CapEx and deposits are excluded from the operating P&L**, and both exclusions are why R-078's sections exist: charging a whole roof against the month it was fitted would show one catastrophic month and eleven flattering ones, and a deposit is money held, not earned. **Every month in the window gets a row even when nothing happened in it** — a missing month reads as a gap in the data rather than a quiet one, and the columns must line up across properties to be comparable. **The first real `<table>` in this app**, a deliberate break from the divided-`<ul>` house style: every other list here is a list of links, but this is a grid where a cell means nothing without both its row and its column, and `<th scope>` is how that pairing is announced. **Rejected: a CSV export.** RPT-02 and RPT-03 both name CSV and RPT-05 does not; the tax export already produces the line-level file, and this report's job is to be read on a screen and sorted worst-first. |

| D-73 | **R-081b: the 1098 is RECORDED, not reconciled; card payments are excluded from the 1099-NEC figure; deposit liability is a BALANCE that respects the disposition date; and the packet ships as a screen and one CSV, with the archived PDF split to R-081d** (R-081b, RPT-07) | **Owner decisions, 2026-08-21.** *The 1098:* `Mortgage` holds the loan and nothing anywhere records a payment against it, so there was nothing to reconcile a 1098 AGAINST — the choice was between recording the lender's own figure, building a whole monthly payment schedule, or leaving Schedule E line 12 blank. `MortgageAnnualStatement` (one row per loan per tax year, `@@unique(mortgageId, taxYear)`, the form itself as a `Document`) gives line 12 a real number and the page says it came from the 1098 rather than from anything this product computed. The uniqueness is load-bearing: two rows would double the interest, which is an overstated deduction rather than untidiness, and the write is an upsert so a corrected 1098 replaces the figure. A `MortgageInterestFact` produces a line with **`bookedOn: null`** — a 1098 covers a calendar year and names no month, which is precisely why it does not appear in R-081a's monthly grid; putting an annual lump into December would invent detail nobody recorded. *The packet form:* screen + CSV now, archived PDF as R-081d, so the numbers landed before the assembly work. **CARD PAYMENTS ARE EXCLUDED FROM THE 1099-NEC REPORTABLE FIGURE**, and this is the correctness point of the whole schedule: a card payment is reported by the PROCESSOR on a 1099-K, not by the payer on a 1099-NEC, so $400 by card plus $300 by cheque is a $300 1099-NEC and not a $700 one — which is exactly why RPT-07 asks for the list "by payment method" rather than by total. Both figures are shown so the difference is never a mystery. **Deposit liability is a balance at 31 December and respects the disposition date**: a deposit disposed of in March 2027 was still held IN FULL on 31 December 2026, so subtracting its applied and refunded amounts from that year would report a liability the owner did not yet have. Vendors under the threshold stay on the list, because "just under" is a judgement for the filer to make with the number in front of them rather than one this product makes silently. | **`Deposit.heldCents` is GROSS and never decremented**, and the schema comment saying it "is what is still held" was wrong — it reads as already net and would double-count a disposition. `rent-roll.ts` has always used `held − applied − refunded` and `computeDisposition`'s own doc comment describes the same thing; the comment is corrected in this item. **The 1099 query is one round trip for every vendor**, not `vendorPaymentTotalsForYear` in a loop — that function takes a single id, and a candidate list built on it would be one query per vendor on a screen opened in January with the whole portfolio in scope. It uses the same hard UTC year window that function does, deliberately: a vendor total is not a per-property figure (one cheque can cover jobs at three houses in two zones), so there is no property clock to read it in, and the 1099 is a calendar-year federal form. **The deposit "as of" is UTC rather than per-property** for the same reason — a balance is a legal position on a date, and one entity reporting its liability as of several different moments is worse than being a few hours out at the boundary. **Rejected: TIN/EIN on `Vendor`.** The packet flags a missing W-9 and names the vendor; the number itself belongs on the W-9 document, and `LegalEntity` already declines to hold an EIN for the same reason (`schema.prisma`'s own comment). **Mileage stays unbuilt** — RPT-07 says "if captured" and nothing captures it; `UNSOURCED_LINES` keeps saying so on the page. |
| D-74 | **R-081d: a `Document` may belong to a LEGAL ENTITY as well as to a property, and the tax packet is archived per export rather than regenerated. Only evidence for figures this product did not itself compute is attached** (R-081d, RPT-07, D-50) | **The packet had nowhere to live, and that turned out to be the item.** `Document` was property-keyed, and a year-end packet is per entity — Schedule E splits by address but deposit liability and the 1099-NEC list are entity-wide, so no one house honestly owns it. Attaching it to an arbitrary property would have been a false claim; leaving both keys null would have made it UNREACHABLE, because `api/documents/[id]/file` scopes staff access through the document's owning record and correctly refuses one with no resource to check against. `Document.legalEntityId` is the honest third option, and `ComplianceItem` has carried exactly this property/entity pair since R-077 for exactly this reason. **The archive is per export, never regenerated** — the same call R-052 made for the ledger statement and R-083 for the attorney packet. A corrected 1098 is upserted, a late invoice is paid, a deposit is disposed of; a packet regenerated in June is a different document, and "which one did the CPA file from" then has no answer. The storage key goes through `generateStorageKey`, so two packets for the same entity, year and day get their own objects rather than the second overwriting the first — a deterministic key would have destroyed the exact property the item exists for. **ONLY THE FORM 1098s ARE ATTACHED, and the rule behind that is worth keeping:** every other schedule is derived from records this system holds and can reproduce on demand, while a 1098 is a figure transcribed off a form this product never saw — so the form is the only evidence in the bundle that could not be regenerated from the database. Padding the packet with every invoice behind every repair line would produce a bundle nobody reads to the end, the same reasoning that kept `EXHIBIT_TYPES` narrow on the attorney packet. D-50 governs the index either way: a 1098 with no uploaded copy, or one whose bytes will not parse, is NAMED on the index, in the document and on the audit row, and those three must agree. That also makes R-081b's deliberate "no document upload for the 1098" gap visible on the artifact instead of invisible. | **A real latent crash, found by reading rather than by a test.** `ATTORNEY_PACKET` has been written by `lib/evictions/packet.ts` since R-083 and was never added to `DOCUMENT_TYPES`, so `RETENTION_RULES[type]` was `undefined` and `retentionCutoff` threw on any portfolio holding an eviction packet. `documentsPastRetention` has no caller outside its own test yet, which is the only reason nobody hit it. Both halves are fixed: the value is in the closed vocabulary now (with `TAX_PACKET`), and `retentionCutoff` no longer trusts the cast — `Document.type` is free-form text, so the type system's guarantee stops at the database boundary, and an unrecognised value now returns `null` (keep the evidence) rather than taking the whole retention report down. **The same hole in reverse, fixed in one line:** entity-level `ComplianceCompletion` documents have been written with no property since R-077 and were therefore undownloadable by every staff member; they now carry `legalEntityId`, and the file route's new entity branch serves both callers. **The exhibit index moved to `packages/core/documents/exhibits.ts`** — two packets stating D-50's honesty rule in two places is one copy that eventually stops being honest. **"Produced at" is stamped in UTC, deliberately.** A packet spans an entity whose houses can sit in several zones (D-3), so there is no property clock to read the act of producing the file in; every figure inside it is already per-property-zone correct upstream. **Rejected: attaching the CapEx invoices.** Useful at an audit, and out of scope for a packet whose job is the numbers — they are named as available from the property file rather than silently absent. |
| D-75 | **R-081c: funnel conversion is measured WITHIN the cohort that reached the previous stage, never stage-count over stage-count; anonymous listing visits are reported beside the funnel and never divided into it; and days-to-fill is a MEDIAN of finished vacancies** (R-081c, RPT-06, MAINT-10) | **A funnel counts people, not events, and that is not a presentation detail.** A prospect who rebooked twice has three `Showing` rows and is one person who viewed the home; dividing rows by rows would report 300% conversion for the entirely ordinary act of moving an appointment. Every stage collapses to that prospect's EARLIEST qualifying moment. **And conversion is a cohort measure**: `applications / showings` can genuinely exceed one, because applying without a recorded viewing is common — a self-serve applicant, a prospect a PM walked through before the software knew. A ratio above 100% reads as a broken report rather than as the true fact it is, so `conversion` answers "of the prospects who had a showing, how many went on to apply", which cannot exceed one, and the people who skipped the stage are reported separately as `skipped` rather than silently inflating a ratio they were never in. **The window is the INQUIRY date for every stage, not each stage's own date** — a funnel whose stages were each filtered by their own timestamp would report approvals for people who inquired before the window and drop the approvals of people who inquired inside it, making every conversion a ratio of two different populations. **`ListingLead` is never joined to the funnel.** A lead is a page visit with a `?src=` tag and no person attached (R-057 built it that way deliberately); a `Prospect` is somebody who typed their name into a form. Nothing keys one to the other, so a visit-to-inquiry rate would be two unrelated populations divided by each other and presented as a rate. They sit side by side, and the page says why. **APPROVED_WITH_CONDITIONS counts as approved**: the household was told yes, and RPT-06 measures whether a channel produces people this business will rent to. **Days-to-fill is a median of the vacancies that actually ENDED** — a mean would follow one house that sat empty through a roof replacement, and a still-vacant unit's running count is a number that has not finished happening, so including it would make the headline improve every time a new vacancy opened. **Repeat-issue chains EXTEND rather than resetting at ninety days**: four leaks eighty days apart are one chronic problem spanning most of a year, and a fixed window from the first ticket would report two unrelated pairs and hide exactly the pattern MAINT-10 exists to find. | **The cost half of "cost/quality by channel" is not built, and its absence is named on the page rather than shown as a zero.** Nothing in this product records what a listing on a network costs — there is no ad-spend entity and no invoice carrying one — so a cost-per-lead column would be a zero standing in for "unknown", which is R-078's own reason for naming its unfillable Schedule E lines instead of blanking them. **Merged tickets are excluded from repeat detection, and here that exclusion is load-bearing rather than tidy**: two people reporting one leak would themselves BE a repeat issue, manufacturing the precise pattern the report hunts for. R-081a already excluded them from ticket counts for a weaker reason. **The two halves of a vendor row are attributed differently, on purpose.** Cost belongs to whoever invoiced the job; the reopen rate comes from `vendorReopenRates`, keyed on `WorkOrderVerification.vendorId` captured when the TENANT ANSWERED, because a reopened job is normally reassigned and blaming whoever eventually fixed it would be exactly backwards (MAINT-07's own reasoning). The page says so, since one row labelled "vendor" carrying two attributions could otherwise look like a bug. **The portfolio reopen rate's denominator is jobs that CLOSED, not jobs dispatched** — an open job has not had its chance to come back, and counting it would let the rate improve simply by having a backlog. **Rejected: a `signed` funnel stage.** Approval is where screening ends; an approved household that never signed is a different problem from one that never applied, and the renewal and turn reports already follow the tenancy. **Found along the way:** a null conversion has two causes that must not read the same — the first row genuinely has no previous stage, while a later row is null only because nobody reached the stage before it, and one label for both told the reader that "Approved" is where the funnel starts. |

| D-76 | **R-082: a reserve BALANCE is recorded by a human with an as-of date, never derived from operating cash — and a split invoice's category IS its Schedule E key** (R-082, PAY-10, PAY-11; owner decisions 2026-08-22) | Two calls, both about refusing to compute something this product cannot honestly know. **The reserve actual is typed in.** There is no bank feed here: distributions taken, mortgage principal and the cash that bought the house are all invisible, so deriving a reserve balance from net operating income would overstate cash on hand — usually badly, always quietly. It is recorded the way R-081b records a lender's 1098, and `balanceAsOf` is required whenever a balance is present (enforced in `validatePropertyReserve` AND as a `CHECK` constraint) because an undated balance reads as current for ever; over a year old renders as a warning. The ENTITY figure is the sum of its properties and is not stored, so there is no second number to disagree. **The split category is a `ScheduleEKey`** (`INVOICE_SPLIT_CATEGORIES`), restricted to lines a vendor can bill — `DEPRECIATION` and anything capitalisable are absent because `CapitalImprovement` already owns that path with the in-service date depreciation needs, and interest comes from a 1098, not a vendor. PAY-10 asks that each split flow "to the right property P&L and export mapping"; recording the mapping itself is the only way to guarantee it, and there is no second vocabulary to keep in step with the form. Consequence worth naming: seven `UNSOURCED_LINES` entries stopped being true — advertising, insurance, commissions, supplies, management fees, travel and property tax are now all recordable — so the list was reworded from "cannot fill" to "nothing on them", and any line with money on it is filtered out entirely. |
| D-77 | **R-082: a work order named by an invoice split is excluded from the export exactly as a capitalised one is — a fifth counted exit door, not a fold into the fourth** (R-082, PAY-10) | The split invoice carries the job's real share of the bill, which is NOT the same number as the job's own `invoiceCents` — that is the whole reason to split. So `WorkOrderFact.splitInvoiced` mirrors `capitalised`: the job is skipped, and the split's line is deducted instead. Three things enforce it rather than one, because a double deduction is silent in every screenshot: `VendorInvoiceSplit.workOrderId` is UNIQUE (two splits cannot claim one job, and a race hits the index), the write refuses a job already carrying a `CapitalImprovement` (the two exclusions would otherwise both fire while both lines were still deducted), and `counts.splitInvoiced` is its own field in the reconciliation identity rather than folded into `capitalised` — `mapped + excepted + outOfYear + capitalised + splitInvoiced === facts`. Folding them would have kept the identity true and made the count useless to a reader chasing a missing deduction. **The splits must sum to the vendor's printed total EXACTLY**, no tolerance: the amounts are integer cents typed off paper, so an off-by-one is a typo rather than drift, and the moment to catch it is while the paper is in hand. The database holds the per-line half (`CHECK amountCents > 0`); the cross-row sum is `validateSplitInvoice`'s, and the invoice plus its lines are written in one transaction so a half-entered bill never lands. |
| D-78 | **R-084: a hold's EFFECTS follow from its type, declared in one table in `packages/core/holds` — there is no per-effect column, and no guard anywhere names a hold type** (R-084, RISK-11, RISK-12) | The shape this arrived as, from the storage build, was a boolean per effect that whoever places the hold ticks by hand. That makes "does a bankruptcy hold stop late fees" a question about what one person remembered to tick on one Tuesday, six weeks before anybody reads the ledger and asks why a stayed debt kept growing — and 11 U.S.C. §362 does not care what was ticked. So the TYPE is the only thing chosen, `HOLD_DEFINITIONS` is the whole policy, and every guard asks `leasesHalted(ids, effect)` rather than testing a type. A seventh hold type is covered by every existing guard on the day it is added, which is the specific failure CLAUDE.md already records for status enums (`VERIFIED` in R-036b). The migration deliberately carries no effect columns so a row asserting "bankruptcy permits late fees" cannot exist. |
| D-79 | **R-084: a hold WARNS on notice-generation and eviction screens and blocks nothing there — the only hard block is access-code issuance** (R-084, RISK-11, RISK-12) | Serving a notice or filing is sometimes lawful under every one of the six hold types: a stay can be lifted, a court can order possession against a servicemember, an estate can be served through its representative. Blocking would mean the product making the legal judgement `packages/core/holds` says it does not make, and the operator's workaround would be to LIFT the hold — losing the record entirely, which is strictly worse than a warning they read. `halt_access_changes` is different and is a hard refusal: re-keying or handing over a code is a possession decision on a tenancy where who is lawfully entitled to possession is exactly what is unsettled, and there the hold IS the whole content of the rule. |
| D-80 | **R-084: `do_not_contact` halts the chase and NOT the late-fee meter** (R-084, RISK-11) | The one line of the effect table that reads as a bug and is not. Somebody who has asked not to be contacted, or who is represented, has not asked to be forgiven — the debt keeps accruing exactly as the lease provides, and what stops is us writing to them about it. An operator who wants the meter off as well places a `dispute` or `payment_plan` hold beside it; effects union, never intersect. Named in its own unit test so a later "tidy-up" that flattens the asymmetry goes red. |
| D-81 | **R-084: "manager-or-above to lift SCRA" is a second permission (`hold.lift_protected`), MFA-gated, not a role hierarchy** (R-084, RISK-12; D-5) | There is no role ordering in this product and inventing one would be D-5's superuser flag wearing a different hat. The existing idiom for "a more consequential variant of the same act" is a separate permission — `template.approve` beside `template.write`, `accesscode.issue` beside `accesscode.reveal` — so lifting a hold whose type is marked `liftIsPrivileged` (SCRA, bankruptcy, deceased) needs `hold.lift_protected` and a proved second factor. Placing a hold is NOT privileged: gating the safe direction behind MFA is how holds stop being placed. Both seeded to owner and manager, so an owner can revoke the protected lift from the manager role without a release, which is what makes "manager-or-above" data rather than code. |
| D-82 | **R-085: the SCRA is the one statute in this product that is NOT jurisdiction configuration** (R-085, RISK-12; D-4) | D-4 is categorical — grace periods, late-fee caps, cure clocks and notice periods are versioned per-state config and never hardcoded. §§3931/3955 are the exception, and the exception is narrow: the SCRA is **federal**, uniform across all fifty states, and it **preempts** state landlord-tenant law where they disagree. A `JurisdictionRule` column for §3955's notice period would be a configuration point with one correct value in fifty rows, and fifty chances to get one wrong. The consequence is not cosmetic: an SCRA termination deliberately does **not** run through `noticePeriodCheck`, because a state's `noticeToVacateDays` is not a floor the servicemember has to clear — it is a rule §3955 overrides, and warning that their notice is "short" would be telling them their federal right is a problem. |
| D-83 | **R-085: no adapter behind the DMDC lookup — the product records that a human ran the search, and stores the certificate** (R-085, RISK-12; D-7, D-15) | D-7 gives this build simulated adapters for screening, e-sign and syndication because each has a real vendor API waiting behind it. DMDC does not: single-record requests go through a web form that returns a signed PDF, bulk requests through an authenticated file upload. There is no per-tenant REST call to stand a simulator in front of, so building one would mint fake certificates from a fake API — D-15's exact call on the retail-cash rail, for the same reason: a driver that invents results is untested code that looks finished. `ScraLookup` records who searched, when, what the certificate said and the file itself. |
| D-84 | **R-085: `INDETERMINATE` is a first-class lookup result, and the §3931 gate refuses on it** (R-085, RISK-12) | A DMDC no-match — usually a missing date of birth or SSN — is **not** "not serving", and collapsing the two is precisely how a knowingly false affidavit gets signed, which §3931(c) makes a criminal offence. So the enum is three-valued in core, in the database and in the UI (where it is amber, never green), and `affidavitReadiness` refuses a default judgment on it, pointing at §3931(b)(4)'s bond route instead. The same reasoning drives `affidavitLookupFor` taking the **worst** result among each tenant's latest search rather than the newest row: a two-adult tenancy where one is serving and the other is not must report the protection, and newest-row-wins would silently mask it. |
| D-85 | **R-085: a default judgment is REFUSED without a search on file — the exception D-79 predicts rather than contradicts** (R-085, RISK-12; D-79, PAY-14) | D-79 settled that a lease hold warns on eviction screens and blocks nothing, because serving and filing are sometimes lawful under every hold and a block would push the operator into lifting the hold instead. This is the one place that reasoning produces a refusal: a default judgment without the §3931 affidavit is not "sometimes lawful", it is voidable on the servicemember's own application, and the operator's workaround here is not to destroy a record — it is to go and run the search, which takes a minute and was always required. A **contested** judgment is not gated at all, so the case page asks "did the tenant appear" as radios rather than a checkbox: an unticked box means "no", and this question genuinely has three states of which "nobody said" must not silently become an answer. |
| D-86 | **R-085: an active-duty certificate places R-084's `military_scra` hold automatically — the only automatic hold in the product** (R-085, RISK-12; D-78) | Every other hold type rests on a human judgement (is this balance really disputed, has the estate really opened). This one rests on a signed federal certificate that has just been read into the record, so leaving it manual would make the whole of R-084's SCRA protection depend on the person who ran the search remembering to switch it on afterwards. Idempotent (skipped if one is already in force), and the reason written onto the hold names the certificate and its date, which is a better reason than most typed ones. The audit row carries `placedBy: 'scra_lookup'` so the trail does not read as somebody clicking a button. **No second military flag exists anywhere** — the hold IS the "military-status flag" the backlog row asks for, because two places recording the same fact is how they come to disagree. |
| D-87 | **R-086: no `Pet` entity and no `Animal` table — an approved request IS the assistance-animal record, and the "distinct from a pet" distinction is enforced at the MONEY** (R-086, RISK-13) | RISK-13 asks that the animal be "recorded as an assistance animal, distinct from a pet". This product has never had a pet entity — pets exist only as `Listing.petsAllowed`, `Prospect.petsDescription`, a `WorkOrder.petWarning` flag and two `ChargeType` values — so inventing one to be *distinct from* would be building an unrequested feature to make a sentence look tidy. The distinction that matters is not a type column somebody must remember to read; it is that pet money cannot attach. `PET_MONEY_TYPES` + `petMoneyAllowed` in `packages/core/accommodations` are the enforcement, and the charge writer calls them. **The cost is stated plainly:** a household may lawfully have both an approved assistance animal and an ordinary pet, and this product refuses pet money outright rather than telling them apart. Refusing a lawful charge is recoverable; levying an unlawful one is a complaint. |
| D-88 | **R-086: `PET_MONEY_TYPES` names all three, including the two nothing writes yet** (R-086, RISK-13) | Exactly one writer in this product creates pet money today — the recurring-charge action, for `PET_RENT`. `PET_FEE` and the `PET` deposit type sit in the vocabulary with no writer at all. Guarding only the live caller would leave the other two to be written later by somebody who has never read the fair-housing rule, which is precisely how the second and third violation happen. So the rule lives in core, names all three, and a `grep` for the constant is how a future author finds it — the same "one guard where all callers route through" discipline the ponytail rule states and R-084's `leasesHalted` already applies. |
| D-89 | **R-086: requesting documentation does NOT pause the response clock** (R-086, RISK-13) | The obvious build pauses it while waiting on the requester. It feels fair and it is exactly the shape a complaint is built from: *"they asked for a letter they were not entitled to ask for, and then treated the delay as ours."* The provider chose to ask; the response is still owed on the original timetable, and if the requester genuinely goes quiet that is what the written determination records. `RESPONSE_TARGET_DAYS = 10` comes from HUD's FHEO-2020-01 (more than ten days is unreasonable absent extenuating circumstances) and is deliberately **not** a `JurisdictionRule` — the FHA is federal and uniform, the same reasoning D-82 sets out for the SCRA. |
| D-90 | **R-086: whether documentation may lawfully be requested is a HARD refusal, computed from two observations recorded at intake** (R-086, RISK-13) | Asking a service-animal handler for a letter, or asking anyone about a readily observable disability, is not a judgement call somebody might have good reason to make anyway — **the asking is itself the violation**, so there is nothing to override with and no reason field offered. `documentationRequestable` decides from the animal kind plus two separate observations (is the disability obvious, is the animal's task obvious — either one closes the question), and both are stored as assessed **at intake** and never recomputed, the same posture `NoticeDelivery.permittedByJurisdiction` takes (D-48). The UI does not render the control at all where it is unlawful, and says why. |
| D-91 | **R-086: lawful denial grounds are prose prompts, never a picklist** (R-086, RISK-13) | A dropdown of denial reasons becomes a dropdown of things it is safe to say: the operator picks the closest label rather than writing what actually happened, and the record ends up worse than free text would have been. `LAWFUL_DENIAL_GROUNDS` is shown beside the box as a reminder of what the FHA actually permits, and `validateDetermination` requires real written text for **both** outcomes — an approval with no record of what was approved is what produces "we never agreed to the second dog" two years later. Two database CHECK constraints back it: a decided request needs date, author and text or none of them, and an approval must name the animal. |
| D-92 | **R-087: disposing of a tenant's belongings is the ONE place in this product where an unconfigured jurisdiction rule REFUSES instead of warning** (R-087, RISK-01; D-4) | Everywhere else an unknown statutory period is reported and the decision left with the human — R-083's cure clock, R-044's grace period, R-087's own `assessEvidence`. The reasoning is always that this product substituting its own ignorance for an attorney's advice is worse than saying "not configured". Disposal is the exception because it is the only step in the workflow that **cannot be undone**: a premature entry can be apologised for and a premature notice re-served, but somebody's photographs, documents and medication once in a skip are gone, and conversion is a tort with damages. The safe direction for every other clock is "let them proceed and record it"; here it is "stop". `belongingsNoticeDays` is separately three-valued — null (not configured) and 0 (expressly none required) are different statements and `disposalReadiness` treats them as such, since a state with a notice rule must not be satisfiable by waiting alone. |
| D-93 | **R-087: nothing in the product declares a unit abandoned — the evidence bar is a house rule that nags and never blocks** (R-087, RISK-01) | RISK-01's risk runs both ways and a product guarding only one direction makes the other worse: act too soon and it is a self-help eviction plus conversion; act too late and a unit nobody has looked inside has a burst pipe, a pet, or a person who has died in it. So `assessEvidence` reports gaps and blocks nothing. `MIN_ATTEMPTS`/`MIN_DISTINCT_METHODS` (3 across 2) are labelled a **house rule, not a statute**, everywhere they surface — no state says a number, what they say is "reasonable belief", and what a court looks at is whether anybody seriously tried. It counts DISTINCT methods because three calls to a disconnected number is not trying. A `REACHED` outcome on any attempt is called out first and unconditionally: whatever else is true, a tenancy the tenant has responded to is not abandoned. |
| D-94 | **R-087: contact attempts are their own table, not `Message` rows** (R-087, RISK-01; D-9) | Half of the recorded methods send nothing at all — a door knock, a neighbour asked, a police welfare check — and those are exactly the evidence an abandonment finding rests on. Recording them as messages would either invent outbound messages nobody sent or leave the most persuasive evidence off the record. `AbandonmentContactAttempt` is an evidence table, not a work queue, so D-9's "one Task entity" is untouched. It is also the only FK in the workflow that CASCADEs: an attempt has no meaning apart from its case, and nothing else points at one. |
| D-95 | **R-087: the welfare-check entry is judged by R-027's existing entry machinery, and warns rather than blocks** (R-087, RISK-01, MAINT-05) | A welfare check is not a special kind of entry in any statute — it is an entry, and the same notice law applies — so it runs through `entryDecision` rather than a second copy that a state's rule change would leave behind. It warns and demands an override reason instead of blocking, exactly as R-027 does: a landlord who believes somebody may be dead inside does not wait twenty-four hours, and a product that made that impossible would teach people to work around it. What it will not allow is doing it without saying why. The audit row snapshots the **basis** and the rule id, because whether the entry was lawful is the whole of an unlawful-eviction defence and must not depend on recomputing a rule that has since been re-versioned (D-4). |
| D-96 | **R-088: there is no `HOARDING` value in `ViolationKind`, in `ViolationGround`, or anywhere else — a hoarding case is a `PREMISES_CONDITION` case whose observations name enforceable grounds** (R-088, RISK-03) | RISK-03 asks for a violation-notice series "targeting lease/safety terms — never the person", and this is the only mechanism that can deliver it. You cannot serve a notice for hoarding: no lease term forbids it, no code section names it, and there is nothing in it a tenant could cure. What is enforceable is the blocked egress, the fire load, the pest harborage, the sanitation, the furnace nobody can reach. Hoarding disorder is also a recognised disability under the FHA, so a case file headed with the word is simultaneously unenforceable and the single most quotable document in the discrimination complaint that follows. **The closed enum is the guardrail, not the prose beside it**: a free-text "reason" box is exactly where "hoarder" gets typed, at 4pm, by somebody who has had a bad visit. `VIOLATION_GROUNDS` is five values, every one of them something a notice could cite, and a unit test asserts that neither the enum values nor their labels contain any of `HOARD`, `CLUTTER`, `HOUSEKEEP`, `FILTH`, `SQUALOR`, `DIRTY`, `MENTAL` or `TENANT` — so a later session widening the vocabulary with a word about the person goes red before it reaches a notice. The e2e spec asserts the same thing about the picker the operator actually sees, because an enum only helps if the form uses it. The cost is real and stated: a situation that fits none of the five has no case to open, and the honest reading of that is that there may be nothing enforceable yet — not that the vocabulary needs a word for the tenant. |
| D-97 | **R-088: an unauthorized occupant is legitimized through the ordinary application and screening path, with no shortcut, and a premises condition cannot be legitimized at all** (R-088, RISK-02; D-7, OQ-6) | RISK-02 names the legitimize path as "the common real-world outcome", and the tempting build is a manager-approves-the-occupant button — the person is already living there, and the paperwork feels like theatre. It is not theatre. Applying a laxer standard to the occupant in front of you than to the applicant on the phone is disparate treatment, and it is disparate treatment recorded in your own system with a name and a date on it. So `ViolationCase.legitimizedApplicantId` points at an **`Applicant`**, not a `Tenant` — `Applicant` is where the `ScreeningReport` hangs, and the fact that matters is that they were judged against the same `ScreeningCriteria` version as everybody else. `validateClosure` refuses a legitimization naming no application, and refuses one whose applicant has no screening decision; a `CHECK` constraint backs the first half. Adding them to the lease afterwards stays `LeaseTenant`'s business and is deliberately not re-recorded here. **`PREMISES_CONDITION` has no legitimize route**: there is no version of a blocked fire exit that becomes permitted by agreement, and offering the outcome is precisely how that agreement gets made — the option is not rendered, `validateClosure` refuses it, and a `CHECK` constraint refuses it again. |
| D-98 | **R-088: `AccommodationRequest` was WIDENED to the general FHA framework rather than joined by a second table** (R-088, RISK-03; D-87, D-89, D-90) | RISK-03 asks for reasonable-accommodation tracking on a hoarding case, and what a tenant asks for there is never an animal — it is time to clear the unit, a support person at the inspection, an exception to a storage rule. R-086 built the table for assistance animals because they are the commonest request by a wide margin, so the choice was widen or duplicate, and two tables recording the same statutory process is two vocabularies to keep in step of which one will rot. **The shape needed no change**, which is what made this the cheap answer: the two observations that decide whether documentation may lawfully be requested are the 2004 Joint Statement's general rule, not an animal rule. Only the ADA two-question carve-out is genuinely animal-shaped, and it is one branch. `AssistanceAnimalKind` became `AccommodationKind` with a third value `POLICY_EXCEPTION`; `animalDescription` became `subjectDescription`. A physical **modification** under §100.203 is deliberately not a value — the tenant usually pays for one and usually has to restore it, which is a different set of obligations, and it has no home in this product yet. **The trap this sprang, and it is the CLAUDE.md enum trap arriving through a reader rather than a writer**: `hasApprovedAssistanceAnimal` counted every APPROVED row, so on the day `POLICY_EXCEPTION` existed an approved request for extra time to clear a unit would have silently refused that tenancy's pet rent. Both queries are now filtered to `ANIMAL_KINDS`. |
| D-99 | **R-088: one hard refusal — an animal case cannot close as "authorized pet" while an accommodation request on the tenancy is undecided** (R-088, RISK-02, RISK-13; D-79, D-85, D-87) | Everything else in this item warns, in D-79's posture: opening, observing, serving and escalating are each sometimes exactly right, and a block would push the operator into deleting the inconvenient record, which is strictly worse than a warning they read. This one is D-85's exception, for D-85's reason. "Authorized pet" is the finding that makes pet money lawful, and recording it while the question of whether this is an assistance animal is still open answers that question in the direction that costs the tenant money, before anybody has decided it. The operator's workaround is not to destroy a record — it is to go and decide the request, which has been owed since it arrived and inside ten days by D-89. The **fork** is the softer half of the same rule and it warns rather than blocking: before serving on any animal, `animalCaseFork` says to ask whether it is a service or assistance animal, because a tenant is not required to volunteer that and a notice served on an assistance animal is a complaint whether or not anybody had been told. Escalating to eviction with a request undecided demands a written reason and then lets it through, and the notice returned still says the determination is owed. |
| D-100 | **R-089: `InsuranceClaim` carries no repair-cost column — the cost is `jobCostCents` summed over the linked work orders** (R-089, RISK-07; D-19, D-42) | RISK-07 asks for "payout vs actual repair cost" and the obvious build is a `repairCostCents` field somebody fills in from the adjuster's worksheet. D-19 already forbade it in general terms and this is the case that would have broken it: the backlog calls the work-order → invoice → P&L chain "the specific place owners abandon software", and it fails identically every time — a total is typed a second time somewhere else, the two copies drift, and neither can be trusted again. So `WorkOrder.insuranceClaimId` links the jobs and `claimPosition` sums R-032e's **existing** `jobCostCents` over them (imported, not re-expressed — D-42 exists because a comment once claimed that function and `actualTotalCents` were the same rule, and they diverge exactly when recorded parts exceed the invoice). The e2e spec asserts there is no `Repair cost` input anywhere on the claim page, so a later session adding one has to delete a test to do it. The same discipline decides `policyId NOT NULL`: the deductible and whether loss of rents is covered are `InsurancePolicy`'s, and a claim against no policy on file is one nobody can evaluate — the panel refuses to render the form rather than accepting a claim it cannot measure. |
| D-101 | **R-089: a loss-of-rents figure is priced off the tenancy's own rent, found rather than asked for, and the SOURCE is part of the answer** (R-089, RISK-07; D-19) | The claim stores a unit, a lease and two dates — never a rent. The tenancy covering the start date is looked up and its `rentCents` used; where none covers the period the unit's asking rent is used instead. That is D-19's discipline applied outside maintenance (a third copy of a number already on the lease and on the unit would drift from both), but the reason it is rendered rather than hidden is different: an adjuster's first question about a loss-of-rents figure is where the rent came from, and **a contract rent under a live lease is evidence while an asking rent on an empty unit is an assertion the carrier will discount**. So `lossOfRents` returns `source` and the claim file prints which was used, in words. The period is inclusive of both ends (a unit down on the 1st and back on the 1st was down a day) and priced at a thirtieth of a month rather than a calendar day, so a February loss is not worth more per day than a July one. A `CHECK` constraint requires both ends or neither: a start date with no end reports an open-ended, growing figure to a carrier. |
| D-102 | **R-089: loss-of-rents proceeds are income; damage proceeds are a counted EXCEPTION on the tax export, never netted against the repair** (R-089, RISK-07; D-71) | The two halves of a settlement are taxed differently and conflating them is the commonest way an owner's return goes wrong after a claim. Rent replacement is rent, so `LOSS_OF_RENTS` maps to `RENTS_RECEIVED` with no ceremony. The rest is refused: whether damage proceeds reduce a deduction, reduce adjusted basis, or defer under §1033's involuntary-conversion rules depends on facts this product holds but has no business adjudicating, and netting them automatically would be `buildTaxExport` making a tax call **silently** — wrong precisely in the cases that matter most. They become exceptions with the reasoning attached, which is the same posture `UNSOURCED_LINES` takes on premiums and the utility bill takes on cash-basis dates. Three consequences worth naming. **`payment.category` is NOT NULL**, because which half a payment belongs to is knowable exactly once — while the cheque and its covering letter are in front of somebody — and is not recoverable the following January from a bank line reading "CLAIM SETTLEMENT". **A claimed work order is NOT excluded from the expense side**: a repair paid for by insurance is still a deductible repair and the proceeds are the offsetting item, so a sixth exclusion door beside `capitalised` and `splitInvoiced` would have deleted the deduction *and* left the proceeds unreported — wrong twice. And **proceeds do not go through `LedgerEntry`**, which is strictly lease-scoped: a carrier's cheque belongs to no tenancy, so it is a dedicated fact table read into `TaxExportFacts`, exactly as `EvictionCost` already is. |
| D-103 | **R-089: the mitigation clock is a house rule with a number on it, and it is the first thing on the claim page** (R-089, RISK-07; D-4, D-93) | RISK-07's parenthesis — "mitigation speed decides water-claim disputes" — is the operative sentence in the whole feature, so `WATER_MITIGATION_TARGET_HOURS = 24` sits above the adjuster's phone number rather than in a details block: an owner opening a fresh water claim has one useful next action and it is not administration. Every property policy carries a duty to mitigate and **none of them states an hour count** — they say "reasonable" and "prompt" — so the number is labelled a trade convention everywhere it surfaces, the same honesty R-087's three-attempts evidence bar uses (D-93). It is deliberately not a `JurisdictionRule`: D-4 governs statutory numbers, and this is a contract term interpreted by an adjuster, not law. It nags and blocks nothing, and it goes urgent only for a water loss with nothing recorded as started — a fire loss has no drying clock and an amber banner that cried wolf on every claim would be ignored on the one that mattered. |
| D-104 | **R-090: a change of occupants keeps the SAME `Lease` row, and everything else RISK-10 asks for follows from that** (R-090, RISK-10; D-11, D-19) | A renewal creates a successor lease (`Lease.renewedFromLeaseId`, R-065). A roommate change must not, and the decision is worth recording because the two look alike and the consequences are opposite. With the same row: **"ledger continuity" stops being a feature** — no lease ended, so no balance moved, no Stripe subscription was cancelled or provisioned, no deposit was charged again; **"the deposit stays with the unit" stops being a rule to enforce** — a disposition is triggered by a tenancy ENDING, and no tenancy ended, so there was never a moment at which anything could have refunded a share. `LeasePartyChange` therefore has **no money column of any kind**, and that absence is the enforcement: RISK-10's hard rule is "no partial mid-tenancy refunds", and the way to make that true is to build no field and no code path that could pay one. A whole-tenancy assignment is the same operation with a longer outgoing list, which is why the backlog row bundles it. |
| D-105 | **R-090: the replacement's screening record is required by a DATABASE CHECK, and there is no override** (R-090, RISK-10; D-7) | RISK-10's "screened to full criteria" is a fair-housing rule, not a preference: the defence to "why was this person refused" is that the same written criteria were applied to everybody, and a roommate let in through a side door is the exception that destroys it. `LeasePartyChangeParty.applicantId` is NOT NULL for an INCOMING party by CHECK constraint, so a replacement with no screening record **cannot exist in the database** — R-088's own precedent (`ViolationCase.legitimizedApplicantId`) applied to the path that actually adds somebody to a lease. Whether the screening was APPROVED is checked in the action, because a decision is a column on `ScreeningReport` that a CHECK cannot reach; a DECLINED applicant is refused with a pointer to R-061's adverse-action path and is filtered out of the picker as well, so the form never offers a person it would then refuse. Deliberately **no override reason field**, unlike R-087's and R-088's house rules — those are our conventions, this one is not ours to waive. |
| D-106 | **R-090: the amendment IS the release, one document signed by everybody, and it applies when the last signature lands — never on a timer** (R-090, RISK-10; D-52) | RISK-10 names a "departing-tenant release form" and an "amendment e-signed by all" and the obvious build is two envelopes. They are the same paper: a release signed only by the person being released is its weakest possible form, and what makes it hold is that the people staying agreed to it too. Guarantors sign as well — their guarantee was of a particular household. **The whole R-063 signing ceremony is reused** through `LeaseEnvelope.kind = AMENDMENT` (per-signer tokens, `/sign/[token]`, the typed-name ceremony, the completion certificate, the executed PDF); `templateId` is nullable because an amendment's text is generated from the change itself, so a portfolio does not need somebody to seed a template before it can move a roommate. **`effectiveOn` is printed, not scheduled**: a job that removed a departing roommate on a future date would take their portal access and stop their notices while they were very possibly still living there, so what the date governs is the release, which is a matter of what the signed document says. Two consequences the panel states plainly: everybody stays on the lease until the last signature, and the release runs forward only — a departing occupant is still liable for what accrued while they were a party. |
| D-107 | **R-091: the table, the route, the audit actions and the nav label all say "confidential" and never what they protect** (R-091, RISK-04, ROLE-05) | RISK-04 is domestic violence. A table called `DvCase`, a URL reading `/dv/abc123`, an audit action `dv.case_opened` or a nav item labelled "DV cases" each disclose the exact fact the access control exists to hold — to anyone reading a schema dump, a browser history, a log line, a database console, or the screen over somebody's shoulder. So the vocabulary outside the permission wall is uniformly "confidential" and stops there, and the labels inside it are neutral too ("restricted party", "what you were shown", "how this ended"). The same rule extends to `AuditLog`: `confidential.case_opened` and its siblings carry the **case id and nothing else** — not the summary, not the restricted party, not the documentation class — because `AuditLog` is the table `audit.read` exists to expose broadly, and a trail that quotes the record it protects has moved the secret rather than kept it. The one exception is the closure reason, which is deliberate: a decision that a safety risk has passed is the thing somebody asks about afterwards. **Nothing raises a `Task`**, which is the opposite of the gap four consecutive items have left behind and is equally deliberate — `Task.title` is free text read by everyone holding `task.read` (manager, maintenance tech, read-only partner), so a task saying anything useful would be the leak and one saying nothing useful is noise in somebody else's queue. |
| D-108 | **R-091: no protective order, police report or provider statement is ever stored — only that one was produced, of what class, seen by whom, on what date** (R-091, RISK-04; DOC-01) | The intuitive build is an upload field on the case, and it is the wrong one: a `Document` row is readable by every role holding `document.read`, which is the manager, the **maintenance tech** and the read-only partner. Uploading a survivor's protective order into a property-management system to prove they qualify for a right would put it in front of the person who comes to fix the boiler. What a state's early-termination statute actually turns on is that documentation of an accepted class **was produced to the landlord**, which is a four-column fact, and a database CHECK keeps those columns whole (a type with no date and no witness is not a record of anything). The form says so in as many words next to the fields, because the operator's instinct is to scan it into the filing cabinet. **Documentation is never required to act**: a survivor who has not been to court has no order to show, and a product that would not change the locks until they did would hold somebody's safety against a filing deadline. What it gates is the statutory right (R-091b), not the operational response. |
| D-109 | **R-091: the case is confidential; its consequences cannot be, so the lock-change work order is an ordinary re-key that names only who MAY be given keys** (R-091, RISK-04; D-6) | The locks genuinely have to be changed, which means a real work order a real dispatcher assigns to a real external locksmith through R-025's magic link, visible on every maintenance screen. Hiding it would mean not doing it. So the job says nothing about why it exists — a unit test asserts the scope and the note contain none of *violence, abuse, assault, protective, restraining* — and the link between case and job is held on the **case** side, because a non-null `confidentialCaseId` on a work order would itself be the disclosure. The note **names who is authorized and never who is not**: "do not give keys to John Smith" tells a locksmith something about a household that is not theirs to know and that they may repeat to the next person who asks, while "release keys only to Jane Doe" is the same protection with nothing disclosed, and a vendor who follows it exactly cannot hand keys to the restricted party without having been told they exist. It is its own column rendered as a bordered note above the address rather than prose inside `scope`, per R-032b's finding that a warning you have to hunt for is one that gets missed. Priority is **URGENT, not EMERGENCY**: emergency is R-029's after-hours paging tier, and a page carries text into SMS and voicemail on several phones — the widest possible surface for the one job whose point is that it is not discussed. |
| D-110 | **R-103: a guard carrying no resource is a bug unless the thing it guards is owned by no property, and there is now a test that says which** (R-103, ROLE-01, ROLE-04; D-5) | `can()`'s `assignmentCovers` ends at `resource.legalEntityId === assignment.legalEntityId`, so `requirePermission('x.read')` with no resource compares `undefined` against a real id and **refuses every entity- and property-scoped actor** — on a page whose own scoped query would have shown them a perfectly real list. `requireScope` exists for exactly that question and has documented the bug in its own comment since R-008. **It came back twice anyway**: R-007 shipped it across the section placeholders, R-008 fixed only Properties and Leases, and R-091 then shipped two fresh pages and an action with it. By the time R-103 measured it, seven shipped sections were unreachable for every non-owner scope in the product. A rule three people have broken is not a rule anybody will remember, so it is now `route-guards.test.ts`'s second block: any resource-less `requirePermission` under `app/` or `lib/` fails the build unless the FILE is on `RESOURCE_LESS_GUARDS` with a written reason — the same reviewable-line shape `PUBLIC_ROUTES` already uses. The detector reads **code lines only**, because half a dozen files do the right thing and say so in a comment quoting the wrong thing verbatim, and a test that cries wolf teaches people to silence it. The exemptions are the genuinely portfolio-wide ones — jurisdiction rules, message/document/inspection templates, vendors, entity creation — where refusing a scoped actor is the CORRECT answer and widening it would hand a property-scoped manager authority over every other property. **The preventive-maintenance pages are the weakest entry and are labelled as such**: the list already computes due counts through `currentScope`, so it was plainly written expecting scoped viewers, but opening it would open the editors beside it, and widening access is the dangerous direction to guess in — the real answer is to split viewing from editing, and this item deliberately did not do it unasked. |
| D-111 | **R-091b: a survivor's early-termination right IS jurisdiction configuration, and the tenancy it ends carries no column saying why** (R-091b, RISK-04; D-4, D-82, D-107, D-108) | D-82 hardcodes the SCRA because §§3931/3955 are **federal**, uniform across fifty states, and preempt state law — a `JurisdictionRule` column for §3955 would be one correct value in fifty rows and fifty chances to get one wrong. The right to leave early on a safety ground is the opposite: VAWA binds federally assisted housing, and a private single-family portfolio answers to state law (e.g. Tex. Prop. Code §92.016 / §92.0161), which differs on **whether the right exists at all**, **how much notice it takes**, and **which documentation classes count**. So D-4 applies unchanged: three new `JurisdictionRule` columns, and `earlyTerminationRightExists` is **three-valued** — the same posture `sourceOfIncomeProtected` and `preMoveOutWalkthroughRequired` already take, and it matters more here because the wrong answer refuses somebody a statutory right. Both stop the statutory path; only one of them is a claim about the law, and the messages say which. **An empty accepted-classes list is not "none"** — it means nobody itemised them, and the decision then takes any class recorded rather than refusing over this product's own gap. **Like §3955 it skips `noticePeriodCheck`** for D-82's reason arriving from a different statute: the state's `noticeToVacateDays` is not a floor a survivor has to clear, and demanding an override reason from somebody exercising a statutory right is the product getting the law backwards. **And unlike §3955 it writes NO basis column on the Lease.** `Lease.scraTerminationBasis` is safe because being a servicemember is not a secret; a `Lease` column naming this basis would be readable by everybody holding `lease.read` and would be the disclosure (D-107). What the tenancy shows is R-066's ordinary tenant-given notice; that the right was claimed is `ConfidentialCase.earlyTerminationRecordedAt`, behind the wall. **Documentation gates the CLAIM and nothing else** (D-108): the lock change never waited on it and the panel says so, because an operator who reads "documentation required" here will otherwise think it gates the safety response too. |
| D-112 | **R-091b: one party-change builder behind two entry points, and there is no "skip signatures" option on the ordinary panel — ever** (R-091b, RISK-04, RISK-10; D-104, D-107) | Bifurcation is R-090's `LeasePartyChange` with the removed party's signature not required, and the obvious way to build it is a checkbox on the lease page's panel. That is wrong twice: it puts "do not ask them to sign" in front of every operator doing an ordinary roommate swap, and it makes the dangerous variant reachable by the permission that covers the safe one. So the **logic** is shared — `party-change-builder.ts`, one assessment, one document, one signing ceremony — and the **entry points** are not: `startPartyChange` passes `unsigned: null` and has no parameter that could say otherwise, while `startConfidentialBifurcation` sits behind `confidential.manage` **and** re-checks `lease.execute`, because taking somebody off a lease is a lease-execution act whatever the reason. `LeasePartyChange.unsignedRemovalBasis` answers "you took me off my lease without my consent" from a column rather than by inference — and it is an **enum, not free text**, because that row renders on the lease page and its reason is copied into `lease.party_changed`: a box an operator types into is an invitation to disclose. The same reasoning fixes the amendment's `reason` to a constant, since it is printed on a document every signer reads and archived where `document.read` reaches the maintenance tech. The enum says a statute excused the signature and **never which statute**. Nothing about the assessment is relaxed: "this would leave nobody on the tenancy" still refuses, because removing the only occupant is the end of a tenancy — with its own disposition and notice periods — and a statutory basis for not asking somebody to sign is not a basis for ending a lease through the roommate-change door. **The panel says what this is not**: it removes somebody from a lease, not from a house, and acting on it alone would be a self-help eviction. |
| D-113 | **R-092: a whole-property export is a privileged act, and what it leaves out is designed** (R-092, DOC-06, RISK-09; D-107, D-109; ROLE-03) | Every fact in the handoff packet is already readable by a manager on the screen it came from. What the packet adds is that it puts all of them in **one portable file that leaves the building**, which makes it the largest single egress this product performs — so it gets its own permission, `property.export`, **owner-only by construction** (the `owner` role carries `PERMISSIONS` entire and no other role names it, so D-5 keeps it an ordinary grant an owner can hand to a manager they trust) and **privileged**, the only read on that list other than `accesscode.reveal`. The docstring on `PRIVILEGED_PERMISSIONS` says reading is never gated, because locking a manager out of the rent roll is how MFA gets switched off by an irritated owner; the exception holds because refusing this one costs nobody their day job — it gates one button and no screen. **Three exclusions, each with its own reason.** *Access codes are inventoried, never printed*: R-005 made every reveal individually audited and R-091 made retiring them a safety act, so a packet printing them would collapse every one of those reveals into a single log line, in a file that gets forwarded — the buyer is told to change them all anyway, because a retired record is not a changed lock. *Nothing from a confidential case, and nothing that would betray one*: the re-key work order R-091 raises IS in the vendor history, because D-109 is explicit that a case's consequences cannot be hidden, but `WorkOrder.restrictedPartyNote` is not, because that column names a household member for a locksmith at a door. *No mortgage and no insurance declarations*: neither transfers with the house, so including them would put the seller's own financing in the buyer's file for nothing. **The exclusions are enforced by a source-level test** in R-103's shape — the packet's own output can never catch a query that starts selecting one more column. |
| D-114 | **R-092: the packet states what the LANDLORD's records say, blanks what it cannot know, and names what it could not attach** (R-092, DOC-06, RISK-09; D-4, D-50) | Three honesty rules, and each replaces a plausible thing that would have been worse. **An estoppel certificate presents its figures as the landlord's record on a date and asks the tenant to correct them** — a certificate presenting our own numbers as agreed fact defeats its own purpose, since what a buyer and their lender rely on it for is exactly the places it differs. It carries a wet-signature block rather than R-063's ceremony: that machinery binds a lease to a `LeaseEnvelope` and applies consequences on completion, and an estoppel has no consequence inside this product at all — it is evidence handed to somebody outside it. **The deposit-transfer notice is a form with visible blanks, not a merge template**: the buyer's name, their address and the closing date are unknowable when the packet is assembled, and R-062's fail-loudly merge validation is the right rule for a lease and the wrong one here — a "template" carrying a plausible invented buyer name reads as a finished letter and somebody sends it. Which notice a state requires when a deposit changes hands is jurisdiction configuration this item did **not** build (D-4), and the packet says so rather than implying the draft is compliant. **The exhibit index names every tenancy with no certificate attached** (D-50), which is why generating certificates is its own action rather than folded into the export: folding them together would either regenerate every certificate on every export — so the one a tenant already signed stops matching the one in the index — or silently reuse a stale one. Three more places the packet refuses to round off: the deposit shown is the REMAINDER after any applied portion, not the gross; a tenancy already under notice is flagged, because a buyer inheriting a tenancy that ends in three weeks is inheriting a vacancy; and an HOA record that was never filled in reads differently from one recording no rental cap. |
| D-115 | **R-094: an unaccompanied entry code goes to a STRANGER, and every rule in the feature follows from that** (R-094, LEASE-08, PROP-03; D-27, D-108) | Every other access code in this product goes to somebody who already has a relationship with the property — a vendor with a dispatched job, a tenant who has signed a lease. This one goes to somebody who saw a listing, so they can walk into a house on their own with nobody there. **A vacant unit, always**: LEASE-08 offers self-showings for vacant units and escorted showings for occupied ones, and an unaccompanied code on an occupied home is a stranger with a key to somebody's house — checked at issue *and* again at every reveal, by the same function, because a unit can be let in between. **The window, not the link, is the control**: the prospect's link lives for days so the identity check can be done in advance, while the code answers only in a fifteen-minute margin around the booked slot. **The code lives on a page and in no message** — a code pasted into an SMS is live for as long as the SMS exists, on a phone that may be lent or forwarded, and no revocation reaches it; on a page, the decision is re-run on every render, so pulling a code takes effect on the prospect's next refresh. **`ShowingAccess.identityCheckId` is NOT NULL at the database**, so there is no code path, now or later, that issues one to somebody unverified. **No photo ID is stored, anywhere** — D-108's reasoning applied to a stranger: a `Document` is readable by every role holding `document.read`, including the maintenance tech, so uploading a driving licence to prove identity would put it in front of whoever comes to fix the boiler, for ever. What is recorded is that a check of a stated class ran, by which provider, with what reference, on what date, and the **name the document gave** — that last one because "the ID said somebody else" is the branch the whole feature exists to catch, and a decision nobody can reconstruct is not a decision. |
| D-116 | **R-094: the name comparison is ours, not the provider's — and a simulated lock is sanctioned where a simulated cash rail was not** (R-094, LEASE-08; D-7, D-15, D-27) | An identity provider can say a document is genuine and readable; **only this system knows who booked the slot**. A real licence belonging to somebody else comes back VERIFIED from any provider, so the comparison against the booking name happens on our side of the seam, and `NAME_MISMATCH` is its own result rather than a flavour of `FAILED` — a failed check is a photo that would not read and the prospect should try again, a mismatch is a document in somebody else's name and is the one outcome a person should look at. D-27 is what shapes the simulator: it is **told** a name and answers with what it was told, never echoing the `Prospect` row the decision then compares against, or the mismatch branch would be dead code no test could reach. The same rule governs the entry log — read back from the device, never assembled from the codes we believe we issued, because a log that agreed with us by construction could never show the entry nobody expected, which is the only kind worth keeping a log for. **Why a simulator at all, when D-15 refused one for retail cash**: D-15's objection was that a driver minting fake settlements is untested code that looks finished, written against a message shape nobody has seen. The test that separates the two cases is whether the *decision logic* can be wrong independently of the vendor's response shape. Here it can, and it is nearly the whole item — who may be given a code, for how long, on what evidence, and what happens when somebody pulls it are ours whatever device is on the door — and a lock API's surface is the same three calls at every vendor, unlike a settlement webhook. |
| D-117 | **R-094: the kill is the cheap path, and it is the DEVICE that has to hear it** (R-094, LEASE-08; D-84 sibling reasoning) | Revoking an entry code is deliberately **not privileged**, and that is a decision rather than an oversight: R-084 settled the shape of it for holds, and gating the safe direction behind MFA is how the safe direction stops being taken. Somebody who has just learned that a stranger holds a live code for an empty house must be able to pull it from whatever device is in their hand. Issuing needs the ceremony, and issuing is not a staff act at all — it is bought by an identity check. **Cancelling a showing revokes its code too**, at the device: without that, a cancelled viewing leaves a stranger holding digits that still open the door while every screen in the product says "cancelled". And the whole point of the module is that **our page refusing to display a code is only the second lock** — the person may already have the digits written on their hand, and the lock has never heard of any of it. So the provider is called first and the row is written after; when the device cannot be reached the row is still written and `revokeReachedDevice` records it, because "pulled, but the lock never heard" is a durable fact that sends somebody to change a lock. **That column exists because a test could not see the warning it was looking for**: the warning had lived in the action's return value, rendered by the very form that a successful revoke unmounts, so the one message in the feature asking for a trip to the property was destroyed by its own re-render, every time. |
| D-118 | **R-094b: one door code per PERSON, and issuing is deliberate while revoking is automatic** (R-094b, PROP-03, LEASE-08; D-84, INSP-01) | A shared household code is what a mechanical lockbox forces, and it makes both of the things this feature exists for impossible: the entry log cannot say who came in, and a roommate leaving cannot be taken off the door without changing the code for everybody staying. Per person, R-090's party change becomes one revoke rather than a re-key. **The asymmetry is the design.** R-069 gates handing a code to a tenant on move-in funds actually clearing (INSP-01) and on no hold halting access changes (R-084), and neither becomes a different question because the lock is electronic — so a code is minted only when somebody with `accesscode.issue` (privileged) decides to, and both gates are hard blocks with no override. Revocation has no gate, is not privileged, and happens **on its own** when the tenancy leaves force and when a party comes off the lease: R-084's rule that gating the safe direction is how the safe direction stops being taken, and here the failure mode of a missed revoke is a former occupant who can still walk in. **There is no `validTo`**, where a showing's window is its whole control (D-115): an expiring tenant code is a paying tenant locked out of their own home at midnight because a fixed term rolled to month-to-month and nobody noticed. The end of a tenancy is an event this system already acts on, not a clock to set in advance. **At most one live code per person** is a partial unique index that lives only in the migration — Prisma cannot express one and would report drift for ever (CLAUDE.md's `SmsOptOut`) — and the action checks it too, because a 23505 is a 500 rather than a sentence. |
| D-119 | **R-094b: no row is written when the device refuses an issue, and the row records it when the device refuses a revoke** (R-094b; D-117) | The two directions fail in opposite ways and so are handled in opposite ways. **Issuing**: if the lock does not answer, nothing is recorded at all. A `TenantLockCode` row for a code the door has never heard of is worse than no row — somebody would read those digits out to a tenant standing at the door. **Revoking**: the row is written anyway, with `revokeReachedDevice = false`, because the alternative is a tenancy that could not end because a lock was offline. The honest record is "we pulled it and the door may not agree", rendered in red on the tenancy for as long as it is true, and it is the one thing on that panel that asks somebody to drive to the property. The same shape R-094 arrived at for a viewer's code, and the device call itself is now one shared helper — not because the call is long, but because the honesty is the part that gets dropped when it is written a second time: the tempting version swallows the error, writes the row, and tells somebody the door is safe. **Killing the portal session was always the smaller half.** `party-change-apply.ts` has invalidated a departing occupant's sessions since R-090, which was the whole of "they no longer have access" while no lock existed; somebody removed from a lease who can still key in at the front door is what this item exists to prevent, and R-091b's bifurcation removes the one person for whom it matters most. |
| D-120 | **R-091c: a confidential re-key revokes EVERY door code on the unit and immediately hands back a new one to everybody still authorized** (R-091c, RISK-04, PROP-03; D-107, D-109, D-118) | R-091's panel says, correctly, that retiring an `AccessCode` closes our record and changes no lock. **A smart-lock door code is the exception, because the code IS the lock** — so before this, a restricted party kept working access to the home until a locksmith physically arrived, which is hours on the one case where hours matter. **Everybody's codes go, not just the restricted party's**, for the same reason the physical re-key exists at all: households share codes, and somebody who was told the survivor's code walks in on the survivor's code. **And the survivor is not left locked out of her own home**: a replacement is minted for every tenant except the restricted party, from the same expression that builds the locksmith's authorized-names note, so who may be handed keys and who gets a new code can never be two different answers. The new codes are shown once, on the case page, to the person who may have the tenant on the phone right now. **The re-issue carries no funds gate and no hold gate**, unlike `issueTenantLockCode`: those guard a MOVE-IN, which is a question about whether somebody is entitled to possession yet, and refusing a sitting tenant her code back because a deposit has not cleared would be the product getting the situation exactly backwards — which is why those gates live in the action and not in the module. **Revoking commits before minting is attempted**: the safety act must not wait on a third party, and a tenant left with no working code is named in red rather than silently stranded. |
| D-121 | **R-097 split, and the native tech app cut outright** (2026-08-24; R-097, MAINT-13, D-8, D-17) | R-097 bundled five deliverables sharing no persona, no query surface and no gate — the backlog's own row said "XL: split before starting", and splitting it turned two of the five into cuts rather than rows. **The native tech app is cut.** D-8 made it conditional on R-028's field data showing the PWA hitting real offline limits; R-028 exists for somebody on payroll standing in a basement with no signal; and D-17 answered OQ-3 with "all external vendors, so there is no in-house tech to be offline". The field data that would justify it can never be collected, so the row is not deferred, it is **removed** — a row whose premise a decision has already destroyed is worse than no row at all, because it reads to the next reader as work waiting to be done. Reversible on the same terms D-17 states: if an in-house tech is ever hired, R-028 comes back and this comes back behind it. **MAINT-13 is done as specified.** Its PRD text is "v1 marks paid; money moves outside", and R-042's vendor invoices already record `paidAt`, `paymentMethod` and the split allocation. What remains is real money movement to vendors, which the PRD scores **[W]** and which needs a licensed rail and an underwriting relationship — so it stays deliberately unbuilt and named, rather than hidden inside a bundle where it looks like the tail end of something already half-finished. **And one of the three surviving rows has no acceptance criteria**: calendar sync appeared only in the bundle's prose and points at no §5 story, so R-097c's first instruction is to write the story or cut it. |
| D-122 | **OQ-9 answered: every tenant is English-primary — R-096 is cut** (owner call, 2026-08-24 — closes OQ-9) | The Spanish tenant surface and attorney-approved notice translations were gated on what share of tenants are not English-primary, precisely because the answer decides Should versus Must. The answer is none, so the row is **cut rather than deferred**, on the same reasoning D-121 applied to the native tech app: a row nobody will build reads to the next person as work waiting to be done. **What is NOT cut is the machinery underneath it.** R-049 already built translation templates with an approval gate (`template.approve` is privileged for exactly this reason), and D-10 governs it; that machinery stays, unused, because it is the part that would be expensive to add back under time pressure when a single non-English-primary household arrives. **Reversible on one fact changing**, and the trigger is explicit: the first tenancy where the household's primary language is not English re-opens this, and the work is then writing and having reviewed the notice translations, not building a localization system. |
| D-123 | **OQ-4 partially answered: one LLC per property, outside partners not stated** (owner call, 2026-08-24) | Every location sits in its own LLC — which is the structure R-004's entity scoping was built for, and it means entity scope is the load-bearing one in this portfolio rather than a rarely-exercised branch. **It does not settle the half of OQ-4 that R-095 turns on.** That question is whether any entity carries an outside partner with distribution rights, because the investor persona and the contribution/distribution ledger exist for a co-owner who is owed money and a statement, not for a sole owner holding several LLCs for liability isolation. A sole owner is already served: R-081a's operating snapshot and R-081b/d's tax packet are both per entity, and the `read_only` role already exists from R-004. **So R-095 stays gated on that one clarification**, and if the answer is "no outside partners" it is a cut in the D-121/D-122 shape rather than a build — the distribution ledger would be a feature with no second party to pay. |
| D-124 | **R-097c: an .ics subscription, not a calendar vendor's API — and what goes into it is a disclosure decision** (owner call 2026-08-24 to build calendar sync; NOTIF-06, R-097c; D-107, D-109) | The obvious build is OAuth against Google Calendar, then Outlook, then iCloud: three integrations, three token-refresh paths, three consent screens. **RFC 5545 subscription is a feature every one of those already has** — one URL, no install, no vendor, and the staff member's own calendar app does the polling. **It also makes two-way impossible by construction**, which is the safe direction rather than a limitation: R-097c's own row warned that a wrong conflict policy silently moves a legally-noticed entry appointment (R-027), which is a compliance failure and not a sync bug, and a read-only feed has no conflict policy because it has nothing to conflict with. **What it costs is named on the panel**: the calendar app decides when to refetch and Google is unhurried about it, so this is where the day's visits come from and not where an urgent change is learned — that is what the notification engine is for. **The feed carries where, when and what KIND of visit, and stops there.** No tenant name, phone or email: a subscribed feed ends up on a phone, in a desktop client and inside a cloud calendar, three copies this product does not control. No `WorkOrder.restrictedPartyNote` (D-107) though the job itself appears (D-109). And **no work-order scope**, for a subtler reason — a scope line is free text somebody typed and can say anything about a household, while a closed vocabulary of visit kinds cannot. A source-level test greps the query for all of them. **The scope is resolved on every fetch, never frozen into the token**, so a person moved to one property or deactivated stops seeing the rest on the next poll — which is what ROLE-06's "access dies within a minute" means for a surface nobody logs out of. **The token is a year long, per person, and re-issuable**: a calendar subscription that expires is one that silently stops updating, which looks exactly like a quiet week; a leak is fixed by pressing a button rather than by rotating something everybody depends on. |
| D-125 | **R-097a: inbound email reuses R-017's routing whole, and adds exactly two things email has that SMS does not** (R-097a, COMM-08; D-7, D-9) | The safety-critical part was already built and is not re-decided: `decideRoute` refuses to guess between candidates, and its own header says why — a refusal costs somebody thirty seconds of triage, a wrong match files one tenant's conversation into another's permanent record with an audit trail saying it was legitimate. Email changes none of that, so `candidatesForEmail` produces the same candidate shape and hands it to the same function, and a tenant live at two properties refuses exactly as they do by phone. **What email adds is a reply key** — `hello+<key>@inbound…`, naming the THREAD — which is the only high-confidence match there is, and which buys **precision, not trust**: it resolves the AMBIGUOUS case that From: matching cannot, and it is only ever trusted to pick between conversations that already exist, never to create one and never to say who somebody is. A key naming no thread falls through to From: matching rather than failing. **`Thread.replyKey` is plaintext where every `AuthToken` is hashed**, because it has to be rendered into a Reply-To header and it authenticates nobody: the identity of an inbound email is only as good as its From: header, which is exactly as weak as the caller ID SMS routing has lived with since R-017. **And a quoted tail**, which SMS does not have: `Message` is append-only, so without stripping, the third reply stores the first two again and the transcript a court reads becomes mostly duplicates of itself. The stripper cuts at the first marker and keeps everything above it — top-posting, which loses a bottom-posted reply, named rather than hidden — and **never returns empty**, because "they replied and it was blank" is itself a fact the evidence trail has to keep. **Subject lines are never used for matching**, deliberately: subject threading is the single commonest way this class of feature puts an unrelated "Re: Maintenance" into somebody else's conversation. |
| D-126 | **R-097d: an inbound email keeps what it carried, and says so when it cannot** (R-097d, COMM-08; D-125, DOC-05) | R-097a filed the words and threw away the photograph — silently, which is what made it a defect rather than a gap: a tenant standing in front of a leak, photographing it and emailing it, got a record saying they mentioned a leak and no evidence that they had one. It also stored **nothing at all** for an HTML-only message, which several mobile clients send by default, so somebody who typed a paragraph got a record saying they said nothing. **Both halves are about not losing what a person actually sent.** The HTML converter is deliberately not a parser and adds no dependency: it only ever DELETES — script and style contents whole, then every tag — and the result goes into an append-only body rendered as text, so the worst a hostile input produces is nonsense somebody reads. Unrecognised entities are left as written, because a visible `&hellip;` is a cosmetic blemish and a wrong guess is a changed sentence in an evidence trail. **Attachments arrive from an unauthenticated stranger**, so: a CLOSED type list (images and PDFs — what tenants and vendors actually send) rather than a block list somebody has to keep ahead of; a size cap and a count cap, because without both one message is an unbounded write to somebody else's storage bill; the declared type recorded but never trusted for anything but the decision, since nothing here executes, renders or parses the bytes; and the filename sanitised for DISPLAY as well as for the storage key, because `invoice.pdf.exe` from a stranger should not read as an invoice on a staff screen. **And nothing is stored for an unrouted message**: a `Document` must have a property, an unrouted message has none, and inventing one is precisely the guess `decideRoute` refuses — so the COUNT is recorded on the row and the triage queue says a photograph was sent and could not be kept. Fixing only the routed half would have left the original defect in the harder one. |
| D-127 | **R-097e: an email opt-out is honoured where it can be, and NAMES what it cannot switch off** (R-097e, COMM-08, NOTIF-02) | **An email opt-out is not an SMS `STOP`, and treating it as one is wrong in both directions.** `STOP` is a single token with legal force that the CARRIER enforces — it blocks the number itself, which is why `optOutReply` deliberately sends no confirmation. Email has no carrier and no command grammar: people write a sentence, and nobody else enforces anything. So the detection reads prose and **the false-positive trade-off inverts**: `classifyOptOutKeyword` is deliberately generous because filing "STOP the leak" as a repair risks texting somebody who asked us not to, while here a tenant whose rent reminders were silently switched off because they wrote "stop" in a sentence about a dripping tap gets a late fee. Every phrase is therefore MULTI-WORD and unambiguous, `stop` alone is not one of them, and detection reads the **stripped** body — almost every marketing email ever sent carries "unsubscribe" in its footer, so a reply quoting one would otherwise opt somebody out every single time. **The dangerous outcome is not "we kept emailing them".** Four categories are locked on by NOTIF-02 and no request can switch them off: a legal notice that did not arrive is a notice that was not served, and an entry notice is a statutory obligation of ours rather than a subscription of theirs. So the failure to avoid is a tenant who believes they switched off every email, misses an entry notice, and finds somebody in their home — **having been misled by us**. That is why one more email goes out after being asked for none, and why it names exactly what will still arrive in `LOCKED_CATEGORIES`' own words. It is sent through `deliverOverChannel` rather than `notify()`, because routing it through the preference engine would let the message announcing a preference change be suppressed by the change it announces. **EMAIL ONLY**: an opt-out arriving by email says nothing about whether somebody wants a text at midnight about a burst pipe, and reading it as portfolio-wide silence is a much larger inference than the sentence supports. And the message stays in the conversation rather than being swallowed by the command — R-040e's rule for a `STOP` text, for the same reason. |
| D-128 | **R-097f: email opens maintenance tickets like SMS does, except that a REPLY is not a REPORT** (R-097f, COMM-08, MAINT-01; D-125) | R-021 turns an inbound text from a tenant with nothing open into a maintenance ticket, and email did none of it — so somebody emailing "my boiler is broken" got a threaded conversation and no work queue item at all. The intake is R-021's shape verbatim, including the ordering that matters: thread FIRST, then consider a ticket, because a message that cannot be routed must never open one — a ticket has to belong to a property and a tenant, and inventing either is exactly what `decideRoute` refuses. **The one real difference is that email is a conversation.** Nobody texts their property manager to say "thanks", so R-021's rule is right for SMS; applied verbatim to email, every "Thursday works" becomes a maintenance ticket and R-023's triage queue stops being real. **And the distinction is drawn on EVIDENCE, never on what the words look like**: a reply key is proof they hit reply on something of ours, and recent outbound in the same conversation is good enough when a corporate mail system stripped the tag. Guessing "is this a complaint?" from prose would be the same mistake `formatSmsTicketDescription` already refuses one level down, where it declines to guess a category because a wrong label on the one intake path with no clarifying prompts is worse than no label. **An opt-out is not a repair either** — a maintenance ticket titled "please unsubscribe me" is the visible half of getting R-097e wrong, so that check comes first. **And the photograph is hung off the ticket**, not only off the message: R-097d stored it where it belongs, but without this the person dispatched to fix the leak never sees the picture. |
| D-129 | **Demo checkpoint 3 defined and walked, and the walk's first finding was that Milestone 8 had no checkpoint at all** (2026-08-24; D-28) | D-28 says every checkpoint line is an acceptance gate for the milestone above it and gets walked before the next milestone's items start. **Milestone 8 closed with none** — twelve risk items shipped without an end-to-end gate, which is the exact condition D-28 exists to prevent, and the same condition that let checkpoint 1's four seam defects through. The path chosen crosses six items that each independently promise something does not leak (R-091, R-091b, R-091c, R-094b, R-092, R-097c), and asserts each promise from **outside** the item that made it — the audit trail, the locksmith's work order, a manager's own screens, the handoff packet and the calendar feed. It is a kept spec rather than a manual pass, because a walk nobody can repeat is a walk that happened once. **What it found was in the tests, not the product**, which is the honest outcome and worth recording as such: the product's five promises all held. What did not hold was the sweep itself and one of the walk's own assertions. |
| D-130 | **Nine e2e specs shared one login rate-limit bucket, and the symptom looked nothing like the cause** (2026-08-24; R-003) | R-003 limits staff sign-in to ten attempts per IP per five minutes. Local e2e traffic carries no `x-forwarded-for`, so every spec that did not set one shared a single bucket — and past roughly the two-hundredth test of a full sweep the eleventh login in five minutes starts being refused. **The failure surfaces as `page.waitForURL` timing out sixty seconds after clicking "Sign in", inside whichever spec happened to be running.** Both failures in the first full sweep were in notice specs and neither had anything to do with notices; in isolation both passed, which is precisely the shape that gets written off as flakiness. Several specs had already solved it inline, which is why it was invisible: the pattern existed, it just was not everywhere. Now `uniqueClientHeaders()` in `e2e/fixtures.ts`, applied to the nine specs that lacked it — **and to every `browser.newContext()` call site, which is the half even the specs that had remembered got wrong**, since a new context does not inherit `page.setExtraHTTPHeaders`. Two of the nine were written in this same session, which is the argument for a helper over a convention. |
| D-131 | **Demo checkpoint 4 defined and walked — the stranger's path, and a different question from checkpoint 3's** (2026-08-24; D-28) | Milestone 9's twelve items shipped with no checkpoint, exactly as Milestone 8's did, and R-097f's PROGRESS entry had already said so. The path chosen crosses six items (R-094, R-094b, R-097a, R-097d, R-097e, R-097f) that share one property: they are the **two surfaces the outside world drives**, and neither can authenticate anybody. A stranger types a name into a page; a stranger sends a `From:` header. So the walk asks **what an unauthenticated stranger is allowed to cause** — a door opening, a maintenance ticket, a permanent line in an evidence trail — where checkpoint 3 asked what leaks. One person is followed the whole way: books a viewing, is refused under the wrong name, is let in under the right one, moves in, is given her own door code, emails about a boiler with a photograph, then asks to be unsubscribed from everything. **The gate is the visit her own email caused**: the entry notice reaches the address she just asked us to stop using, because a locked category outranks any opt-out (NOTIF-02) — and the "did we fix it?" follow-up is `SUPPRESSED / preference_off`. Both halves, or neither proves anything: if everything survived an opt-out the opt-out would not work. R-027 and R-097e were built four milestones apart and meet nowhere else. Kept as `e2e/golden-path-4.spec.ts`, because a walk nobody can repeat is a walk that happened once. |
| D-132 | **Inbound email never routed from a real mail client: the `<…>` unwrap existed, on the wrong half** (2026-08-24; R-097a) | `extractReplyKey` unwrapped `Display Name <addr@example.com>` for the **recipient** list, where the reply key is. Nothing unwrapped it for **`From:`**, where the routing is — so `candidatesForEmail` compared the entire header value against `Tenant.email` and matched nothing, and every inbound email from a client that sets a display name (which is all of them) landed in the unrouted queue. **Everything downstream of routing therefore did nothing for a real tenant**: R-097f opened no ticket, R-097d counted the photograph rather than keeping it, R-097e did not honour the opt-out. **And it is invisible**, which is why four items shipped over it: an unrouted message is what that queue is *for*, so the failure looks exactly like mail from a stranger we cannot place, and every one of those items' own tests passed a bare address. Found the first time anything sent a `From:` header shaped the way a mail client shapes one. Fixed as `emailAddressOnly()` in `packages/core/comms/email-reply.ts`, used by `extractReplyKey` **and** by the webhook's `From` — one expression, so the two halves can never disagree again. The webhook also stopped splitting `From:` on commas: that header is a single mailbox, and splitting it turned `"Vaughan, Dorothy" <d@example.com>` into `"Vaughan` a second way. **The general lesson is the one D-28 keeps paying for**: a helper written for one caller inside the item that needed it is a helper the next caller does not know exists. |
| D-133 | **OQ-4 fully answered: no outside partners, so R-095 is a cut rather than a build** (owner call, 2026-08-24) | D-123 settled the structural half — one LLC per property — and left the half R-095 actually turns on: whether any entity carries an **outside partner with distribution rights**. It does not. **So the investor role is a cut, on D-121's reasoning and R-096's precedent**: a row nobody will build reads as work waiting to be done, and a distribution ledger with no second party to pay is dead code that would need testing, scoping and a permission surface for nobody. **The sole owner is already served** and that is why this costs nothing: R-081a's operating snapshot and R-081b/d's tax packet are per legal entity, and `read_only` has existed since R-004 — so "show me one LLC's numbers without letting them touch anything" works today. **What survives**: entity scoping itself, which D-123 already made the load-bearing scope in this portfolio and which is what an investor role would have been built on. **Re-opens on one fact** — the first entity that takes on a partner with a distribution right, at which point the work is the contribution/distribution ledger and the ownership-split model, not the read-only access, which exists. |
| D-134 | **A guard that refuses silently, and the wrong reason it gave** (2026-08-24; Golden Path 5, D-28) | **Found by walking the money path, and it is two defects with one root.** R-084 wired `halt_dunning` into `pastGraceLeaseIds`, which is the function R-044 re-reads "immediately before anything is sent" — so the chase on a tenancy under an automatic stay was correctly **refused**. What it was not was **explained**. `pastGraceLeaseIds` subtracts held leases from the same set as paid ones and returns a bare `Set`, so the caller could not tell the two apart and reported both as *"not past the grace period"* — to the operator, and into the `message.bulk_sent` audit row. That sentence is not merely unhelpful, it is **false**: it sends somebody looking for a payment that was never made, and under 11 U.S.C. §362 or the SCRA the reason a chase was withheld is precisely the thing the record has to carry. **The second half is the screen.** `rentRoll()` was never hold-aware at all — only the send-time re-check was — so a held tenancy kept its checkbox, was counted in "select all N past grace", and gave no reason in its row. `rent-roll-table.tsx`'s own header states the rule it was breaking: a row that is not chaseable gets **no control and a written reason**, because a control that looks available and refuses is worse than no control. Fixed at both ends rather than at the message alone — `RentRollRow` now carries `chaseHeld`, deliberately **separate from `pastGrace`**, so the debt stays visible, aged and owed while the asking stops. **Invisible from inside R-084**, which is the shape D-28 exists for: its own tests assert the guard returns the right set, and it does. Nothing asked what the operator was told. | R-044, R-084, R-085 |
| D-135 | **The other half of the `From:` fix: a recipient list is split quote-aware, and it is deliberately not an RFC 5322 parser** (2026-08-24; D-132) | D-132 fixed `From:` and named what it left: `To:`/`Cc:` **are** lists, so they must be split, but splitting on every comma turns `"Vaughan, Dorothy" <d@example.com>, hello+<key>@inbound…` into two fragments that each look like a recipient. **What that costs is the reply key**, not the mangled name — the key is the only high-confidence match inbound email has, so losing it drops a tenant with tenancies at two properties into `decideRoute`'s AMBIGUOUS case and then into the unrouted queue, which is the same invisible failure D-132 was. `splitAddressList` in `packages/core/comms/email-reply.ts` tracks quoted strings (including RFC 5322 quoted-pairs) and angle brackets, and lives **beside `emailAddressOnly`** so the two halves of header parsing cannot drift apart a second time. **`extractReplyKey` splits again itself** rather than trusting its caller to have done it — it is the shared function every recipient list reaches, and D-132's lesson was that a helper beside one caller is a helper the next caller does not know exists; it is idempotent, so a pre-split list is unharmed. **Explicitly NOT a full parser**: no groups, comments, folded headers or encoded words. Those need a dependency or a large file, and this header's only job here is to be scanned for one tag — the scope is the comma that actually breaks on the mail people send. | R-097a |
| D-136 | **OQ-2 fully answered: no voucher tenancies, so R-048 is a cut rather than a build** (owner call, 2026-08-24) | D-13 settled the *schema* half in 2026-08-01 and deliberately left the *phase* half open, because OQ-2 was then "unsure / possible later" — which is enough to justify a shape and not enough to schedule recertification proration and HAP reconciliation against. Re-asked and answered: **none now, none planned**. R-048 was gated on exactly this question because the answer decides build versus cut, so it is **cut**, on D-121's reasoning and R-095/R-096's precedent — a row nobody will build reads to the next reader as work waiting to be done. | **What is cut is the voucher FEATURES**, not the shape: recertification proration on effective dates, one bulk HAP deposit reconciled across many tenant projections, housing-authority / caseworker / HAP-contract / inspection / abatement records, dunning that never pursues the HAP portion, and failed-HQS auto-work-orders against the re-inspection deadline. **The schema stays and is not dead weight**: `LeasePayer` + `PayerAllocation` mean an ordinary lease is one payer carrying the whole charge and allocation always runs, so it is **one code path rather than a special case waiting for a tenant who will not come**. **This is what D-13 was for.** Taking the two-payer question as a schema decision rather than a feature decision is what makes today's answer cheap in both directions — cut costs nothing, and a first voucher tenancy re-opens it as a feature build against a schema that already fits, never as a migration of every financial query. The risk register's Section 8 entry was retired by D-13, not by this cut, and that distinction is the point. | R-048 (cut), R-002, D-13 |
| D-137 | **Stored bytes are retyped and dispositioned at the point they meet a browser, never trusted from the upload** (2026-08-24; security review) | **A real stored-XSS reachable by a stranger.** `Document.contentType` is `file.type` off the uploader's browser and `validateDocument` only ever checked it was non-empty; all four routes that serve bytes — `/api/documents/[id]/file`, `/listings/[id]/photos/[documentId]`, `/sign/[token]/document`, `/vendor/[token]/documents/[documentId]` — hand-wrote the same response with `Content-Type: document.contentType` and `Content-Disposition: inline`, and none set `nosniff`. There is no CSP on this app. So an applicant holding an `apply/[token]` link could upload `payslip.svg` declaring `image/svg+xml`, and the letting agent opening it to read the payslip would execute the attacker's script **in this app's origin with their session attached**. The same reaches in from a tenant's maintenance photo, a vendor's invoice and a tenant's insurance certificate. | **Fixed at the one place bytes meet a browser** (`lib/documents/serve.ts`), not at the four uploads: guarding the uploads leaves the fifth nobody has written yet, and refusing types at upload would reject real evidence somebody needs to keep. An **allowlist of types that cannot execute** renders inline; everything else is still stored and still downloadable, but retyped `application/octet-stream` and dispositioned `attachment`. Retyped as well as dispositioned, because `attachment` alone still lets a browser reaching the URL another way act on the declared type. `nosniff` on every response, including the renderable path, so a mislabelled `image/png` that is really HTML cannot be sniffed back into the hole. **PDF stays inline deliberately** — it can carry JavaScript, but browsers run it in their own sandboxed viewer, and PDF is what this product's entire evidence trail is made of. **The tell that it was an oversight, not a decision:** the inbound-EMAIL attachment path (R-097d) has exactly the right allowlist, because that item was written knowing a stranger was on the other end — and an applicant, a tenant and a vendor are strangers in precisely the same sense. Four copies of one response is how four routes got it wrong identically. | R-097d, D-14 |
| D-138 | **A nonce-based CSP, enforced rather than report-only, and measured against the e2e suite** (2026-08-25; follows D-137) | D-137's own note said a CSP was the defence that would have contained it and that one added without measuring is one somebody disables a week later. **A nonce, not `unsafe-inline`.** Next's App Router inlines hydration and streaming scripts on every page, so a policy without a nonce must permit inline script wholesale — which allows exactly what a CSP exists to stop. `middleware.ts` mints a nonce per request and sets the policy on the **request** headers as well as the response, which is how Next finds it to stamp on its own scripts. | **Enforced, not report-only**, because the 1006-test e2e suite drives essentially every screen: running it under the real policy is a better measurement than a header nobody reads. **The matcher excludes `/api/` and the three non-api routes that serve document bytes**, because `documentResponse` sets a far stricter `default-src 'none'; sandbox` there and a blanket page policy would have silently replaced the D-137 fix this policy exists to back up — `csp.spec.ts` asserts that exclusion, since a regex with one character wrong protects nothing while every other test stays green. **`style-src` keeps `'unsafe-inline'`**, a bounded concession: React and Stripe both set inline styles, nonce-ing them is a large change to component code, and inline style cannot execute — the worst it buys is defacement against a `script-src` that still refuses to run anything. **`'strict-dynamic'` paired with the Stripe host** so a modern browser trusts what an already-trusted script loads (Stripe.js, Next's own chunks) and an older one falls back to the host entry. **The cost, named:** a per-request nonce means a page carrying one cannot be statically generated; nearly everything here is behind a session and already dynamic, but a future item wanting a prerendered page has to reckon with this. | D-137 |
| D-139 | **A prerendered page cannot carry a nonce, so D-138 silently switched off the JavaScript on four of them** (2026-08-25; corrects D-138) | D-138 shipped with 1006 tests green and **four pages serving zero working script**. The CSP's nonce is per request; statically prerendered HTML is fixed at build time, so Next stamps no nonce on it — and `'strict-dynamic'` makes `'self'` inert, so all fourteen script tags on `/`, `/forgot-password`, `/login/mfa` and `/portal/login` were refused by the browser. **Nothing went red**, because this product uses real `<form action>` rather than `onClick` (a standing rule in CLAUDE.md): every page still worked server-side, so no assertion in the suite could see it. Measured, not guessed — `/login` served 14 of 14 script tags nonced, `/` served 0 of 14. | **The four pages now render per request** (`export const dynamic = 'force-dynamic'`), which is what lets the nonce exist; the cost is one uncached render of a page nobody hits in a loop. **`'strict-dynamic'` is kept**, because the alternative reading — that it is too strict — is backwards: it is what made a page with no nonce fail loudly enough to find, and without it the same pages would have kept their chunks while silently losing only their inline hydration. **The real lesson is that D-138's own test could not have caught this**: asserting the header's text proves nothing about what the browser did with it, which is why `csp-browser.spec.ts` now watches `securitypolicyviolation` events on real page loads. That file leads with a **negative control** — an injected inline event handler that must be refused AND must be seen by the detector — because an empty violation list is equally the result of a correct policy and of a listener that was never registered, which is CLAUDE.md's most-repeated trap. **Also learned and written down:** `createElement('script')` + `textContent` DOES execute under `'strict-dynamic'`, correctly — trust propagates to what a trusted script loads — so the realistic payload is markup the parser inserts, not a DOM-API script. | D-138, D-137 |
| D-140 | **Demo readiness is real work, and the money half of it is not a seeding problem** (owner call, 2026-08-25) | Asked whether the product could give full demos. It cannot be opened and shown as a working business: the demo seed is still R-013's walking skeleton (entities, properties, units, tenants, leases, one overdue charge), so roughly eighty items' worth of screens open empty — and `notificationAdapter` is `LoggingChannelAdapter` in production too, so nothing has ever actually sent. Any single flow demos live today, because 1024 e2e tests drive those exact paths through the real UI. Owner chose **seed first, then sending**. | Split into **R-100a/b/c plus R-101** at the start rather than halfway, and the split follows a constraint found while scoping rather than a guess at size: **D-11 forbids writing `LedgerEntry` directly**, so the maintenance and leasing stories can be seeded as ordinary rows while the money story cannot. R-100c must choose deliberately between replaying Stripe test-mode webhooks (the real thing, costs a fixture harness) and a `Charge`-only demo (cheap, leaves the rent roll thin) — recorded here so that choice is made rather than defaulted. **The reset is the hidden cost in R-100a**: today `reset()` survives only because the seed writes no audited or append-only rows, and it already retires audited PROPERTIES rather than deleting them; seeding messages and real actions pushes that same problem down onto leases and tenants. | R-013, D-11, D-15 |
| D-141 | **Money arriving on an invoice is projected from `invoice.updated`'s `amount_paid` DELTA, and from no other event** (2026-08-25; R-038a; amends D-11's event list and D-31) | **`interpretStripeEvent` handles `invoice.updated` and no longer handles `invoice.payment_succeeded`.** The amount projected is `amount_paid` minus `previous_attributes.amount_paid`; an `invoice.updated` that does not list `amount_paid` as changed is ignored, which is what keeps a high-volume event narrow. The live webhook endpoint's subscription was changed in the same breath - the code alone is inert without it. | **Two facts, both MEASURED against the real Stripe test account rather than reasoned about (the R-038a probe).** FIRST: **Stripe emits no `invoice.payment_succeeded` for an invoice paid out of band.** A `paid_out_of_band` invoice emits exactly `invoice.created`, `invoice.updated`, `invoice.finalized`, `invoice.updated`, `invoice.paid`. D-31 routes every offline check, money order and cash payment through that call, and the pipeline subscribed to none of the events it produces - so since R-038 shipped, **every offline payment reached Stripe, wrote our `Payment` row, and never reached the ledger.** The charge posted at finalization stayed on the books and the tenant's balance never moved, which under D-11 is what dunning, late fees, the rent roll and notices all read. SECOND: **`amount_paid` is cumulative**, so a part-payment and the instalment that closes the invoice both report a number that is not what arrived; only `invoice.updated` carries `previous_attributes`, and only a delta can be projected into an append-only ledger without double-counting. **Why one event rather than adding `invoice.paid` beside the old one**: `invoice.updated` with an `amount_paid` delta fires on EVERY money path - card, out-of-band, and a partial attach, all three measured - so it is a strict superset, and subscribing to two would credit every card payment twice. The lazy fix and the root-cause fix are the same fix: one guard in the shared interpreter rather than a patch on the offline caller, which would have left the same hole open for the next path. **What it costs**: `invoice.updated` is high-volume, and the pipeline now claims and ignores rows for edits that moved no money. That is a row in `ProcessedStripeEvent` per invoice edit, accepted deliberately - the alternative is a filter that has to be right about which edits matter. **Not fixed here**: the payments already recorded with no ledger entry behind them. Reconciliation cannot see them either (both sides are empty), which is stated in `reconcile.ts` rather than left to be discovered; a corrective backfill is a money mutation and needs its own item and an explicit go. |
| D-142 | **Offline part-payments are attached as a PaymentRecord, and the PAY-12 hold is checked at the counter** (2026-08-25; R-038a; supersedes D-31's stated limit) | **`recordOutOfBandPayment` reports a guaranteed `PaymentRecord` and attaches it to the invoice** (`POST /v1/invoices/{id}/attach_payment`), replacing `POST /v1/invoices/{id}/pay` with `paid_out_of_band`. Whole or part - a partial leaves the invoice `open` with the remainder still owed. `offlinePaymentDecision` no longer refuses a part-payment; it refuses one that exceeds the open invoice, and refuses one on a payer whose `blockPartialPayments` switch is on. | **D-31 named this limit in words and handed it to R-038a**: `paid_out_of_band` settles the WHOLE invoice, so half the rent in cash would have forgiven the other half silently. **The customer-balance route the backlog proposed was measured and rejected**: a credit posted against an open invoice leaves `amount_remaining` untouched - it waits for the NEXT finalization, so the tenant who paid half would go on being dunned for all of it. `attach_payment` was measured doing the right thing: 50000 against a 100000 invoice leaves it `open` at 50000 remaining. Works at the pinned `2024-06-20`, so no API-version change. **Idempotency is not optional here and R-038's protection does not carry over**: the old call was safe because Stripe refuses to pay a paid invoice, and there is no equivalent state for a partial - a retried submission would be money the tenant never handed over. The key is on the FACT (payer, day, channel, check number, amount), so a retry reports the same record, and Stripe then refuses to attach it twice. **Why `blockPartial` is part of this item and not a later one**: R-038 could not take a part-payment at all, so PAY-12's switch was satisfied by accident. Making them possible without the check would reopen the exact hazard the switch exists for - in many states accepting a partial after serving notice can void the eviction, and the counter is where that happens. **Deliberately NOT extended**: `certifiedFundsOnly` is still unchecked on the offline path. That gap predates this item, and whether a personal check, a money order or cash counts as certified funds is a legal question for the review D-4 already requires - not one to guess at while fixing something else. |
| D-143 | **A demo property is deleted whole or retired whole, never unpicked from the bottom** (2026-08-25; R-100a; widens D-28's demo posture) | **`--reset` decides per PROPERTY, before removing anything.** A property is sticky if it carries any `AuditLog` row or any `Thread` holding messages; sticky properties are deactivated and renamed with their whole subtree left intact, and their tenants deactivated, while clean ones are deleted subtree-first as before. Portfolio-level demo rows (vendors, PM schedules) are deleted only when nothing sticky survives, and otherwise retired by rename. Retiring a work order revokes its live vendor link. | **Seeding conversations ended the old approach and the failure mode was the reason to change shape rather than patch.** `Message` is append-only by trigger and `Message.ticketId`/`workOrderId` are `onDelete: Restrict` - deliberately, because a `SET NULL` cascade is an UPDATE the trigger rejects. So a delete that walks down to tickets throws PARTWAY, after the charges, leases and tenants above them are already gone: **a half-deleted demo, which is worse than either outcome.** Moving the decision up to the property makes the two branches impossible to mix. **Why renaming the retired rows matters as much as deactivating them**: `reset()` finds what it owns BY NAME, so a retired vendor left holding its original name is found again by the next reset, which then tries to delete it and is refused by the retired property's work orders. That was caught by running three consecutive resets rather than one. **Why the link revocation is part of it**: without it every reset left another unexpired `VENDOR_WORK_ORDER` token pointing at a retired job - a credential to a gate code nobody would ever think to revoke. Measured before and after: four live tokens accumulating, versus one held steady. **The accepted cost**: retired properties accumulate, three per reset. That is the evidence trail outliving the entity it describes, which is the product working; the console line says how many were retired and why. **Not seeded**: `Task` rows, `LedgerEntry` (D-11 forbids it, R-100c owns the money story) and `Notification` (nothing sends yet, R-101). If a later item seeds a Notification, it belongs in the sticky set, and that is the whole maintenance burden of this design. |
| D-144 | **Proof of service is a third thing a reset may not erase, and compliance items are the one thing it always may** (2026-08-25; R-100b; extends D-143) | **`NoticeDelivery` joins `AuditLog` and `Message` in the sticky set**: a property whose notice was actually SERVED is retired, never deleted. **`ComplianceItem` is deleted on BOTH branches**, scoped by entity as well as by property, and before either branch touches a property. | **D-143 predicted this exact maintenance burden and named the wrong table** - it guessed `Notification` would be next; it was `NoticeDelivery`, which is append-only by a `BEFORE UPDATE OR DELETE` trigger the four-table list in CLAUDE.md does not mention (the real count is six: `AuditLog`, `LedgerEntry`, `Message`, `Notification`, `NoticeDelivery`, `TenantConsent`). Proof of service is the strongest evidence this product holds, so it would be strange if it were the one thing a reset could erase. **Compliance items are the opposite case and worth stating for the same reason**: the seed writes them uncompleted, so they carry no completion, no document and no proof - just a due date somebody invented - which is what makes them safe to delete unconditionally where nothing else is. **Both bugs were found by COUNTING, not by crashing.** The leak was three rows per run against every other number holding steady - 3, 6, 9 - and the cause was that an entity-scoped item has `propertyId: null` and so matched no property-keyed delete at all. Fixing that surfaced the second: `ComplianceItem` has a check constraint that exactly one of `propertyId`/`legalEntityId` is set, so the SET NULL a property delete cascades makes the row illegal and the DELETE fails - reported at the property delete, which reads as an unrelated error. Same shape as the `JobRun` unique-key collision this file already documents, and the same fix: delete it before the property, not after. **A leak that grows linearly is the one kind still catchable by looking**, which is the argument for printing counts a reset can be read against. |
| D-145 | **The demo's money story is built by replaying Stripe test-mode webhooks, not by seeding the ledger** (owner call, 2026-08-25; answers R-100c's own 'decide before building') | **R-100c creates real Stripe TEST-MODE objects for the demo payers and drives the resulting events through the real projection pipeline**, so the rent roll, payment history, autopay state and a tenancy mid-chase are built exactly the way production builds them. The rejected alternative was a `Charge`-only demo where the rent roll ages from open charges and payment history is honestly empty. | **D-11 forbids writing `LedgerEntry` directly** - it is a projection of Stripe, and a row Stripe does not know about is a reconciliation bug - so the money slice was never a seeding problem in the way R-100a and R-100b were. The owner chose fidelity over cost: the rent roll is the single most demo-worthy screen in the product, and a charge-only version leaves it thin with no payments, no autopay and a chase sequence with nothing behind it. **R-038a is the argument that settled it**: the webhook path is exactly where the surprises live - Stripe emits no `invoice.payment_succeeded` for an out-of-band payment, and `amount_paid` is cumulative - so a demo that route-arounds that path demonstrates a system nobody has driven. **A CONSTRAINT FOUND WHILE SCOPING, recorded so the next session does not re-derive it**: `demo-seed.mts` is a plain node script and CANNOT reach `processStripeEvent`. That module is `server-only`, and `server-only` is not a resolvable package outside Next's bundler at all - it is not in `node_modules`, so the import fails with 'Cannot find package' rather than the guard's own error. Three routes exist and the choice belongs to R-100c: (a) POST signed event bodies to the running app's `/api/webhooks/stripe`, which is the highest fidelity - real route, real signature check, real pipeline - has in-repo precedent in `stripe-webhook.spec.ts`, and costs a running app plus `STRIPE_WEBHOOK_SECRET`, which PROGRESS records as deliberately unset locally; (b) move the money seeding into `apps/web` where the pipeline is importable; (c) run it under a resolver that aliases `server-only` away, which `vitest.config.ts` already does for exactly this reason. **Also settled here**: the demo seed currently creates no `LeasePayer` rows at all, so R-100c owns those and the Stripe customers behind them, not just the events. |
| D-146 | **The demo seed reaches the app's server-only billing pipeline through a `module.registerHooks` resolver, under `tsx`** (build decision, 2026-08-25; answers the choice D-145 left to R-100c) | `db:seed:demo` now runs under `tsx` rather than bare `node`, and `demo-seed.mts` registers a resolve hook that maps `server-only` to the same empty stub `vitest.config.ts` already uses and `@/` to `apps/web`. It then dynamically imports `provisionLeaseBilling`, `processStripeEvent` and `getBillingProvider` and drives authored Stripe envelopes through the real pipeline in-process. This is D-145's route (c). | **Route (a) - POSTing signed bodies to the running app - was rejected for two reasons, and the second is the one that decides it.** First, it makes a seed script depend on a server being up and on `STRIPE_WEBHOOK_SECRET`, which PROGRESS records as deliberately unset locally. Second, and structurally: **the simulated billing provider holds its state per process.** Customers this script creates live in this script's `SimulatedBillingProvider`, so an autopay enrolment handled inside the app would be asking a simulator that has never heard of them. Route (b) - moving the money seeding into `apps/web` - answers nothing, because the question was never where the code lives but what resolves its imports. **Two things were found while building it and are worth not re-deriving**: bare `node` cannot run this graph at all even with the resolver, because its strip-only TypeScript refuses a constructor parameter property in the screening simulator - hence `tsx`, which is already a devDependency; and `registerHooks` is not in the installed `@types/node`, so the import is cast. **Also settled here**: the CUSTOMERS AND SUBSCRIPTIONS are real - `provisionLeaseBilling` is the product's own path, so with `STRIPE_SECRET_KEY` set they are genuine test-mode objects - but the EVENTS are authored, because a demo needs three months of rent history on the day it is seeded and Stripe would take three months to emit it. D-145's own route (a) already assumed this: 'POST signed event bodies' is the same authored body with an HTTP hop in front. |
| D-147 | **The Resend and Twilio drivers are `fetch`, not SDKs, and an unconfigured provider logs everywhere except on a production deployment** (build decision, 2026-08-25; R-104 spends D-15's seam) | `apps/web/lib/notifications/live-adapter.ts` is one file holding both drivers: a JSON POST to `api.resend.com/emails` and a form POST to `api.twilio.com/.../Messages.json`, wired at the single assignment in `provider.ts` that D-15 promised would be the whole change. No `resend` or `twilio` dependency was added. When a provider's variables are absent the driver delegates to `LoggingChannelAdapter` - so a laptop and CI behave exactly as they did before - **except on a production deployment (`VERCEL_ENV=production`), where the channel reports itself unsupported and the engine records SUPPRESSED / `unsupported_channel` instead.** | **Three things a later session must not silently reverse.** *No SDKs*: each provider is one documented request with one documented error shape, so an SDK would buy two dependency trees and two release cadences to save about fifteen lines. *The production guard is the honesty control*: falling back to the console on a real deployment would write `SENT` on a delivery row for a message that reached nobody, which is precisely the failure the whole engine exists to prevent - and `unsupported_channel` is the path the engine was already written for, per-channel, so a missing SMS provider still lets the email go. *`VERCEL_ENV`, never `NODE_ENV`*: the e2e suite runs a production build (R-042), so `NODE_ENV === 'production'` is true on a laptop with no keys and would switch the whole suite's notifications off. **Also settled here**: outbound email is text-only, because every template renders plain text and wrapping it in tags is a new, untested rendering of legally significant wording; every SMS carries `StatusCallback={AUTH_URL}/api/sms/status`, which is what finally reaches the callback R-040e built and nothing could call; and the drivers have **never been executed against a real Resend or Twilio account** - D-15's original objection to writing them, now answered by the production guard rather than by a live run, since an unconfigured production deploy suppresses visibly instead of lying. |
| D-148 | **A Texas notice to vacate starts the cure clock: `CURE_NOTICE_TYPES` is one list, read by both the picker and the clock** (owner decision, 2026-08-25; Milestone 10 demo walk, amends R-083/D-70) | `attachableNotices` offered `PAY_OR_QUIT` **and** `NOTICE_TO_VACATE` as the notices you file under an eviction case, while `cureClockFor` counted only `PAY_OR_QUIT`. Two lists reading the same free-form `Notice.type` and disagreeing about it - the exact shape of R-036b's `VERIFIED` trap, which CLAUDE.md already warns is never one edit. The consequence was not cosmetic: a case whose only notice was a notice to vacate reported `hasNotice: false`, so the page said **"Notice not yet served - no cure period is running"** with the served notice listed above it, and `readyToFile` refused the filing for ever with *"Attach the pay-or-quit notice to this case"*. **Both types now start the clock**, from `CURE_NOTICE_TYPES` in `packages/core/evictions/cure.ts`, which both the query and the picker read. | **The owner chose the wider reading over the narrower one, and the reason is the default jurisdiction.** Tex. Prop. Code 24.005 names the non-payment instrument a NOTICE TO VACATE - there is no separate pay-or-quit to serve in Texas at all - so an operator picking the type their own statute names must not dead-end. The narrower fix (drop `NOTICE_TO_VACATE` from the picker, seed `PAY_OR_QUIT`) was the smaller diff and was rejected for that reason. **It costs nothing where both types exist**: `cureClock` runs from the EARLIEST good service, so in a state that serves pay-or-quit first and a notice to vacate after the cure lapses, the earlier notice still sets the date and the later one cannot move it. Supersedes the comment at `queries.ts:88` ("a notice to vacate runs a different clock and must not be mistaken for this one"), which was never a recorded decision. D-70's substance is untouched - the clock is still DERIVED from `NoticeDelivery.permittedByJurisdiction`, still runs from the earliest good service, and `readyToFile` is still the one place this product refuses. Found by walking the demo (D-28) rather than by a test: the seeded eviction case held exactly the notice Texas uses. |
| D-149 | **A surcharge you cannot prove is lawful is one you do not levy: `cardSurchargePolicy` is three-state, and an unknown funding type means no fee** (build decision, 2026-08-25; R-037b, narrows R-037/D-4) | `JurisdictionRule.cardSurchargePermitted` (Boolean) becomes `cardSurchargePolicy` (`NONE` / `CREDIT_ONLY` / `ALL`), and `cardFeeFor(rule, amount, funding)` takes the card's funding type - `credit`, `debit`, `prepaid` or `unknown`. Under `CREDIT_ONLY` a surcharge is permitted only on funding known to be `credit`; `prepaid` is barred alongside `debit` because §604A.003 names "debit or stored value card". **Texas is seeded `CREDIT_ONLY`, and because both call sites pass `unknown`, no card surcharge is charged in Texas at all.** The migration backfills every `true` to `CREDIT_ONLY`, not to `ALL`. Separately, `railsFor` loses its `cardPermitted` parameter and the card rail is always offered. | **A boolean could not express the commonest US rule, and the value it held was a violation.** Tex. Bus. & Com. Code §604A.003 permits the surcharge on credit and bars it on debit and stored-value; the row said `true`, so every debit card paying Texas rent was surcharged unlawfully. The seed's own `notes` had apologised for this since R-037 - the gap was known and recorded and still charging money. **Why `unknown` forgoes the fee rather than assuming credit**: Stripe reports the funding type only once a payment method exists, and PAY-01 requires the number be disclosed BEFORE the tenant picks a rail, so the quote genuinely cannot know. Charging and hoping is a statutory violation on every debit card that comes through and this product cannot tell afterwards which those were; under-collecting is a cost. Only one of those two errors is ours to choose, and it is the same refusal-to-guess `graceUnknown` and `sourceOfIncomeProtected` already make. **Why the migration backfills to `CREDIT_ONLY` rather than preserving behaviour as `ALL`**: `true` meant "permitted, debit-vs-credit not distinguished" - never a reviewed statement that debit surcharging is lawful - so carrying it forward as `ALL` would launder an unreviewed assumption into a name that now asserts something specific. `CREDIT_ONLY` is safe under both readings. **`funding` has no default parameter**, deliberately: `unknown` would let a future caller that DOES know silently forget to pass it, and `credit` would quietly reintroduce the defect. **The `railsFor` change is the half that makes the rest safe to ship, and it was a pre-existing bug**: its only caller fed `cardSurchargePermitted` into `cardPermitted`, so a jurisdiction that forbade the surcharge had the card rail closed and the tenant told "Card payments are not available for this property." Setting Texas to `CREDIT_ONLY` would have turned card payments off across the portfolio. Not surcharging means the OWNER absorbs the cost - which `cardFeeFor` had always returned correctly and the caller then threw away. Nothing in the schema says a property does not take cards, so the flag had no honest source and is gone rather than rewired. **Still owed**: the attorney review D-4 requires (`reviewedBy` is still null), and the flow change that would learn the funding type before quoting and let a lawful credit-card surcharge be charged again. |
| D-150 | **A focus ring names its own colour, and a pending button is `aria-disabled` and never `disabled`** (build decision, 2026-08-25; R-107a, first spend of the 2026-08-25 accessibility audit) | Three rules, each enforced by `apps/web/components/focus-visibility.test.ts` rather than by review. **(1)** Any class list carrying `focus-visible:ring-2` must also carry `ring-ring`; the base `* { outline-ring/50 }` loses its alpha. **(2)** `SubmitButton` uses `aria-disabled` + `aria-busy` + a click guard, never the native `disabled` attribute. **(3)** `--input` and `--ring` are re-toned to clear 3:1 on white (0.72 and 0.55), and `--border` is split away from `--input` for the first time and deliberately left soft. A new non-client `components/ui-classes.ts` holds `PRIMARY_BUTTON_CLASSES` for the thirteen hand-copied dark buttons. | **All three were invisible to every instrument this repo already runs.** Typecheck, lint, 2,745 unit tests, 1,034 e2e tests and axe on eleven pages all passed over them, because a class list that reads `focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:outline-none` *looks* handled and `disabled={pending}` looks like care. **The ring rule is the root cause, not the thirteen buttons.** Tailwind v4 defaults `--tw-ring-color` to `currentcolor` and the offset to white, so on a `text-background` button the ring was white-on-white at 1.00:1 with the browser's own indicator already removed - a plain 2.4.7 failure that survived thirteen copies. Ten FURTHER call sites were relying on `currentcolor` happening to be dark: passing by luck, and one restyle away from breaking silently. Naming the colour everywhere is what makes the invariant checkable, which is why those ten were fixed even though none of them was broken. **Why `aria-disabled` rather than `disabled`**: a focused element that becomes `disabled` is blurred by the browser and leaves the accessibility tree, so ~108 forms threw keyboard focus to `<body>` on every press AND swallowed the "Working…" they swapped to - the result then being announced by `FormAlerts` into a region the user was no longer near. The click guard does the one thing `disabled` was genuinely needed for. `opacity-60` went with it: faded primary over white is about 2.2:1, so the pending label was least readable exactly when it was the only thing worth reading. **Why a source-walking test rather than an e2e one**: focus visibility is a computed-style property at a moment a test cannot easily hold, and the failure mode here is a fourteenth copy in a future diff - which is a source property, caught the same way and for the same reason `app/route-guards.test.ts` catches a missing route guard. **Not decided here**: `--border` stays at 1.26:1 as a decorative hairline, which 1.4.11 permits and which is a judgement a designer may want to revisit; and the nine components with their own hand-rolled `disabled={pending}` buttons still blur focus - they keep `disabled:opacity-60` and are R-107b-or-later work, recorded rather than swept in. |
| D-151 | **A tenancy's age is ALLOCATED from its balance, not read off the oldest `Charge` row** (owner decision, 2026-08-27; R-118, narrows D-11) | `delinquencyFor`'s `openCharges` becomes `charges` - EVERY unwaived charge, paid or not - and gains a required `monthlyRentCents`. The anchor is now computed: the charges plus one debt for the current period's rent (`rentCents` dated `nearestRentDueOn`) are sorted NEWEST-FIRST and the balance is consumed across them; the debt the balance runs out on is `oldestDueOn`. Payments settle oldest-first (D-11's allocation order), so what is still owed sits on the newest debts. Running out of debts before the balance is covered anchors at the oldest known one. | **`Charge` has no paid marker and cannot be given one cheaply, so the old filter was filtering nothing.** Under D-11 a `Charge` is the instruction pushed to Stripe and settlement returns as one aggregate balance with no per-charge allocation record - so `waivedAt: null` was read as "still outstanding" and meant "ever billed". `oldestDueOn` and everything descending from it (`daysLate`, `bucket`, **`pastGrace`**) was therefore aged from the oldest charge the lease had ever had. On the demo that made a tenant owing exactly this month's rent read **Over 30 days**, anchored to a move-in proration due 2025-07-03 and paid on time. **The consequence is not cosmetic**: an older anchor can only make `pastGrace` true EARLIER, so a tenant one day late with any paid charge on file was chaseable on day one whatever the statute allows - the precise failure `aging.ts`'s own header says the module exists to prevent. **The owner chose allocation over the two alternatives.** Deleting `charges` entirely and aging from `nearestRentDueOn` alone - what `late-fees.ts` already does - is the smaller diff and provably never chases early, but it makes a genuinely unpaid year-old utility read as at most one month late. Persisting per-charge settlement (a `settledCents` column fed from Stripe webhooks through the existing, production-dead `allocatePayment`) is the only fully correct answer and is a migration, a webhook change and a backfill - out of scale for this item, and D-11 says settlement lives in Stripe. Allocation strictly dominates the deletion: it gives the same answer wherever the deletion is right, and keeps real arrears visible where it is not. **Which direction it can still be wrong in, and why that is the acceptable one**: only towards reporting a tenancy NEWER than it is, because it can never anchor to a debt the balance cannot account for. That is the same asymmetry `graceUnknown` and D-149's `unknown` funding type already choose - under-chasing is a cost, chasing early is a fair-housing complaint. **`monthlyRentCents` is required and not optional-with-a-default**: a caller that omitted it would leave the rent debt absorbing nothing, every balance would outrun the known debts, and the fallthrough would restore this exact defect through a missing argument. **`late-fees.ts` was never affected and stays that way** - it passes `charges: []` by construction, and the two callers disagreeing is what made the defect findable at all. |
| D-152 | **`npm audit` is triaged by REACHABILITY, not by severity: two findings reached a shipping tree and are fixed, thirteen are dev tooling and are accepted with the reason written down** (build decision, 2026-08-28; R-127, spends R-113's leftover) | `next` 16.2.12 → **16.3.3** in `apps/web/package.json` (`eslint-config-next` with it), which carries `sharp` 0.34.5 → 0.35.4 and `postcss` 8.4.31 → 8.5.23. Two `overrides` in the root `package.json` — `nanoid: ^3.3.18` and `hono: ^4.13.5` — both in-range patches of what their parents already ask for. Remaining and ACCEPTED: the ten in the `@lhci/cli` tree (`lighthouse`, `puppeteer-core`, `@puppeteer/browsers`, `extract-zip`, `tmp`, `uuid`, `inquirer`, `external-editor`, `@lhci/utils`, `@lhci/cli`) and the three in the `prisma` CLI tree (`prisma`, `@prisma/config`, `deepmerge-ts`). CI's audit step stays `continue-on-error: true`. **Four e2e assertions across three files had to move with the bump**, every one an ambiguous locator the bump only exposed: `maintenance-phone-log.spec.ts`'s `getByText('Habitability')` and `violations.spec.ts`'s heading match now also match Next's own `#__next-route-announcer__`, which on 16.3.3 holds the new page's heading text; `retaliation-guard.spec.ts`'s two `…after this tenant.s no heat complaint` matches also match the form-level error alert that repeats the panel's sentence. | **Severity is the wrong axis when 14 of 18 highs are a CLI's transitive tree.** The two that ship are `sharp` (four libvips CVEs, and it decodes image bytes an applicant, tenant or vendor uploads — the one place in this list where a stranger controls the input) and the `postcss` sourceMappingURL path traversal, which runs at build time on our own CSS; both cleared by a MINOR framework bump, so they were never worth the two weeks they sat there. **The thirteen have no fix that is not a downgrade.** `npm audit fix --force` offers `@lhci/cli@0.1.0` (from 0.15.1, which is the LATEST published) and `prisma@6.12.0` — npm proposing to walk backwards past the advisory rather than forwards. `@lhci/cli` pins `lighthouse` at an exact `12.6.1` and asks for `tmp ^0.1.0`, `uuid ^8.3.1`, `inquirer ^6.3.1`, so no in-range override reaches a patched version; forcing `lighthouse@13.4.1` across that exact pin was rejected because **`lhci autorun` runs only in CI's e2e job and cannot be exercised on this laptop** — an untestable override on a tool that is already unrun (R-113) trades a documented advisory for an undocumented breakage. **The `prisma` finding is in the PRODUCTION tree and that is worth not re-deriving**: the lockfile does not mark `prisma`, `@prisma/config` or `deepmerge-ts` as `dev` even though our only declaration is a root devDependency, because `@prisma/client` declares `prisma` as an OPTIONAL PEER — `npm ls --omit=dev` confirms it survives a production install. It is nonetheless unreachable: no file under `apps/`, `packages/` or `scripts/` imports the `prisma` package (only `@prisma/client`), and `deepmerge-ts` is reached solely by the CLI's config loader merging our own `prisma.config`. The real fix is prisma 6 → 8, whose stable line is still `8.0.0-rc`, and that is a migration item, not an audit line. **Why no CI gate was added**: an `npm audit --omit=dev --audit-level=high` step would be red on its first run over exactly the finding this row accepts, and a gate that is red on day one is a gate nobody reads — the same failure mode as the nine days of `continue-on-error` output that produced this backlog row. This row is the record instead; re-triage against it rather than from zero. **What the bump cost, and it is the part worth carrying forward**: four e2e tests went red on 16.3.3 and every one of them was a locator matching two elements — the collision CLAUDE.md already warns about, latent because Playwright's strict mode only fires when both elements are in the DOM at the same instant. On 16.2.12 the retaliation alert and the retaliation panel landed in different paints and the assertion caught the first one alone (an `allTextContents()` probe on 16.2.12 returns `[]` at the moment of the call and one element after the retry); on 16.3.3 they arrive together. **A framework bump is a race-condition detector**, so the sweep is not optional on one — and the fix in each case was to assert the MORE specific string (`exact: true`, `getByRole('heading')`, and the `(date)` parenthetical that only the panel carries, which RISK-06 asked for anyway), never to relax the assertion. |
| D-153 | **A raw `BusinessDate` interpolated into prose is the same defect as `toISOString().slice(0, 10)`, and it is a DIFFERENT grep — `packages/core` is fixed, `apps/web` is R-129** (build decision, 2026-08-29; R-128, found by the D-28 demo walk) | Every date a stranger receives now goes through `friendlyBusinessDate`: the 11 notification-template sites in `packages/core/notifications/templates.ts` (`paidOn`, `debitOn`, `dueOn`, and `effectiveOn` on both the non-renewal and the lease-amendment invite), plus the served notice (`notices/document.ts`), the generated lease (`leases/generation.ts`), the amendment (`leases/party-change.ts`), the generic document template (`documents/template.ts`), the chargeback notice (`workorders/chargeback.ts`) and the handoff/estoppel packet (`property/handoff.ts`). Those context fields are retyped `string` → `BusinessDate`. **`CardExpiringContext.expiresOn` is deliberately NOT included** and carries a comment saying why. Two `handoff.test.ts` assertions moved from `2026-09-19` to `19 Sept 2026`. The ~40 remaining sites in `apps/web/components` and `apps/web/app` are **R-129**, with the grep predicate written into that row. | **R-119 and R-121 each swept this class and each missed it, which is the reason this is a decision and not a bug fix.** Both hunted `toISOString().slice(0, 10)` — a value being FORMATTED wrongly. This is a value already correctly held as a `BusinessDate` and then printed verbatim, so it survives that grep untouched while producing the identical symptom: `We received your payment for 310 Magnolia Dr on 2026-08-01.`, sent to a tenant by email and SMS and written into the append-only `Message` trail. R-116 built `friendlyBusinessDate` precisely so this would not happen, and nothing made the callers use it. **Why `packages/core` first and `apps/web` second**: the split is who reads the string. A staff member reading `2026-08-25` in a rent-roll column is looking at an ugly date; a tenant reading it in a served notice, a signed lease or a deposit letter is looking at a document that is part of the evidence trail this product's premise rests on. The second class is also the one that cannot be corrected after the fact. **Typing the field is the guardrail, not the call site.** `friendlyBusinessDate` throws a `RangeError` on anything that is not `YYYY-MM-DD`, so formatting inside the template makes every caller's contract explicit and compile-checked — which is the only reason it is safe to format there rather than at each of the six call sites, and it is what stops the next template from reintroducing the class. Every one of those callers was confirmed to pass a real `BusinessDate` first (`businessDate(...)`, `utcToBusinessDate(...)`, or a `type="date"` field validated by `assessPartyChange`), and three grep hits that LOOKED like this defect were confirmed correct and left alone: the deposit disposition letter already formats a real instant through the property zone, and the eviction packet and exhibit list already receive `friendlyDate` from their callers. |
| D-154 | **A date-shaped grep produces CANDIDATES, not defects — the check is where the value comes from, never what the field is called** (build decision, 2026-08-29; R-129) | D-153's own predicate returned 46 hits in `apps/web`; **9 were already correct**, because their page builds the prop with `friendlyDate(...)` and the field is still named `placedOn`, `issuedOn`, `openedOn`, `dueOn`, `createdOn`, `closedOn` or `startsOn`. `friendlyBusinessDate` throws a `RangeError` on `"29 Aug 2026"`, so wrapping one of those is not a cosmetic mistake — it is a **500 on the page**, which is how the first full sweep died at test 5 on `/leases/[id]`. The rule before touching any render: grep the page that constructs the prop for `friendlyDate\|friendlyBusinessDate\|friendlyTimestamp` against that field name. **The predicate also under-reports in the other direction** — it scans JSX in `components` and `app`, and misses `apps/web/lib`, where six staff-facing strings carried the same raw date and never pass through a component: three `Task` titles (compliance alerts, periodic inspections, deposit-disposition start), the SCRA hold reason, and the recurring-charge and early-termination action notices. And it misses spec assertions that are not `getByText`: `scra.spec.ts` asserts the hold reason with `expect(hold.reason).toContain('2026-08-15')`. | **The predicate is now the third artefact of this class to be trusted further than it earns.** R-119 and R-121 swept `toISOString().slice(0, 10)` and each declared the class closed; D-153 wrote a better grep into R-129's row and that grep was 80% precise, in both directions. What survives is not a pattern but a procedure: **the grep gives you a list of places to LOOK, and the source of the value decides**. Two consequences worth keeping. **Formatting in the renderer only works if the renderer is the only formatter** — a prop that arrives pre-formatted turns "format inside the component" from a guardrail into a crash, so the page and the component have to agree about who owns it, and the raw `BusinessDate` reaching the component is the side that scales. **A function that throws is a trust boundary.** Two form actions (`recordScraLookup`, `addRecurringCharge`) fed user input straight to `friendlyBusinessDate` with no format check — `<input type="date">` cannot send anything else, a hand-made POST can, and both now validate `YYYY-MM-DD` before formatting. `ScraTerminationPanel` was being passed the literal string `'—'` as a "no date" sentinel for the same reason: nothing had ever had to be a real date before. |
| D-155 | **A portfolio-wide report's "today" is the LATEST local day in scope — the exact opposite of `complianceToday`, and for the same reason** (owner decision, 2026-08-29; R-131, spends R-130's filed leftover) | `apps/web/lib/scope/report-today.ts` exports `reportToday(scope, now)`: `businessDate` in every selected property's zone, sorted, last one wins, UTC when nothing is selected. Three callers, all of which read `new Date().toISOString().slice(0, 10)` before it — `reports/maintenance/page.tsx` and `reports/leasing/page.tsx` for their default `to` bound (their `from` bounds now come off that day via `addBusinessDays(-364)` / `addBusinessDays(-89)` rather than millisecond subtraction), and `api/reports/rent-roll/route.ts` for the exported CSV's filename. `ResolvedScope.availableProperties` gains a required `timezone`, selected in the query `currentScope` already runs, so no caller pays a second round trip. | **R-130 filed these three deliberately unfixed because the semantic is an owner call, and it is: `complianceToday` was the obvious helper to reach for and it is wrong here.** It returns the EARLIEST local day — *late everywhere before it is called late* — which is right for judging an obligation overdue and backwards for a report, because `to` is an INCLUSIVE end. Earliest-day would have cut today's rows out of every property east of the westernmost one, quietly, in a report nobody would think to distrust. The two rules are opposites because both are the conservative direction for their own question. **The rent-roll filename is the same defect wearing a different hat, and it is the one that leaves the building.** A roll exported at 20:00 in Houston was stamped `rent-roll-2026-08-30.csv` and handed to a lender as a point-in-time financial statement dated tomorrow. That is the third distinct shape of D-153/D-154's class: not a value formatted wrongly (R-119/R-121), not a `BusinessDate` printed verbatim (D-153), but a **calendar day derived from a UTC clock in the first place** — invisible to both of those greps, and reachable only by asking which clock a value was read off. **Why the zones ride on `ResolvedScope` rather than a query per caller.** `currentScope` already fetches exactly these rows for the property switcher; adding one column to its `select` is free, and the alternative — each report deciding for itself which properties to ask about — is how the three call sites came to disagree with each other in the first place. It is a REQUIRED field, not optional: an optional `timezone` lets the next caller silently fall back to UTC, which is the defect. **Selection, not visibility.** `reportToday` reads `propertyIds` (what the switcher currently shows) rather than `availableProperties` wholesale, because that is the set the report covers — a manager filtered down to one Houston property should not have their range end moved by an eastern property they are not looking at. **Not decided here**: staff users still have no timezone of their own, and a reader in New York running a Texas-only report still sees Texas's day. That is the right answer for a report ABOUT properties and the wrong one for a personal "today"; a `StaffUser.timezone` column plus a settings screen is a separate item nobody has asked for. |
| D-156 | **A check that has never run is not a check, and `lhci autorun` had never run — D-152's "cannot be exercised on this laptop" is corrected here** (build decision, 2026-08-29; R-132) | `npm run lighthouse` (= `build:test` then `npx lhci autorun`) makes the Core Web Vitals budgets runnable off CI, in about two minutes. `lighthouserc.json` changes in three ways: `startServerCommand` `npm run start` → **`npm run start:test`**, so a local run cannot point at the Neon dev branch; the `url` list goes from **one page to three** — `/`, `/login`, `/forgot-password`, every page a stranger reaches without a token; and **`errors-in-console` is asserted at `error`**, which is the one thing the first run actually caught. `apps/web/app/icon.svg` is new, and it is the **same mark the PWA manifest already inlines** (`#1f2937` tile, white house), which took best-practices from 0.96 to 1.00 on every page. `/icon.svg` joins the proxy's matcher exclusions and `csp-browser.spec.ts`'s `MAY_BE_PRERENDERED`. | **The check was green, and that is the finding.** `lhci` lives only in CI's e2e job, CI has not started a job since 2026-08-17, and `lighthouserc.json` asserted `categories:accessibility: minScore 1`, an LCP ceiling and a CLS ceiling against **one URL** — so months of budgets had produced exactly zero evaluations. D-152 asserted, while triaging `@lhci/cli`'s own advisories, that it *"runs only in CI's e2e job and cannot be exercised on this laptop"*, and used that to justify not forcing a `lighthouse` upgrade. **The refusal to force the upgrade was still right; the reason given was false**, and this is the third time in this repo a check has been described as CI-only and turned out not to be (`db:ci` was the second). **What one run found**: every page load logged `GET /favicon.ico 404` to the console. There has never been a favicon — and the proxy's matcher had excluded `favicon.ico` by name since it was written, which is why nobody noticed a file that did not exist. **The favicon is copied, not designed.** `manifest.webmanifest/route.ts` has inlined a placeholder house-glyph SVG since D-8's PWA work, with a comment saying whoever adds real branding replaces those two entries; a third, different-looking mark would have been the defect, so `icon.svg` is that exact rect and path and the manifest's comment now names it. **`errors-in-console` was added because the run's one finding was invisible to every assertion already in the file** — accessibility, performance, SEO, LCP and CLS were all green over a 404 on every page. **`start:test` is safe in CI even though CI has no `.env.test`**: `dotenv-cli` skips a missing file, which is already load-bearing there, since `test:e2e` → `e2e:server` → `build:test` names the same file and CI was green on it before the billing block. **Not fixed, and named rather than swept**: `startServerCommand` is a bare `next start`, so `lhci autorun` on its own assumes a build exists — in CI that holds only because the e2e step runs immediately before it, an ordering dependency nothing in the workflow states. `npm run lighthouse` carries the build so a human cannot get it wrong; the CI step is left alone rather than made to build twice, which is the same trade the workflow's own comment already makes for e2e. **And the budgets still cannot see a signed-in page** — Lighthouse has no session, so the portal, the vendor link and every admin screen are audited by axe in the e2e suite and by nothing else. The two are not substitutes and neither covers the other. |
| D-157 | **Prisma sizes its pool as if it were the only client on the machine, and the two runners start fifteen of them — a LOCAL database gets `connection_limit=8`, a remote one is left alone** (build decision, 2026-08-29; R-135) | `localPoolCap(url)` in `packages/db/index.ts`, applied at the single `new PrismaClient` site the whole repo has. It sets `connection_limit=8` when the host is `localhost` or `127.0.0.1`, leaves every other parameter intact, and returns the URL untouched when the host is remote, when a `connection_limit` is already present, or when the URL is unset or unparseable. `packages/db/pool-cap.test.ts` asserts those six branches. | **The ceiling was exceeded by arithmetic, not by chance, which is why it presented as flakiness.** Prisma's default `connection_limit` is `physical_cpus * 2 + 1` — measured here, 21 backends from one client — and nothing shares them, because there is one `PrismaClient` per PROCESS and both runners are process-parallel: e2e is 5 Playwright workers plus the `next start` server under test (6 × 21 = 126), vitest's forks pool is up to 9 (9 × 21 = 189), against a `max_connections` of **100** with three reserved for superusers. The pools open lazily, so the run gets most of the way through before the arithmetic bites. **The symptom names the wrong culprit**: `FATAL: sorry, too many clients already` is raised inside whichever fixture happened to be seeding at peak, so it reads as a broken test — five unit tests across three files after R-129's sweep, and three e2e tests in `maintenance.spec.ts` at test 766 of 1,054, reported as **flaky** and therefore exiting 0. **Measured, not assumed**: the pre-cap sweep peaked past 100 and ended `1049 passed, 3 flaky` in 14.2m; the post-cap sweep peaked at **42** and ended `1051 passed, 1 failed` in **8.7m**. Some of that speedup is simply not stalling on an exhausted server, and it is not claimed as a clean benchmark. **8 is deliberately one number rather than one per runner**: the worker count is not knowable from inside `packages/db`, and a single value that fits the worse of the two shapes (48 and 72 backends) is the whole fix. It is marked `ponytail:` as a fixed constant sized against a 100-connection server. **Remote is none of our business.** Neon is reached through its own pooler, sized on the far side of the connection, and capping it would be guessing at somebody else's capacity. **CI's `postgres:17` service IS localhost and deliberately gets the cap** — local and CI should fail and pass for the same reasons. **What the fix then exposed, and it is the part worth carrying forward**: a faster sweep is a race detector exactly as D-152 said a framework bump is. `workorders.spec.ts`'s `getByText('Turn the unit')` had matched the page's `<h1>` and Next's `#__next-route-announcer__` copy of it for as long as both existed, and won every previous run by landing in different paints; on the faster sweep it lost, twice. That is the **fifth** instance of R-127's trap and it took the same fix — `getByRole('heading')`, which a `role="alert"` div cannot match — never a relaxed assertion. **Its cleanup was the second defect the same failure exposed**: `afterAll` deleted work orders by a collected-id list, so a test that failed before pushing its id orphaned the row, `unit.deleteMany` then died on `WorkOrder_unitId_fkey`, and the property was left **active** — which is precisely the marker `notifications.test.ts` reads as *a spec that may still be running* (R-109). Cleanup is now scoped by `propertyId`, the ownership marker the database already carries, which also catches the rows the APP created and the id list never held. **A second flake surfaced on the confirming sweep and is the same lesson from a third angle**: `getByText('7392')`, a hard-coded access code, collided with `<option>Axe House-e7392d7f</option>` in the property switcher, because an all-digit code is also valid hex and every staff page lists every in-scope property as `<Name>-<8 hex>`. Four sites now use `{ exact: true }`. The 15 other short-literal `getByText` calls in the suite are **candidates, not defects** — they can only collide via a property's name, not its suffix — and are deliberately left for a reader who will check each page, which is D-154's rule applied to locators instead of dates. |
| D-158 | **A dead WRITER is a broken behaviour; a dead reader is only dead code — so an export with no caller is wired, not deleted, when a live consumer gates on what only it can produce** (build decision, 2026-09-01; R-143) | The sweep: every `export function` in `apps/web/lib` and `packages/core` whose name has no reference outside its own definition and its own tests. 44 of 1,062, classified as **14 readers** (a query with no screen), **13 rules** (a pure function nothing applies), **14 helpers and type guards**, and **3 writers**. The three writers were wired to forms: `recordConsent`/`withdrawConsent` in `ConsentPanel` on `/leases/[id]`, `switchCollectionMethod` in `BillingPanel`. The other 41 are recorded and untouched. | **R-138 found one instance and treated it as one bug.** `grantAssignment` had had no caller for 131 items; nobody asked whether the shape repeated. It repeats 44 times, and the classification is what makes the list usable — most of it is noise, and the part that is not is small and specific. **The consent case is why the rule is written this way.** `send.ts` refuses every tenant SMS without a `TenantConsent` row; the only two writers of that table had no caller; so every tenant SMS in a deployed environment was `SUPPRESSED / no_consent` — recorded honestly, invisible in every test, and permanent. The lazy reading of a dead export is to delete it, and here that would have made "no tenant can be texted" a design rather than an omission. **The predicate to run, not the list to trust**: a dead export matters exactly when something live reads what only it can write. `switchCollectionMethod` is the weaker second case — nothing gated on it, but D-29's entire purpose is a switch nobody could perform. **A dead reader is not this.** `documentsPastRetention`, `listRuleVersions`, `recentDrift` and eleven others are screens never built; deleting or building them is a product choice per function, not a sweep. **What the sweep cannot see**: `allocatePayment` — D-11/D-12's allocation order, configurable in the jurisdiction form and applied nowhere — escaped the list because two other files mention it in comments. A name-reference sweep over-reports helpers and under-reports anything discussed in prose. |
| D-158 | **The two staff lockouts are refusals, not warnings, and a deactivation leaves the assignments standing** (build decision, 2026-08-31; R-138) | `apps/web/lib/staff/rules.ts` refuses two acts outright rather than confirming them: **revoking the last active `owner` assignment**, and **deactivating your own account**. Self-*revocation* is allowed while a second owner grant survives, so an owner can correct their own over-broad grant and a two-owner business can demote one of them. `deactivationRefusal` also refuses deactivating the only person holding an active owner grant. Twelve unit tests, no database. Deactivation sets `active: false`, `deactivatedAt` and bumps `sessionsValidFrom`, and **deliberately does not revoke the assignments** — reactivation restores what the person held, and `active: false` already refuses every request. | **There is no superuser and no back door (D-5), so "no active owner" is not a degraded state somebody can support their way out of** — it is a deployment whose only route back in is `db:create-owner --force` on the server. That makes it a trust boundary, not a confirm dialog. **Self-deactivation is refused unconditionally where self-revocation is not, because the two fail differently**: a revoke takes effect on the next request and another owner can undo it, while a deactivation bumps the watermark and ends the actor's own session within a minute — the mis-click destroys the session that would reverse it. **Leaving the assignments standing is the ROLE-06 reading**: "deactivation preserves all history" is what `revokedAt` exists for, and a reactivated leaver who silently came back with no access at all would be a second, quieter defect. |
| D-159 | **An auth link is a notification like everything else, and `account_access` is locked, EMAIL-only and property-less** (build decision, 2026-08-31; R-139) | `deliverAuthLink` now calls `notify()` + a SCOPED `dispatchPendingNotifications`, exactly as R-025's vendor dispatch does. New category `account_access` with three properties, each set for its own reason: **LOCKED**, because somebody who muted the message that lets them sign in could not reach the page to unmute it; **EMAIL only** via `CATEGORY_CHANNELS`, because PORTAL would deliver a sign-in link to a surface you must already be signed in to read, and SMS is simply not offered yet; and **no `propertyId`**, which is what keeps quiet hours out of it — `notify()`'s own rule that a property-less notification never defers, not an exemption written for this. Four templates became three: `auth.tenant_magic_link`, `auth.staff_password_reset`, and a SEPARATE `auth.staff_setup_link` for R-138's invite. `issueToken` now returns the `AuthToken` row's `id`, and `auth-link:<tokenId>` is the idempotency key. `apps/web/lib/auth/vendor-link.ts` was deleted whole — nothing had imported it since R-025. | **The bug was that this file had never been finished, and nothing could see it.** It was R-003's placeholder verbatim, unchanged since `62700de`: `if (NODE_ENV === 'production') { console.warn(…); return }`. Its own comment said *"R-030 replaces this"*; R-030 built the engine, **R-104 wired Resend and Twilio into it, and neither touched this file**. So on a deployed environment the tenant magic link was never sent — and `tenant-magic-link` is the only tenant provider wired in `auth.ts`, so **no tenant could sign in at all** — and neither was a staff password reset. **It survived because the vendor path does not come through here**: R-025 built its own `lib/vendors/link.ts` and dispatches through the engine, so the link anybody actually watches was fine, and the dead `auth/vendor-link.ts` that did come through here had no callers to notice. **The key had to be the TOKEN, not the recipient.** R-025's precedent keys on the work order plus the token's expiry; keyed on the person instead, a second password reset would be swallowed as a duplicate and send nothing, which is the one failure a sign-in link cannot have. The `AuthToken` id is the natural key of "this link was issued" and cannot collide inside a millisecond the way an expiry can. **A separate setup template from the reset template**, even though both mint `STAFF_PASSWORD_RESET`: the recipient's situation is opposite. Nobody asked for an invite, so *"ignore this if you did not request it"* — correct on a reset — is exactly the wrong advice on the message that is somebody's first contact with the product. **What this deliberately accepts: the link now lives in `Notification.body`, which is append-only.** That is a live credential at rest, and it is the posture the product already took — `vendorDispatchTemplate` has carried a work-order link since R-025. It is NOT the posture of the audit log, which records that a link was issued and never its value, and that asymmetry is right: a message has to contain the link to be the message; an audit entry does not. **`tenant_password_reset` was cut rather than built** — `TenantCredential.passwordHash` is schema-only and no flow writes it, so a fourth template would have been a guess. |
| D-160 | **Every stranger-reachable token purpose gets a live demo link, from one table, and the demo state that makes each one open is part of the seed** (build decision, 2026-09-01; R-140) | `STRANGER_ROUTES` in `demo-seed.mts` maps each `AuthTokenPurpose` a stranger can reach to the route its link opens, and `mintDemoLink` writes the row and records the printed link. Eleven links now print where one did. Three subjects had to be seeded because the product had no demo state for them: a WORK_COMPLETE work order hanging off a TICKET (`TENANT_VERIFY` refuses a link whose recorded tenant is not still the ticket's tenant, so a standalone job can never carry one), a PENDING_APPROVAL work order with `WorkOrderBid` rows for two vendors, and a `Showing` on the vacant unit with a `SmartLock`. `WorkOrderPlan.vendorIndex` became OPTIONAL so a job collecting bids names nobody and carries no `dispatchedAt`. `reset()` gained `WorkOrderBid`, `ShowingAccess`/`Showing`, `IdentityCheck`, `SmartLock`, `LockEvent` and `TenantLockCode`. | **Rejected: minting only for the purposes whose subjects already existed.** It would have printed eight links instead of eleven and left the three most interesting flows — the verify loop's entry point, bid collection, and self-showing access — exactly as unwalkable as before, which is the whole complaint. The three missing subjects are one `create` each. **Rejected: a lint rule or a filesystem check that every `[token]` route has an entry.** The table is keyed by PURPOSE, not by path, and the mapping is not mechanical — `SHOWING_BOOKING` and `SHOWING_ACCESS` are two purposes on sibling paths, and `CALENDAR_FEED` is fetched by a machine. A unit test asserting the planned set EQUALS `Object.keys(STRANGER_ROUTES)` is the same guarantee for one assertion. **Why the keyed table rather than a string at each call site**: a new purpose cannot then be minted without somebody deciding, in one place, where its link points — which is precisely the decision nobody made eight times over. **What this accepts:** eleven raw tokens printed to a terminal, unrecoverable after the run, and every one of them a live credential for its lifetime. That is the posture R-100a already took for the vendor link and the argument is unchanged — only the hash reaches the database, a link not printed is a link nobody can open, and `refuseUnlessDemoDatabase` means none of this can be minted anywhere but a local `rental_demo`. |
| D-161 | **The projection ASKS core which debt a settled payment paid off, and the rent debt is in that list even though it has no `Charge` row** (build decision, 2026-09-01; R-144, spends R-035's `allocatePayment` and narrows D-11/D-12) | `apps/web/lib/ledger/allocate.ts` — `planAllocation` reads `outstandingCharges`, `leaseBalanceCents` and the versioned `JurisdictionRule`, adds ONE synthetic `RENT` debt sized at the part of the balance no charge row accounts for and dated `dueDateOnOrBefore(today, debitDay ?? rentDueDay)`, and hands the lot to `allocatePayment`. `webhook.ts` calls it BEFORE the transaction (the balance must be the one before this payment's own entries exist) and writes one linked `PAYMENT` entry per application plus one unlinked entry for what landed on rent and for any overpayment. Only `payment_succeeded` takes this path: a finalized invoice raises charges and a refund or void re-opens a balance, and for both the invoice line already names what moved. `outstandingCharges` and the pay screen both gain `waivedAt: null`. | **D-12 IS NOT VIOLATED BY IT AND IS THE REASON FOR IT.** The projector still computes no amount — what moved is Stripe's number; the ORDER is the jurisdiction's, and until this item nothing read `paymentAllocationOrder` at all, so the form validated and stored a statutory choice the product ignored. What the projection did instead was settle every charge the invoice named in full and give the leftover to rent — fee-before-rent under any order, including the RENT-first one Texas is seeded with. **Leaving the rent debt out would have been the bug rather than a simplification**: ordinary rent mints no `Charge` (D-11/D-40), so a list of charge rows is a list of everything except the debt the order is mostly about, and allocation over it alone would still have paid every fee first. It is represented exactly as `delinquencyFor` already represents it (R-118/D-151). **A missing rule falls back to core's `DEFAULT_ALLOCATION_ORDER` rather than refusing**, which is the opposite of what late fees and deposits do with an unconfigured state — and the difference is that those decline to ACT while this one has already been paid; refusing would mean refusing to project money that arrived. **Two payments settling concurrently can plan against the same balance**: the balance stays right either way (each writes entries summing to its own amount) and the cost is a mis-attributed link, not lost money. |
| D-162 | **A carrier keyword reply addresses a NUMBER, not a person, so it is the one documented exception to "every module sends through the notification engine"** (build decision, 2026-09-01; R-145, narrows R-030) | `replyToKeyword` in `apps/web/lib/comms/sms-intake.ts` calls `notificationAdapter.send({channel:'SMS', to, body})` directly with `optOutReply`'s text. HELP gets the carrier disclosure, START gets the resubscribe confirmation, STOP gets nothing. Business name is the same `'Rental Operations'` constant `email-opt-out.ts` already uses. A send failure is caught and logged, never thrown. | **The engine resolves a PERSON** - recipient id, type, preferences, quiet hours and TCPA consent - and an inbound keyword arrives from a number that routinely belongs to nobody we know; `receiveInboundMessage` returns `unrouted` for exactly that, and it is the case the carrier requirement is most about. A path that could only answer recognised tenants would be silent precisely where it must not be. **Bypassing the consent gate is correct rather than convenient**: a HELP response is what a carrier requires of us, not a message anybody consents to receive. **STOP returns null deliberately** - the carrier sends its own confirmation and then blocks the number, so anything queued would fail for ever. **A failed reply must not fail the intake**: the inbound message is already recorded, and throwing hands Twilio a 500, which retries and duplicates it. The route's standing refusal to auto-reply (*"an auto-reply is COMM-07's"*) is untouched and still right - a conversational auto-reply is a different thing from a carrier keyword response. |
| D-163 | **Brand direction: Homestead — Albert Sans throughout, copper `#9A5B32` on the existing monochrome tokens** (owner decision, 2026-09-01; chosen over "Ledger" — serif + ledger green — and "Dispatch" — grotesque + mono figures + cobalt) | One humanist face (Albert Sans, four weights) for display, body and labels; warm copper accent for links, section eyebrows, sparklines and the chase affordance; tint family `#9A5B32` / `#7A4523` / `#B4713F`. Everything else the shipped system already decided stays: near-black `--primary` buttons, `--input` at 3.05:1, `--ring` as the only focus indicator, `--destructive` red reserved for alarm, 44px targets, light-only. The design canvas (three direction sheets plus dashboard, lease-page IA, mobile rent roll and empty-state patterns, all converged on Homestead) is the visual record. | **Chosen for the people who never asked to use software** — tenants on magic links and vendors with one page are half the audience, and Homestead is the direction kindest to the portal, the pay screen and the maintenance wizard. **The stated tradeoff is accepted and is a standing rule**: copper sits nearest the destructive red's hue family, so alarm states must never lean on the accent — red keeps its exclusive meaning, and any state that needs urgency uses red plus an icon plus words, never copper. Implementation is NOT part of this decision: adopting the accent and face in the app is its own backlog item, token-first (`--accent` currently resolves to the same grey as `--muted`), never per-screen. |

## Needs owner decision

These gate the items named. Answer them in this file as new D-numbers rather than deciding silently mid-build. They are the PRD §10 open questions, narrowed to the ones with an actual build consequence.

| # | Question | Gates | Why it can't be deferred |
|---|---|---|---|
| OQ-1 | **Which states does the portfolio operate in today, and what's the 24-month plan?** | R-010 (jurisdiction seed), Milestone 6 (deposits), R-062 (notices) | Texas is seeded per D-4, but deposit deadlines and entry-notice hours are wrong-by-default for any other state, and wrong defaults manufacture compliance failures at scale. |
| ~~OQ-2~~ | ~~**Any Section 8 / HCV tenancies now or planned?**~~ **FULLY ANSWERED 2026-08-24: none now, none planned → see D-136.** | ~~R-048~~ (cut) | Closed. The *schema* half was settled 2026-08-01 and built (R-002 ships `LeasePayer` + `PayerAllocation`, D-13); the *phase* half was re-asked at the point it actually blocked something and answered none, so R-048 is cut rather than left gated. Re-opens on one fact — a first voucher tenancy — and re-opens as a feature build against a schema that already fits. |
| ~~OQ-3~~ | ~~**In-house maintenance tech, all external vendors, or both?**~~ **ANSWERED 2026-08-06 — see D-17: all external vendors.** | R-028 (offline tech flow), Milestone 2 sequencing | Answered. R-028 deferred to Phase 3; the magic-link path (D-6, R-025) is the entire maintenance edge. |
| ~~OQ-4~~ | ~~**How many legal entities (LLCs), and do any have outside partners with distribution rights?**~~ **FULLY ANSWERED — structural half 2026-08-24 (D-123): one LLC per property. Partner half 2026-08-24 (D-133): none, so R-095 is cut.** | R-004 (entity scoping), R-085 (investor role) | Entity scoping is schema-level and cheap now; adding it later touches every financial query. Partner distributions decide whether the investor persona is real or cut. |
| OQ-5 | **What share of tenants will never use a portal, and how do they pay today?** | R-036 (offline payment recording), activation planning | Sizes the staff-mediated path. The PRD treats it as a hard requirement either way; this decides how much UI it deserves. |
| OQ-6 | **Are written screening criteria documented today?** | ~~R-060 (screening)~~ — **no longer a hard build-block, still open for real use** | **Answered 2026-08-18: no, and the owner chose to have R-060 draft placeholder defaults rather than block the item.** `ScreeningCriteria` v1 (income ≥3x rent, credit floor 600, 7-year eviction/criminal lookback) is seeded with `reviewedBy: null` and a `notes` field saying plainly these are unreviewed. R-060 itself is built and shipped; what's still open is the real question - an owner/counsel review that either approves these numbers or replaces them with a new version before the product is used on a real applicant. |
| ~~OQ-7~~ | ~~**Lease templates: attorney-drafted per state already, or starting from scratch?**~~ **ANSWERED 2026-08-18: no, and the owner chose to have R-063 draft a placeholder template rather than block the item - see D-52.** | ~~R-063 (lease generation)~~ — **no longer a hard build-block, still open for real use** | Merge-field template work is small; sourcing a defensible lease is not. |
| OQ-8 | **Who does the bookkeeping today, in what system, cash or accrual?** **PARTIALLY ANSWERED 2026-08-21: "both, basis is a parameter" → see D-71.** Still open: WHOSE books and in WHICH system, which is what a per-entity chart-of-accounts mapping would need. | ~~R-078 (QuickBooks export mapping)~~ — **no longer a build-block**; R-093 (QBO API) | The cash/accrual half is settled and built. The remaining half decides whether the default QuickBooks account names need per-entity overrides, and whether R-093 needs the QBO API or the CSV suffices indefinitely. R-078 shipped CSV. |
| OQ-9 | **What share of tenants are non-English-primary?** | R-096 (Spanish surface) — row corrected 2026-08-23; it said R-090, which is the roommate-change item and never owned this | Decides Should vs Must, and which notices need attorney-approved translations rather than machine translation. |
| OQ-10 | **Who is on-call after hours today, and what is the actual protocol?** | ~~R-029 (after-hours routing)~~ — **no longer a hard gate, see D-18** | Still worth answering, but R-029 shipped without it on purpose: it encodes no arrangement, only the mechanism (a self-service on-call window, an acknowledgement that stops the escalation clock, a chain reaching everybody else), and falls back to paging everybody when nobody is on call. Answering this decides whether a rota-scheduling UI is worth building on top — not whether the routing is right. |
| ~~OQ-11~~ | ~~**Autopay or partial payments?**~~ **ANSWERED 2026-08-10 by the owner: option (a) — see D-29.** | R-037, R-039, R-043 — **no longer blocking** | Answered. Collection method is per payer, not per product: autopay tenants on `charge_automatically`, partial-paying tenants on `send_invoice`, switched as their circumstances change. Milestone 3 is unblocked. |
| ~~OQ-12~~ | ~~**Section 8 / HCV under Stripe Billing.**~~ **CONFIRMED 2026-08-01 and built → see D-13.** | R-048 | Resolved as proposed: two Stripe Customers and two Subscriptions per voucher lease, joined in our projection so the lease shows one balance. `LeasePayer` holds the Stripe ids; `PayerAllocation` holds the split. Nothing left to decide here — R-048 now inherits a schema that already fits. |

## How to use this log

When starting a Claude Code session on a backlog item, include this file alongside the relevant PRD section. If an item's PRD text contradicts a decision here, the decision wins. New conflicts discovered during the build should be appended here with a new D-number rather than resolved silently. An item whose gating question above is unanswered should not be started — build to the PRD's stated default and record the assumption, or pick a different item.
| D-164 | **Next arc after backlog completion: operator backlog review** (owner decision, 2026-09-02; chosen over a hardening pass, declaring the project done, or R-093 groundwork) | With every buildable row through R-152 ✅ and the remainder cut (D-122/D-133/D-136), deferred (D-17) or partner-gated (R-093), the next arc is sourced the way the original backlog was: the rental-operator agent reviews the shipped product against real SFR operations, and its prioritized findings become new backlog rows. | The cuts' re-open triggers stay authoritative — the review must not re-open D-122, D-133 or D-136 without their named facts, and partner-gated integrations stay out of scope. |
| D-165 | **A `Charge.amountCents` may be negative — the one case is a move-out credit** (build decision, 2026-09-03; R-160, mirrors D-12's move-in proration) | `moveOutProration` (core) returns the credit as a positive magnitude, same shape as `moveInProration`; `chargeMoveOutProration` (wiring) is the one place that negates it, writing `Charge.amountCents` and the pushed `addInvoiceItem` amount as the same negative number, and pushing a NEGATIVE invoice item — Stripe's own credit mechanism, the identical pattern `waiveCharge` already uses for a waived fee. Charge type stays `RENT`, same reasoning as move-in: it IS rent, credited back for days paid and not lived. | Rejected: a `CREDIT` `ChargeType` (exists in the schema's sibling `LedgerEntryType` enum, never written) or a separate reversing-entry table. `Charge` was already unconstrained on sign (no existing `CHECK amountCents > 0`), `LedgerEntry` is explicitly documented signed ("positive increases the balance owed, negative reduces it"), and `waiveCharge` had already established the negative-invoice-item wiring — inventing a second mechanism would fork the pattern for no reason. **Runs BEFORE `startDepositDisposition`** in `changeLeaseStatus` (`apps/web/lib/leases/actions.ts`), so the disposition's ledger read is net of it by the time disposition starts — the backlog's "minus balance owed is a settled number." **e2e fallout**: `e2e/leases.spec.ts`'s cleanup deleted `Lease` rows with `TurnoverProject` cleared first but no equivalent for `Charge` (`Charge_leaseId_fkey` is `ON DELETE RESTRICT`, same as `TurnoverProject`) — the ending-a-tenancy subscription test now posts a real Charge every run, and cleanup needs the same one-line fix `TurnoverProject` already got from R-084. |
| D-166 | **The external reconciliation check (R-163) compares a LEASE's total balance, not one event's amount** (build decision, 2026-09-04; narrows D-25) | `detectLeaseBalanceDrift` (core, separate from `detectDrift`) takes `{leaseId, stripeBalanceCents, projectedBalanceCents}`: Stripe's side is `getOpenInvoiceAmountCents` summed across every active `LeasePayer`'s subscription for that lease (amount_due minus amount_paid, on every open invoice), our side is `leaseBalanceCents`. Null on Stripe's side (could not be asked) skips the lease rather than reporting drift — same distinction `switchDecision` already draws between "unknown" and "zero". Runs whenever `billingIsLive()`; alarms onto the same `ledger.drift_detected` audit action R-147's `/money` panel already reads, reusing `Drift`'s `amount_mismatch` kind rather than adding one. | D-25 named this comparison as the thing "external reconciliation done properly" would need and left it out of scope; this is that comparison, scoped to what actually reproduces R-038a's miss (an event Stripe never sent moves a lease's true balance without moving either side of the internal, event-keyed check). Rejected: extending `detectDrift` itself to carry a lease-keyed comparison — its whole shape is "one Stripe event vs the rows it should have produced," and a lease balance has no event to key on. `Drift` gained a `leaseId: string | null` field (null on every existing internal-check row) rather than forcing this check's output through `stripeEventId`/`ledgerEntryId`, which would misname what actually disagreed. |
| D-167 | **A guarantor's release is a party moving off the lease, same table as a tenant swap, not a second mechanism** (build decision, 2026-09-04; R-165) | `LeasePartyChangeParty.tenantId` is now nullable and a sibling `guarantorId` column carries a guarantor party — a CHECK enforces exactly one of the two is set, and a second CHECK pins a guarantor party to `OUTGOING` (there is no path here for adding a guarantor, only releasing one who already exists). `buildPartyChange` takes an optional `outgoingGuarantorIds`, skips `assessPartyChange`'s tenant-replacement rules for them entirely (screening, household income, "cannot empty the tenancy" — none apply to a guarantor leaving), and the amendment gets its own "Guarantors released from this lease" section rather than folding a guarantor into the "Occupants leaving" clause, which would misdescribe them as an occupant. On completion, the guarantor row is never deleted — `active: false` plus a bumped `sessionsValidFrom`, mirroring exactly how a departing tenant's portal session dies, so the (append-only-adjacent) evidence of who guaranteed what and when survives the release. `Guarantor` also gained `active`/`sessionsValidFrom`, the same pair `Tenant` already carries, for the identical reason: a magic-link session has to be revocable. | Rejected: a parallel `GuarantorRelease` table. It would duplicate `LeasePartyChange`'s whole signed-amendment ceremony (envelope, signers, effective-date handling, void/withdraw) for one party type, doubling the machinery R-090's own header already argued against splitting for tenant swaps. **What is left open**: a served notice's portal "read" receipt (`NoticeDelivery`, method `PORTAL`) is not per-recipient — whichever of the tenant or guarantor on a lease opens it first is who the record shows as having read it. Fine for now (nothing currently asks "did the guarantor specifically read this"); the fix is a recipient column on `NoticeDelivery` if a dispute ever turns on it. Also open: `orderedSigners` puts every CURRENT guarantor on a lease's amendment, tenant swap or guarantor release alike, so a lease with two guarantors releasing one would still ask the other to sign a document about a release that is not theirs — undetected because no seed or spec here has ever put two guarantors on one lease; narrowing the signer list to only the guarantor(s) actually named in the change is the fix if that ever becomes real. |
| D-168 | **A deposit batch groups by legal entity, day and receiver — entity is a constraint the real world imposes, not a grouping choice** (build decision, 2026-09-04; R-166, narrows PAY-05's "by date and receiver") | `groupForDeposit` (core) keys on `(legalEntityId, receivedOn, receivedByStaffId)`. Two properties under the SAME entity can share one slip — one trip, one bank account (D-9's separate-entities design already makes this true); two different entities cannot, because that would print a slip claiming money moved between LLCs that never actually happened. There is no `PaymentBatch` table — a batch is `Payment.depositBatchId` (a plain generated id, no FK target) stamped onto every row in it, `depositedAt`, and `depositSlipDocumentId` pointing at the archived PDF; `groupForDeposit` re-derives the grouping fresh from undeposited rows every time rather than a batch being an entity with its own lifecycle. The slip's `Document` is anchored to `legalEntityId`, never `propertyId` — a batch can span every property under an entity, and no single property would be honest about that. A counter receipt is the same shape one level down: `Payment.receiptDocumentId` (unique — generating one is idempotent, same posture as a notice's PDF), anchored to `propertyId` since a receipt is always for one payment on one property. | Rejected: a `PaymentDepositBatch` row. Nothing about a batch needs its own lifecycle beyond "these payments share a value" — no status, no workflow, no field a table would hold that `Payment.depositBatchId` does not already carry once every row in it is stamped. `createDepositBatch` re-derives and re-checks the entity/day/receiver facts from the payment ids a submission actually names rather than trusting the group the page rendered, because a tampered submission naming payments from two receivers would produce a slip that misdescribes what physically happened — the one document in this product whose entire job is that a bank line reconciles to it. **What is left open**: that re-check has no dedicated test reaching it through the UI, because the ordinary form can only ever submit one rendered group's ids — `groupForDeposit`'s own unit tests cover the grouping rule the UI relies on to make a mixed submission unreachable in practice. |
| D-169 | **Every offline payment writes TWO `Payment` rows, and this item found it and did not fix it** (bug found, 2026-09-04; R-166; not a decision this item made, recorded so it is not lost) | `recordOfflinePayment` (`offline.ts`) writes ONE rich `Payment` row (channel `OFFLINE_CHECK`/`MONEY_ORDER`/`OFFLINE_CASH`, `checkNumber`, `receivedByStaffId`) *and*, as a side effect of the SAME call, `provider.recordOutOfBandPayment` fires an `invoice.updated` event that `webhook.ts`'s `writePayment` independently interprets as `kind: 'payment_succeeded'` and writes a SECOND, generic row (channel `OTHER`, no check number, no receiving staff) for the identical payment. `writePayment`'s own dedup — a unique-constrained lookup on `stripePaymentIntentId` — never fires, because an out-of-band payment has no PaymentIntent; the two writers cannot see each other. Confirmed with a stack-traced call count: `recordOfflinePayment` runs exactly once per press, and two rows still land. Confirmed as a simulator-independent, real-Stripe-shape bug, not an artifact of the test adapter: `writePayment`'s classification of `invoice.updated` and its `stripePaymentIntentId`-only dedup key are unconditional, so the same gap exists whenever a real Stripe webhook later delivers the identical event. | **Left exactly as found, deliberately, rather than patched under this item.** The two candidate fixes both touch the shared webhook/billing-provider boundary every payment type (card, ACH, refund, NSF) runs through — either widen `writePayment`'s dedup key to also match on `stripeInvoiceId` for a PaymentIntent-less event (risks collapsing two real different-day payments against the simulator's single stable per-payer invoice id, per R-038a's own note on that coarseness), or stop `offline.ts` writing its own row and instead let `writePayment` create it, updating it afterward with the offline-specific fields (needs the provider adapter to hand back which row it wrote, a real interface change to both the simulated and Stripe adapters). R-038a's own entry is the precedent for how much investigation a webhook/payment-row question like this warrants before touching it - a rushed fix here risks a worse, quieter bug in money the ledger already trusts. **What this item did instead**: `listUndepositedDepositGroups` and `createDepositBatch` already filter to the three offline channels, so the generic `OTHER` row is correctly invisible to deposit batching and never blocks it - the bug is real but this feature is not exposed to it. Whichever item fixes `writePayment` should start from `e2e/deposits.spec.ts`'s own comment on the poll it had to scope by channel to work around this. |
