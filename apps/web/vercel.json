{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "cd \"$(git rev-parse --show-toplevel)\"||exit 1;git fetch -q --deepen=50 2>/dev/null;git cat-file -e \"$VERCEL_GIT_PREVIOUS_SHA^{commit}\" 2>/dev/null&&git diff --quiet \"$VERCEL_GIT_PREVIOUS_SHA\" HEAD -- . ':(exclude)docs' ':(exclude)*.md'||exit 1",
  "crons": [
    {
      "path": "/api/cron",
      "schedule": "0 * * * *"
    },
    {
      "path": "/api/cron/escalations",
      "schedule": "*/5 * * * *"
    }
  ]
}
